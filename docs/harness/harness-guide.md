# How to Build PoF with PoF + UE5

> Generated by PoF Harness | 2026-04-01 | 52 iterations
> Total build time: 5.0h

## Prerequisites

- UE5 5.7.3+ installed
- PoF app running (npm run dev)
- Project connected via Setup Wizard
- Project path: C:/Users/kazda/Documents/Unreal Projects/PoF

## Build Order (dependency-sorted)

1. **arpg-character** — areas: character-foundation, character-camera-input
2. **input-handling** — areas: input-handling
3. **physics** — areas: physics
4. **arpg-animation** — areas: animation-locomotion, animation-montages
5. **arpg-gas** — areas: gas-core, gas-abilities
6. **models** — areas: models
7. **audio** — areas: audio
8. **ui-hud** — areas: ui-hud
9. **arpg-combat** — areas: combat-melee, combat-feedback
10. **arpg-enemy-ai** — areas: enemy-ai-core, enemy-ai-advanced
11. **ai-behavior** — areas: ai-behavior
12. **materials** — areas: materials-core, materials-advanced
13. **animations** — areas: animations
14. **arpg-inventory** — areas: inventory-items, inventory-equipment
15. **arpg-loot** — areas: loot-tables, loot-drops
16. **arpg-ui** — areas: ui-hud-gas, ui-screens
17. **dialogue-quests** — areas: dialogue-quests-core, dialogue-quests-system
18. **arpg-progression** — areas: progression-xp, progression-skills
19. **arpg-world** — areas: world-zones, world-encounters, world-interactive
20. **level-design** — areas: level-design-core, level-design-advanced
21. **multiplayer** — areas: multiplayer-core, multiplayer-advanced
22. **arpg-save** — areas: save-core, save-features
23. **save-load** — areas: save-load
24. **arpg-polish** — areas: polish-debug, polish-performance
25. **packaging** — areas: packaging

---

### Phase 1: Character Foundation

**Module:** `arpg-character`
**Description:** Base character class, game mode, player controller, and core movement (walk, sprint)
**Duration:** 3.3min

**PoF Checklist Items to Run:**
- `ac-1`
- `ac-2`
- `ac-3`
- `ac-4`

**What Gets Built:**
- Implemented AARPGCharacterBase — Abstract base with movement defaults, orient-to-movement, ASC, motion warping, attribute init from DataTable+CurveTable. Well-structured with clear movement gating, acceleration curves, and root motion tracking.
- Implemented AARPGPlayerCharacter — Concrete player subclass with health/mana, XP/leveling, attribute allocation, ability loadout, interaction scanning, cursor aim, death/respawn, perception stimuli, overhead widget. Comprehensive delegates for UI binding.
- Implemented WASD movement — Camera-relative movement via Enhanced Input with Swizzle/Negate modifiers. Diagonal normalization. Respects camera yaw rotation (Q/E). CanMove() gate blocks input during dodge/attack/stagger/stun/root motion.
- Implemented Sprint system — Hold-shift sprint with smooth MaxWalkSpeed interpolation, stamina drain (20/s) with regen delay (1s), auto-stop when stationary, re-engage when moving again, FOV offset (+5), spring arm pullback (+100), exhaustion handling.
- Implemented Dodge/dash — Space to dodge with directional montage selection (4-way + default fallback), custom movement mode with quadratic ease-out, wall sliding, invulnerability (timer or anim-notify driven), cancel window (last 40%), stamina cost (25), cooldown (1s), delegates for VFX/SFX.
- Implemented Isometric camera — Spring arm at -55 pitch with camera lag, zoom (scroll wheel, 600-2000 range), Q/E rotation (45-degree steps with interpolation), collision probe, movement sway (roll on strafe, pitch on forward/back).
- Implemented GameMode — Sets DefaultPawnClass to AARPGPlayerCharacter and PlayerControllerClass to AARPGPlayerController. Minimal but correct.
- Implemented GameInstance — Persistent progression data, play time tracking (1s timer), map transitions with pre/post delegates, save system integration via subsystem, sync to/from save data.



**Verification:** PASS git-clean, PASS source-exists

---

### Phase 2: Camera & Input System

**Module:** `arpg-character`
**Description:** Camera setup, input mapping context, enhanced input actions
**Duration:** 2.3min

**PoF Checklist Items to Run:**
- `ac-5`

**What Gets Built:**
- Implemented AARPGPlayerController — Full Enhanced Input with 15+ actions, WASD/mouse/hotbar/pause all wired. Clean CDO sub-object pattern.
- Implemented Enhanced Input actions — IA_Move, IA_Look, IA_Interact, IA_PrimaryAttack, IA_Dodge, IA_Sprint, IA_Zoom, IA_RotateCamera, IA_Pause, IA_UsePotion, IA_AbilitySlot1-4 with IMC_Default.
- Implemented Isometric camera — Spring arm with lag, collision, Q/E rotation, mouse wheel zoom, sprint FOV/arm offset, movement sway. All smoothly interpolated.
- Implemented Movement feel tuning — Added speed-dependent rotation rate (720 idle → 360 at speed), exposed terrain properties (step height 55, walkable angle 50, braking 2400, perch width 40), horizontal velocity maintenance on ramps, all as designer-tunable UPROPERTYs.

**Files:**
- `Source/PoF/Character/ARPGCharacterBase.h`
- `Source/PoF/Character/ARPGCharacterBase.cpp`

**Gotchas:**
- UCharacterMovementComponent::PerchRadiusThreshold controls how close to ledge edges the character can stand
- bMaintainHorizontalGroundVelocity is key for consistent speed on ramps/slopes

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 3: Input Handling

**Module:** `input-handling`
**Description:** Enhanced Input, rebinding, gamepad
**Duration:** 11.6min

**PoF Checklist Items to Run:**
- `ih-1`
- `ih-2`
- `ih-3`
- `ih-4`
- `ih-5`
- `ih-6`

**What Gets Built:**
- Implemented Enhanced Input actions — 16 input actions with correct value types (Axis2D, Axis1D, Boolean) created as CDO sub-objects. Added IA_RotateCameraAnalog for gamepad smooth camera rotation.
- Implemented Input Mapping Context setup — IMC_Default with full KB/M bindings (WASD, mouse, Q/E, 1-5, Escape) plus complete gamepad bindings. Swizzle/negate modifiers for 2D axis composition.
- Implemented Key rebinding system — Runtime RemapAction() preserves modifiers/triggers, chains re-remaps, saves/loads via USaveGame. ResetBindingsToDefault() reverses all rebinds. FindInputActionByName() for UI integration.
- Implemented Gamepad support — Full gamepad: left stick move (deadzone), right stick camera/zoom (deadzone+negate), face buttons (ABXY), shoulders (LB/RB camera rotate), triggers (LT sprint, RT attack), D-pad ability slots, Start pause. Tunable deadzone properties.
- Implemented Input mode management — SetInputModeGameOnly/UIOnly/GameAndUI with automatic cursor show/hide. Pause menu refactored to use these. Auto-cursor toggle on device switch via InputKey override.
- Implemented Context-sensitive input — PushInputContext/PopInputContext for IMC stacking with priority. Enables vehicle, swimming, menu contexts via the Enhanced Input subsystem's built-in priority system.

**Files:**
- `Source/PoF/Input/ARPGInputTypes.h`
- `Source/PoF/Player/ARPGPlayerController.h`
- `Source/PoF/Player/ARPGPlayerController.cpp`

**Gotchas:**
- APlayerController::InputKey(FInputKeyParams) is final in UE 5.7 — override InputKey(const FInputKeyEventArgs&) instead, include InputKeyEventArgs.h. Use Params.IsGamepad() for device detection.

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 4: Physics & Collision

**Module:** `physics`
**Description:** Profiles, materials, projectiles, destruction
**Duration:** 8.0min

**PoF Checklist Items to Run:**
- `phy-1`
- `phy-2`
- `phy-3`
- `phy-4`
- `phy-5`
- `phy-6`

**What Gets Built:**
- Implemented Collision profiles and channels — 4 custom channels (Projectile, Interactable, WeaponTrace, Destructible) in DefaultEngine.ini with 3 named profiles. ARPGCollisionChannels.h provides typed constants.
- Implemented Physics materials — UARPGPhysicalMaterial extends UPhysicalMaterial with EARPGSurfaceType enum (10 types), per-material impact VFX/SFX/footstep sound, and scale multiplier.
- Implemented Projectile system — Enhanced AARPGProjectile with gravity scale, bounce (configurable count/bounciness), homing (acceleration-based), area-of-effect damage with distance falloff, impact VFX/SFX, surface material awareness. Uses custom Projectile collision channel.
- Implemented Chaos destruction — AARPGDestructibleActor with GeometryCollectionComponent, accumulated damage threshold, impulse on break, break VFX/SFX, debris lifetime auto-cleanup, OnBroken delegate. Uses Destructible collision profile.
- Implemented Physics constraints — UPhysicsConstraintComponent used in AARPGPhysicsDoor (hinge door with kick-open, open/close toggle, SFX) and AARPGPhysicsLever (toggle lever with impulse and activation delegate).
- Implemented Trace utilities — UARPGTraceUtilities blueprint function library with Line/Sphere/SphereMulti/Box trace + SphereOverlap, all channel-filtered with debug draw and phys material return. AutoCreateRefTerm for ActorsToIgnore.

**Files:**
- `Source/PoF/Physics/ARPGCollisionChannels.h`
- `Source/PoF/Physics/ARPGPhysicalMaterial.h`
- `Source/PoF/Physics/ARPGPhysicalMaterial.cpp`
- `Source/PoF/Physics/ARPGTraceUtilities.h`
- `Source/PoF/Physics/ARPGTraceUtilities.cpp`
- `Source/PoF/Physics/ARPGDestructibleActor.h`
- `Source/PoF/Physics/ARPGDestructibleActor.cpp`
- `Source/PoF/Physics/ARPGGrabComponent.h`
- `Source/PoF/Physics/ARPGGrabComponent.cpp`
- `Source/PoF/Physics/ARPGPhysicsDoor.h`
- `Source/PoF/Physics/ARPGPhysicsDoor.cpp`
- `Source/PoF/Physics/ARPGPhysicsLever.h`
- `Source/PoF/Physics/ARPGPhysicsLever.cpp`
- `Source/PoF/Physics/ARPGMovingPlatform.h`
- `Source/PoF/Physics/ARPGMovingPlatform.cpp`
- `Source/PoF/PoF.Build.cs`
- `Config/DefaultEngine.ini`
- `Source/PoF/Combat/ARPGProjectile.h`
- `Source/PoF/Combat/ARPGProjectile.cpp`

**Gotchas:**
- UHT cannot parse TArray<AActor*>() as a default parameter — use AutoCreateRefTerm meta specifier and move the param before defaulted params
- UPhysicalMaterial already has a SurfaceType member — custom subclasses must use a different name (e.g. GameSurfaceType)
- FVector_NetQuantize (from HitResult.ImpactPoint) cannot be used in ternary with FVector — explicit FVector() cast needed
- ChaosSolverEngine, GeometryCollectionEngine, FieldSystemEngine modules needed for Chaos destruction in UE 5.7

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 5: Animation Locomotion

**Module:** `arpg-animation`
**Description:** AnimInstance, blend spaces, locomotion state machine
**Duration:** 2.4min

**PoF Checklist Items to Run:**
- `aa-1`
- `aa-2`
- `aa-3`
- `aa-4`

**What Gets Built:**
- Implemented UARPGAnimInstance — Comprehensive AnimInstance with Speed, Direction, IsInAir, SpeedRatio, lean, sprint blend, state machine enum, transition rules, upper-body blending, montage tracking. Death state now wired up via IsDead().
- Implemented Locomotion Blend Space — BS1D_Locomotion created via AnimAsset commandlet with Speed axis [0,600]. Sample anims must be assigned in editor.
- Implemented Animation state machine — 5-state machine (Locomotion/Attacking/Dodging/HitReact/Death) fully defined in C++ with EARPGAnimState enum and transition rules. AnimBP graph must be built in editor.
- Implemented Root motion toggle — RootMotionMode set to RootMotionFromMontagesOnly — locomotion uses velocity, attacks/dodges use root motion. bIsFullBodyMontage flag suppresses locomotion blending during montages.

**Files:**
- `Source/PoF/Character/ARPGCharacterBase.h`
- `Source/PoF/Player/ARPGPlayerCharacter.h`
- `Source/PoF/Character/ARPGEnemyCharacter.h`
- `Source/PoF/Animation/ARPGAnimInstance.cpp`

**Gotchas:**
- Virtual IsDead() on base character class enables AnimInstance death state without coupling to specific subclass bIsAlive flags

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 6: Montages & Motion Warping

**Module:** `arpg-animation`
**Description:** Attack montages, anim notifies, motion warping, root motion
**Duration:** 3.9min

**PoF Checklist Items to Run:**
- `aa-5`
- `aa-6`
- `aa-7`
- `aa-8`

**What Gets Built:**
- Implemented Attack montages — Full combo montage system via GA_MeleeAttack with sectioned montages, combo window events, input buffering, and section jumping. Asset commandlet creates montage shells.
- Implemented Anim Notify classes — All 5 notifies implemented: ComboWindow (toggle+GAS event), HitDetection (now with multi-point weapon tracing and frame-to-frame sweep), SpawnVFX (Niagara), PlaySound (attached/world).
- Implemented Motion Warping — MotionWarpingComponent on base character. GA_MeleeAttack now auto-acquires nearest enemy in forward cone (configurable radius/angle/stop distance) and sets warp target before each combo section. Clears on ability end. Re-acquires on combo advance.
- Implemented Mixamo import & retarget pipeline — Plugin infrastructure present (IKRig, PythonScriptPlugin enabled). Retargeting requires editor-side Python scripts which are asset-level, not C++ compilable. Pipeline ready for script creation.
- Implemented Asset automation commandlet — Already verified: PoFEditor module with UAnimAssetCommandlet creates BS1D_Locomotion, AM_MeleeCombo, AM_Dodge_*, AM_HitReact, AM_Death in 0.06s headless.

**Files:**
- `Source/PoF/AbilitySystem/GA_MeleeAttack.h`
- `Source/PoF/AbilitySystem/GA_MeleeAttack.cpp`
- `Source/PoF/Animation/AnimNotifyState_HitDetection.h`
- `Source/PoF/Animation/AnimNotifyState_HitDetection.cpp`

**Gotchas:**
- UE MotionWarping warp targets must be set BEFORE montage playback starts — the MotionWarping notify state reads them during the montage window
- Frame-to-frame weapon sweep tracing (previous vs current socket positions) catches fast-swing hits that single-frame traces miss
- Re-acquiring warp target on each combo section advance keeps magnetism tracking moving targets during multi-hit combos

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 7: Gameplay Ability System Core

**Module:** `arpg-gas`
**Description:** Ability system component, attribute sets, gameplay effects, ability base class
**Duration:** 3.8min

**PoF Checklist Items to Run:**
- `ag-1`
- `ag-2`
- `ag-3`
- `ag-4`
- `ag-5`

**What Gets Built:**
- Implemented Core AttributeSet — Already comprehensive: Health, MaxHealth, Mana, MaxMana, Strength, Dexterity, Intelligence, Armor, AttackPower, CriticalChance, CriticalDamage, plus Progression (XP, Level) and Meta attributes (IncomingDamage, IncomingCrit, IncomingHeal). PreAttributeChange clamping and PostGameplayEffectExecute with death/hit-react events and damage number broadcasting.
- Implemented Core Gameplay Effects — GE_Damage (instant, execution calc), GE_Heal (instant, +25 HP), GE_Buff_Strength (duration 10s, +15 STR), GE_Regen_Health (periodic infinite, +5 HP/2s), GE_Regen_Mana (NEW: periodic infinite, +3 MP/2s), plus GE_InitAttributes, GE_ManaCost, GE_Stun, GE_DodgeCooldown, GE_DifficultyScaling, cooldown GEs for all abilities.
- Implemented Damage execution calculation — UARPGDamageExecution with full formula: BaseDamage + AttackPower*Scaling, crit roll, diminishing-returns armor reduction, invulnerability check, outputs via IncomingCrit + IncomingDamage meta attributes.
- Implemented Default attribute initialization — FARPGAttributeInitRow DataTable struct, GE_InitAttributes with SetByCaller per attribute, LevelScalingCurveTable support, XPRequirementsCurveTable, all wired through AARPGCharacterBase::InitializeAttributes() called from PossessedBy.

**Files:**
- `Source/PoF/AbilitySystem/Effects/GE_Regen_Mana.h`
- `Source/PoF/AbilitySystem/Effects/GE_Regen_Mana.cpp`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.h`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.cpp`
- `Source/PoF/AbilitySystem/Effects/GE_DodgeCooldown.cpp`
- `Source/PoF/AbilitySystem/GA_Dodge.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 8: GAS Abilities & Tags

**Module:** `arpg-gas`
**Description:** Concrete abilities, gameplay tags, gameplay cues, ability tasks
**Duration:** 7.3min

**PoF Checklist Items to Run:**
- `ag-6`
- `ag-7`
- `ag-8`

**What Gets Built:**
- Implemented Gameplay Tags hierarchy — Comprehensive Ability/State/Damage/Event/Cooldown/Item/Input/Data/GameplayCue tag hierarchy with 110+ tags
- Implemented Base GameplayAbility — UARPGGameplayAbility with mana cost, cooldown, auto-end, effect helpers, tag blocking, 7 concrete abilities
- Implemented Damage execution with resistances — ARPGDamageExecution now factors armor (diminishing returns), crits, AND elemental resistances (Fire/Ice/Lightning) via dynamic asset tags
- Implemented Elemental resistance attributes — Added FireResistance/IceResistance/LightningResistance to attribute set, init data, init GE, and character init pipeline
- Implemented Damage type tagging — All abilities now tag damage specs with Damage.Physical/Fire via AddDynamicAssetTag — enables resistance lookup and damage-type-aware VFX
- Implemented GameplayCue integration — 10 GameplayCue tags defined, hit impact cues fire per damage type from PostGameplayEffectExecute, heal cues on IncomingHeal
- Implemented Attribute initialization — Data Table + Curve Table pipeline with SetByCaller for all 17 attributes including resistances

**Files:**
- `Source/PoF/AbilitySystem/ARPGAttributeSet.h`
- `Source/PoF/AbilitySystem/ARPGAttributeSet.cpp`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.h`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.cpp`
- `Source/PoF/AbilitySystem/ARPGAttributeInitData.h`
- `Source/PoF/AbilitySystem/Effects/ARPGDamageExecution.h`
- `Source/PoF/AbilitySystem/Effects/ARPGDamageExecution.cpp`
- `Source/PoF/AbilitySystem/Effects/GE_InitAttributes.cpp`
- `Source/PoF/AbilitySystem/GA_Fireball.cpp`
- `Source/PoF/AbilitySystem/GA_GroundSlam.cpp`
- `Source/PoF/AbilitySystem/GA_DashStrike.cpp`
- `Source/PoF/AbilitySystem/GA_MeleeAttack.cpp`
- `Source/PoF/AbilitySystem/GA_EnemyMeleeAttack.cpp`
- `Source/PoF/AbilitySystem/GA_EnemyChargeAttack.cpp`
- `Source/PoF/Combat/ARPGProjectile.h`
- `Source/PoF/Combat/ARPGProjectile.cpp`
- `Source/PoF/Character/ARPGCharacterBase.cpp`

**Gotchas:**
- FGameplayEffectSpec::AddDynamicAssetTag() is the proper way to attach damage type metadata to a GE spec — the damage execution and PostGameplayEffectExecute can both read it via GetDynamicAssetTags()
- UAbilitySystemComponent::ExecuteGameplayCue() fires a one-shot cue without needing a GE — useful for hit impact feedback from PostGameplayEffectExecute
- Elemental resistances should be capped below 1.0 (0.9 cap) in PreAttributeChange to prevent full immunity

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 9: 3D Models & Characters

**Module:** `models`
**Description:** Import pipelines, procedural meshes, data tables
**Duration:** 4.8min

**PoF Checklist Items to Run:**
- `mod-1`
- `mod-2`
- `mod-3`
- `mod-4`
- `mod-5`
- `mod-6`

**What Gets Built:**
- Implemented Static mesh import pipeline — FARPGMeshImportConfig with full FBX settings (scale, pivot, normals, tangents, lightmaps, degenerates), UARPGMeshImportSettings data asset with per-category defaults
- Implemented LOD generation — FARPGLODTier structs with screen size thresholds and reduction percentages, category-specific LOD tier presets (3 for props/chars, 4 for foliage, 2 for architecture/equipment)
- Implemented Collision setup — EARPGCollisionPreset enum (None/SimpleBox/ConvexDecomposition/ComplexAsSimple/CustomConvex), per-category collision defaults, validation in MeshValidationCommandlet
- Implemented Nanite mesh enabling — Nanite toggle in import config, architecture/foliage default to Nanite enabled, validation skips LOD warnings for Nanite meshes
- Implemented Nanite Foliage setup — Foliage category preset with Nanite enabled, aggressive 4-tier LOD fallback, no lightmap UVs (dynamic lighting), covered by import config system
- Implemented Data Table mesh registry — FARPGMeshRegistryRow (FTableRowBase) with display name, soft mesh refs, category, Nanite flag, LOD count, tri count, bounds, and full import config
- Implemented Procedural mesh generation — AARPGProceduralMeshActor with GeneratePlatform (subdivisions), GenerateRamp, GenerateCylinder (caps), GenerateCustomMesh, proper normals/UVs/collision on all shapes
- Implemented Skeletal mesh import — Character category in import config with LOD tiers, skeletal mesh field in registry, MeshValidationCommandlet validates SK_ naming, bone count, LODs, physics asset, materials

**Files:**
- `Source/PoF/Mesh/ARPGMeshTypes.h`
- `Source/PoF/Mesh/ARPGMeshTypes.cpp`
- `Source/PoF/Mesh/ARPGProceduralMeshActor.h`
- `Source/PoF/Mesh/ARPGProceduralMeshActor.cpp`
- `Source/PoFEditor/MeshValidationCommandlet.h`
- `Source/PoFEditor/MeshValidationCommandlet.cpp`
- `Source/PoF/PoF.Build.cs`

**Gotchas:**
- UStaticMesh::NaniteSettings is deprecated in 5.7 — use IsNaniteEnabled() and GetNaniteSettings() accessor functions instead

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 10: Audio & Music

**Module:** `audio`
**Description:** Spatial audio painter, zone reverb, emitters, sound manager codegen
**Duration:** 6.2min

**PoF Checklist Items to Run:**
- `aud-1`
- `aud-2`
- `aud-3`
- `aud-4`
- `aud-5`
- `aud-6`

**What Gets Built:**
- Implemented Sound manager subsystem — UGameInstanceSubsystem with per-category volume control (Master/Music/SFX/Ambient/UI/Voice), sound pooling (32 max, recycle timer), 2D/3D/attached playback, default concurrency settings
- Implemented Ambient sound system — AARPGAmbientSoundActor with looping/random-interval modes, configurable attenuation radius, occlusion, pitch variation, enable/disable control
- Implemented Dynamic music system — UARPGMusicManager subsystem with 7 music states (Exploration/Combat/Boss/Stealth/Menu/Cinematic), dual-track crossfading, combat intensity scaling, track registration, volume control, state change delegates
- Implemented Audio volumes and reverb — AARPGAudioVolume with 8 reverb presets (SmallRoom/LargeHall/Cave/Forest/Underwater/Cathedral/Arena/Custom), box overlap detection, interior ambient sounds with fade in/out, configurable priority and fade time
- Implemented MetaSounds integration — UARPGMetaSoundInterface component wrapping MetaSound sources with typed parameter bindings (float/int/bool), trigger support, auto-play, fade out, default parameter application
- Implemented Sound concurrency settings — UARPGSoundConcurrencyConfig data asset with 6 category presets (Weapon/Footstep/Ambient/UI/Ability/Impact), configurable max count, resolution rules, volume scaling

**Files:**
- `Source/PoF/Audio/ARPGSoundManager.h`
- `Source/PoF/Audio/ARPGSoundManager.cpp`
- `Source/PoF/Audio/ARPGAmbientSoundActor.h`
- `Source/PoF/Audio/ARPGAmbientSoundActor.cpp`
- `Source/PoF/Audio/ARPGMusicManager.h`
- `Source/PoF/Audio/ARPGMusicManager.cpp`
- `Source/PoF/Audio/ARPGAudioVolume.h`
- `Source/PoF/Audio/ARPGAudioVolume.cpp`
- `Source/PoF/Audio/ARPGCombatAudioComponent.h`
- `Source/PoF/Audio/ARPGCombatAudioComponent.cpp`
- `Source/PoF/Audio/ARPGMetaSoundInterface.h`
- `Source/PoF/Audio/ARPGMetaSoundInterface.cpp`
- `Source/PoF/Audio/ARPGSoundConcurrencyConfig.h`
- `Source/PoF/Audio/ARPGSoundConcurrencyConfig.cpp`

**Gotchas:**
- UAudioComponent::bEnableDefaultMultichannelEvaluation does not exist in UE 5.7 — removed without replacement
- EMaxConcurrentResolutionRule is a namespace enum in UE 5.7, not an enum class — must use TEnumAsByte<EMaxConcurrentResolutionRule::Type> for UPROPERTY
- UAudioComponent::SetBoolParameter() exists natively in UE 5.7 — no need for int workaround

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 11: Character Foundation

**Module:** `arpg-character`
**Description:** Base character class, game mode, player controller, and core movement (walk, sprint)
**Duration:** 1.6min

**PoF Checklist Items to Run:**
- `ac-1`
- `ac-2`
- `ac-3`
- `ac-4`

**What Gets Built:**
- Implemented AARPGCharacterBase — Abstract base with movement, sprint, dodge, stamina, camera, GAS, motion warping, death, animation helpers. Added IsDead() check to CanMove().
- Implemented AARPGPlayerCharacter — Health/mana/XP/leveling, interaction, death/respawn, ability loadout, cursor aim, attribute allocation, perception stimuli, overhead widget.
- Implemented WASD movement — Camera-relative movement with orient-to-movement, diagonal normalization, speed-dependent rotation rate, acceleration curves.
- Implemented Sprint system — Hold-to-sprint with speed interp, stamina drain/regen with delay, FOV shift, arm length offset, auto-stop/re-engage logic.
- Implemented Dodge/dash — 4-directional dodge with custom movement mode, quadratic ease-out, wall sliding, invulnerability (timer or anim-notify driven), cancel window, montage support, cooldown.

**Files:**
- `Source/PoF/Character/ARPGCharacterBase.cpp`

**Gotchas:**
- CanMove() should always check IsDead() as first gate to prevent dead characters from processing movement input — especially important for enemy AI that may call AddMovementInput after death

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 12: Camera & Input System

**Module:** `arpg-character`
**Description:** Camera setup, input mapping context, enhanced input actions
**Duration:** 1.4min

**PoF Checklist Items to Run:**
- `ac-5`

**What Gets Built:**
- Implemented AARPGPlayerController — Full Enhanced Input with 16 actions, KB/M + gamepad bindings, key rebinding with save/load, device detection, context stacking, pause menu, input mode management
- Implemented Enhanced Input actions — IA_Move, IA_Look, IA_Interact, IA_PrimaryAttack, IA_Dodge, IA_Sprint, IA_Zoom, IA_RotateCamera (L/R/Analog), IA_UsePotion, IA_Pause, IA_AbilitySlot1-4 — all with proper modifiers (swizzle, negate, deadzone)
- Implemented Isometric camera — Spring arm with configurable pitch, zoom (min/max with smooth interp), Q/E + analog rotation, camera lag, collision, sprint FOV/arm offset, movement sway (roll+pitch)



**Verification:** PASS git-clean, PASS source-exists

---

### Phase 13: Input Handling

**Module:** `input-handling`
**Description:** Enhanced Input, rebinding, gamepad
**Duration:** 3.0min

**PoF Checklist Items to Run:**
- `ih-1`
- `ih-2`
- `ih-3`
- `ih-4`
- `ih-5`
- `ih-6`

**What Gets Built:**
- Implemented Enhanced Input actions — 16 input actions with correct value types covering all gameplay needs
- Implemented Input Mapping Context setup — Full KB/M and gamepad bindings with proper modifiers (swizzle, negate, deadzone)
- Implemented Key rebinding system — Runtime rebinding with save/load persistence, reset to defaults, chained remap simplification, and NEW conflict detection
- Implemented Gamepad support — Full gamepad layout (sticks, face buttons, shoulders, triggers, d-pad, start), configurable deadzones
- Implemented Input mode management — Game/UI/GameAndUI modes. Fixed cursor bug where device switch would hide cursor during UI mode
- Implemented Context-sensitive input — IMC push/pop stacking with priority. Works for vehicle/swimming/menu overlays

**Files:**
- `Source/PoF/Player/ARPGPlayerController.h`
- `Source/PoF/Player/ARPGPlayerController.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 14: Physics & Collision

**Module:** `physics`
**Description:** Profiles, materials, projectiles, destruction
**Duration:** 17.3min

**PoF Checklist Items to Run:**
- `phy-1`
- `phy-2`
- `phy-3`
- `phy-4`
- `phy-5`
- `phy-6`

**What Gets Built:**
- Implemented Collision profiles and channels — 4 custom channels (Projectile, Interactable, WeaponTrace, Destructible) with full DefaultEngine.ini profiles. Added missing WeaponTrace profile name constant.
- Implemented Physics materials — Extended UPhysicalMaterial with game surface types, impact VFX/SFX, footstep sounds, and scale multiplier. 10 surface types.
- Implemented Projectile system — Full-featured projectile with homing, bouncing, AoE with distance falloff, GAS damage integration with dynamic asset tags, surface-aware impact FX.
- Implemented Chaos destruction — GeometryCollection-based destructibles with damage accumulation, break VFX/SFX, debris cleanup, events. Added loot drop system with eject impulse and spawn radius.
- Implemented Physics constraints — Physics door (hinge + kick), lever (toggle hinge), and new constraint preset data asset with 5 types (Hinge, BallAndSocket, Prismatic, Weld, Custom) for ragdoll and generic joints.
- Implemented Trace utilities — Line/sphere/box/capsule traces with channel filtering, debug draw, multi-trace, sphere overlap, and phys material helper. Added capsule trace.

**Files:**
- `Source/PoF/Physics/ARPGPhysicsConstraintPreset.h`
- `Source/PoF/Physics/ARPGPhysicsConstraintPreset.cpp`
- `Source/PoF/Physics/ARPGCollisionChannels.h`
- `Source/PoF/Physics/ARPGDestructibleActor.h`
- `Source/PoF/Physics/ARPGDestructibleActor.cpp`
- `Source/PoF/Physics/ARPGMovingPlatform.h`
- `Source/PoF/Physics/ARPGMovingPlatform.cpp`
- `Source/PoF/Physics/ARPGTraceUtilities.h`
- `Source/PoF/Physics/ARPGTraceUtilities.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 15: Animation Locomotion

**Module:** `arpg-animation`
**Description:** AnimInstance, blend spaces, locomotion state machine
**Duration:** 60s

**PoF Checklist Items to Run:**
- `aa-1`
- `aa-2`
- `aa-3`
- `aa-4`

**What Gets Built:**
- Implemented UARPGAnimInstance — 30+ exposed variables: locomotion (Speed, Direction, IsInAir, SpeedRatio, LeanAngle, TurnRate, RunSpeedNormalized, SprintBlendAlpha, StaminaRatio), combat state (bIsAttacking, bIsDodging, bIsHitReacting, bIsDead), EARPGAnimState enum with priority-based state machine, transition interrupt rules, upper-body blending with cached pose documentation, montage tracking
- Implemented Locomotion Blend Space — AnimAssetCommandlet creates both BS1D_Locomotion (SpeedRatio 0-1 or absolute speed) and BS_Locomotion 2D (Direction x Speed) via FProperty reflection on protected BlendParameters. Configurable via -SpeedMax and -UseSpeedRatio params
- Implemented Animation state machine — EARPGAnimState enum (Locomotion/Attacking/Dodging/HitReact/Death) computed with priority ordering. Comprehensive transition rules documented in header comments with interrupt support (dodge cancel, attack recovery, hit-react pulse)
- Implemented Root motion toggle — RootMotionMode set to RootMotionFromMontagesOnly in constructor — locomotion uses velocity-driven movement, attacks/dodges use root motion. bIsFullBodyMontage flag for suppressing locomotion blending during montages



**Verification:** PASS git-clean, PASS source-exists

---

### Phase 16: Montages & Motion Warping

**Module:** `arpg-animation`
**Description:** Attack montages, anim notifies, motion warping, root motion
**Duration:** 3.8min

**PoF Checklist Items to Run:**
- `aa-5`
- `aa-6`
- `aa-7`
- `aa-8`

**What Gets Built:**
- Implemented Attack montages — AM_MeleeCombo (3 sections linked), AM_HeavyCombo (2 sections), dodge/hitreact/death shells all created by AnimAssetCommandlet. Added directional hit react montage shells (Front/Back/Left/Right) and heavy combo montage.
- Implemented Anim Notify classes — All 5 notifies implemented: ComboWindow (instant), ComboWindow (state — new), HitDetection (state with multi-point sweep + frame-to-frame tracing), SpawnVFX (Niagara), PlaySound. Fixed ComboWindow editor color bug (was set once in ctor, now dynamic via GetEditorColor).
- Implemented Motion Warping — MotionWarpingComponent on character base, GA_MeleeAttack auto-acquires nearest enemy in configurable forward cone, re-acquires per combo section advance, clears on end. Stop distance prevents warping into targets.
- Implemented Mixamo import & retarget pipeline — MixamoImportCommandlet delegates to Python pipeline (mixamo_pipeline module) for FBX import with bone prefix stripping, IK Retargeter fuzzy chain mapping, and root motion extraction. Python module is external dependency.
- Implemented Asset automation commandlet — AnimAssetCommandlet creates BS1D, BS2D, AM_MeleeCombo, AM_HeavyCombo, 4x AM_Dodge, AM_HitReact, 4x AM_HitReact directional, AM_Death, CT_XPRequirements. 14 assets total. Sections linked sequentially for combos.

**Files:**
- `Source/PoF/Animation/AnimNotifyState_ComboWindow.h`
- `Source/PoF/Animation/AnimNotifyState_ComboWindow.cpp`
- `Source/PoF/Animation/AnimNotify_ComboWindow.h`
- `Source/PoF/Animation/AnimNotify_ComboWindow.cpp`
- `Source/PoF/Animation/ARPGAnimInstance.h`
- `Source/PoF/Animation/ARPGAnimInstance.cpp`
- `Source/PoFEditor/AnimAssetCommandlet.cpp`

**Gotchas:**
- UAnimNotify::NotifyColor set in constructor won't update when properties change in editor — override GetEditorColor() instead for dynamic color based on property values
- AnimNotifyState is more designer-friendly than two bookend instant notifies for window-based concepts (combo window, invulnerability frames) — the duration bar is intuitive
- Montage_GetCurrentSection() returns the active section name — useful for section-aware AnimBP transitions and per-section logic

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 17: Gameplay Ability System Core

**Module:** `arpg-gas`
**Description:** Ability system component, attribute sets, gameplay effects, ability base class
**Duration:** 2.6min

**PoF Checklist Items to Run:**
- `ag-1`
- `ag-2`
- `ag-3`
- `ag-4`
- `ag-5`

**What Gets Built:**
- Implemented Core AttributeSet — Health/Mana/Str/Dex/Int/Armor/AP/CritChance/CritDamage plus elemental resistances, progression, and 3 meta attributes. Full PreAttributeChange clamping and PostGameplayEffectExecute with damage numbers, GameplayCues, death/hitreact events.
- Implemented Core Gameplay Effects — GE_Damage (execution-based), GE_Heal (fixed: now routes through IncomingHeal meta for proper GameplayCue pipeline), GE_Buff_Strength (duration), GE_Regen_Health/Mana (periodic infinite), GE_Stun, GE_ManaCost, GE_InitAttributes, GE_DodgeCooldown, GE_AwardXP, GE_DifficultyScaling, plus 4 ability cooldown GEs.
- Implemented Damage execution calculation — Full formula: BaseDamage + AP*Scaling, crit roll, diminishing-returns armor reduction, elemental resistance lookup from dynamic asset tags, invulnerability check. Outputs via meta attributes.
- Implemented Default attribute initialization — FARPGAttributeInitRow data table struct, per-level curve table scaling, GE_InitAttributes with SetByCaller for all 17 attributes, XP requirements curve table. InitializeAttributes called from PossessedBy.

**Files:**
- `Source/PoF/AbilitySystem/Effects/GE_Heal.h`
- `Source/PoF/AbilitySystem/Effects/GE_Heal.cpp`
- `Source/PoF/AbilitySystem/Effects/GE_ManaCost.cpp`

**Gotchas:**
- GE_Heal should modify IncomingHeal meta attribute (not Health directly) to go through the full heal pipeline with GameplayCue support in PostGameplayEffectExecute

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 18: GAS Abilities & Tags

**Module:** `arpg-gas`
**Description:** Concrete abilities, gameplay tags, gameplay cues, ability tasks
**Duration:** 5.8min

**PoF Checklist Items to Run:**
- `ag-6`
- `ag-7`
- `ag-8`

**What Gets Built:**
- Implemented Gameplay Tags hierarchy — 130+ tags across Ability.*, State.*, Damage.*, Input.*, Cooldown.*, Event.*, Data.*, Item.*, GameplayCue.* namespaces. Added 6 new tags: Ability.UsePotion, Ability.HitReact, Ability.Enemy.Melee/Ranged/Charge
- Implemented Base GameplayAbility — UARPGGameplayAbility with mana cost, cooldown tag, auto-end, helper methods (ApplyEffectToTarget/Self, GetARPGCharacter/ASC/AttributeSet), cost check/apply overrides, UI accessors
- Implemented Damage Execution — Full damage formula: BaseDamage + AttackPower*Scaling, crit roll, diminishing armor reduction, elemental resistance lookup from dynamic asset tags, invulnerability check, outputs to IncomingDamage/IncomingCrit meta attributes
- Implemented Attribute Init — GE_InitAttributes with 17 SetByCaller modifiers, FARPGAttributeInitRow data table struct, curve table row name constants for per-level scaling
- Implemented Concrete Abilities — 9 player/shared abilities: MeleeAttack (combo chain + motion warping), Fireball (projectile AoE), GroundSlam (AoE stun), DashStrike (sweep damage), WarCry (self-buff), Dodge (4-directional with stamina), Death (event-triggered), HitReact (NEW), UsePotion (NEW)
- Implemented Enemy Abilities — 3 enemy abilities: EnemyMelee (front-arc overlap), EnemyRanged (BB-targeted projectile), EnemyCharge (motion warp AoE). Fixed tag collision — each now uses distinct Ability.Enemy.* tags
- Implemented Gameplay Effects — 14 GE classes: Damage (execution-based), Heal (IncomingHeal meta), Stun (duration+tag), Buff_WarCry (+20AP/+15Armor/15s), ManaCost (SetByCaller), 5 cooldowns, 2 regen (Health/Mana periodic), InitAttributes, DifficultyScaling
- Implemented Ability Unlock System — Full component with learn/upgrade via skill points, level requirements, rank progression, data table driven, delegates for UI

**Files:**
- `Source/PoF/AbilitySystem/GA_HitReact.h`
- `Source/PoF/AbilitySystem/GA_HitReact.cpp`
- `Source/PoF/AbilitySystem/GA_UsePotion.h`
- `Source/PoF/AbilitySystem/GA_UsePotion.cpp`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.h`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.cpp`
- `Source/PoF/AbilitySystem/GA_EnemyMeleeAttack.cpp`
- `Source/PoF/AbilitySystem/GA_EnemyRangedAttack.cpp`
- `Source/PoF/AbilitySystem/GA_EnemyChargeAttack.cpp`
- `Source/PoF/Character/ARPGCharacterBase.h`

**Gotchas:**
- Enemy abilities should use distinct Ability.Enemy.* tags rather than reusing player ability tags (e.g. Ability.Melee.LightAttack) to prevent tag-based filtering collisions between player and enemy ability sets
- GA_HitReact should block on State.Attacking/Invulnerable/Dashing to avoid interrupting active player actions — hit react is a lower priority response
- GA_UsePotion is fire-and-forget (CommitAbility + ApplyEffectToSelf + EndAbility) — no montage needed, cooldown gates usage

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 19: 3D Models & Characters

**Module:** `models`
**Description:** Import pipelines, procedural meshes, data tables
**Duration:** 3.3min

**PoF Checklist Items to Run:**
- `mod-1`
- `mod-2`
- `mod-3`
- `mod-4`
- `mod-5`
- `mod-6`

**What Gets Built:**
- Implemented Static mesh import pipeline — FARPGMeshImportConfig with per-category defaults (scale, lightmap UVs, normals, tangents, degenerate removal). UARPGMeshImportSettings data asset with InitializeDefaults for all 6 categories.
- Implemented LOD generation — FARPGLODTier with screen size thresholds and reduction percentages. Category-appropriate LOD tiers (Foliage=4, Prop/Character=3, Arch/Equipment=2, VFX=0).
- Implemented Collision setup — EARPGCollisionPreset with 5 presets (None, SimpleBox, ConvexDecomposition, ComplexAsSimple, CustomConvex). Per-category collision defaults matching gameplay needs.
- Implemented Nanite mesh enabling — bEnableNanite in import config, enabled by default for Architecture and Foliage. Validation commandlet checks Nanite status and LOD fallback.
- Implemented Nanite Foliage setup — Added FARPGNaniteFoliageConfig with WPO wind, fallback LOD, cull distance, density scale. UARPGNaniteFoliageSettings data asset with 4 presets (Tree, Bush, Grass, Rock).
- Implemented Data Table mesh registry — FARPGMeshRegistryRow with display name, soft mesh refs (static+skeletal), category, Nanite flag, LOD count, tri count, bounds, and full import config.
- Implemented Procedural mesh generation — AARPGProceduralMeshActor with Platform (subdivided), Wall, Ramp, Cylinder, and CustomMesh generation. All with collision, material support, and proper normals/UVs.
- Implemented Skeletal mesh import — FARPGSkeletalMeshImportConfig with morph targets, physics asset, bone influence, max bones per vertex, target skeleton, LOD tiers. UMixamoImportCommandlet for headless Mixamo FBX pipeline with retarget and root motion extraction.

**Files:**
- `Source/PoF/Mesh/ARPGMeshTypes.h`
- `Source/PoF/Mesh/ARPGMeshTypes.cpp`
- `Source/PoF/Mesh/ARPGProceduralMeshActor.h`
- `Source/PoF/Mesh/ARPGProceduralMeshActor.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 20: Audio & Music

**Module:** `audio`
**Description:** Spatial audio painter, zone reverb, emitters, sound manager codegen
**Duration:** 5.0min

**PoF Checklist Items to Run:**
- `aud-1`
- `aud-2`
- `aud-3`
- `aud-4`
- `aud-5`
- `aud-6`

**What Gets Built:**
- Implemented Sound manager subsystem — Pooling, per-category volume with master multiplier, volume persistence via GConfig (save/load to GameUserSettings.ini)
- Implemented Ambient sound system — Looping, random-interval, and now fully implemented TimeOfDay mode with configurable active hours, midnight-wrapping, and fade transitions
- Implemented Dynamic music system — Fixed critical bug: audio components now lazily created with world registration (EnsureAudioComponentsReady) so they actually produce sound. Crossfade, combat intensity, state machine all working
- Implemented Audio volumes and reverb — Fixed reverb: now creates real UReverbEffect and calls ActivateReverbEffect/DeactivateReverbEffect on the audio device. Supports 8 presets + custom. Added overlap counting for correct deactivation
- Implemented MetaSounds integration — Typed parameter interface with float/int/bool/trigger, auto-play, fade out. Clean component wrapper for MetaSound sources
- Implemented Sound concurrency settings — Data asset with 6 category presets (weapon, footstep, ambient, UI, ability, impact). Configurable max count, resolution rule, volume scaling

**Files:**
- `Source/PoF/Audio/ARPGAmbientSoundActor.h`
- `Source/PoF/Audio/ARPGAmbientSoundActor.cpp`
- `Source/PoF/Audio/ARPGMusicManager.h`
- `Source/PoF/Audio/ARPGMusicManager.cpp`
- `Source/PoF/Audio/ARPGAudioVolume.h`
- `Source/PoF/Audio/ARPGAudioVolume.cpp`
- `Source/PoF/Audio/ARPGSoundManager.h`
- `Source/PoF/Audio/ARPGSoundManager.cpp`

**Gotchas:**
- UAudioComponent created via NewObject on a subsystem (no world) won't produce sound — must RegisterComponentWithWorld after a world exists, or create lazily
- FAudioDeviceHandle from World->GetAudioDevice() provides ActivateReverbEffect/DeactivateReverbEffect for runtime reverb control without AReverbVolume actors
- GConfig->SetFloat/GetFloat with GGameUserSettingsIni is the lightweight way to persist audio settings without a full SaveGame system

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 21: UI / HUD

**Module:** `ui-hud`
**Description:** UMG menus, HUD, inventory, settings
**Duration:** 15.4min

**PoF Checklist Items to Run:**
- `ui-1`
- `ui-2`
- `ui-3`
- `ui-4`
- `ui-5`
- `ui-6`
- `ui-7`

**What Gets Built:**
- Implemented Main menu widget — New Game/Continue/Settings/Quit with save detection, embedded settings page, version text
- Implemented HUD framework — AHUD subclass managing all widget lifecycle, show/hide/toggle API, input mode transitions, notification routing. ARPGHUDWidget has GAS-bound health/mana bars with smooth interp and low-health pulse
- Implemented Inventory screen — 6x5 grid with drag-drop, 9 equipment slots, rarity borders, hover tooltips with affix display. Already production quality
- Implemented Settings menu — 3 tabs (Graphics/Audio/Controls) with resolution detection, quality presets, VSync, frame limit, volume sliders, mouse sensitivity, keybinding display
- Implemented Floating damage numbers — Elemental color coding, crit display, world-to-screen projection, fade/float animation, pooling with max limit, global GAS delegate subscription
- Implemented Loading screen — Progress bar, status text, randomized tips, fade-in/out. Ready for async level load integration
- Implemented Widget animation system — Fade state machines in EnemyHealthBar, LoadingScreen, NotificationWidget; smooth bar interpolation in HUD; damage number float/fade; cooldown sweep material. Animation is per-widget rather than a shared utility, which is appropriate for UMG

**Files:**
- `Source/PoF/UI/ARPGHUD.h`
- `Source/PoF/UI/ARPGHUD.cpp`
- `Source/PoF/UI/MainMenuWidget.h`
- `Source/PoF/UI/MainMenuWidget.cpp`
- `Source/PoF/UI/NotificationWidget.h`
- `Source/PoF/UI/NotificationWidget.cpp`
- `Source/PoF/UI/LoadingScreenWidget.h`
- `Source/PoF/UI/LoadingScreenWidget.cpp`

**Gotchas:**
- UDragDropOperation already has a Payload member — do not re-declare it in subclasses or UHT will error on shadowing

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 22: Combat — Melee & Combos

**Module:** `arpg-combat`
**Description:** Melee attack ability, combo system, hit detection, GAS damage application
**Duration:** 3.3min

**PoF Checklist Items to Run:**
- `acb-1`
- `acb-2`
- `acb-3`
- `acb-4`

**What Gets Built:**
- Implemented Melee attack ability — GA_MeleeAttack fully implemented with montage-driven combos, motion warping magnetism, SetByCaller damage, per-section damage multipliers. Fixed null deref on GetHitResult() in OnMeleeHit.
- Implemented Combo system — Full combo chain: input buffering, combo window open/close via gameplay events, montage section advancement, timeout reset. AnimNotifyState_ComboWindow + instant AnimNotify_ComboWindow both available.
- Implemented Hit detection — AnimNotifyState_HitDetection with multi-point base-to-tip tracing, frame-to-frame sweep for fast swings, TSet dedup, debug visualization, configurable trace channel/radius/socket names.
- Implemented GAS damage application — Full pipeline: GE_Damage → ARPGDamageExecution (AttackPower scaling, crit roll, armor diminishing returns, elemental resistance with 0.9 cap) → IncomingDamage meta attr → PostGameplayEffectExecute (health reduction, damage numbers, gameplay cues, death/hit-react events).
- Implemented Dodge ability (GAS) — GA_Dodge with directional montage selection, stamina cost, State.Invulnerable via ActivationOwnedTags, GE_DodgeCooldown, root motion. Full delegate system for VFX/SFX hooks.
- Implemented Death flow — GA_Death event-triggered by Event.Death, cancels all abilities, disables movement/collision, plays death montage, optional ragdoll, player input disable + respawn, enemy destroy timer. State.Dead persists until respawn.

**Files:**
- `Source/PoF/AbilitySystem/GA_HitReact.cpp`
- `Source/PoF/AbilitySystem/GA_MeleeAttack.cpp`
- `Source/PoF/Character/ARPGCharacterBase.h`

**Gotchas:**
- GA_HitReact must set/clear bIsHitReacting on the character so CanMove() properly gates movement during hit reactions — without this, the character can walk freely while playing a hit-react montage
- FGameplayAbilityTargetData::GetHitResult() can return nullptr even when TargetData.Num() > 0 — always null-check before dereferencing

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 23: Combat — Feedback & Death

**Module:** `arpg-combat`
**Description:** Hit reactions, death flow, combat feedback, dodge ability (GAS)
**Duration:** 5.5min

**PoF Checklist Items to Run:**
- `acb-5`
- `acb-6`
- `acb-7`
- `acb-8`
- `acb-9`

**What Gets Built:**
- Implemented Hit reaction system — GA_HitReact: event-triggered via Event.HitReact, plays montage, sets bIsHitReacting to gate movement, blocked by State.Attacking/Invulnerable/Dashing. Full pipeline from PostGameplayEffectExecute through GAS events.
- Implemented Dodge with i-frames — GA_Dodge: directional 4-way montages, stamina cost, cooldown via GE_DodgeCooldown, State.Invulnerable via ActivationOwnedTags, rotation snap, root motion, cancel window, anim-notify-driven invuln timing.
- Implemented Death flow — GA_Death: event-triggered via Event.Death, State.Dead persists, cancels all abilities, disables movement/collision, plays death montage, ragdoll, player respawn timer, enemy destroy timer + XP reward. Now also handles test dummy.
- Implemented Combat feedback — CombatFeedbackComponent: camera shake (light/heavy), hitstop (per-actor CustomTimeDilation with restore), Niagara VFX at impact, damage-type-aware SFX with pitch variation, crit layering. Added controller vibration with configurable intensity for normal/crit hits.
- Implemented Floating damage numbers — DamageNumberManagerComponent + DamageNumberWidget: global delegate subscription, world-to-screen projection, float + fade animation, damage type colors, crit scaling, max active limit.
- Implemented Combat test dummy — New AARPGCombatTestDummy: static target with GAS health, floating health bar, GA_HitReact + GA_Death, auto-respawn with full state reset (ragdoll undo, attribute re-init, collision restore), invulnerability toggle.

**Files:**
- `Source/PoF/Combat/ARPGCombatTestDummy.h`
- `Source/PoF/Combat/ARPGCombatTestDummy.cpp`
- `Source/PoF/Combat/CombatFeedbackComponent.h`
- `Source/PoF/Combat/CombatFeedbackComponent.cpp`
- `Source/PoF/AbilitySystem/GA_Death.cpp`

**Gotchas:**
- APlayerController::PlayDynamicForceFeedback() is the simplest way to trigger gamepad vibration — no ForceFeedbackEffect asset needed, just intensity + duration + channel booleans
- Test dummy respawn requires cancelling all GAS abilities first (to clear State.Dead from ActivationOwnedTags), then re-granting abilities and re-initializing attributes

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 24: Enemy AI — Controllers & Perception

**Module:** `arpg-enemy-ai`
**Description:** AI controller, enemy character, AI perception, behavior tree basics
**Duration:** 17.3min

**PoF Checklist Items to Run:**
- `ae-1`
- `ae-2`
- `ae-3`
- `ae-4`

**What Gets Built:**
- Implemented AARPGAIController — BT/BB/perception. Added target validation (filters dead/enemy actors), ClearTarget(), HomeLocation tracking, leash distance, aggro timeout config.
- Implemented AARPGEnemyCharacter — Already production quality — archetype system, health bar, XP scaling, death/BT-stop, ability granting. No changes needed.
- Implemented AI Perception — Sight + damage senses. Fixed OnPerceptionUpdated to validate targets (IsValidTarget rejects dead chars and other enemies).
- Implemented Behavior Tree — Fixed critical NodeMemory bugs in 3 tasks (ChargeToTarget, ExecuteAttackAbility, MaintainDistance). Added BTService_UpdateCombatState, BTDecorator_HealthBelow, BTTask_ReturnToHome, IsInAttackRange reads from BB.
- Implemented EQS queries — 7 EQS components: PatrolPoints, AttackPositions, CoverPositions generators + FlankAngle, PathExists, LineOfSight, ElevationAdvantage tests + TargetActor context. All pre-existing and production quality.

**Files:**
- `Source/PoF/AI/BTService_UpdateCombatState.h`
- `Source/PoF/AI/BTService_UpdateCombatState.cpp`
- `Source/PoF/AI/BTDecorator_HealthBelow.h`
- `Source/PoF/AI/BTDecorator_HealthBelow.cpp`
- `Source/PoF/AI/BTTask_ReturnToHome.h`
- `Source/PoF/AI/BTTask_ReturnToHome.cpp`
- `Source/PoF/AI/ARPGAIController.h`
- `Source/PoF/AI/ARPGAIController.cpp`
- `Source/PoF/AI/BTTask_ChargeToTarget.h`
- `Source/PoF/AI/BTTask_ChargeToTarget.cpp`
- `Source/PoF/AI/BTTask_ExecuteAttackAbility.h`
- `Source/PoF/AI/BTTask_ExecuteAttackAbility.cpp`
- `Source/PoF/AI/BTTask_MaintainDistance.h`
- `Source/PoF/AI/BTTask_MaintainDistance.cpp`
- `Source/PoF/AI/BTDecorator_IsInAttackRange.h`
- `Source/PoF/AI/BTDecorator_IsInAttackRange.cpp`

**Gotchas:**
- BT task nodes are shared singletons by default (bCreateNodeInstance=false) — per-instance state MUST use GetInstanceMemorySize()/NodeMemory, not member variables. Storing FDelegateHandle or float state as member vars corrupts when multiple enemies share the same BT.
- FPathFollowingRequestResult is forward-declared in AIController.h — include Navigation/PathFollowingComponent.h to use it (known UE 5.7 gotcha)
- WaitForMessage(OwnerComp, UBrainComponent::AIMessage_MoveFinished) is the cleanest way to wait for MoveTo completion in BT tasks without manual delegate plumbing

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 25: Enemy AI — EQS, Archetypes & Spawning

**Module:** `arpg-enemy-ai`
**Description:** EQS queries, enemy archetypes, enemy abilities, spawn system
**Duration:** 4.9min

**PoF Checklist Items to Run:**
- `ae-5`
- `ae-6`
- `ae-7`
- `ae-8`

**What Gets Built:**
- Implemented EQS queries — 6 EQS classes: PatrolPoints generator, AttackPositions ring generator (with inner ring), CoverPositions geometry-traced generator, FlankAngle/PathExists/LineOfSight/ElevationAdvantage tests, TargetActor context. Production-quality with nav mesh projection.
- Implemented Enemy archetypes — 3 archetypes (MeleeGrunt/RangedCaster/Brute) with full config: AttackRange, PreferredCombatDistance, RetreatDistance, AttackCooldown, PrimaryAbilityTag, ChargeSpeedMultiplier. BTDecorator_CheckArchetype for BT branching. Archetype defaults auto-applied on possess and written to BB.
- Implemented Enemy Gameplay Abilities — 3 enemy abilities: GA_EnemyMeleeAttack (front-arc overlap + montage), GA_EnemyRangedAttack (projectile spawning from BB target), GA_EnemyChargeAttack (motion warping + AoE). Fixed critical bug: BTTask_ExecuteAttackAbility was responding to ALL ability ends, not just the activated one.
- Implemented Spawn system — EnemySpawnPoint (single-point with respawn, difficulty scaling, nav-projected jitter) + SpawnVolume (wave spawning with trigger overlap, difficulty scaling from GameInstance). Added kill-based wave advancement option and per-wave difficulty escalation.

**Files:**
- `Source/PoF/AI/BTService_UpdateCombatState.h`
- `Source/PoF/AI/BTService_UpdateCombatState.cpp`
- `Source/PoF/AI/BTTask_ExecuteAttackAbility.h`
- `Source/PoF/AI/BTTask_ExecuteAttackAbility.cpp`
- `Source/PoF/AI/BTTask_MaintainDistance.h`
- `Source/PoF/AI/BTTask_MaintainDistance.cpp`
- `Source/PoF/Spawning/SpawnVolume.h`
- `Source/PoF/Spawning/SpawnVolume.cpp`

**Gotchas:**
- BTService nodes are also shared singletons like BTTask nodes — NoLOSTimer as a member variable corrupts when multiple enemies share the same BT. Must use GetInstanceMemorySize()/NodeMemory.
- ASC->OnAbilityEnded fires for ALL abilities ending on that ASC, not just the one you activated — must filter by FGameplayAbilitySpecHandle to avoid premature BT task completion
- WaitForMessage(OwnerComp, UBrainComponent::AIMessage_MoveFinished) is safer than lambda-based OnRequestFinished delegates in BT tasks — avoids dangling reference captures on OwnerComp

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 26: AI / NPC Behavior

**Module:** `ai-behavior`
**Description:** Behavior trees, EQS, perception, combat AI, scenario-based testing sandbox
**Duration:** 8.9min

**PoF Checklist Items to Run:**
- `ai-1`
- `ai-2`
- `ai-3`
- `ai-4`
- `ai-5`
- `ai-6`

**What Gets Built:**
- Implemented AI Controller base — Comprehensive AAIController with blackboard, BT, perception (sight+damage+hearing), leash/aggro timeout, target validation
- Implemented Behavior Tree system — 11 custom BT nodes: 5 tasks (ExecuteAttackAbility, MaintainDistance, FindPatrolPoint, FaceTarget, ReturnToHome, ChargeToTarget), 2 services (UpdateCombatState, SquadCoordination), 4 decorators (HasTarget, IsInAttackRange, HealthBelow, CheckArchetype). All use proper NodeMemory for shared singleton safety
- Implemented AI Perception setup — Added hearing sense (UAISenseConfig_Hearing) alongside existing sight and damage. Hearing only aggros when no current target, doesn't set LOS. Configurable HearingRange
- Implemented EQS queries — 6 EQS classes: 3 generators (PatrolPoints, AttackPositions, CoverPositions), 4 tests (FlankAngle, PathExists, LineOfSight, ElevationAdvantage), 1 context (TargetActor)
- Implemented Group AI coordination — New UARPGSquadManager with attack token system (limits simultaneous attackers), flanking position assignment (even angular spread), focus fire coordination, and coordinated retreat signaling. BTService_SquadCoordination integrates with BT
- Implemented AI debugging tools — New FGameplayDebuggerCategory_ARPG registered as ARPG_AI category. Displays AI state, archetype, health, target/LOS/distance, attack range/cooldown, active BT task, and GAS tags with color-coded output
- Implemented State Tree AI system — 3 State Tree nodes: FSTTask_ActivateAbility (fire-and-forget ability activation by tag), FSTCondition_HasTarget (target existence + alive check), FSTEvaluator_CombatData (ticking evaluator exposing target/distance/LOS/health/range as bindable output properties). StateTree + GameplayStateTree plugins enabled

**Files:**
- `Source/PoF/AI/ARPGSquadManager.h`
- `Source/PoF/AI/ARPGSquadManager.cpp`
- `Source/PoF/AI/BTService_SquadCoordination.h`
- `Source/PoF/AI/BTService_SquadCoordination.cpp`
- `Source/PoF/AI/Debug/GameplayDebuggerCategory_ARPG.h`
- `Source/PoF/AI/Debug/GameplayDebuggerCategory_ARPG.cpp`
- `Source/PoF/AI/StateTree/STTask_ActivateAbility.h`
- `Source/PoF/AI/StateTree/STTask_ActivateAbility.cpp`
- `Source/PoF/AI/StateTree/STCondition_HasTarget.h`
- `Source/PoF/AI/StateTree/STCondition_HasTarget.cpp`
- `Source/PoF/AI/StateTree/STEvaluator_CombatData.h`
- `Source/PoF/AI/StateTree/STEvaluator_CombatData.cpp`
- `Source/PoF/AI/ARPGAIController.h`
- `Source/PoF/AI/ARPGAIController.cpp`
- `Source/PoF/PoF.cpp`
- `Source/PoF/PoF.Build.cs`
- `PoF.uproject`

**Gotchas:**
- GameplayDebugger module in UE 5.7 is at Source/Runtime/GameplayDebugger, module name is 'GameplayDebugger'
- StateTree plugin modules: 'StateTreeModule' (runtime) and 'GameplayStateTreeModule' (AI integration)
- State Tree tasks/conditions/evaluators are USTRUCT-based (not UCLASS) with separate instance data structs
- FGameplayDebuggerCategory::SetDataPackReplication<T> requires T to have a Serialize(FArchive&) method
- UAISenseConfig_Hearing uses HearingRange (not SightRadius-style naming)

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 27: Materials — Core

**Module:** `materials`
**Description:** Master material, dynamic instances, MPC, material functions
**Duration:** 17.0min

**PoF Checklist Items to Run:**
- `mat-1`
- `mat-2`
- `mat-3`
- `mat-4`

**What Gets Built:**
- Implemented Master material config — UARPGMasterMaterialConfig data asset with scalar/vector/texture parameter definitions and CreateConfiguredInstance() for stamping MIDs with defaults
- Implemented Dynamic material instances — UARPGDynamicMaterialManager component with damage flash, dissolve, highlight, freeze/burn effects; auto-creates MIDs on BeginPlay, tick-driven with auto-disable when idle
- Implemented Material parameter collections — UARPGMaterialParameterCollectionManager world subsystem driving GlobalTime, WindDirection/Strength, GlobalTint, and Wetness via UKismetMaterialLibrary each tick
- Implemented Environment material set — UARPGEnvironmentMaterialSet data asset with FARPGSurfaceMaterialDef (PBR textures, scalar overrides, detail texture, macro variation) and CreateSurfaceInstance()/FindSurfaceByName()
- Implemented Material functions library — UARPGMaterialFunctionLibrary with UV ops (world-aligned, triplanar, rotate, pan, tile), noise (value, fBm, Voronoi), blending (height, angle, distance fade), and convenience helpers (batch params, CreateDynamicMaterialsForMesh, LerpScalarParameter)

**Files:**
- `Source/PoF/Materials/ARPGMasterMaterialConfig.h`
- `Source/PoF/Materials/ARPGMasterMaterialConfig.cpp`
- `Source/PoF/Materials/ARPGDynamicMaterialManager.h`
- `Source/PoF/Materials/ARPGDynamicMaterialManager.cpp`
- `Source/PoF/Materials/ARPGMaterialParameterCollectionManager.h`
- `Source/PoF/Materials/ARPGMaterialParameterCollectionManager.cpp`
- `Source/PoF/Materials/ARPGEnvironmentMaterialSet.h`
- `Source/PoF/Materials/ARPGEnvironmentMaterialSet.cpp`
- `Source/PoF/Materials/ARPGMaterialFunctionLibrary.h`
- `Source/PoF/Materials/ARPGMaterialFunctionLibrary.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 28: Materials — Advanced

**Module:** `materials`
**Description:** Post-process, HLSL custom nodes, layer system, Substrate
**Duration:** 6.1min

**PoF Checklist Items to Run:**
- `mat-5`
- `mat-6`

**What Gets Built:**
- Implemented Post-process materials — PostProcessChain component with blendable management, per-effect enable/disable, blend weight animation, scalar/vector parameter overrides. Attaches to PostProcessComponent or CameraComponent.
- Implemented Material layer system — UARPGMaterialLayer data asset with full PBR properties, UARPGMaterialLayerStack with base+N blend layers, 4 blend modes (linear, height, angle, vertex color), runtime blend weight control. Indexed parameter naming convention for master material compatibility.
- Implemented HLSL custom nodes — 12 HLSL snippets: dissolve edge, directional dissolve, cel shading, rim outline, hatching, caustics, hex grid, shield ripple, radial blur, chromatic aberration, vignette, gradient noise 3D, flow mapping. Ready for Custom node paste in material editor.
- Implemented Material performance optimization — World subsystem with shared MID pooling, distance-based material LOD (3 default tiers), quality tier management, material complexity profiling with instruction/texture/feature estimation, automatic stale entry cleanup.

**Files:**
- `Source/PoF/Materials/ARPGPostProcessChain.h`
- `Source/PoF/Materials/ARPGPostProcessChain.cpp`
- `Source/PoF/Materials/ARPGMaterialLayerSystem.h`
- `Source/PoF/Materials/ARPGMaterialLayerSystem.cpp`
- `Source/PoF/Materials/ARPGCustomMaterialExpression.h`
- `Source/PoF/Materials/ARPGCustomMaterialExpression.cpp`
- `Source/PoF/Materials/ARPGMaterialPerformanceManager.h`
- `Source/PoF/Materials/ARPGMaterialPerformanceManager.cpp`

**Gotchas:**
- UMaterialInterface::GetUsedTextures() with quality/featurelevel params is deprecated in 5.7 — use the parameterless overload
- TArray<T*>::Sort() with const T* lambda fails due to TDereferenceWrapper — use Algo::Sort() with Algo/Sort.h include

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 29: Animations

**Module:** `animations`
**Description:** AnimBP, locomotion states, montages, notifies
**Duration:** 3.3min

**PoF Checklist Items to Run:**
- `anim-1`
- `anim-2`
- `anim-3`
- `anim-4`
- `anim-5`
- `anim-6`
- `anim-7`

**What Gets Built:**
- Implemented Custom AnimInstance base — UARPGAnimInstance with full state machine (5 states), locomotion proxy vars, lean/sprint blend, montage tracking, root motion, upper-body blending. Production-quality.
- Implemented Blend Space setup — AnimAssetCommandlet creates BS1D_Locomotion (SpeedRatio or absolute) and BS_Locomotion (2D Direction x Speed) via FProperty reflection on protected BlendParameters.
- Implemented Animation state machine — EARPGAnimState enum (Locomotion/Attacking/Dodging/HitReact/Death) with priority-based ComputeAnimState(), transition interrupt rules, and state time tracking.
- Implemented Montage system — Commandlet creates AM_MeleeCombo (3 sections linked), AM_HeavyCombo (2 sections), AM_Dodge_* (4 dirs), AM_HitReact (5 directional variants), AM_Death. Section tracking in AnimInstance.
- Implemented Anim Notify framework — 10 notify classes: ComboWindow (instant+state), HitDetection (multi-point sweep), SpawnVFX (Niagara), PlaySound, CameraShake, FootstepEffect (surface-aware), InvulnerabilityFrames, GameplayEvent (new), RootMotionScale (new).
- Implemented Motion Warping setup — MotionWarping plugin dependency in Build.cs. Warp target setup handled by GA_MeleeAttack. AnimInstance tracks root motion velocity/direction.
- Implemented Root motion configuration — RootMotionFromMontagesOnly mode. bIsUsingRootMotion, bIsFullBodyMontage, RootMotionSpeed/Direction tracked per frame. New AnimNotifyState_RootMotionScale for per-window scaling.
- Implemented Animation retargeting — MixamoImportCommandlet with full Python pipeline: FBX import, IK Rig/Retargeter creation, batch retargeting, root motion extraction. IKRig+IKRigDeveloper module deps in PoFEditor.
- Implemented Motion Matching — ARPGMotionMatchingConfig data asset with FMotionMatchEntry catalog, cost weights, trajectory params, tag-based filtering. Ready for PoseSearch plugin integration.
- Implemented Blendshape sculpting — ARPGMorphTargetController component: named profile presets, smooth blending, additive stacking, health-driven damage morphs (fixed: now reads Health/MaxHealth from GAS AttributeSet instead of stamina placeholder).
- Implemented Editor Commandlet automation — AnimAssetCommandlet (blend spaces + montages + XP curve), MixamoImportCommandlet (Python retarget pipeline), MeshValidationCommandlet (mesh quality checks). All headless-compatible.

**Files:**
- `Source/PoF/Animation/AnimNotify_GameplayEvent.h`
- `Source/PoF/Animation/AnimNotify_GameplayEvent.cpp`
- `Source/PoF/Animation/AnimNotifyState_RootMotionScale.h`
- `Source/PoF/Animation/AnimNotifyState_RootMotionScale.cpp`
- `Source/PoF/Animation/ARPGMorphTargetController.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 30: Item Definitions & Inventory

**Module:** `arpg-inventory`
**Description:** Item definition, item instance, inventory component
**Duration:** 2.3min

**PoF Checklist Items to Run:**
- `ai-1`
- `ai-2`
- `ai-3`

**What Gets Built:**
- Implemented UARPGItemDefinition — Already comprehensive PrimaryDataAsset with name, icon, type, rarity, effects, tags, affix pool, equipment slots. Added Weight and RequiredLevel properties.
- Implemented UARPGItemInstance — Full runtime object with definition ref, stack count, item level, affixes, unique ID, replication, serialization, affix name decoration. No changes needed.
- Implemented UARPGInventoryComponent — Already had Add/Remove/Move/Sort/Find/Equip/Unequip/UseItem/Save/Load with GAS integration. Added SplitStack, MergeStacks, RemoveItemByDefinition, FindAllItemsByDefinition, HasItem, GetAllItems, GetAllEquippedItems, GetTotalWeight, WouldExceedWeight, MaxCarryWeight, and auto-merge on MoveItem for same-definition stacks.

**Files:**
- `Source/PoF/Inventory/ARPGItemDefinition.h`
- `Source/PoF/Inventory/ARPGInventoryComponent.h`
- `Source/PoF/Inventory/ARPGInventoryComponent.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 31: Equipment & Consumables

**Module:** `arpg-inventory`
**Description:** Equipment slots, equip/unequip GAS flow, consumables, affix system
**Duration:** 3.1min

**PoF Checklist Items to Run:**
- `ai-4`
- `ai-5`
- `ai-6`
- `ai-7`
- `ai-8`

**What Gets Built:**
- Implemented Equipment slot system — EEquipmentSlot enum (Weapon, OffHand, Helm, Chest, Gloves, Boots, Ring1, Ring2, Amulet) with TMap<EEquipmentSlot, UARPGItemInstance*> for equipped items. Added IsSlotOccupied() convenience query.
- Implemented Equip/unequip GAS flow — ApplyEquipEffect/RemoveEquipEffect applies base OnEquipEffect GE + per-affix GEs with SetByCaller magnitude. Swap-on-equip returns old item to inventory. Added RequiredLevel enforcement gate.
- Implemented Consumable usage — UseItem applies OnUseEffect via ASC, applies GE_PotionCooldown for cooldown gating, decrements stack and removes on empty. UseFirstConsumable for quick-use. Added RequiredLevel enforcement.
- Implemented Affix system — 5 rarity tiers with weighted random affix rolling, tier-gating (MinRarity), item-level scaling, reroll support. Added shared GetRarityColor() and GetRarityDisplayName() utilities on UARPGItemDefinition.
- Implemented Inventory operations — Full suite: AddItem (auto-stack), RemoveItem, MoveItem (auto-merge), SplitStack, MergeStacks, Sort, RemoveByDefinition, FindAll, HasItem, weight encumbrance, save/load with GE handle restoration.

**Files:**
- `Source/PoF/Inventory/ARPGItemDefinition.h`
- `Source/PoF/Inventory/ARPGItemDefinition.cpp`
- `Source/PoF/Inventory/ARPGInventoryComponent.h`
- `Source/PoF/Inventory/ARPGInventoryComponent.cpp`

**Gotchas:**
- UHT parameter shadowing: UFUNCTION params cannot share names with UPROPERTY members in the same UCLASS — use InRarity instead of Rarity to avoid 'shadowing is not allowed' UHT error

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 32: Loot Tables & Selection

**Module:** `arpg-loot`
**Description:** Loot table data, weighted random selection, world items
**Duration:** 5.5min

**PoF Checklist Items to Run:**
- `al-1`
- `al-2`
- `al-3`

**What Gets Built:**
- Implemented UARPGLootTable — Improved existing loot table with weighted rarity distribution (exponentially decreasing weights: 100/50/20/5/1) and RarityBonusMultiplier parameter for boss/archetype scaling
- Implemented Weighted random selection — RollWeightedRarity static method with configurable bonus multiplier that boosts higher tier weights via power scaling
- Implemented AARPGWorldItem — Dropped item actor with mesh from definition, rarity-colored point light (Uncommon+), vertical bob + rotation animation, Interactable tag for interaction system
- Implemented Loot drop on death — UARPGLootDropComponent auto-binds to OnEnemyDeath, supports configurable NumRolls and RarityBonusMultiplier. Built into AARPGEnemyCharacter with per-archetype defaults (Brute: 2 rolls, 2x rarity bonus)
- Implemented Item pickup — Player PerformInteraction() now handles WorldItem tag — calls TryPickup with weight check, partial pickup support, and inventory-full handling
- Implemented Chest/container actors — AARPGLootChest with lid open animation (pitch rotation), configurable loot table + rolls, optional respawn timer, Interactable+Chest tags for interaction system

**Files:**
- `Source/PoF/Loot/ARPGWorldItem.h`
- `Source/PoF/Loot/ARPGWorldItem.cpp`
- `Source/PoF/Loot/ARPGLootDropComponent.h`
- `Source/PoF/Loot/ARPGLootDropComponent.cpp`
- `Source/PoF/Loot/ARPGLootChest.h`
- `Source/PoF/Loot/ARPGLootChest.cpp`
- `Source/PoF/Inventory/ARPGLootTable.h`
- `Source/PoF/Inventory/ARPGLootTable.cpp`
- `Source/PoF/Character/ARPGEnemyCharacter.h`
- `Source/PoF/Character/ARPGEnemyCharacter.cpp`
- `Source/PoF/Player/ARPGPlayerCharacter.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 33: Loot Drops & Feedback

**Module:** `arpg-loot`
**Description:** Drop on death, pickup, visual feedback, chest/container actors
**Duration:** 4.1min

**PoF Checklist Items to Run:**
- `al-4`
- `al-5`
- `al-6`
- `al-7`
- `al-8`

**What Gets Built:**
- Implemented Loot visual feedback — Added: RolledRarity on instances (loot table now stores rolled rarity), rarity-scaled mesh size, rarity-scaled light radius, faster spin/bob for epic+ items, spawn launch arc physics, auto-pickup for common items on overlap, despawn timer with blink warning, pickup sound hook, pickup notification delegates (per-actor + static global for HUD), GetEffectiveRarity() helper

**Files:**
- `Source/PoF/Loot/ARPGWorldItem.h`
- `Source/PoF/Loot/ARPGWorldItem.cpp`
- `Source/PoF/Loot/ARPGLootDropComponent.cpp`
- `Source/PoF/Inventory/ARPGItemInstance.h`
- `Source/PoF/Inventory/ARPGItemInstance.cpp`
- `Source/PoF/Inventory/ARPGLootTable.cpp`

**Gotchas:**
- FOnItemPickedUpStatic (static multicast delegate) allows HUD to bind once globally without per-actor references — useful for pickup notification feeds

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 34: HUD & GAS Bindings

**Module:** `arpg-ui`
**Description:** Main HUD widget, GAS attribute binding, enemy health bars, ability cooldowns
**Duration:** 4.8min

**PoF Checklist Items to Run:**
- `au-1`
- `au-2`
- `au-3`
- `au-4`

**What Gets Built:**
- Implemented Main HUD widget — Health/mana bars with smooth interpolation and low-health pulse. Added stamina bar, XP bar with smooth fill, level display, and minimap placeholder border.
- Implemented GAS attribute binding — Real-time Health/MaxHealth/Mana/MaxMana via GetGameplayAttributeValueChangeDelegate. Stamina/XP polled from player character. Critical fix: ARPGHUD::BeginPlay now auto-binds all widgets to player ASC — previously widgets were created but never bound.
- Implemented Enemy health bars — Floating UWidgetComponent with fade-in on damage, fade-out after delay, death fade. Added level-colored text (red=danger, white=normal, gray=trivial) based on player/enemy level difference.
- Implemented Ability cooldown UI — 4 ability slots with icon, cooldown sweep overlay, mana cost, keybind label. Cooldowns polled from ASC each tick. Auto-refreshes from loadout on OnLoadoutChanged delegate.

**Files:**
- `Source/PoF/UI/ARPGHUDWidget.h`
- `Source/PoF/UI/ARPGHUDWidget.cpp`
- `Source/PoF/UI/ARPGHUD.h`
- `Source/PoF/UI/ARPGHUD.cpp`
- `Source/PoF/UI/EnemyHealthBarWidget.h`
- `Source/PoF/UI/EnemyHealthBarWidget.cpp`
- `Source/PoF/Character/ARPGEnemyCharacter.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 35: UI Screens

**Module:** `arpg-ui`
**Description:** Inventory screen, character stats, floating damage numbers, pause/settings
**Duration:** 6.2min

**PoF Checklist Items to Run:**
- `au-5`
- `au-6`
- `au-7`
- `au-8`

**What Gets Built:**
- Implemented Inventory screen — Grid inventory with drag-drop, rarity borders, tooltips, equipment panel. Fixed: bound to InventoryComponent from HUD, added right-click equip/use on inventory slots, right-click unequip on equipment slots, added I key toggle input action.
- Implemented Character stats screen — All GAS attributes displayed with base+bonus, damage reduction from armor, live updates via attribute change delegates. Fixed: bound to ASC from HUD, added C key toggle input action.
- Implemented Floating damage numbers — World-to-screen projected, colored by damage type (physical/fire/ice/lightning/heal), crit variant with larger font and CRIT! prefix. Fixed: positioning (SetPositionInViewport instead of SetRenderTranslation), added crit scale punch animation (1.5x→1.0x).
- Implemented Pause/settings menus — Pause menu with Resume/Settings/Save/Quit. Settings has Graphics/Audio/Controls tabs with resolution, quality, VSync, fullscreen, frame limit, volume sliders, mouse sensitivity, invert-Y, keybinding display. Added inventory/stats key entries to keybinding display.

**Files:**
- `Source/PoF/Player/ARPGPlayerController.h`
- `Source/PoF/Player/ARPGPlayerController.cpp`
- `Source/PoF/UI/ARPGHUD.cpp`
- `Source/PoF/UI/DamageNumberWidget.h`
- `Source/PoF/UI/DamageNumberWidget.cpp`
- `Source/PoF/UI/InventoryScreenWidget.h`
- `Source/PoF/UI/InventoryScreenWidget.cpp`
- `Source/PoF/UI/InventorySlotWidget.h`
- `Source/PoF/UI/InventorySlotWidget.cpp`
- `Source/PoF/UI/EquipmentSlotWidget.h`
- `Source/PoF/UI/EquipmentSlotWidget.cpp`
- `Source/PoF/UI/SettingsMenuWidget.cpp`

**Gotchas:**
- SetRenderTranslation offsets from the widget's layout anchor — for world-projected damage numbers use SetPositionInViewport with screen coordinates instead
- UMG widgets created in HUD::BeginPlay but never bound to data sources are inert — must call BindToInventory/BindToAbilitySystem from BindWidgetsToPlayer

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 36: Dialogue & NPC Interaction

**Module:** `dialogue-quests`
**Description:** Dialogue data assets, branching conversations, NPC interaction
**Duration:** 9.8min

**PoF Checklist Items to Run:**
- `dq-1`
- `dq-2`
- `dq-3`

**What Gets Built:**
- Implemented Dialogue data asset — UARPGDialogueTree (UPrimaryDataAsset) with nodes, choices, conditions (quest/level/tag/item), actions (give quest/complete/XP/tags), speaker portraits, one-shot support, validation
- Implemented Branching conversation system — UARPGDialogueComponent drives full conversation state: priority-ordered tree selection, condition evaluation (AND logic with invert), action execution on node entry and choice selection, auto-advance for choiceless nodes
- Implemented Conversation UI — UDialogueWidget with speaker portrait, name (gold), body text, dynamic choice buttons, continue button. Auto-binds to dialogue component events. Programmatic UMG layout.
- Implemented Quest tracker subsystem — UARPGQuestSubsystem (GameInstanceSubsystem) with full lifecycle: accept/complete/fail/abandon, objective progress tracking, ReportEvent for kill/collect/location/talk auto-matching, auto-complete, prerequisites, save/restore, delegates for UI
- Implemented Quest tracker HUD widget — UQuestTrackerWidget shows active quests with objective progress, auto-refreshes on quest/objective events, max 3 displayed with overflow indicator
- Implemented NPC interaction integration — PerformInteraction auto-detects UARPGDialogueComponent on target actors, starts dialogue, and shows HUD widget. Any actor with the component becomes an NPC.

**Files:**
- `Source/PoF/Dialogue/ARPGDialogueTypes.h`
- `Source/PoF/Dialogue/ARPGDialogueTree.h`
- `Source/PoF/Dialogue/ARPGDialogueComponent.h`
- `Source/PoF/Dialogue/ARPGDialogueComponent.cpp`
- `Source/PoF/Quest/ARPGQuestTypes.h`
- `Source/PoF/Quest/ARPGQuestDefinition.h`
- `Source/PoF/Quest/ARPGQuestSubsystem.h`
- `Source/PoF/Quest/ARPGQuestSubsystem.cpp`
- `Source/PoF/UI/DialogueWidget.h`
- `Source/PoF/UI/DialogueWidget.cpp`
- `Source/PoF/UI/QuestTrackerWidget.h`
- `Source/PoF/UI/QuestTrackerWidget.cpp`
- `Source/PoF/Player/ARPGPlayerCharacter.cpp`
- `Source/PoF/UI/ARPGHUD.h`
- `Source/PoF/UI/ARPGHUD.cpp`

**Gotchas:**
- UUserWidget has no SetRootWidget() method — use WidgetTree->RootWidget = widget to set programmatic root
- UFUNCTION cannot return const USTRUCT* — use a copy-out pattern (bool GetFoo(FName ID, FStruct& Out) const) for Blueprint exposure
- UARPGInventoryComponent methods take UARPGItemDefinition* not FName — dialogue item actions need Blueprint wiring or asset manager lookup

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 37: Quest System

**Module:** `dialogue-quests`
**Description:** Quest tracker, objectives, quest log UI
**Duration:** 3.0min

**PoF Checklist Items to Run:**
- `dq-4`
- `dq-5`
- `dq-6`

**What Gets Built:**
- Implemented Quest tracker — Full lifecycle with objective tracking, pinning, save/load, rewards, prerequisite chains. Fixed: OnQuestFailed now bound in tracker widget, pinned quest persisted in save/load.
- Implemented Quest objectives — Kill/Collect/Location/TalkTo/Manual + new Escort/Defend types. ReportEvent safely handles TMap mutation. Category-sorted quest log with section headers.
- Implemented Quest log UI — Active/Completed/Failed tabs, category headers, detail panel with objectives/rewards, pin/abandon buttons. Now sorts quests by category with visual separators.

**Files:**
- `Source/PoF/Quest/ARPGQuestTypes.h`
- `Source/PoF/Quest/ARPGQuestSubsystem.h`
- `Source/PoF/Quest/ARPGQuestSubsystem.cpp`
- `Source/PoF/Dialogue/ARPGDialogueTypes.h`
- `Source/PoF/Dialogue/ARPGDialogueComponent.cpp`
- `Source/PoF/UI/QuestTrackerWidget.h`
- `Source/PoF/UI/QuestTrackerWidget.cpp`
- `Source/PoF/UI/QuestLogWidget.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 38: XP & Leveling

**Module:** `arpg-progression`
**Description:** XP/level attributes, XP curve, XP on kill, level-up detection
**Duration:** 6.8min

**PoF Checklist Items to Run:**
- `ap-1`
- `ap-2`
- `ap-3`
- `ap-4`

**What Gets Built:**
- Implemented XP and Level attributes — CurrentXP, XPToNextLevel, CharacterLevel already in AttributeSet with proper clamping (level 1-50). Added IncomingXP meta attribute for clean XP pipeline.
- Implemented XP curve table — XPRequirementsCurveTable on CharacterBase + CalculateXPForLevel power curve (100 * level^1.5). Data-driven via curve table with code fallback.
- Implemented XP award on enemy death — Fixed critical bug: GE_AwardXP now targets IncomingXP meta attribute (was targeting CurrentXP directly, bypassing level-up detection). Enemy death → GE_AwardXP → PostGameplayEffectExecute → AddExperience → level-up. Level-scaled XP with archetype bonuses.
- Implemented Level-up detection — Fixed critical disconnect: AddExperience now syncs GAS CurrentXP/CharacterLevel/XPToNextLevel after level-ups. LevelUp fires GameplayCue.LevelUp, syncs all GAS attributes, awards skill+attribute points. Max level 50 enforced. Multi-level jump support.
- Implemented Active abilities — 5 abilities: GA_Fireball (projectile AoE), GA_GroundSlam (melee AoE+stun), GA_DashStrike (dash+sweep), GA_WarCry (self-buff), GA_ArcaneBeam (new channeled beam with mana drain, sphere sweep, lightning damage).

**Files:**
- `Source/PoF/AbilitySystem/GA_ArcaneBeam.h`
- `Source/PoF/AbilitySystem/GA_ArcaneBeam.cpp`
- `Source/PoF/AbilitySystem/Effects/GE_Cooldown_ArcaneBeam.h`
- `Source/PoF/AbilitySystem/Effects/GE_Cooldown_ArcaneBeam.cpp`
- `Source/PoF/AbilitySystem/ARPGAttributeSet.h`
- `Source/PoF/AbilitySystem/ARPGAttributeSet.cpp`
- `Source/PoF/AbilitySystem/Effects/GE_AwardXP.cpp`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.h`
- `Source/PoF/AbilitySystem/ARPGGameplayTags.cpp`
- `Source/PoF/Player/ARPGPlayerCharacter.cpp`

**Gotchas:**
- GAS meta-attribute pattern (IncomingXP like IncomingDamage) is the clean way to bridge GE-based XP awards to local game logic — avoids dual-tracking between GAS attributes and local state
- ApplyModToAttribute with EGameplayModOp::Override does NOT trigger PostGameplayEffectExecute, only attribute change delegates — safe for syncing GAS back from local authoritative state without recursion

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 39: Abilities & Skill Trees

**Module:** `arpg-progression`
**Description:** Active abilities, unlock system, attribute points, ability loadout
**Duration:** 7.7min

**PoF Checklist Items to Run:**
- `ap-5`
- `ap-6`
- `ap-7`
- `ap-8`

**What Gets Built:**
- Implemented Active abilities — Fireball, GroundSlam, DashStrike, WarCry already implemented with full GE integration, montage-driven flow, cursor aim, AoE/projectile systems
- Implemented Ability unlock system — Data-table-driven unlock/upgrade component with skill point spending, rank tracking, per-rank bonuses. New AbilityUnlockWidget UI with learn/upgrade buttons, rank display, requirement checks, and skill point counter. Auto-assigns newly learned abilities to first empty hotbar slot.
- Implemented Attribute point allocation — Str/Dex/Int allocation on player character with permanent GAS attribute modification. AttributeAllocationWidget UI now properly bound to player (fixed missing BindToPlayer call in HUD). Live-updating stat previews.
- Implemented Ability loadout — 4-slot hotbar system on player character with AssignAbilityToSlot/TryActivateAbilitySlot. AbilityBarWidget displays icons, mana cost, cooldown sweep. Controller has 1-4 key and D-pad bindings. Loadout change auto-refreshes ability bar via delegate.

**Files:**
- `Source/PoF/UI/AbilityUnlockWidget.h`
- `Source/PoF/UI/AbilityUnlockWidget.cpp`
- `Source/PoF/UI/ARPGHUD.h`
- `Source/PoF/UI/ARPGHUD.cpp`
- `Source/PoF/Player/ARPGPlayerController.h`
- `Source/PoF/Player/ARPGPlayerController.cpp`
- `Source/PoF/AbilitySystem/ARPGAbilityUnlockComponent.h`
- `Source/PoF/AbilitySystem/ARPGAbilityUnlockComponent.cpp`
- `Source/PoF/Framework/ARPGSaveGame.h`

**Gotchas:**
- UButton::OnClicked is a dynamic multicast delegate (no lambda support) — use a shared UFUNCTION + IsHovered() TMap lookup pattern for programmatically-created button arrays
- BindWidget meta on UUserWidget requires a Blueprint subclass to provide the slot — programmatic children are added into those containers, not replacing them

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 40: World Zones & Blockouts

**Module:** `arpg-world`
**Description:** Zone layout design, blockout levels, NavMesh coverage
**Duration:** 6.0min

**PoF Checklist Items to Run:**
- `aw-1`
- `aw-2`
- `aw-3`

**What Gets Built:**
- Implemented Zone layout design — Data-driven UARPGZoneDefinition with 5 difficulty tiers (Safe/Easy/Medium/Hard/Boss), encounter groups, zone connections graph, level gating, quest gating, fast travel, streaming support
- Implemented Blockout levels — AARPGBlockoutRoom actor with configurable dimensions, room purpose enum (Corridor/SmallRoom/LargeRoom/Arena/Hub/Transition), auto-scaling floor mesh, bounds indicator, scale reference comments
- Implemented NavMesh coverage — AARPGNavModifierVolume with 4 modifier types (Avoidance/Preferred/Blocked/Chokepoint), UNavModifierComponent integration, editor property change updates. Spawn points already project to NavMesh.

**Files:**
- `Source/PoF/World/ARPGZoneDefinition.h`
- `Source/PoF/World/ARPGZoneDefinition.cpp`
- `Source/PoF/World/ARPGZoneManager.h`
- `Source/PoF/World/ARPGZoneManager.cpp`
- `Source/PoF/World/ARPGZoneVolume.h`
- `Source/PoF/World/ARPGZoneVolume.cpp`
- `Source/PoF/World/ARPGZoneTransition.h`
- `Source/PoF/World/ARPGZoneTransition.cpp`
- `Source/PoF/World/ARPGNavModifierVolume.h`
- `Source/PoF/World/ARPGNavModifierVolume.cpp`
- `Source/PoF/World/ARPGEncounterVolume.h`
- `Source/PoF/World/ARPGEncounterVolume.cpp`
- `Source/PoF/World/ARPGBlockoutRoom.h`
- `Source/PoF/World/ARPGBlockoutRoom.cpp`
- `Source/PoF/World/ARPGPlayerStart.h`
- `Source/PoF/World/ARPGPlayerStart.cpp`
- `Source/PoF/Spawning/EnemySpawnPoint.h`
- `Source/PoF/Spawning/EnemySpawnPoint.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 41: Encounters & Bosses

**Module:** `arpg-world`
**Description:** Enemy spawn placement, boss encounter, environmental hazards
**Duration:** 7.8min

**PoF Checklist Items to Run:**
- `aw-4`
- `aw-5`
- `aw-6`

**What Gets Built:**
- Implemented Enemy spawn placement — EnemySpawnPoint + SpawnVolume + EncounterVolume already solid with zone-aware difficulty arcs, wave system, respawn, nav-mesh projection
- Implemented Interactive world objects — Added ARPGInteractableBase (trigger + prompt), ARPGLockedGate (quest/item gated, slide/rotate open). Existing: LootChest, PhysicsDoor, PhysicsLever, DestructibleActor
- Implemented Boss encounter — ARPGBossCharacter with multi-phase health thresholds, per-phase abilities/buffs/cooldown overrides, transition invulnerability. ARPGBossEncounter arena manager with fog walls, spawn/pre-placed boss, retrigger. BossHealthBarWidget with phase indicator. BTDecorator_BossPhase + BTTask_BossSpecialAttack for BT branching
- Implemented Environmental hazards — ARPGEnvironmentalHazard with fire/poison/spike/ice types, periodic GE damage via SetByCaller, one-shot trap mode with rearm, cycle on/off with phase offset, damage type auto-mapping from hazard type

**Files:**
- `Source/PoF/World/ARPGInteractableBase.h`
- `Source/PoF/World/ARPGInteractableBase.cpp`
- `Source/PoF/World/ARPGLockedGate.h`
- `Source/PoF/World/ARPGLockedGate.cpp`
- `Source/PoF/World/ARPGEnvironmentalHazard.h`
- `Source/PoF/World/ARPGEnvironmentalHazard.cpp`
- `Source/PoF/Character/ARPGBossCharacter.h`
- `Source/PoF/Character/ARPGBossCharacter.cpp`
- `Source/PoF/World/ARPGBossEncounter.h`
- `Source/PoF/World/ARPGBossEncounter.cpp`
- `Source/PoF/UI/BossHealthBarWidget.h`
- `Source/PoF/UI/BossHealthBarWidget.cpp`
- `Source/PoF/AI/BTDecorator_BossPhase.h`
- `Source/PoF/AI/BTDecorator_BossPhase.cpp`
- `Source/PoF/AI/BTTask_BossSpecialAttack.h`
- `Source/PoF/AI/BTTask_BossSpecialAttack.cpp`

**Gotchas:**
- UWidgetTree::ConstructWidget<T>() in UE 5.7 takes (TSubclassOf<T>, FName) — cannot pass TEXT() directly as first arg, must pass StaticClass() + FName
- UAbilitySystemComponent::GetGameplayAttributeValue() returns float and takes a bool& bFound output param — cannot assign to two separate float variables
- TObjectPtr<USphereComponent> implicit conversion to USceneComponent* fails without the full type definition — include the component header in the cpp

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 42: Interactive World

**Module:** `arpg-world`
**Description:** Interactive world objects, zone transitions
**Duration:** 4.8min

**PoF Checklist Items to Run:**
- `aw-7`
- `aw-8`

**What Gets Built:**
- Implemented Environmental hazards — Fire/poison/spike/ice hazards with GE damage, cycling, one-shot, zone difficulty scaling, AI avoidance, debuff support. Fixed: pre-existing overlap detection on BeginPlay, debuff spam prevention via actor tracking.
- Implemented NavMesh and AI pathing — NavModifier volumes with Avoidance/Preferred/Blocked/Chokepoint types, custom NavArea classes (Hazard, LowCost, Chokepoint), hazard auto-avoidance via NavArea_Hazard.
- Implemented Zone transitions — Level streaming portals with screen fade, input freezing, cooldown, quest/level gating, spawn point teleport. Fixed: null-check on GetGameInstance().
- Implemented Boss encounters — Multi-phase boss arena with fog walls, player death reset, retrigger support. Fixed: player death delegate leak causing double-binding on retrigger.
- Implemented Zone volume tracking — Zone volumes with overlap detection and ZoneManager integration. Fixed: null-check on GetGameInstance() in overlap callback.

**Files:**
- `Source/PoF/World/ARPGBossEncounter.cpp`
- `Source/PoF/World/ARPGZoneTransition.cpp`
- `Source/PoF/World/ARPGZoneVolume.cpp`
- `Source/PoF/World/ARPGEnvironmentalHazard.h`
- `Source/PoF/World/ARPGEnvironmentalHazard.cpp`

**Gotchas:**
- AARPGCharacterBase::InitializeAttributes() is protected — cannot call from external actors like BossEncounter
- Dynamic delegate RemoveDynamic must be called in ALL exit paths (not just CompleteEncounter but also OnPlayerDied) to prevent double-binding on retrigger
- GetOverlappingActors() on BeginPlay is needed for hazards too — actors spawned inside a hazard volume won't generate overlap events

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 43: Level Design — Core

**Module:** `level-design`
**Description:** Blockout geometry, spawn points, level streaming, zone transitions
**Duration:** 5.3min

**PoF Checklist Items to Run:**
- `ld-1`
- `ld-2`
- `ld-3`
- `ld-4`

**What Gets Built:**
- Implemented Spawn point placement — EnemySpawnPoint with respawn/difficulty/zone integration, SpawnVolume with wave-based spawning, difficulty scaling, retrigger support. Added EndPlay timer cleanup and max concurrent enemy cap.
- Implemented Level streaming setup — ARPGZoneManager handles streaming load/unload with polling, timeout, fade transitions. ZoneDefinition data assets drive zone graph. Already mature from prior iterations.
- Implemented Zone transition system — ARPGZoneTransition with overlap/interact modes, level/quest gating, cooldown protection, screen fade. ARPGPlayerStart for zone-aware spawn targets. ARPGZoneVolume for spatial zone tracking.

**Files:**
- `Source/PoF/World/ARPGBlockoutRoom.h`
- `Source/PoF/World/ARPGBlockoutRoom.cpp`
- `Source/PoF/Spawning/SpawnVolume.h`
- `Source/PoF/Spawning/SpawnVolume.cpp`
- `Source/PoF/Spawning/EnemySpawnPoint.h`
- `Source/PoF/Spawning/EnemySpawnPoint.cpp`
- `Source/PoF/World/ARPGEncounterVolume.cpp`

**Gotchas:**
- AARPGEncounterVolume inherits from ASpawnVolume — adding UPROPERTY to parent that child already declares causes UHT shadowing error
- SpawnVolume's SpawnNextInWave is not virtual — max enemy cap must be enforced in the base class for subclasses to benefit

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 44: Level Design — Advanced

**Module:** `level-design`
**Description:** Environmental hazards, NavMesh, procedural gen, PCG, vegetation
**Duration:** 8.6min

**PoF Checklist Items to Run:**
- `ld-5`
- `ld-6`
- `ld-7`

**What Gets Built:**
- Implemented Procedural level generation — Room/tile-based procedural layout with template pool, weighted selection, connection slot matching, overlap avoidance, and BlockoutRoom spawning. Graph-based room placement with configurable depth, corridor lengths, and seeded RNG.
- Implemented PCG graph setup — HISM-based scatter system with biome definitions (ARPGBiomeDefinition data asset + ARPGVegetationScatter actor). Supports density, slope/altitude rules, min spacing, surface alignment, exclusion zones, and scatter layers for props/debris. Uses HierarchicalInstancedStaticMesh for efficient rendering.
- Implemented Procedural Vegetation (PVE) — Biome definition data asset with per-vegetation-type slope, altitude, density, spacing, scale, alignment, and NavMesh blocking rules. VegetationScatter actor performs line traces to find ground surface, validates placement rules, and uses HISM components for Nanite-compatible instanced rendering.
- Implemented Environmental storytelling — ARPGEnvironmentalProp actor groups static mesh props, ground decals, and accent point lights into placeable story clusters. Story type classification (Abandoned, BattleScar, Ritual, PathMarker, Warning, TreasureHint, Atmosphere) for design documentation.
- Implemented Encounter arenas — ARPGEncounterArena discovers cover points within bounds, builds tactical point data with elevation tiers (Ground/Elevated/High), flanking detection, and cover queries. ARPGCoverPoint provides claim/release, peek positions, and directional coverage checks for AI tactical behavior.
- Implemented Room templates — ARPGRoomTemplate data asset with connection slots (directional, width-matched), cover point definitions, prop placements, encounter settings, and generation weight/count constraints.

**Files:**
- `Source/PoF/LevelDesign/ARPGRoomTemplate.h`
- `Source/PoF/LevelDesign/ARPGRoomTemplate.cpp`
- `Source/PoF/LevelDesign/ARPGLevelGenerator.h`
- `Source/PoF/LevelDesign/ARPGLevelGenerator.cpp`
- `Source/PoF/LevelDesign/ARPGBiomeDefinition.h`
- `Source/PoF/LevelDesign/ARPGBiomeDefinition.cpp`
- `Source/PoF/LevelDesign/ARPGVegetationScatter.h`
- `Source/PoF/LevelDesign/ARPGVegetationScatter.cpp`
- `Source/PoF/LevelDesign/ARPGCoverPoint.h`
- `Source/PoF/LevelDesign/ARPGCoverPoint.cpp`
- `Source/PoF/LevelDesign/ARPGEncounterArena.h`
- `Source/PoF/LevelDesign/ARPGEncounterArena.cpp`
- `Source/PoF/LevelDesign/ARPGEnvironmentalProp.h`
- `Source/PoF/LevelDesign/ARPGEnvironmentalProp.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 45: Multiplayer — Replication

**Module:** `multiplayer`
**Description:** Replicated properties, RPC framework, GameState replication
**Duration:** 6.2min

**PoF Checklist Items to Run:**
- `mp-1`
- `mp-2`
- `mp-3`
- `mp-4`

**What Gets Built:**
- Implemented Replicated properties — Added DOREPLIFETIME_CONDITION with COND_SkipOwner for bandwidth optimization, added MaxStamina to vitals, fixed SnapshotVitals to read from GAS attributes (works for all character types, not just player), disabled client tick
- Implemented RPC framework — Fixed Multicast_AbilityCosmetics to use GameplayCue instead of activating GAS abilities on simulated proxies (was causing gameplay on clients), added autonomous proxy skip
- Implemented Iris replication system — Added FillReplicationParams overrides on GameState (AlwaysRelevant) and MultiplayerGameMode (AlwaysRelevant) using UE 5.7 Iris beta API. Character spatial filtering deferred to per-actor config.

**Files:**
- `Source/PoF/Multiplayer/ARPGReplicationTypes.h`
- `Source/PoF/Multiplayer/ARPGReplicationComponent.cpp`
- `Source/PoF/Multiplayer/ARPGGameState.h`
- `Source/PoF/Multiplayer/ARPGGameState.cpp`
- `Source/PoF/Multiplayer/ARPGMultiplayerGameMode.h`
- `Source/PoF/Multiplayer/ARPGMultiplayerGameMode.cpp`

**Gotchas:**
- BeginReplication() is deprecated in UE 5.7 — override FillReplicationParams() for per-actor Iris config and OnReplicationStartedForIris() for post-start setup
- FActorReplicationParams::AlwaysRelevant bypasses spatial filtering for actors that must replicate to all connections (GameState, GameMode)
- FActorReplicationParams lives in Net/Iris/ReplicationSystem/EngineReplicationBridge.h
- UActorComponent has OnReplicationStartedForIris but not FillReplicationParams — filtering is actor-level only

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 46: Multiplayer — Sessions & Prediction

**Module:** `multiplayer`
**Description:** Session management, network prediction, net relevancy
**Duration:** 6.0min

**PoF Checklist Items to Run:**
- `mp-5`
- `mp-6`
- `mp-7`

**What Gets Built:**
- Implemented Client-side prediction — Ability prediction with server confirmation/rejection, position reconciliation with error tolerance and smooth/hard snap correction, input buffering at 30Hz, latency estimation from RTT samples
- Implemented Interest management — 5-tier relevancy system (AlwaysRelevant/Critical/Standard/Background/Dormant), auto-tier by distance bands, combat relevancy elevation, dormancy support, configurable per-tier update freq/priority/cull distance. Uses UE 5.7 SetNetUpdateFrequency/SetNetCullDistanceSquared APIs
- Implemented Session management — Enhanced with session state machine (8 states), server travel support, rejoin/reconnection with configurable attempts and delay, network/travel failure handling with auto-reconnect for recoverable errors, cached connection strings for rejoin

**Files:**
- `Source/PoF/Multiplayer/ARPGNetPredictionComponent.h`
- `Source/PoF/Multiplayer/ARPGNetPredictionComponent.cpp`
- `Source/PoF/Multiplayer/ARPGInterestManager.h`
- `Source/PoF/Multiplayer/ARPGInterestManager.cpp`
- `Source/PoF/Multiplayer/ARPGSessionSubsystem.h`
- `Source/PoF/Multiplayer/ARPGSessionSubsystem.cpp`

**Gotchas:**
- AActor::NetUpdateFrequency is deprecated in UE 5.7 — use SetNetUpdateFrequency()/GetNetUpdateFrequency() instead
- AActor::NetCullDistanceSquared is deprecated in UE 5.7 — use SetNetCullDistanceSquared()/GetNetCullDistanceSquared() instead
- UFUNCTION returning uint32 is not supported by Blueprint — use int32 for BP-exposed prediction sequence numbers

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 47: Save/Load Core

**Module:** `arpg-save`
**Description:** USaveGame, custom serialization, save and load functions
**Duration:** 3.6min

**PoF Checklist Items to Run:**
- `as-1`
- `as-2`
- `as-3`
- `as-4`

**What Gets Built:**
- Implemented UARPGSaveGame — Complete save data structure with progression, inventory (legacy + rich v2), quest states, world state, equipment slots, settings. Versioned at v3.
- Implemented Custom serialization — FARPGItemSaveData with affixes/rarity/level already existed. Added FARPGWorldState with TSet<FName> for bosses, containers, doors, zones, flags. GatherSaveData/RestoreFromSaveData on InventoryComponent bridges live items to save format.
- Implemented Save versioning — Version-stepped migration chain v1→v2 (legacy inventory to rich items) and v2→v3 (adds quest + world state). Future-version rejection in ValidateSaveGame. Referential integrity check on EquippedSlots.
- Implemented Save function — GatherStateFromWorld() pulls from GameInstance (progression), InventoryComponent (items + equipment), and QuestSubsystem (quest states + pinned quest). World state is always in cache. BuildSaveGameFromCache serializes all.
- Implemented Load function — RestoreStateToWorld() pushes progression to GameInstance, quest states to QuestSubsystem. Inventory deferred to BeginPlay timing via cached accessors. Validation + migration run before apply.
- Implemented Auto-save — Timer-based auto-save (configurable interval, default 5min) + zone transition auto-save via OnPostMapChange delegate. Separate AutoSave slot. Toggle on/off.
- Implemented World state persistence — FARPGWorldState tracks defeated bosses, opened containers, unlocked doors, discovered zones, and generic world flags. Blueprint-callable mark/query API on subsystem.

**Files:**
- `Source/PoF/Framework/ARPGSaveGame.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.cpp`

**Gotchas:**
- GameInstanceSubsystem::Deinitialize must clear timer handles since the world may be torn down concurrently
- DECLARE_DYNAMIC_MULTICAST_DELEGATE does not support AddLambda — must use AddDynamic with a UFUNCTION for dynamic delegates
- Inventory restoration after load must be deferred to BeginPlay timing — the pawn may not exist yet when LoadFromSlot runs

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 48: Save Features

**Module:** `arpg-save`
**Description:** Auto-save, slot system, save versioning
**Duration:** 4.0min

**PoF Checklist Items to Run:**
- `as-5`
- `as-6`
- `as-7`
- `as-8`

**What Gets Built:**
- Implemented Save function — Full state gathering from ASC/inventory/quests/world into SaveGame, with sync and async paths
- Implemented Load function — Validates, migrates (v1→v4), restores all systems. Deferred inventory restore for BeginPlay timing.
- Implemented Auto-save — Added async auto-save on zone transitions (existing), boss kills (new OnBossDefeated binding), level-ups (new OnPlayerLevelUp binding), and timer. All use SaveToSlotAsync.
- Implemented Save slot system — 3 manual slots (SaveSlot_0-2) + 1 auto-save slot. FARPGSaveSlotInfo struct with character name, level, playtime, zone, timestamp, isAutoSave. GetAllSlotInfo/GetSlotInfo/SaveToManualSlot/LoadFromManualSlot/LoadFromAutoSave API.
- Implemented Save versioning — Stepped migration chain v1→v2→v3→v4. v4 adds CharacterName to progression. Future versions add new case blocks. ValidateSaveGame rejects unknown future versions.

**Files:**
- `Source/PoF/Framework/ARPGSaveGame.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 49: Save / Load

**Module:** `save-load`
**Description:** USaveGame, auto-save, slot system
**Duration:** 4.5min

**PoF Checklist Items to Run:**
- `sl-1`
- `sl-2`
- `sl-3`
- `sl-4`
- `sl-5`
- `sl-6`

**What Gets Built:**
- Implemented USaveGame subclass — UARPGSaveGame with progression, inventory (legacy+rich), quests, world state, settings. Well-structured with clear field comments.
- Implemented Save and load functions — Sync/async save, load with validation, migration, state gather/restore from all live systems. Added save-in-progress guard to prevent concurrent async saves.
- Implemented Auto-save system — Timer-based (5min default) + event-triggered (zone transition, level-up, boss kill). Added 10s throttle cooldown to prevent rapid-fire from simultaneous events.
- Implemented Save slot UI — New SaveSlotWidget with Save/Load mode toggle, 3 manual + 1 auto-save slots, metadata display, overwrite/delete confirmation overlay, auto-refresh on save/load completion. Integrated into HUD with ToggleSaveScreen.
- Implemented Save versioning — v1→v2→v3→v4 stepped migration chain. Validates integrity (level, health, equipped slot referential consistency). Rejects future-version saves gracefully.
- Implemented Custom serialization — UPROPERTY-based serialization covers all types including TMap, TSet, TArray of USTRUCTs. FArchive custom serialize not needed — UE handles these natively.

**Files:**
- `Source/PoF/UI/SaveSlotWidget.h`
- `Source/PoF/UI/SaveSlotWidget.cpp`
- `Source/PoF/Framework/ARPGSaveGame.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.h`
- `Source/PoF/Framework/ARPGSaveSubsystem.cpp`
- `Source/PoF/UI/ARPGHUD.h`
- `Source/PoF/UI/ARPGHUD.cpp`

**Gotchas:**
- UHorizontalBox::AddChildToHorizontalBox returns UHorizontalBoxSlot* (not lvalue reference) — cannot bind to auto&
- Include Components/HorizontalBoxSlot.h for SetSize/SetHorizontalAlignment on HorizontalBox slots

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 50: Debug & Logging

**Module:** `arpg-polish`
**Description:** Structured logging, debug draw helpers, console commands
**Duration:** 4.5min

**PoF Checklist Items to Run:**
- `apl-1`
- `apl-2`
- `apl-3`

**What Gets Built:**
- Implemented Structured logging — 10 custom log categories covering all systems: Combat, AI, Inventory, Ability, Quest, World, Save, UI, Audio, Network
- Implemented Debug draw helpers — Blueprint-callable static library with master toggle CVar (ARPG.Debug.Draw), 8 draw functions: weapon trace, ability range, hit impact, AI path, perception range, attack cone, spawn point, box volume, overlap sphere. ENABLE_DRAW_DEBUG guards for shipping.
- Implemented Debug console commands — UCheatManager subclass with 8 commands: ARPGGod (invulnerable+mana), ARPGSetLevel, ARPGGiveXP, ARPGHeal, ARPGKillAll, ARPGSpawnEnemy (BP path+level), ARPGDamageTarget (crosshair trace), ARPGShowStats. Registered on PlayerController via CheatClass.

**Files:**
- `Source/PoF/Debug/ARPGLogCategories.h`
- `Source/PoF/Debug/ARPGLogCategories.cpp`
- `Source/PoF/Debug/ARPGDebugDrawHelpers.h`
- `Source/PoF/Debug/ARPGDebugDrawHelpers.cpp`
- `Source/PoF/Debug/ARPGCheatManager.h`
- `Source/PoF/Debug/ARPGCheatManager.cpp`
- `Source/PoF/Player/ARPGPlayerController.cpp`

**Gotchas:**
- FColor::Orange and other FColor constants cannot be used as UHT default parameters — use FLinearColor for Blueprint-exposed color params
- GetPlayerViewPoint returns FRotator not FVector for the rotation output — use ViewRot.Vector() for direction

**Verification:** PASS git-clean, PASS source-exists

---

### Phase 51: Performance Optimization

**Module:** `arpg-polish`
**Description:** Object pooling, tick optimization, async asset loading
**Duration:** 3.8min

**PoF Checklist Items to Run:**
- `apl-4`
- `apl-5`
- `apl-6`
- `apl-7`
- `apl-8`

**What Gets Built:**
- Implemented Object pooling — UARPGObjectPool GameInstanceSubsystem with Acquire/Release/PreWarm/SetMaxPoolSize, weak-pointer cleanup, deactivation/activation lifecycle
- Implemented Tick optimization — UARPGTickOptimizer WorldSubsystem with distance-based tick interval (near/mid/far tiers), plus direct fixes: CombatFeedbackComponent tick-on-demand, LootChest tick-on-animation-only, WorldItem auto-registers with optimizer
- Implemented Async asset loading — UARPGAsyncLoader GameInstanceSubsystem wrapping FStreamableManager with single/batch async loading, priority support, handle retention, and sync fallback

**Files:**
- `Source/PoF/Performance/ARPGObjectPool.h`
- `Source/PoF/Performance/ARPGObjectPool.cpp`
- `Source/PoF/Performance/ARPGTickOptimizer.h`
- `Source/PoF/Performance/ARPGTickOptimizer.cpp`
- `Source/PoF/Performance/ARPGAsyncLoader.h`
- `Source/PoF/Performance/ARPGAsyncLoader.cpp`
- `Source/PoF/Combat/CombatFeedbackComponent.cpp`
- `Source/PoF/Loot/ARPGLootChest.cpp`
- `Source/PoF/Loot/ARPGWorldItem.cpp`


**Verification:** PASS git-clean, PASS source-exists

---

### Phase 52: Packaging

**Module:** `packaging`
**Description:** Build config, automation, versioning
**Duration:** 3.3min

**PoF Checklist Items to Run:**
- `pkg-1`
- `pkg-2`
- `pkg-3`
- `pkg-4`
- `pkg-5`

**What Gets Built:**
- Implemented Build configuration — Game target has Shipping/Development defines (POF_SHIPPING, POF_DEV_TOOLS), logging disabled in Shipping. Editor target left clean to share build environment.
- Implemented Cooking settings — DefaultGame.ini configured with pak compression, skip editor content, share material shader code, deterministic shader order, directories to always/never cook, async loading, GC tuning.
- Implemented Platform configuration — Win64 DX12 default, staging directory, renderer settings. Console/mobile deferred until SDK availability.
- Implemented Build automation — BuildPackage.bat with development/shipping args, auto build-number increment, compile, content validation, UAT BuildCookRun with pak/compress/stage/archive.
- Implemented Version numbering system — UARPGBuildVersionSubsystem reads major.minor.patch.build from DefaultGame.ini, auto-incremented by build script. MainMenuWidget pulls live version string from subsystem.
- Implemented Content validation — UContentValidationCommandlet checks missing asset references and oversized textures, integrated into build pipeline.

**Files:**
- `Source/PoF/Framework/ARPGBuildVersion.h`
- `Source/PoF/Framework/ARPGBuildVersion.cpp`
- `Source/PoFEditor/ContentValidationCommandlet.h`
- `Source/PoFEditor/ContentValidationCommandlet.cpp`
- `BuildPackage.bat`
- `Source/PoF.Target.cs`
- `Source/PoFEditor.Target.cs`
- `Source/PoF/UI/MainMenuWidget.cpp`
- `Config/DefaultGame.ini`

**Gotchas:**
- Editor targets that share build environment with UnrealEditor cannot add GlobalDefinitions — UBT rejects property modifications on shared targets. Only Game/Server targets with unique build environments can use custom defines.
- GConfig->GetInt/GetString with GGameIni reads from DefaultGame.ini section paths matching the UCLASS path

**Verification:** PASS git-clean, PASS source-exists

---

## Accumulated Learnings

- UCharacterMovementComponent::PerchRadiusThreshold controls how close to ledge edges the character can stand
- bMaintainHorizontalGroundVelocity is key for consistent speed on ramps/slopes
- APlayerController::InputKey(FInputKeyParams) is final in UE 5.7 — override InputKey(const FInputKeyEventArgs&) instead, include InputKeyEventArgs.h. Use Params.IsGamepad() for device detection.
- UHT cannot parse TArray<AActor*>() as a default parameter — use AutoCreateRefTerm meta specifier and move the param before defaulted params
- UPhysicalMaterial already has a SurfaceType member — custom subclasses must use a different name (e.g. GameSurfaceType)
- FVector_NetQuantize (from HitResult.ImpactPoint) cannot be used in ternary with FVector — explicit FVector() cast needed
- ChaosSolverEngine, GeometryCollectionEngine, FieldSystemEngine modules needed for Chaos destruction in UE 5.7
- Virtual IsDead() on base character class enables AnimInstance death state without coupling to specific subclass bIsAlive flags
- UE MotionWarping warp targets must be set BEFORE montage playback starts — the MotionWarping notify state reads them during the montage window
- Frame-to-frame weapon sweep tracing (previous vs current socket positions) catches fast-swing hits that single-frame traces miss
- Re-acquiring warp target on each combo section advance keeps magnetism tracking moving targets during multi-hit combos
- FGameplayEffectSpec::AddDynamicAssetTag() is the proper way to attach damage type metadata to a GE spec — the damage execution and PostGameplayEffectExecute can both read it via GetDynamicAssetTags()
- UAbilitySystemComponent::ExecuteGameplayCue() fires a one-shot cue without needing a GE — useful for hit impact feedback from PostGameplayEffectExecute
- Elemental resistances should be capped below 1.0 (0.9 cap) in PreAttributeChange to prevent full immunity
- UStaticMesh::NaniteSettings is deprecated in 5.7 — use IsNaniteEnabled() and GetNaniteSettings() accessor functions instead
- UAudioComponent::bEnableDefaultMultichannelEvaluation does not exist in UE 5.7 — removed without replacement
- EMaxConcurrentResolutionRule is a namespace enum in UE 5.7, not an enum class — must use TEnumAsByte<EMaxConcurrentResolutionRule::Type> for UPROPERTY
- UAudioComponent::SetBoolParameter() exists natively in UE 5.7 — no need for int workaround
- CanMove() should always check IsDead() as first gate to prevent dead characters from processing movement input — especially important for enemy AI that may call AddMovementInput after death
- UAnimNotify::NotifyColor set in constructor won't update when properties change in editor — override GetEditorColor() instead for dynamic color based on property values
- AnimNotifyState is more designer-friendly than two bookend instant notifies for window-based concepts (combo window, invulnerability frames) — the duration bar is intuitive
- Montage_GetCurrentSection() returns the active section name — useful for section-aware AnimBP transitions and per-section logic
- GE_Heal should modify IncomingHeal meta attribute (not Health directly) to go through the full heal pipeline with GameplayCue support in PostGameplayEffectExecute
- Enemy abilities should use distinct Ability.Enemy.* tags rather than reusing player ability tags (e.g. Ability.Melee.LightAttack) to prevent tag-based filtering collisions between player and enemy ability sets
- GA_HitReact should block on State.Attacking/Invulnerable/Dashing to avoid interrupting active player actions — hit react is a lower priority response
- GA_UsePotion is fire-and-forget (CommitAbility + ApplyEffectToSelf + EndAbility) — no montage needed, cooldown gates usage
- UAudioComponent created via NewObject on a subsystem (no world) won't produce sound — must RegisterComponentWithWorld after a world exists, or create lazily
- FAudioDeviceHandle from World->GetAudioDevice() provides ActivateReverbEffect/DeactivateReverbEffect for runtime reverb control without AReverbVolume actors
- GConfig->SetFloat/GetFloat with GGameUserSettingsIni is the lightweight way to persist audio settings without a full SaveGame system
- UDragDropOperation already has a Payload member — do not re-declare it in subclasses or UHT will error on shadowing
- GA_HitReact must set/clear bIsHitReacting on the character so CanMove() properly gates movement during hit reactions — without this, the character can walk freely while playing a hit-react montage
- FGameplayAbilityTargetData::GetHitResult() can return nullptr even when TargetData.Num() > 0 — always null-check before dereferencing
- APlayerController::PlayDynamicForceFeedback() is the simplest way to trigger gamepad vibration — no ForceFeedbackEffect asset needed, just intensity + duration + channel booleans
- Test dummy respawn requires cancelling all GAS abilities first (to clear State.Dead from ActivationOwnedTags), then re-granting abilities and re-initializing attributes
- BT task nodes are shared singletons by default (bCreateNodeInstance=false) — per-instance state MUST use GetInstanceMemorySize()/NodeMemory, not member variables. Storing FDelegateHandle or float state as member vars corrupts when multiple enemies share the same BT.
- FPathFollowingRequestResult is forward-declared in AIController.h — include Navigation/PathFollowingComponent.h to use it (known UE 5.7 gotcha)
- WaitForMessage(OwnerComp, UBrainComponent::AIMessage_MoveFinished) is the cleanest way to wait for MoveTo completion in BT tasks without manual delegate plumbing
- BTService nodes are also shared singletons like BTTask nodes — NoLOSTimer as a member variable corrupts when multiple enemies share the same BT. Must use GetInstanceMemorySize()/NodeMemory.
- ASC->OnAbilityEnded fires for ALL abilities ending on that ASC, not just the one you activated — must filter by FGameplayAbilitySpecHandle to avoid premature BT task completion
- WaitForMessage(OwnerComp, UBrainComponent::AIMessage_MoveFinished) is safer than lambda-based OnRequestFinished delegates in BT tasks — avoids dangling reference captures on OwnerComp
- GameplayDebugger module in UE 5.7 is at Source/Runtime/GameplayDebugger, module name is 'GameplayDebugger'
- StateTree plugin modules: 'StateTreeModule' (runtime) and 'GameplayStateTreeModule' (AI integration)
- State Tree tasks/conditions/evaluators are USTRUCT-based (not UCLASS) with separate instance data structs
- FGameplayDebuggerCategory::SetDataPackReplication<T> requires T to have a Serialize(FArchive&) method
- UAISenseConfig_Hearing uses HearingRange (not SightRadius-style naming)
- UMaterialInterface::GetUsedTextures() with quality/featurelevel params is deprecated in 5.7 — use the parameterless overload
- TArray<T*>::Sort() with const T* lambda fails due to TDereferenceWrapper — use Algo::Sort() with Algo/Sort.h include
- UHT parameter shadowing: UFUNCTION params cannot share names with UPROPERTY members in the same UCLASS — use InRarity instead of Rarity to avoid 'shadowing is not allowed' UHT error
- FOnItemPickedUpStatic (static multicast delegate) allows HUD to bind once globally without per-actor references — useful for pickup notification feeds
- SetRenderTranslation offsets from the widget's layout anchor — for world-projected damage numbers use SetPositionInViewport with screen coordinates instead
- UMG widgets created in HUD::BeginPlay but never bound to data sources are inert — must call BindToInventory/BindToAbilitySystem from BindWidgetsToPlayer
- UUserWidget has no SetRootWidget() method — use WidgetTree->RootWidget = widget to set programmatic root
- UFUNCTION cannot return const USTRUCT* — use a copy-out pattern (bool GetFoo(FName ID, FStruct& Out) const) for Blueprint exposure
- UARPGInventoryComponent methods take UARPGItemDefinition* not FName — dialogue item actions need Blueprint wiring or asset manager lookup
- GAS meta-attribute pattern (IncomingXP like IncomingDamage) is the clean way to bridge GE-based XP awards to local game logic — avoids dual-tracking between GAS attributes and local state
- ApplyModToAttribute with EGameplayModOp::Override does NOT trigger PostGameplayEffectExecute, only attribute change delegates — safe for syncing GAS back from local authoritative state without recursion
- UButton::OnClicked is a dynamic multicast delegate (no lambda support) — use a shared UFUNCTION + IsHovered() TMap lookup pattern for programmatically-created button arrays
- BindWidget meta on UUserWidget requires a Blueprint subclass to provide the slot — programmatic children are added into those containers, not replacing them
- UWidgetTree::ConstructWidget<T>() in UE 5.7 takes (TSubclassOf<T>, FName) — cannot pass TEXT() directly as first arg, must pass StaticClass() + FName
- UAbilitySystemComponent::GetGameplayAttributeValue() returns float and takes a bool& bFound output param — cannot assign to two separate float variables
- TObjectPtr<USphereComponent> implicit conversion to USceneComponent* fails without the full type definition — include the component header in the cpp
- AARPGCharacterBase::InitializeAttributes() is protected — cannot call from external actors like BossEncounter
- Dynamic delegate RemoveDynamic must be called in ALL exit paths (not just CompleteEncounter but also OnPlayerDied) to prevent double-binding on retrigger
- GetOverlappingActors() on BeginPlay is needed for hazards too — actors spawned inside a hazard volume won't generate overlap events
- AARPGEncounterVolume inherits from ASpawnVolume — adding UPROPERTY to parent that child already declares causes UHT shadowing error
- SpawnVolume's SpawnNextInWave is not virtual — max enemy cap must be enforced in the base class for subclasses to benefit
- BeginReplication() is deprecated in UE 5.7 — override FillReplicationParams() for per-actor Iris config and OnReplicationStartedForIris() for post-start setup
- FActorReplicationParams::AlwaysRelevant bypasses spatial filtering for actors that must replicate to all connections (GameState, GameMode)
- FActorReplicationParams lives in Net/Iris/ReplicationSystem/EngineReplicationBridge.h
- UActorComponent has OnReplicationStartedForIris but not FillReplicationParams — filtering is actor-level only
- AActor::NetUpdateFrequency is deprecated in UE 5.7 — use SetNetUpdateFrequency()/GetNetUpdateFrequency() instead
- AActor::NetCullDistanceSquared is deprecated in UE 5.7 — use SetNetCullDistanceSquared()/GetNetCullDistanceSquared() instead
- UFUNCTION returning uint32 is not supported by Blueprint — use int32 for BP-exposed prediction sequence numbers
- GameInstanceSubsystem::Deinitialize must clear timer handles since the world may be torn down concurrently
- DECLARE_DYNAMIC_MULTICAST_DELEGATE does not support AddLambda — must use AddDynamic with a UFUNCTION for dynamic delegates
- Inventory restoration after load must be deferred to BeginPlay timing — the pawn may not exist yet when LoadFromSlot runs
- UHorizontalBox::AddChildToHorizontalBox returns UHorizontalBoxSlot* (not lvalue reference) — cannot bind to auto&
- Include Components/HorizontalBoxSlot.h for SetSize/SetHorizontalAlignment on HorizontalBox slots
- FColor::Orange and other FColor constants cannot be used as UHT default parameters — use FLinearColor for Blueprint-exposed color params
- GetPlayerViewPoint returns FRotator not FVector for the rotation output — use ViewRot.Vector() for direction
- Editor targets that share build environment with UnrealEditor cannot add GlobalDefinitions — UBT rejects property modifications on shared targets. Only Game/Server targets with unique build environments can use custom defines.
- GConfig->GetInt/GetString with GGameIni reads from DefaultGame.ini section paths matching the UCLASS path


---

## Post-Run Cleanup

After following this guide, clean up temporary files:

```bash
# Remove worktrees (can be 100+ MB per run)
rm -rf .claude/worktrees/*/
git worktree prune

# Remove execution logs
rm -f .claude/logs/terminal_exec-*.log

# Archive and remove harness state
cp .harness/guide.md docs/harness/
cp .harness/AGENTS.md docs/harness/harness-learnings.md
rm -rf .harness/
```

Run in both the PoF webapp and UE5 project directories.
