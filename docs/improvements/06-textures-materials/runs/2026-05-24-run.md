# 06-TM run findings — 2026-05-24

Closes the remaining `06-textures-materials` pending items after the
2026-05-23 run. Two work packages: **App** (fully verified here — typecheck +
vitest) and **UE** (authored, compile/run is operator-side — I cannot build UE).

## App — verified, committed `c3ecbd5` (local only)

`npm test` green across the whole suite (1134 tests / 127 files); typecheck +
lint clean. 11 files, TDD throughout.

### §1 — Leonardo advanced controls (ControlNet / inpaint / unzoom)
`transparency` + `tiling` already shipped 2026-05-23; this adds the rest:
- **`leonardo.ts`** — `GenerateImageOptions` gains `controlnets[]` (passed
  verbatim) and `inpaint` (sets `init_image_id` + `canvas_request` +
  `canvas_request_type: 'INPAINT'` + optional `mask_file_id`). New
  `unzoomImage(id, { prompt? })` → `POST /variations/unzoom`.
- **`/api/leonardo`** — new `mode: 'unzoom'`; `mode: 'image'` already forwards
  the new opts.
- **`AdvancedTexturePanel`** — three new tiles (Unzoom, ControlNet, Inpaint)
  alongside the existing Scenario PBR + Universal Upscaler tiles, so every
  capability has a UI entry point (§1's "one tile per capability").
- **Honesty:** these advanced params are doc-derived and key-gated — only basic
  `tiling` generation was ever smoke-tested live. The shapes are isolated +
  body-shape-asserted so a live field-name correction is a one-line change
  (same posture as `scenario.ts`).

### §6 — texture-quality Gemini step (automated)
- **`/api/verify/visual`** — new `mode: 'texture'` with a server-owned
  seamless/tileable prompt (`TEXTURE_CHECK_PROMPT`) + verdict normalisation
  (a non-tileable result / seam maps to the recorded "defect" flag). `'hud'`
  remains the default — back-compatible.
- **`visual-check.ts`** — new `buildTextureCheckSection()` dispatch section
  (inspects the texture PNG itself, POSTs `mode:'texture'`). Advisory, never
  loops. (This file also gained a folder-05 `lighting` mode from a *concurrent*
  session; both coexist — see Collisions below.)

### Tests
- **`download-then-delete-protocol.test.ts`** (tests.md PoF #2) — a source-level
  guard: scans `leonardo.ts` and flags any exported async fn that POSTs to a
  generation/model CREATE endpoint without a cleanup path. Catches a *future*
  caller that forgets the DELETE.
- ControlNet/inpaint/unzoom client + route cases; panel tiles; route texture-mode.
- **e2e:** `texture-pass.spec.ts` (Advanced panel renders all texture-pass tiles
  + one CI-safe `/api/scenario` round-trip) and `texture-pass-3d-endpoint.spec.ts`
  (codifies the dead-3D-endpoint fallback contract at the API boundary — no live
  external call).

## UE — authored, **UNVERIFIED COMPILE/RUN** (operator must build)

I cannot compile or run UE in this environment. The C++ mirrors patterns already
in the compiled tree (`Mesh->GetMaterial(i)`, `GetStaticMaterials().Num()`, FName
parameter access) and adds **no** new module dependencies (no `UnrealEd`/
`MaterialEditor`), so build risk is low — but it is real. **Build
`PoFEditor Win64 Development` before relying on these.**

### Tests (tests.md UE side)
- **`AVSArenaMaterialBindingTest`** (#1) — `Source/PoF/Test/Materials/`. Loads
  `SM_Arena`, asserts every slot non-null; for instanced slots, asserts the
  `Albedo` texture resolves and `Normal`/`Roughness` are sampled non-sRGB.
  Asset-inspection only → safe sibling, deterministic under `-nullrhi`.
- **`AVSMasterMaterialInstanceTest`** (#3) — asserts `M_ARPG_Surface_Master`
  exists and `MI_Arena_{Floor,Wall,Pillar}` + `MI_EnemyRed` are
  `UMaterialInstance`s parented to it (pointer identity). Inspects `/Game/Materials/`
  directly → **independent of SM_Arena's slot state** (robust to the #5 clobber).
  All five assets confirmed present in the tree, so it should pass once compiled.
- **`test_material_pin_connection.py`** (#2) — the Constant3Vector gotcha as a
  Python editor automation (NOT C++): `connect_material_property(c3v, "", …)`
  succeeds, `(c3v, "RGB", …)` fails. Deliberately Python — it needs the
  editor-only `MaterialEditingLibrary`, and the runtime PoF module must not gain
  an editor dependency. Zero build risk.
- **`place_material_tests.py`** — places the two C++ test actors into
  VerticalSlice (mirrors `place_arena_tests.py`). Prereq: the C++ compiles.
  Edits the shared `.umap` — run deliberately.

### Game §2-4 (Python; zero build risk)
- **§3 detail map** — `build_master_material.py` extended: a `DetailNormal`
  sampler at `DetailTiling` (default 8.0) blended over the base normal via
  `BlendAngleCorrectedNormals`. Defensive — falls back to base-normal-only if the
  engine function is unavailable, so the master always builds. **Takes effect on
  the next `build_master_material.py` run** (the committed `.uasset` is unchanged
  until then; existing instances don't set DetailNormal, so no visual change).
- **§4 texture library** — `build_texture_library.py` duplicates the arena
  textures into `/Game/Textures/Surfaces/` under stable names
  (`T_DungeonStone_01_*`, `T_DungeonBrick_01_*`, `T_DungeonMetal_01_*`).
  Non-destructive + idempotent.
- **§2 config wiring** — `build_master_material_config.py` creates
  `DA_ARPGSurfaceMasterConfig` (a `UARPGMasterMaterialConfig`) documenting the
  master's scalar/vector/texture params, wiring the SP-B-generated header to the
  actual master.

## Collisions (shared-tree, observed live)
- **`visual-check.ts` / `.test.ts`** — a concurrent folder-05 session added a
  `lighting` verification mode to the SAME files while I added the `texture`
  section. They merged cleanly (both features present, all tests green). To avoid
  racing that session, my commit `c3ecbd5` **excludes** the shared
  `visual-check.ts`/`.test.ts` pair — `buildTextureCheckSection` sits on disk and
  will be committed by whoever commits that file. My `/api/verify/visual` texture
  mode IS in the commit (self-contained; doesn't import the prompt helper).
- `cli-task.ts` + several test files are foreign-modified by other live sessions;
  left untouched (targeted `git add` of my files only).

## Still blocked / deferred (unchanged from 2026-05-23)
- **App §2 / Game §1** — Leonardo 3D PBR-texture endpoint is dead (404,
  SDK-confirmed). Scenario is the wired substitute; live use is key-gated.
- **Game §5** — `MI_EnemyRed` swap onto `BP_VSEnemy` deferred (skeletal-mesh CDO
  material persistence is flaky). The instance exists as a consolidation artifact.
- **Game §6** — audio is a future folder (`09-audio`), not a material concern.
- **SM_Arena slot clobber** — whether the arena uses `MI_Arena_*` or branch #5's
  rebuilt `M_Arena_*` is a merge-time reconciliation across branches.
- Live verification of Scenario field names + PolyHaven fallback ids (no keys).

## Push status
App commit `c3ecbd5` is local only (user pushes the app repo). UE C++/Python
committed locally in `pof-exp` (operator pushes; the tree is mid-flight with
interleaved multi-session commits).
