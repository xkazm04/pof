# Scenario Run — 2026-05-19 (live mode)

**Result:** 0/2 steps passed. 0 skipped. 2 failed.

**Started:** 2026-05-19T17:30:26.311Z
**Finished:** 2026-05-19T17:30:32.241Z

## Per-step results

| Step | Status | Duration | Notes |
|------|--------|----------|-------|
| Step 6 (LIVE): Verify build | ❌ fail | 0ms | pof-setup-wizard-build-verify-btn not visible — Step 6 cannot dispatch |
| Step 8 ih-1 (LIVE): Input Actions | ❌ fail | 0ms | No Run/Claude/Play button inside ih-1 row |

## Captured dispatches

- CLI prompt dispatches: 0
- Cook execute dispatches: 0

Full dispatch payloads at `e2e/artifacts/dispatched-prompts-1779211832241.json`.

## Findings for sub-project D2 (live-mode prerequisites)

- [ ] Claude Code CLI authenticated (`claude --version` should work in PoF's shell environment).
- [ ] UE5 5.7 installed at expected engine path (verify via project-setup status checks).
- [ ] Visual Studio 2022 + Windows SDK + .NET tools installed.
- [ ] UE5 project at `C:\Users\kazda\Documents\Unreal Projects\PoF` builds clean.
- [ ] `~/.pof/pof.db` initialised.

## Findings for sub-project D3 (iteration)

The live run completed in 6 seconds — both steps fast-failed before any Claude Code dispatch fired. The harness wiring (waitForCliComplete, page.route mocks, navigation) all worked. The blockers are upstream:

### Finding A — `pof-setup-wizard-build-verify-btn` doesn't render on the default project-setup screen

**Symptom:** Step 6's check `await page.getByTestId('pof-setup-wizard-build-verify-btn').count()` returns 0. The button is wired (testId added in sub-project C commit 0ebc6f2 inside `BuildVerifyPanel.tsx`) but the panel itself isn't rendered.

**Likely cause:** `BuildVerifyPanel` only renders when project-setup has reached a particular state (build status known, project context loaded). The harness reaches the project-setup landing page with an unconfigured store; the panel stays unmounted.

**D3 mitigation options:**
- (a) **Seed the project store** via `page.evaluate` to force `projectPath`, `projectName`, `ueVersion`, and `dynamicContext` before navigation. May require store exposure on `window` (PoF source change) OR using `localStorage.setItem('pof-project-store', JSON.stringify({state: {...}}))` to populate the persisted Zustand state.
- (b) Inspect `ProjectSetupModule.tsx` for what governs `BuildVerifyPanel` visibility; navigate the harness through whatever sub-flow renders it.
- (c) Replace the click with a direct CLI dispatch via `page.evaluate(() => window.dispatchEvent(new CustomEvent('pof-cli-prompt', { detail: { tabId: '...', prompt: '...' } })))` — bypasses the button entirely.

### Finding B — No `Run|Claude|Play`-text button exists inside checklist item rows

**Symptom:** Step 8 ih-1's check `ih1.locator('button').filter({ hasText: /Run|Claude|Play/i }).first().count()` returns 0. The row testId resolves; the button regex finds nothing.

**Root-cause investigation needed (D3 first task):** read `src/components/modules/shared/RoadmapChecklist.tsx` Cards layout (line ~444+) to find the actual dispatch trigger. The "Claude" button uses an `AccentButton` (line 649) with the label `"Claude"` — but it's icon-prefixed and may render the text in a way that `hasText` doesn't catch (e.g., wrapped in a span, or only visible on hover via the `opacity-30 ... group-hover:opacity-100` style at line 613).

**D3 mitigation options:**
- (a) **Hover the row first** to make the action buttons visible: `await ih1.hover()` then `await ih1.getByRole('button', { name: /Claude/i }).click()`.
- (b) **Click via accessible name** instead of text content: `getByRole('button', { name: 'Claude' })` is more reliable than `filter({ hasText: ... })`.
- (c) **Trigger via keyboard** — RoadmapChecklist.tsx:487 wires `Space` key on the row to call `onRunPrompt(item.id, item.prompt)`. The harness could focus the row + send `Space`.
- (d) **Use CompactChecklist instead of Cards layout.** The compact layout (default per RoadmapChecklist.tsx:169 `useState<LayoutMode>('compact')`) has a hover-only Play button at line 798-807 styled with text-2xs no label — only a `Play` icon. Definitely won't match `/Run|Claude|Play/i` hasText. Switching to cards layout might be necessary, or use the icon-only locator.

### Finding C — Zero CLI prompt dispatches in BOTH stub-mode AND live-mode runs after D2 fixes

**Symptom:** D2's regenerated stub findings AND D2's live findings both show `CLI prompt dispatches: 0`. The /pof/status mock did NOT fix the underlying dispatch issue (which D1 had blamed on it).

**Revised root cause:** Findings A + B are the actual blocker. No CLI dispatches fire because no dispatch-triggering button is ever successfully clicked. The buttons either don't render (Step 6 case) or don't match the harness's locator regex (Step 8 case). The `/pof/status` mock was solving a problem we didn't have.

**D3 priority order:** (1) fix the per-row button locator (Finding B; smallest, highest impact). (2) decide whether to seed projectStore state (Finding A; medium). Once a single live dispatch successfully fires + captures, expand live scope incrementally.

### Net D2 outcome

- **Cook stub now captures** (was 0 before D2 fixes; now 1 in stub mode). Fix for D1 finding 2 confirmed working.
- **CLI dispatches still 0** — D1's diagnosis was wrong; the real blockers are A + B above. D2 ruled out `/pof/status` as the cause.
- **Live-wait helpers work correctly** — both live steps reached `waitForCliComplete` only because the per-step body returned an early failure (button-not-found) before calling it. The 10s "indicator never appeared" path was never exercised.
- **30-min timeout never hit** — test completed in 6 seconds.

### Recommended D3 scope

1. **First task:** investigate the per-row Claude button — likely a one-line fix in the spec (Finding B option b: switch to `getByRole`). Re-run live D2 spec; expect waitForCliComplete to actually exercise.
2. **Second task:** if Finding A blocks Step 6, either skip it or implement option (b)/(c).
3. **Third task:** once a single live dispatch + completion works, expand scope to a 3rd dispatch step (e.g., arpg-gas ag-1) to validate the pattern repeats.
4. **Defer:** seeding strategy for projectStore; full 24-step live run; Phase 8 (.exe launch + gameplay assert).
