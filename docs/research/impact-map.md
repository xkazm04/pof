# PoF Research Impact-Map

> **Purpose:** a fast triage index so `/research` (and any contributor) can map an idea to the handful of files it touches **without re-scanning the whole codebase**. Each entry: subsystem → key file anchors → *what kind of idea lands here* → *known gaps* / *already-has* → doc links.
>
> **How to use (Phase 6):** find the impacted subsystem here → it names the likely anchors and whether the area already has something → confirm with ONE targeted grep + a short read → land the change. **How to grow (Phase 9):** if a run touches an area that's missing/thin here, or finds a new anchor/gap/structural fact, append it. The map's value compounds — keep entries thin.
>
> Seeded 2026-06-18 from `docs/README.md`, `docs/architecture/*`, `docs/catalog/*`, and project memory. Buckets mirror `.claude/skills/research/SKILL.md`.

---

## A — Headless tooling (the automation loop)

### Harness (autonomous dev loop)
- **Anchors:** `src/lib/harness/` — `orchestrator.ts` (run loop, self-heal), `executor.ts` (`executeArea`), `claude-session.ts` (`spawnClaudeSession`/`buildClaudeArgs`, `@@HARNESS_RESULT` marker), `verifier.ts`, `visual-gate.ts`, `plan-builder.ts`, `checkpoint.ts`, `state-io.ts`.
- **Lands here:** new autonomous-build behaviors, per-area execution, self-heal passes, budget/cost governance, result-marker contracts.
- **Already-has:** stream-json parse, cost tracking (`total_cost_usd`/`cost_usd`), SIGTERM timeout, allowed-tools/bare/verbose flags, **optional `--mcp-config` via `enableMcp`** (Phase 1, commit `059c95a`).
- **Docs/memory:** `[[project_harness]]`.

### CLI terminal service (app-spawned Claude)
- **Anchors:** `src/lib/claude-terminal/cli-service.ts` (`startExecution`/`buildCliArgs`, stream-json events, `@@CALLBACK`), `mcp-config.ts` (`resolveAutonomousMcpArgs`, `POF_CLI_MCP_CONFIG`), `skills.ts`, components in `src/components/cli/`.
- **Lands here:** how the app spawns Claude Code, MCP wiring, callback capture, session reuse, prompt skills injection.
- **Already-has:** `--dangerously-skip-permissions`, stream-json, resume, autonomous `--mcp-config` opt-in.
- **⚠️ (2026-07-29) Model policy does NOT reach a dispatch unless the task type is MAPPED — check `taskClassForDispatchType` before assuming governance.** `resolveDispatchModelChoice` had exactly ONE call site (`api/claude-terminal/query`), and `taskClassForDispatchType('one-shot-step')` hit `default → null`, so the lab's live CLI produce (`POST /api/one-shot/step`, `mode:'cli'` — the catalog pipeline's only real CLI produce) spawned **unpinned** on whatever the session model was. Fixed: mapped to `produce-text` (it is gated to the brief/graph/rules text archetypes) and the route now passes the pin. Lesson for future runs: an unmapped task type is a SILENT no-op, not an error — grep the switch, don't assume. New: **`GET /api/cli-spend?action=dispatch-plan&taskType=X`** + pure `src/lib/cli-spend/dispatchPlan.ts` (`describeDispatchPlan`, `ONE_SHOT_STEP_TASK_TYPE`) join model policy with `getTaskTypeEstimate` so a panel can name the model/effort/cost; honesty rules live in that copy (unpinned says so; no history says so rather than showing a `$0.00` that reads as free).
- **Docs/memory:** `docs/architecture/prompts-and-cli.md`.

### pof-mcp (headless MCP over the app API)
- **Anchors:** `tools/pof-mcp/src/` — `index.ts`, `pofClient.ts`, `tools/{pipeline,harness,sims,ue,design}.ts`.
- **Lands here:** new headless MCP tools that drive catalog pipelines / harness / sims from the CLI. **Thin adapter over the app HTTP API (`:3000`); never touches UE directly** (raw UE = mcp-unreal).
- **Docs/memory:** `[[project_pof_mcp_layer]]`.

### Verification / test-gate runner (Tiers of Truth)
- **Anchors:** `src/lib/test-gate-runner/` — `bridgeExecutor.ts` (`:30040` automation), `spawnExecutor.ts` (headless `UnrealEditor-Cmd`, `observations.json`), `visualExecutor.ts` (`/pof/snapshot/capture` → Gemini), `drainAll`.
- **Lands here:** L3 runtime / L4 visual gates, observation contracts, the "no done without a ground-truth observation" moat.
- **Docs/memory:** `docs/catalog/L3-L4-RUNNER.md`, `[[project_llm_ue_interface]]` (Observation Spine).

### Experiment Lab (theory → output — validate a concept live on UE 5.8)
- **Anchors:** `src/lib/ue-experiment/` (`runner.ts` `runExperiment` — launch headless 5.8 → run Python probe → capture frame → log markers → optional Gemini verdict; `job-store.ts`; `experiment-db.ts` run history), `src/app/api/experiment/{run,status/[id],screenshot/[id],history,runs/[id]}/route.ts`, `src/components/experiment-lab/` (`ExperimentLab.tsx` + `ExperimentHistory.tsx` + `seed.ts` + `assertions.ts` + `client.ts`), page `/experiment`.
- **Lands here:** running an ad-hoc concept on the connected project + seeing output. **Two modes:** Editor-Python (probe + screenshot) and **Gameplay Scenario** (`-game -PoFScenario` — drive the player with inputs + observe behavioral metrics `summarizeObservations` (maxSpeed/displacement/montage) + the peak-action frame, reusing the observation spine). **Behavioral assertions** (moved/animated/montage/attribute-drop → `behavioralVerdict`, via the gate-runner's `parseScenarioVerdict`) make a scenario pass/fail on measured behavior. **Run history + A-B compare** (`experiment_runs` table; `/history` + `/runs/:id`; side-by-side UI). **A research finding (a `UE_GOTCHAS` entry) can be SEEDED into a live experiment** (`seedFromGotcha`) — the theory→output bridge for the research loop. TopBar "Experiment" tab → `/experiment`; 30s poll.
- **Already-has:** reuses `ue-launch` (launch/python/capture/markers) + `/api/verify/visual` (L4) + `parseScenarioVerdict` + the `harness-runs-db` persistence pattern. **Proven live 2026-06-19** (real UE 5.8: 439 KB frame + `RESULT=5.8.0`; gameplay scenario: 8 samples; behavioral assertion `moved` discriminates live — pass@5/fail@50 on real observations). Headless-per-run; ad-hoc; in-app UI. Default scenario input now `action:/Game/Input/Actions/IA_Move value:[0,1]` (documented-preferred over Boolean-key).
- **RESOLVED 2026-06-19 (was a low-displacement gap):** headless Move-injection drove ~10u with `speed 0`/`anim_speed 0` — diagnosed NOT as injection fidelity but **combat interference**: VerticalSlice enemies damage the player → `ARPGCharacterBase::CanMove()` false (stagger/hit-react) → `HandleMove` drops the injected movement. Fix = a **`disable_ai` scenario flag** (`UScenarioController` destroys all AI-possessed pawns at start, class-agnostic via `AAIController`; UE commit `c8111f9` on `feature/arpg-movement-feel`, editor rebuilt), plumbed `ScenarioSpec.disableAI → buildScenarioInbox → "disable_ai"` + UI "Remove enemies" toggle (default on). **Proven live:** forward walk → **1241u @ 600 speed, health flat 100** (was ~10u / speed 0 / health dropping). Build note: UBA's low-memory heuristic can loop-kill compiles when system RAM is cache-heavy → rebuild with `-NoUBA`.
- **Docs/memory:** `docs/superpowers/specs/2026-06-19-ue-experiment-lab-design.md`, `[[project_ue58_official_mcp]]`, `[[project_llm_ue_interface]]`.

---

## B — UI / catalog workflow

### Catalog pipeline + `/layout` lab (reference surface)
- **Anchors:** `src/components/layout-lab/` — `steps/` (`StepFrame.tsx`, `shared/CliProduce.tsx`, `shared/ChartPanel.tsx`, `shared/CandidateGallery.tsx`, `ArchetypeStep`), `steps/index.ts` (`getStepComponent`), `Baseline.tsx`, `CatalogTree.tsx`, `LabBridgeStrip.tsx`. Registry/specs under `src/lib/catalog/`.
- **Lands here:** new pipeline steps, step archetypes, View/Produce/Acceptance UIs, the server-backed produce→persist→rollup loop.
- **Already-has:** 30+ catalog pipelines, StepSpec chassis, server-derived acceptance (`pipeline_artifacts` table), e2e walker; batch-kept candidate history + re-select (`CandidateGallery`/`genHistory`); per-step replay (`POF_REPLAY_SPECS`). **Operator-workflow surface (checked 2026-07-29 — do NOT re-propose these):** a per-entity next-action coach (`NextStepCoach` + cross-catalog `GlobalCoach`, both ranked by the ONE `coachLadder.ts`, with jump + drain CTAs), a persisted per-step produce-failure banner (`ProduceErrorBanner` ← `LabStepArtifact.error`) and a sync-failure banner with retry, "Retry with same prompt" on `CliProduce`, `RawArtifactDisclosure` (verbatim stored payload), explicit `RefreshFromServer` reconciliation that REPORTS adopted/removed/kept, and `data.produceDirection` stamped on every artifact (`produceDirection.ts`). Style reference = **Style DNA** (2026-07-13). Spend tracking = `cli-spend-db` + `getSpendDashboard`.
- **(2026-07-29, Tesana studio-UX run — 6 builds) The lab's operator loop gained history, honesty and citation.** (1) **`coachProvenance.ts`** — the coach's terminal "All done." was an unqualified claim over an entity whose passes may all rest on shape-only checkers with `JUDGE: NONE`; it now counts what the passes stand on from the same `step-facts.json` the strip reads (rank: unwired > shape-only > unjudged > unaudited), states the count, and offers a jump to the weakest link. (2) **`produceLog.ts` + `ProduceLogPanel`** — a per-entity newest-first log of every produce (direction, outcome, verbatim failure reason, orphaned steps shown but unjumpable), with a needs-attention count visible while collapsed; forced a store fix, `LabStepArtifact.errorAt` (`markFailure` preserves `at` for the surviving content, so a failure had no time of its own). (3) **model policy reached the lab** — see the A-bucket note below. (4) **`stepEvidence.ts`** — a corrective produce cites the real served artifacts on screen (never a swatch). (5) **`pipeline_artifact_revisions`** — see Stores/API below. (6) **`StepLibraryPicker` + `libraryReference.ts`** — reference `asset_library` assets from inside a step, license-first. All six are in the Shared Component Manifest (`.claude/CLAUDE.md`).
- **Packaging truth engine (2026-07-13, Flow demo → BUILT same day):** the 30 "UE Packaging" steps were the largest UNPOWERED block (step-facts: produce never touches disk/UE; judges caught the input-schemes phantom glyph atlas) → **Tier 1 SHIPPED**: `src/lib/catalog/packaging/{collect,packageArtifacts}.ts` + `acceptance/packagingVerify.ts` + `GET/POST /api/pipeline-artifacts/verify-packaging` (drain rebuilds each row's package from SIBLING artifacts — files hashed in place, data-URL art materialized under `generated/packages/` — and grades from the manifest; label-match covers all 30 pipelines with zero per-pipeline edits). Live dry-run: 32 graded, 30 real-file pass / 2 honest deferred. See `docs/research/packaging-truth-engine-spec.md` (Built section) + `docs/catalog/WIRING-AND-ACCEPTANCE.md` §3. Remaining: POST-apply in a gap-loop wrap + step-facts flips.
- **Docs/memory:** `docs/catalog/*` (AUTHORING, WIRING-AND-ACCEPTANCE, PIPELINE_REVIEW), `docs/architecture/ui-shell.md`, `[[project_ui_identity_lab]]`, `[[project_catalog_pipeline]]`.

### Modules (game-dev domains)
- **Anchors:** `src/components/modules/{core-engine,content,game-systems,evaluator,game-director,visual-gen,shared}/`. Registry `src/lib/module-registry.ts`; deps `src/lib/feature-definitions.ts`.
- **Lands here:** new module views, checklists/quick-actions, feature-matrix/NBA, evaluator passes.
- **Docs/memory:** `docs/architecture/module-system.md`.

### Visual-gen / asset pipeline (2D→3D→rig→texture→assemble)
- **Anchors:** `src/lib/visual-gen/` — `providers.ts` (3D-gen providers, e.g. Tripo/TripoSR), `prompt-chips.ts` + `style-keywords.ts` (no-jargon visual prompt builder), `reference-roles.ts` (reference-role scaffold + generation-prompting best practices — block-first/role-tagged/multi-view), `rig-presets.ts` (retarget *targets*: UE5 Mannequin/MetaHuman/Minimal — NOT rig tools), `lighting-presets.ts` (Lumen best-practice configs per tier; consumed by prompts/pipelines), `polycount-presets.ts` (per-asset-class face budgets → Tripo `face_limit` + class-aware `critiqueMesh` thresholds; 2026-07-13), `input-gate.ts` (Tier-0 VLM gate on the 2D concept BEFORE image→3D dispatch — single-subject/plain-bg/canonical-pose, over the anim-critique `makeQwenVision` seam; `POST /api/visual-gen/input-gate`; 2026-07-13), `style-dna.ts` + `style-dna-db.ts` (mood board → VLM-distilled per-project style profile PALETTE/MATERIALS/MOOD/RENDER/MOTIFS, single active profile in SQLite; `POST/GET/PATCH /api/visual-gen/style-dna`; opt-in injection via `/api/leonardo` `applyStyleDna:true` → `styleDnaApplied` in the response; realizes idea `project-style-dna-art-direction` for $0; 2026-07-13), `material-db.ts` + `biome-textures.ts` (PBR/material gen), `generators/{terrain,dungeon,vegetation}.ts`, `asset-library-db.ts`, `ue5-import-templates.ts`, `poll.ts`; UI `src/components/modules/visual-gen/auto-rig/AutoRigView.tsx` + `material-lab/`.
- **Lands here:** new 2D/3D/material/texture providers, auto-rig methods, lighting/Lumen presets, procedural generators, asset library.
- **(2026-07-24) 2D-generation front is now TWO providers.** Leonardo (`scripts/visual-gen/pof_leonardo.mjs`, `gpt-image-2`+`RENDER_3D`, paid credits, always-delete cleanup) stays the keeper for **character/concept** 2D → Hunyuan3D. Added alongside it: **`src/lib/visual-gen/qwen-image-runner.ts`** (`runQwenImage` + pure cores `buildQwenImageBody`/`parseImageUrl`, injectable HTTP/fs seam, 11 tests) — DashScope `qwen-image-2.0-pro` → `qwen-image-max` → `qwen-image-plus`, endpoint `…/api/v1/services/aigc/multimodal-generation/generation`. Its niche is **text-in-image**: Qwen-Image 3.0's stated strength is readable ~10px text, 12 languages / 20+ fonts, and multi-layer UI + infographic layouts in one pass — exactly Leonardo's weak axis. Target the UI-facing catalog steps (icon sets, HUD mockups, labelled sheets), NOT character concepts. Reuses `QWEN_API_KEY` (same DashScope account as the Qwen-VL seam) but a **different endpoint with a separate per-model quota**, so generation spend can't eat the critique/gate budget. Not yet wired to a route/StepSpec — runner + tests only.
- **Already-has:** Tripo-family 3D gen, Material Lab PBR, retarget presets, same-style/modular-kit workflow, (2026-06-18) Lumen `lighting-presets.ts`, generation-prompting `reference-roles.ts` (block-first + role-tagged refs for on-model consistency). **(2026-06-20) FIRST EXECUTABLE GENERATOR — local TripoSR image→3D:** `src/lib/visual-gen/triposr-runner.ts` (`runTriposr`, pure args/parse + spawn seam, 8 tests) drives committed `scripts/visual-gen/pof_triposr.py` via a TripoSR venv (`POF_TRIPOSR_ROOT`; `runnerBacked` flag on the provider). `providers.ts` was registry-only before this. **Proven live** (chair.png → 41.8K-vert / 1.68 MB `.glb` on a 4090, ~15.5 s, $0; output gitignored under `/generated/`). Zero-budget baseline; the seam lets Tripo/Meshy/Rodin cloud slot in later. **Upstream text→image step (2026-06-20):** `scripts/visual-gen/pof_txt2img.py` (SDXL via `diffusers`, $0 local) completes the FRONT of the pipeline — `text → 2D → TripoSR(.glb) → CLIP+geometry+VLM critique`. Demonstrated end-to-end on a "Jinx" character: SDXL full-body → TripoSR → recognizable 3D (46–57K faces, watertight, CLIP 0.86, Qwen-VL-4B 7/10 "stretched limbs / missing facial features"). Confirms characters are the hard case for single-image→3D (soft/multi-component vs the chair's clean single mesh) — the critique tiers correctly score them lower. **Setup gotchas:** skip `torchmcubes` (fragile MSVC+CUDA Windows build) by patching `tsr/models/isosurface.py` → scikit-image marching cubes (heavy NeRF stays GPU, MC on CPU); write a minimal script (TripoSR's `run.py` hard-imports xatlas/moderngl for texture/render we don't need); `pip install --no-cache-dir` (a locked cache wheel aborted the first install). **Stage 1 DONE (2026-06-20):** job-based `/api/visual-gen/generate` (+ `/status`) runs TripoSR via `triposr-job-store.ts`; `useForgeStore.submitLocalJob` posts the uploaded image (data URL → server file via `parseImageDataUrl`) + polls; asset-forge Image-to-3D → TripoSR now executes (gated by the provider's `runnerBacked` flag). **Stage 2 DONE:** `src/lib/visual-gen/ue-import.ts` `importGlbToUE` reuses `runExperiment` (Editor-Python) → glTF `AssetImportTask` → Static Mesh; **proven live** (chair.glb → `/Game/Generated/chair/StaticMeshes/TripoSRChair.uasset` on disk). **Stage 3 DONE (critique = FREE-LOCAL quality gate, not Gemini):** `src/lib/visual-gen/mesh-critique.ts` — `scripts/visual-gen/pof_mesh_critique.py` (trimesh: watertight / connected-components-floaters / degenerate / bbox / volume) → pure `scoreMesh` deterministic pass/warn/fail + reasons (the asset analog of the experiment lab's `behavioralVerdict`); auto-runs on each TripoSR job (`triposr-job-store` `critique` field) + exposed via `/status`. **Proven live** (chair.glb → pass/100: watertight, 1 component, 0 degenerate). User-chosen **tiered** design: geometry first (free); optional later tiers = reference-image similarity (CLIP) + a LOCAL VLM (LLaVA/Qwen-VL on the 4090) — both deferred (still $0). Geometry gate catches BROKEN meshes; semantic "is it a good chair" = the next tier. Scorecard UI in `GenerationQueue` (`CritiqueBadge`) DONE. **Tier-2 fidelity DONE (free, $0):** `pof_triposr.py --fidelity` renders NeRF views (torch/GPU, no moderngl) + CLIP-compares to the input (`transformers` CLIP — no new install) → `clipMax/clipMean` + a preview thumbnail; enabled by default, surfaced as a `fidelity:` chip. **Proven live** (chair → CLIP 0.97). **Self-correction `best-of-n.ts` `generateBestOf` built** (param sweep → critique each → pick best by `combinedScore` = geometry+fidelity) **but MARGINAL for TripoSR**: a foreground-ratio sweep on the chair was a near-tie (CLIP 0.9676–0.9704) because TripoSR is deterministic/feed-forward — param variants are near-identical. **Free-ceiling finding:** real self-correction needs a STOCHASTIC/steerable generator OR upstream 2D-input variation (regenerate the concept image) — neither free with TripoSR alone; CLIP fidelity is valuable as a GATE signal (rejects off-target gens), weak for ranking near-identical variants. **Remaining:** preview-thumbnail serve route + a local VLM tier for richer aesthetic judgment + *structured* critique. **VLM research 2026-06-20 (free/local, $0 on the 4090, all via `transformers` — same pattern as the CLIP tier):** top pick **InternVL3.5-8B/14B (MIT — commercial-OK, IQA-validated in arxiv 2603.24578)**; **Qwen3-VL-8B (Apache, newer than the Qwen-VL most mean)**; lighter **Molmo-7B / Pixtral-12B (Apache)** / **Phi-4-multimodal (MIT, ~5B)**. Prefer MIT/Apache (the CubePart license lesson). A VLM yields discriminating structured reasons CLIP can't ("legs fused / back missing") and could rescue the best-of-N near-tie that CLIP cosine couldn't separate. **EXPERIMENT 2026-06-20 (`feature/vlm-critique-tier`, `scripts/visual-gen/pof_vlm_critique.py` — generic `AutoModelForImageTextToText` path, doc `docs/research/vlm-critique-experiment.md`):** Qwen3-VL-4B vs Gemma 4 12B as critics. **Qwen3-VL-4B WINS** — useful structured verdict (`SCORE=7; DEFECTS=warped legs, …` — named the real TripoSR artifact CLIP missed), 8.9 GB, 3 s, works on transformers 4.57 AND 5 (coexists with TripoSR). **Gemma 4 12B BLOCKED** (not VRAM — fits 8.3 GB @ 4-bit): needs transformers 5 which **breaks TripoSR gen** (ViT keys renamed `encoder.layer.N…query`→`layers.N…q_proj`); bitsandbytes 4-bit AND 8-bit inference crash (`LayerNorm int-dtype`); fp16 OOMs 24 GB. → **productize Qwen3-VL-4B (transformers-4-compatible) as the critic; keep VLM in TripoSR's venv or a separate one so it can't break the generator.** Env note: torchvision (Qwen-VL dep) bumped torch 2.10→2.11; restored transformers to 4.57.3. **OFFICIAL PIPELINE CONSOLIDATED (2026-06-20):** keeper = **Leonardo GPT Image 2 (`RENDER_3D` preset) 2D → Hunyuan3D-2 shape (OFFICIAL image-to-3D provider) → CLIP/geometry/Qwen-VL-4B critique**. (1) 2D: `scripts/visual-gen/pof_leonardo.mjs` — Leonardo **v2** endpoint, model `gpt-image-2` + preset `RENDER_3D`, native sizes only (e.g. 1024×1536); submit-v2 / poll-v1 / download / ALWAYS-delete cleanup. HUGE character-2D jump over local SDXL (near-canonical Jinx; ~388 credits @ HIGH). Leonardo CANNOT mesh-from-image via API (probed: no `/generations-3d`; `/models-3d/uploads` = texture-an-existing-mesh only). (2) 3D: `src/lib/visual-gen/hunyuan-runner.ts` (`runHunyuan`, pure args/parse + spawn seam, 9 tests) + `hunyuan-job-store.ts` drive `scripts/visual-gen/pof_hunyuan.py` (Hunyuan3D-2 `Hunyuan3DDiTFlowMatchingPipeline`; `POF_HUNYUAN_ROOT` + shared TripoSR venv / `POF_HUNYUAN_VENV`); `providers.ts` `hunyuan3d` = `official:true`+`runnerBacked` (`getOfficialProvider()`), generate/status routes dispatch+poll it, forge defaults to it. **Proven live** (Jinx → 360K-face / 6.5 MB glb, ~8× TripoSR detail, 6 GB VRAM, 31 s; braids/outfit/face modeled where TripoSR blobbed — but NOT-watertight / 14-components / NON-COMMERCIAL license; SHAPE only, texturing deferred = custom rasterizer / Leonardo PBR). **TripoSR kept = MIT/commercial-safe fast fallback.** TRELLIS evaluated + REJECTED (Windows-hostile: flash-attn + diffoctreerast/vox2seq CUDA builds, Linux-first). **Disk cleanup:** deleted Gemma-4-12B (23G) + SDXL (6.7G) caches + TRELLIS clone (~30G freed). **Off-domain (declined):** AI promo-video relight/VFX/bg-replace (OpenArt) — PoF makes game assets, not promo videos. **Auto-rig direction = MetaHuman conform (UE 5.8)** — Candidate B in `ue5-capability-integration-candidates.md`; UniRig was a placeholder. **(2026-07-05) Automatability GROUND-TRUTHED on the installed 5.8.0** (2 headless introspection probes, `-run=pythonscript -nullrhi`): the conform API is **fully Python-exposed AND loads headless** — `MetaHumanCharacterEditorSubsystem.conform_to_target_meshes` / `conform_body` / `set_body_mesh` / `get_face_model_coefficients` / `set_face_model_coefficients` (+ `MetaHumanIdentityFace.conform`). **Catch:** the `MetaHumanCharacter` (Experimental) plugin must be enabled (`PoF.uproject` doesn't; `-EnablePlugins=MetaHumanCharacter` takes MH-class count 108→243) + install MetaHuman Optional Content for full fidelity. So a **scripted HEADLESS conform is feasible** — the earlier "editor-dependent, no commandlet" read was too pessimistic; residual manual point-align is a quality gate (mitigated by input-prep), not an API gap. **PROVEN LIVE end-to-end 2026-07-05:** `generated/jinx-leo/jinx_hunyuan.glb` → import → `get_mesh_data_for_conforming` (180K verts) → `MetaHumanCharacter` → `conform_body_to_target`→True → saved `MH_JinxConform.uasset`, fully headless. `MetaHumanCharacter` plugin now enabled in `PoF.uproject`. **New committed seam `src/lib/visual-gen/metahuman-conform.ts`** (`buildConformPython`/`conformMeshToMetaHuman`, mirrors `ue-import.ts`, 6 tests) dispatches it via the `ue-experiment` runner. (Use `conform_body_to_target`, not the higher-level `conform_to_target_meshes` wrapper — the latter runs headless but returns False without the interactive keypoint/curve targets + MetaHuman Optional Content.) Recipe + probe repro = `docs/research/ai-to-metahuman-conform-recipe.md`; closes the "generated SHAPE has no rig" gap. **Lighting-pipeline spec:** `docs/research/lighting-pipeline-spec.md` (K4 — a new `environment-lighting` catalog pipeline, not built). **(2026-07-05) image→3D INPUT-PREP practices** added to `reference-roles.ts` `GEN_PROMPTING_PRACTICES` (isolate the subject on a plain/white bg + use a simple canonical pose before meshing → fewer Hunyuan/TripoSR artifacts; upstream of `triposr-runner.ts`'s post-hoc bg-removal flag). **(2026-07-06) CLOUD generation route BUILT next to the local route — Tripo3D REST API:** `src/lib/visual-gen/tripo-runner.ts` (`runTripo` — pure cores `buildCreateTaskBody`/`parseTaskCreate`/`parseTaskStatus`/`parseUpload` + injectable HTTP seam; upload→create-task→poll→download .glb; 20 tests) + `tripo-job-store.ts` (mirrors hunyuan store, auto-critique; 4 tests); `providers.ts` `tripo3d` flipped `coming-soon`→**`free`+`runnerBacked`** (`isLocal:false`); `generate/route.ts` gained a `tripo3d` branch (text-to-3d OR image-to-3d, PBR) + `generate/status` resolves the Tripo store; env `TRIPO_API_KEY` (saved in gitignored `.env`); CLI smoke `scripts/visual-gen/pof_tripo.mjs` (`POF_TRIPO_*` markers). **Adds TEXT-to-3D (local Hunyuan/TripoSR are image-only) + PBR-textured image-to-3D with zero local VRAM.** **Live-PROVEN end-to-end 2026-07-06:** `node scripts/visual-gen/pof_tripo.mjs --prompt "…warrior…" --output generated/tripo3d/warrior.glb --pbr` → task queued→success (0→100%), downloaded a valid **9.67 MB glTF-binary v2** in ~50s, **cost 20 credits** (text-to-3d + PBR). Tier-1 geometry gate on it: 167K verts / 310K faces, winding-consistent, but **214 disconnected components / not watertight** → `scoreMesh`=**fail, 5/100** — the documented "characters fragment under single-shot gen" pattern (needs remesh/retopo or Tripo `quad`/`face_limit` before game use; the seam is proven, the raw char geometry isn't game-ready as-is). **CREDIT MODEL (ground-truthed):** API credits are a SEPARATE wallet from Studio credits (dual system) — the free Studio 200/mo can't be spent via the API; the developer console (platform.tripo3d.ai) has its own balance (top-up $0.01/cr, min 100). This key's wallet funded to 600 → gens work. Free-tier output NON-COMMERCIAL (CC BY 4.0, like Hunyuan; TripoSR MIT stays commercial-safe). **(2026-07-06) ANIMATION CHAIN explored + proven — `scripts/visual-gen/pof_tripo_animate.mjs`:** Tripo's rig/animation services (which PoF never solved locally) work via `animate_prerigcheck` (→`riggable:true, rig_type:biped`) → `animate_rig` (`spec:tripo`/`mixamo`, `rig_type:biped`) → `animate_retarget` (`animation:"preset:run"` — also idle/walk/jump/slash/…, `bake_animation:true`, `export_with_geometry:true`, `out_format:fbx`). **Proven live end-to-end on a Jinx concept:** Leonardo `jinx_gptimg2.png` → `image_to_model` (recognizable textured Jinx, MUCH better than text-to-3d — image-to-3d from a strong 2D concept is the quality path) → rig → run-retarget → `jinx_run.fbx` (11.7 MB, 108-bone skeleton + baked run AnimSequence + PBR). Full pipeline cost ~65 credits. Quality: model good; run gait cycles correctly but reads as an aggressive forward-lean sprint + rigid auto-rigged braids (no secondary motion). **Visual via BLENDER 4.2 headless** (`blender --background --python`, EEVEE-Next, sample N anim frames → filmstrip) — the reliable headless render path since UE headless render is blocked (below).
- **(2026-07-28) MESH-FINISH BUILT + LIVE-PROVEN — the retopo→UV→bake stage the pipeline never had.** `src/lib/visual-gen/mesh-finish.ts` (`runMeshFinish` + pure cores `resolveBlenderPath`/`unwrapPlan`/`buildMeshFinishArgs`/`parseMeshFinishOutput`, injectable spawn seam, 20 tests — mirrors `triposr-runner.ts`) drives committed `scripts/visual-gen/pof_mesh_finish.py` (headless `blender --background --python`: import glb/fbx/obj → join parts → optional mirror → **copy** as low-poly → decimate to a face budget → `smart_project` unwrap → Cycles **selected-to-active bake** normal/AO high→low → GLB export; `POF_MESHFINISH_*` markers, judged by markers not exit code). **Proven live 2026-07-28 on `generated/tripo3d/jinx_hd.glb`: 1,492,072 → 40,000 faces, UV=1, normal+AO 1024 maps with real content (normal 160 distinct levels, AO full 0-255 range — not blank), 45 MB → 1.84 MB, $0, no new dependency.** **Two practices ENFORCED in code, not prompts:** `unwrapPlan` refuses to unwrap an undecimated mesh or one above `UNWRAP_FACE_CEILING` (200k) and reports `unwrapSkippedReason` (island explosion), and a bake with no UVs is dropped rather than faked. **Status target:** `character-pipeline` step 09 **Game-Tier Convert** (`character-pipeline.ts:221-234`) produces hand-typed constants (`faceLimit: 40000, sizeMB: 4.3`) with `fieldsPopulated` acceptance and step-facts `checkerMeaningful: false` — this is the $0 local engine that can measure those numbers instead (wiring it to the step + the `mesh-critique` Tier-1 gate is the remaining slice; also unwired: an API route + the `polycount-presets.ts` face budget as the default `targetFaces`). **Gap it closes:** repo-wide there was NO bake / retopo / character unwrap — `blender-mcp/scripts/optimize-mesh.ts` is remove-doubles only and `uv-projection.ts` is world-aligned planar for LEVEL geometry; the blender-mcp path also needs a live Blender + addon over TCP, whereas this is headless CLI (blender resolved via `POF_BLENDER` → known installs, same list as `/api/visual-gen/blender/detect`).
- **(2026-08-14, Mixar run) mesh-finish now produces a TEXTURED asset, and can keep authored UVs (commit `2b19d876`).** Two gaps the AI-Blender-fork demo surfaced against our own script. (1) **Bakes were normal+AO only, and each bake CLEARED the low-poly's material slots** (`ensure_bake_material` built a fresh material per map) — so a two-map run exported the mesh wearing only the LAST map and **no base colour at all**. Now: one shared `pof_bake_mat`, bake targets added as image nodes inside it (`bake_target_node` sets the active node — that is what Cycles bakes into), and `wire_baked_map` links each finished map to its Principled socket (normal via a `ShaderNodeNormalMap`; **AO stays a loose file — glTF carries occlusion separately, there is no Principled socket**). Added `diffuse` (albedo only: `use_pass_direct/indirect=False`, else lighting double-shades in engine) + `roughness`; `NON_COLOR_MAPS = (normal, roughness)`. (2) **`unwrap()` always `smart_project`ed**; new `--uv-mode pack-existing` re-packs the islands the source parts already carried (`bpy.ops.uv.pack_islands`) — joining N textured parts stacks N layouts in one 0-1 space, and re-projecting throws away authored seams. Asking for pack-existing when no UVs survived the join emits `UV_MODE_FALLBACK` rather than quietly projecting. **`metallic` is refused BY NAME** (`bakePlan` → `skipped[{map,reason}]`, `BAKEABLE_MAPS` excludes it): Cycles has **no metallic bake pass**, faking it needs an emission re-wire of every source material, and a silent drop would let a 3-map result read as the full PBR set. ~~Still unproven live~~ → **PROVEN LIVE 2026-08-14** (by the AI-low-poly run's smoke test, Blender 4.2, `jinx_hd.glb` 1,492,072→40,000 faces, `--uv-mode smart`): all four bakes produced real content at 1024² — normal **160 distinct levels** (47-213), AO **256 levels, full 0-255**, diffuse **184** (0-183), roughness **193** (0-223); none blank, output 2.4 MB. The shared-material fix holds — a four-map run exports base colour instead of only the last map.
- **Already-has (checked 2026-08-14 — do NOT re-propose):** multi-part **join→single-mesh consolidation** (`join_meshes(import_mesh(...))`, `pof_mesh_finish.py:229`); **mood-board→art-direction** (Style DNA `style-dna.ts`, `POST/GET/PATCH /api/visual-gen/style-dna`). **Backlog delta — LOD chain:** a Blender-side LOD ladder duplicates the engine (UE's importer auto-computes static-mesh LODs and mips cover the texture-res ladder); PoF already templates `bAutoComputeLodDistances` (`ue5-import-templates.ts:68`) but **defaults it OFF** (`lodCount: 0`, `:146`). The narrow real question is whether that default should flip per asset class — an import-template decision needing UE-side evidence, NOT a mesh-finish build.
- **(2026-08-14, AI-low-poly run) Three narrow gaps closed around the finishing path.** (1) **Shading — the premise was WRONG and the live run caught it** (`78d2aa48` then corrected by `79956f6e`; read the correction, not the original). The claim "generated meshes leave the finishing path flat-shaded" is **false**: a Tripo `.glb` imports with **custom split normals already set and every polygon already smooth**, custom normals **override** the smooth flag, and `shade_auto_smooth` adds no modifier — so the pass was a **silent no-op that still emitted a `SHADING` marker**. Measured on Blender 4.2 / `jinx_hd.glb`: **0 of 30,967 exported normals changed** (identical byte size; only asset metadata differed). Clearing the custom normals first DOES make it bite — and that is why it must not: same mesh, **99.9% of normals rewritten by a mean of 73° (max 179°, flipped)**. The generator's normals carry real surface information; a 30° crease guess is worse. `apply_shading` now runs **only when `has_custom_normals` is false** (the `.obj`/`.fbx`-without-normals class that genuinely imports faceted) and emits **`SHADING_SKIPPED=<reason>`** otherwise → `shadingSkippedReason` — the same refuse-with-a-reason shape as `unwrapPlan`/`cullLimitReason`/`bakePlan`. **Both branches proven live:** `jinx_hd.glb` (1,492,072→40,000) skips with the reason; a 12-face `.obj` with no `vn` lines applies `auto_smooth@30`. **Standing lesson: a marker proving the OPERATOR ran is not proof the ARTIFACT changed — diff the exported file.** (2) **`generateUntilAcceptable`** in `best-of-n.ts` — the module was a param sweep hard-bound to `runTriposr`, and the recorded conclusion ("real self-correction needs a STOCHASTIC generator") had never been acted on, so a broken cloud gen shipped silently. The new loop is **provider-agnostic** (takes a `roll` closure, imports no runner): roll → critique → stop on the first mesh clearing the Tier-1 gate. A roll that produced no mesh is never critiqued/accepted; an exhausted budget returns `accepted:false` with best-so-far surfaced, so best-of-a-bad-set cannot read as a pass. Cap defaults to 3 — a cloud generator charges per task. **WIRED same day (`69fb447e`):** `tripo-job-store.ts` now drives the loop, so a gate-failing cloud mesh is regenerated rather than delivered indistinguishably from a passing one (the 5/100 warrior is the case it closes). `attemptPath` gives each retry its own `_aN` path (attempt 1 keeps the requested path → single-shot jobs byte-identical); **cost is opt-in and store-capped** — `TripoSpec.maxAttempts` defaults to **1**, `MAX_GENERATION_ATTEMPTS` (3) bounds it in the STORE rather than trusting the request, and the loop stops on first success. A still-failing mesh is **delivered with `accepted:false` + `gateReason`**, surfaced with the attempt count through `GET /api/visual-gen/generate/status` — a job can be `done` and unaccepted, and status alone would hide it. Reachable via `POST /api/visual-gen/generate` (`maxAttempts` in the body). (3) **`planPartBudget`** in `polycount-presets.ts` (`faef59da`) — the `modular-part` rationale *claimed* the per-part budget left headroom for the assembled whole, but nothing enforced it (8k × 8 parts = 64k against a 40k character budget); it now computes the per-part limit, `constrained` flags when it had to shrink, and a non-positive part count / unknown class returns undefined rather than inventing a budget.
- **Already-has (checked 2026-08-14, AI-low-poly run — do NOT re-propose):** **part-split generation** (`GEN_PROMPTING_PRACTICES` #7 + the `ai-lowpoly-generation-not-final` gotcha); **high-poly → retopo → bake normal/AO** (`mesh-finish.ts`, proven live); **component-count calibration for assembled characters** — `classifyComponents` (`mesh-critique.ts:92`) already separates parts from specks by face share and `POLYCOUNT_PRESETS.character.maxComponents: 24` overrides the blunt default of 8, so the old "214 components → 5/100" framing is FIXED and must not be re-raised; **multi-view reference input** (`reference-roles.ts`); **Hunyuan3D runs LOCALLY** (`hunyuan-runner.ts` — the "20 free credits/day, make multiple accounts" pitch is moot and ToS-hostile); **Tripo texturing/PBR** (`tripo-skins.ts`); **43k-tri assembled character** validates `character-pipeline` step 09 `faceLimit: 40000`. **Gate-declined by standing user-preference:** AccuRig ("Acrylic") = GUI-only. **Still descoped (user, 2026-08-12, unchanged):** Tripo P2 quad topology — this source CONFIRMS the wait ("we still do not have quads available on Tripo Smart Mesh").
- **(2026-08-14, AI-vs-human-retopology run) ⛔ CONSOLIDATED CATCH — the Tripo REST API has NO mesh-upload path, and this is the SECOND run to be misled by a video claiming otherwise.** The source states you can "bring any 3D model here and do the retopology"; that is **Tripo Studio (the web GUI)**, not the API. Checked against the official SDK reference: `smart_lowpoly`, `refine_model`, `convert_model`, `mesh_completion` and `mesh_segmentation` **all** key off `original_model_task_id` / `draft_model_task_id` — a PRIOR TRIPO TASK — and `upload_file()` accepts **images only**. This is the identical constraint the 2026-07-28 run re-scoped for `texture_model`, rediscovered from a different direction. **Do not spend the web budget on "can we upload a mesh to Tripo" again** — the answer is no for every mesh-consuming task type; a demo showing a mesh being uploaded is showing the GUI. **Backlog delta (the narrow capability that IS reachable):** `smart_lowpoly` performs quad retopology on geometry **we generated through Tripo**, whose task ids `tripo-job-store` already holds — the one thing `mesh-finish.ts`'s Blender *decimate* cannot do (decimate collapses to triangle soup with no edge loops, which is precisely the "not suitable for animation" charge the video opens with). Reuse the `tripo-runner.ts` HTTP seam + a new task `type`; gated by the same free-tier credit/non-commercial limits as the other four Tripo backlog entries.
- **(2026-08-14, AI-vs-human-retopology run) THE FACE BUDGET NOW HAS A UNIT, AND DELIVERIES ARE HELD TO IT (`1bda8be7`, `e781d60b`).** The video's most expensive mistake was a unit ambiguity — a commissioned "15K" came back as 15K **quads** (~2x the intended triangles), caught only at review — and PoF carried the same ambiguity live across three layers: `polycount-presets.ts` authored budgets with no unit, `tripo-runner.ts` exposes `quad` ("quad topology *instead of triangles*"), and `mesh-critique.ts` measures through trimesh, which triangulates on load. Tripo's own docs do not state whether `face_limit` counts quads or triangles, so it cannot be assumed. **New `src/lib/visual-gen/face-budget.ts`** (pure, 17 tests) fixes ONE authored unit — TRIANGLES — and converts explicitly at the provider edge (`providerFaceLimit` halves for quad topology; `quadBudgetFromTriangles` FLOORS so a rounding never authorises a mesh past its own budget), plus `gradeFaceBudget(measured, request)` → `honored | over | **unmeasured**`. A ~2x overrun names the quad/triangle trap by hypothesis; other ratios do not (it never blames quads for a 15x miss). **Two wiring holes this exposed and closed:** (1) `scoreMesh` only ever compared a mesh to its class CEILING (`maxFacesWarn`), never to the budget the caller REQUESTED — a character delivered at 55k passed clean under the 60k ceiling while being 1.4x the 40k asked for; `scoreMesh` now takes the request and reports a separate `budget` grade. (2) **`critiqueThresholdsFor` had ZERO production call sites** — the class-aware thresholds shipped 2026-08-09 were reachable only from tests, so every generated mesh was in fact graded against the class-blind 200k default, and `assetClass` was read at `generate/route.ts` purely to look up a face limit and then dropped. `critiqueDepsForSpec` / `critiqueDepsForFinish` now carry both class thresholds and budget onto the gate. Also `TripoResult.formatMismatch`: `quad` is documented to change the delivered container, so the delivered URL's extension is **measured** against the destination path rather than assumed — a `.glb` holding FBX bytes would lie to trimesh, the GLB viewer and the UE import alike.
- **(2026-08-14) ✅ RESOLVED — `runMeshFinish` HAS A CALLER (`e781d60b`).** The zero-call-sites blocker named in the 2026-07-28 entry and again on 2026-08-09 (where it blocked a second consumer) is closed: `src/lib/visual-gen/mesh-finish-job-store.ts` (`startMeshFinishJob` / `getMeshFinishJob` / `critiqueDepsForFinish`, 10 tests) + `POST /api/visual-gen/mesh-finish` + `GET /api/visual-gen/mesh-finish/status`. Job-based like the generator stores because a headless decimate+unwrap+Cycles bake runs minutes past any HTTP timeout. The finished low-poly is **auto-graded by the Tier-1 gate**, which is what makes `targetFaces` load-bearing: it is the budget the caller asked for, so the critique holds the delivery to it and not merely to the class ceiling (a run with no target invents no budget). The status route reports each absent thing as absent — skipped unwrap with its reason, a cull that could not see between shells with its limit — so a partial result cannot read as a complete one. **Remaining:** a StepSpec consumer (`character-pipeline` step 09 **Game-Tier Convert** still produces hand-typed constants `faceLimit: 40000, sizeMB: 4.3` with `checkerMeaningful: false` — this route is the engine that can MEASURE them), and `polycount-presets` as the default `targetFaces` at the route is wired but the step is not.
- **(2026-07-28) SKINS BUILT — one geometry, N texture sets.** `src/lib/visual-gen/tripo-skins.ts` (`runTripoSkinSet` + pure cores `buildTextureTaskBody`/`skinSlug`/`planSkinSet`/`checkSkinGeometry`; reuses `tripo-runner.ts`'s `TripoHttp` seam + `parseTaskCreate`/`parseTaskStatus`, so the fan-out is unit-tested with no network or credits; 22 tests). A skin is a **texture set plus its material settings over unchanged geometry** (gold is more metallic than base) — PoF could previously only express a variant by re-generating the model, which loses geometry parity and makes in-engine material swapping impossible. Fan-out is over `texture_model` keyed on ONE `original_model_task_id`, varying `texture_seed`/`texture_quality`. Design points worth keeping: `planSkinSet` validates the whole set **before spending a credit** (a slug collision would silently overwrite an already-paid-for skin), a partial failure keeps the skins that succeeded and names the one that didn't, and `checkSkinGeometry` grades face/vertex parity across the produced files into `consistent`/`divergent`/**`unmeasured`** — it never claims parity it didn't measure, and a divergent set fails the run because a re-meshed variant is not a skin. Surfaced as **`character-pipeline` step 10 "Skins"** (12 steps now; `manifest` view over `skinSet`, L0 `fieldsPopulated` — the spec linter enforces that the displayed field is the graded one). Not wired: an API route + a job store (the runner is the seam), and the geometry `measure` dep is unbound (wire `mesh-critique` to activate the parity verdict live).
- **(2026-08-12) Tileable-texture derivable set COMPLETE + SkinTokens rig backlog.** (a) `src/lib/texture-maps.ts` now derives **normal + height + roughness** from a generated albedo (height = the exported luminance field, usable as landscape displacement; roughness = documented inverted-luminance heuristic, `invert:false` opt-out; metalness deliberately NOT derived — no albedo signal, would be invented data); `POST /api/texture-maps` returns all three. Already-has for this area: Leonardo `tiling:true` + Scenario seamless-PBR gen, `seam-check.ts` wrap-edge gate, `biome-textures.ts` themed prompts. **Gap: the route has ZERO client callers** — material-lab/biome flows don't consume it yet. (b) **Backlog delta (rigging):** SkinTokens/TokenRig (VAST-AI, MIT, UniRig successor, GLB→rigged-GLB CLI, arbitrary creatures) is PoF's best shot at the missing non-humanoid rig path (bestiary "3D & Rig" A1) — specced in `docs/research/skintokens-rigging-spec.md`, install user-gated on flash-attn (TRELLIS-killer risk); see descoped-reopenable 2026-08-12.
- **(2026-07-28) `mesh-finish` gained `cullInterior`** — `bpy.ops.mesh.select_interior_faces` + delete, run on the low-poly copy **before** decimation so the face budget is spent on visible surfaces (the high-poly keeps its interior faces; it is only ever the bake source). Reports `facesCulled`, left `undefined` when no cull ran rather than a 0 it never measured. For an assembled multi-part character (armour over body, helmet over scalp) the buried surfaces cost budget and take UV-atlas islands while never reaching a pixel.
- **(2026-08-09) MULTI-PART CHARACTER IS NOW A FIRST-CLASS SHAPE in the Tier-1 gate (anime-character workflow run).** A production character is ASSEMBLED (head, lashes, brows, layered eyes, interior mouth with teeth+tongue, body, hands, hair, cape, accessories) — every part its own connected component. `mesh-critique.ts` hard-coded `maxComponentsFail: 8` **class-blind** (`scoreMesh` failed anything above it as "fragmented / floaters"), and `polycount-presets.ts` `critiqueThresholdsFor` only ever overrode `maxFacesWarn` — so a structurally correct 18-part character was rejected. Shipped: `pof_mesh_critique.py` emits **`COMPONENT_FACES`** (per-component face histogram, largest-first, capped `MAX_COMPONENT_HISTOGRAM`=4096) **+ `COMPONENT_FACES_OMITTED`**; pure `classifyComponents(faces, omitted)` splits **parts vs specks by FACE SHARE** (`FLOATER_FACE_SHARE` 0.005 / `FLOATER_MIN_FACES` 8) instead of by count; `scoreMesh` fails on **floaters** (`maxFloatersFail` 4) and on **substantial parts over a per-class budget**, and **falls back to the original blunt count rule whenever the histogram is absent** (no silent loosening on old data); `POLYCOUNT_PRESETS` gained **`maxComponents`** (character 24, environment 40, weapon/prop 6, modular-part 3) now returned by `critiqueThresholdsFor`. **Truncation is honest** — the histogram is largest-first so omitted entries are the specks; omitted are counted as floaters when the smallest kept entry is already a speck, else as parts (always the harsher branch, never a manufactured pass). **The checker got MORE discriminating, not weaker — live-proven on real Tripo output:** `jinx.glb` 375 components previously read as one blunt "375 disconnected components"; it now resolves to **61 substantial parts + 314 specks holding 94,331 faces = 36% of the face budget** — two distinct defects with two distinct fixes. Both real meshes still FAIL (correctly). New display-only `faceRigReadiness(metrics)` answers "can this head be expression-rigged at all" from geometry (`FACE_RIG_MIN_SHELLS` 4; returns `ready: null` when unmeasured) — deliberately NOT folded into `scoreMesh`, since one shell is perfect for a prop and merely unsuitable for a head. Gotcha SHIPPED: `assembled-character-is-multi-shell`.
- **(2026-08-09) ⛔ PROVEN — `cullInterior` is a SILENT NO-OP on the input class it was written for.** `pof_mesh_finish.py`'s cull calls `bpy.ops.mesh.select_interior_faces()`, which selects only faces whose every edge has **>2 face users** — i.e. WELDED interior. **Live Blender 4.2 headless probe:** a small cube fully enclosed inside a big cube and joined into ONE object → **0 of 12 faces selected**; a welded shared wall → **1 selected**. So the docstring's own premise ("the body under the chest plate, the scalp under the helmet") is exactly what it CANNOT reach — those are separate shells — and `facesCulled: 0` read as a measurement meaning "nothing hidden". Also settles the inverse worry: **the interior mouth/teeth/tongue and layered eyes are SAFE** from the cull. Shipped: the script emits `CULL_UNEVALUATED_SHELLS` (loose-shell count), pure `cullLimitReasonFor(facesCulled, shells)` → `cullLimitReason` on `ParsedMeshFinish`/`MeshFinishResult` naming the shells it could not evaluate, and both docstrings corrected to state the welded-only scope. **Backlog (not built):** real occlusion culling BETWEEN shells needs visibility testing (raycast or render-based coverage), not a topology operator.
- **(2026-08-09) Already-have catches (11th Stefan 3D AI run — expect a high catch rate from this channel).** The video's high-poly → sculpt-fix → retopo fallback, its per-PART generation, and its geometry-final-before-texture ordering are **all already shipped** in the `ai-lowpoly-generation-not-final` gotcha (`ue-gotchas.ts`, generate HIGH-poly per part → refine → retopo → unwrap on clean topology → bake), mined from this same channel's 2026-07-27 run; the structural half is enforced in code by `mesh-finish.ts` `unwrapPlan`. Per-part triangle budgets are covered by `polycount-presets.ts` (`modular-part`). Every TOOL demoed was GUI/credit-based (PixAI, Modif, Faceit, VC Face, Mixamo web, Tripo Studio) and pre-filtered by the standing user-pref rule; VRM/MToon + Project ARI are VTuber-avatar targets, off-domain for a UE5 game pipeline. **A "gate-fail → regenerate high-poly → decimate" routing policy still has nowhere to attach: `runMeshFinish` has ZERO call sites** (no API route, no StepSpec) — the wiring named in the 2026-07-28 entry is still the blocker, and it now blocks a second consumer.
- **(2026-07-24) ⛔ CATCH — `qwen-image-*` is GENERATION-ONLY; never propose it for recognition.** The Qwen-Image family (`qwen-image-2.0-pro` / `-max` / `-plus` / `qwen-image-edit`) does text-to-image + image editing on the `multimodal-generation` / `text2image` endpoints and has **no VQA / understanding capability**. Qwen-Image 3.0 (2026-07-21) also shipped **without open weights, benchmarks, or a technical report** — API-only (Qwen Chat / Qwen Studio / Alibaba API). Image RECOGNITION stays on the separate Qwen-**VL** seam `src/lib/anim-critique/qwen.ts` (`makeQwenVision`, OpenAI-compatible `…/compatible-mode/v1/chat/completions`), whose primary `qwen3.7-plus` is already the current DashScope VL flagship — **there is nothing newer to promote it to**. The two families share `QWEN_API_KEY` and nothing else.
- **(2026-07-24) Already-has — the VL quota-fallback hierarchy.** `qwen.ts` already implements per-model separate-quota fallback (`qwen3.7-plus` → `qwen3.6-flash` → `qwen3.6-plus`): `isQuotaError()` (429 / quota markers) falls through to the next model, a non-quota error throws immediately so the chain isn't burned. Chain is now re-tierable **by config** via `QWEN_CRITIQUE_FALLBACKS` (comma-separated; `none` = primary only) alongside the existing `QWEN_CRITIQUE_MODEL`. Three call sites ride the seam: `visual-gen/input-gate.ts`, `visual-gen/style-dna.ts`, `api/verify/animation/route.ts`.
- **Gaps / verdicts:** ⛔ **AccuRig is GUI-only (no CLI/headless/API)** → a manual tool, off the automation-loop goal and inferior to the planned MetaHuman-conform path (run `weekend-game-pipeline` 2026-06-18). Backlog providers (not picked): Tripo smart-mesh/segmentation-V2/retopology; Patina/Poliigon cheap PBR-set-from-image for Material Lab. **Named-part segmentation reinforced 2026-06-20** (CubePart run): segmenting a fused AI mesh into NAMED parts is the bridge `3D-gen → auto-rig / modular swap-slots` — promote Tripo-V2/Rodin segmentation from backlog to a real `visual-gen` provider+step (M-L) if pushing the asset pipeline; commercial-OK via their APIs. **Descope-reopenable: Roblox CubePart** (open/local named-part 3D segmentation) — research-only license + 24GB+ VRAM + lossy latent-resample (recreates a lower-detail mesh, doesn't split the input) → reconsider if it relicenses to MIT (as Pixel3D did). **License RE-CHECKED 2026-07-28 (same video re-submitted to `/research`) — UNCHANGED: Research-Only RAIL-MS** (Roblox/cube `cubepart`, SIGGRAPH '26 paper arXiv 2605.28763). Trigger has NOT fired; the 2026-06-20 verdict stands and the run yields nothing new — **don't re-research this URL (`youtube.com/watch?v=V3vTYFbHFJs`) again; re-check the license only.** **Tripo cloud multi-attachment backlog (2026-07-06, now that a live API key exists — the generation route is BUILT above; these are the OTHER Tripo endpoints, each a distinct pipeline attachment point, NOT built — reuse the `tripo-runner.ts` HTTP seam + a new task `type`):** (a) **texturing** (`texture_model`) — ⛔ **RE-SCOPED 2026-07-28, the original framing was WRONG:** `texture_model` takes `original_model_task_id` — *a prior Tripo task id* — and **nothing else**. There is no upload/file-token path, so it **cannot texture a Hunyuan3D shape, a TripoSR mesh, or a `mesh-finish.ts` low-poly**, and the pro workflow's "download → Blender UV → re-upload → texture" ordering is NOT reproducible through this API. Texturing externally-authored meshes needs a different engine entirely (projection+inpaint, or a texturing service that accepts mesh uploads) — that gap is still open. What `texture_model` DOES do natively is re-texture ONE Tripo geometry N times → **BUILT as skins, below**; (b) **segmentation** (`segment`/smart-mesh) → the reinforced 3D-gen→rig / modular-swap-slots bridge above (Tripo now has a live key, promoting it over the CubePart descope); (c) **rig/animation** (`animate_rig`/`animate_retarget`) → an alternative to the MetaHuman-conform auto-rig path; (d) **format conversion** (`convert_model` → fbx/usdz) for UE import. All gated by the same free-tier credit/non-commercial limits.
- **Animation engine backlog (2026-07-13, Curtis-Holt human-animation-pipeline run):** PoF's animation is non-generative (Tripo preset-retargets only; `/status` animation steps are UNPOWERED). **MetaHuman Animator (video→face+body markerless mocap) is a real automatable engine — its solve API is Python-exposed AND loads HEADLESS on our installed 5.8** (live probe: `unreal.MetaHumanPerformance` `start_pipeline`/`set_blocking_processing`/`set_processing_range`/`export_animation`/`diagnostics_indicates_processing_issue`; `MetaHumanPerformanceExportUtils.export_animation_sequence`; `MetaHumanIdentity.start_frame_tracking_pipeline`/`export_dna_data_to_files`; `FootageCaptureData.image_sequences`; plugins `MetaHuman`+`MetaHumanAnimationTools` installed, not yet enabled in `PoF.uproject`). Input = a plain camera clip, NO mocap gear — the "extract animation from ordinary video" capability. **Spec written** `docs/research/metahuman-animator-video-to-anim-spec.md` (reuse the `metahuman-conform.ts`/`ue-import.ts` seam via `runExperiment`; windowed solve + root-drift stitch = the 2 new gotchas; critique via `anim-critique/` L4). **2nd probe under `-RenderOffScreen` (2026-07-13):** `MetaHumanPerformance()` **instantiates under a real RHI**; `input_type` defaults to **`DataInputType.MONO_FOOTAGE`** (an ordinary single-camera clip — the no-mocap-gear premise, confirmed at the API level); `can_process()`=**False** until BOTH `identity` (`MetaHumanIdentity`) and `footage_capture_data` (`FootageCaptureData`) are set — that's the exact input gate; ingest classes (`MetaHumanCaptureSource`/`MetaHumanFootageComponent`) resolve headless; project has 0 footage/identity assets; `control_rig_class` deprecated in 5.8. **XL, NOT built.** **(2026-07-28) INGEST BLOCKER RESOLVED — 5 headless probes (`Content/Python/mha_ingest_probe{,2,3,4,5}.py`, markers `POF_MHAING*`).** The spec targeted the WRONG API: `UMetaHumanCaptureSource`/`…Sync` are **deprecated in 5.7** → the live path is **`unreal.CaptureManagerIngestBlueprintLibrary`** (`ingest_mono_video_sync` / `_stereo_video_` / `_take_archive_` / `_live_link_face_` / `_calibration_`; the `*_sync` docs literally say *"Intended for Python scripts"*). **Proven on a real mp4, no GUI:** `ingest_mono_video_sync(<mp4>,"","PoFProbeMono",1,CaptureManagerConversionParams())` → `(UFootageCaptureData, "")` under `-run=pythonscript -nullrhi` with `-EnablePlugins=MetaHuman,MetaHumanAnimationTools,CaptureManagerCore,CaptureManagerDevices,CaptureManagerApp,CaptureManagerEditor`; decoded 144 PNG frames to `…/AppData/Local/CaptureManager/Media/PoF/MonoVideo/PoFProbeMono_1/Video/` (**outside `Content/` — an external dep for the packaging collector**) + saved `/Game/CaptureManager/Imports/PoFProbeMono_1/CD_PoFProbeMono_1`. Catches: ingested `frame_rate` comes back **0** on both `FootageCaptureMetadata` and `ImgMediaSource.frame_rate_override` (writable; `IsInitialized()` rejects invalid rates). **BODY-only solve = PERMITTED** — engine source `MetaHumanPerformance::CanProcess` requires `Identity` only on the DEPTH_FOOTAGE branch; MONO_FOOTAGE needs footage + (`bFaceTracking` OR (`bBodyTracking` + the `IMetaHumanBodyTrackerInterface` modular feature)), so locomotion/combat needs no face identity. `MetaHumanBodyTracker` mounts headless (DLL loads; registers as a modular feature, exposes no Python UClasses). **Residual `can_process()`=False is engine-level, not footage:** `FMetaHumanSupportedRHI::IsSupported()` + **`FMetaHumanAuthoringObjects::ArePresent()`** — the same Optional-Content family as the conform gate. Remaining gates: (1) MetaHuman EULA clearance for solved data (esp. exported to Blender); (2) **MetaHuman Optional Content install (user, Epic Launcher)** — now the live blocker. → descope-reopenable on those two only. Knowledge SHIPPED: `metahuman-footage-ingest-capturemanager` `ue-python` gotcha. Two `ue-python` gotchas SHIPPED (`metahuman-animator-headless-memory-window` = window with `set_processing_range` vs the GUI's forced 30s pre-chunking; `metahuman-animator-window-root-stitch` = re-anchor per-window root/pelvis offset + Euler→quaternion compose). **Already-had catch:** the "AI mocap is reference-not-final, needs cleanup" eval criterion (`module-eval-prompts.ts` `arpg-animation`, line ~110).
- **DATAFLOW RIG-TRANSFER backlog (2026-07-27, Unreal Fest 5.8 rigging talk; AUTHORING PROVEN headless, build not started):** 5.8 Dataflow gained skeletal-mesh authoring + Python — Epic's Zebra→Monster **rig REUSE** (skin weights + morph targets + polygroups + skeleton transferred to a different mesh, and low→high-res up-res) is authorable headless via the SAME `DataflowEditorBlueprintLibrary` the Chaos-Cloth probes proved. Live probe 2026-07-27 (`rig_transfer_probe.py`): Dataflow asset created via `DataflowAssetFactory`, all 8 chain nodes added by F-prefixed struct name (`FGetSkeletalMeshDataflowNode→FSkeletalMeshToCollectionDataflowNode→FCollectionToMeshDataflowNode_v2` source / `FGetStaticMeshDataflowNode→FStaticMeshToMeshDataflowNode_v2` target / `FTransferMeshAttributesDataflowNode` / `FMeshToSkeletalMeshTerminalNode_v2` writes a rigged USkeletalMesh), all 6 connections True, saved. API deltas vs cloth: `add_dataflow_node` REQUIRES the 4th `location` (Vector2D) arg and returns the node NAME. Remaining = evaluate-on-real-data (probe-2 analog: donor SKM_Manny → a real generated mesh; AttributeProxies scripting; transfer quality gate). This is the "generated mesh gets a real rig" engine for non-MetaHuman characters — spec `docs/research/dataflow-rig-transfer-spec.md`; gotcha `dataflow-rig-transfer-5-8` shipped. Same probe verified **ControlRigDynamics** fully Python-exposed (secondary-motion chains for generated chars — the 2026-07-07 braid-physics descope's missing solver; still needs bone-chain insertion first).
- **TEXT→MOTION — PROVEN LIVE 2026-07-15 (was backlog):** full chain **text prompt → UE 5.8 AnimSequence, headless, $0, ~1 s GPU/clip**: ARDY installed at `kiro/ardy` (venv, torch 2.13+cu126; gated-Llama solved via `TEXT_ENCODERS_DIR` local assembly — McGill LLM2Vec adapters + public NousResearch Llama-3-8B-Instruct mirror, no code edits) → `scripts/generate.py` (4 verbs: slash/run/roll/idle — all coherent at seed 0; run 8.3 m @ 5.1 m/s w/ flight phases, roll with full tuck) → repo scripts `scripts/visual-gen/ardy/{pof_filmstrip,pof_npz_to_bvh,pof_bvh_blender}.py` (BVH is FK-exact vs posed_joints — 0.000 mm anti-scramble gate; Blender bakes a skinned proxy FBX) → UE `Content/Python/ardy_import.py` commandlet (needs `Interchange.FeatureFlags.Import.FBX 0` — gotcha updated) → `/Game/Generated/Ardy/*` SkeletalMesh+Skeleton+AnimSequence (3.95 s each, verified). **Remaining = productization:** IK-retarget to Manny (Core-27 names ARE Mixamo-convention — `mixamo_retarget.py` chains apply), `ardy-runner.ts` seam + Tier-1 npz motion gate, license review (NVIDIA Open Model + Llama 3 Community; request official meta-llama access to retire the mirror). Spec (now recipe): `docs/research/ardy-text-to-motion-spec.md`.
- **TEXT→MOTION original backlog note (2026-07-14, superseded above):** [nv-tlabs/ardy](https://github.com/nv-tlabs/ardy) (SIGGRAPH 2026, Apache-2.0 code + NVIDIA Open Model weights, headless `scripts/generate.py`, ~33 ms/step real-time on a 4090) generates full-body skeletal motion from **text + kinematic constraints (root paths/keyframes/joints) — NO footage, NO mocap, NO camera**. **Strategy reframe for the animation engine:** for BODY motion this sidesteps BOTH MetaHuman Animator's footage-ingest blocker AND the V3 generated-video quality ceiling (fused feet) — MHA stays the *face* path. Export is `.npz` joints/rotations/foot-contacts on Core-27/G1 skeletons (no FBX) → the UE chain is npz → scripted-Blender FBX bake → IK Retargeter (gotcha `ai-motion-generator-ue-ingestion` SHIPPED). Foot contacts in the export feed a $0 Tier-1 motion-sanity gate (mesh-critique analog). Spec: `docs/research/ardy-text-to-motion-spec.md`. Blocked on the user-gated local install (HF gated-Llama token + ~16 GB weights + CMake build) → descoped-reopenable.
- **CLOTHING-PHYSICS backlog (2026-07-22, Stefan 3D AI "Easy Clothing with AI" run; NOT built):** the character pipeline has **no apparel/clothing modality** — generated chars wear only baked-in geometry, never simulated garments. UE 5.8 makes this far more automatable: the node-based **Chaos Cloth Asset** editor is the default/production cloth path and **5.8 Dataflow added Python scripting** + a Python-exposed `unreal.ChaosClothComponent` — the same "authoring API scripts headless" signal that unblocked MetaHuman-conform / ARDY. **PROBE 1 DONE — PROVEN headless 2026-07-22** (installed UE 5.8, `-run=pythonscript -nullrhi -EnablePlugins=ChaosClothAsset,ChaosClothAssetEditor,ChaosClothAssetDataflowNodes`; `Content/Python/cloth_probe.py`): plugins load under `-nullrhi`; 70 cloth/dataflow classes resolve; **`ChaosClothAssetFactory` → `create_asset` → save → `does_asset_exist` True** — creating+saving a ClothAsset headless WORKS; the asset exposes `dataflow_asset`/`dataflow_terminal` (the authoring graph is reachable) and `DataflowBlueprintLibrary`/`DataflowEditorBlueprintLibrary` are exposed (the node-wiring path); transfer method is a reflected enum (`ChaosClothAssetTransferSkinWeightsMethod`). **PROBE 2 DONE — graph authoring PROVEN headless 2026-07-22** (`cloth_probe2d.py`): the graph-editing API is `DataflowEditorBlueprintLibrary` (`add_dataflow_node`/`set_dataflow_node_property`/`connect_dataflow_nodes`) + `DataflowBlueprintLibrary` (`evaluate_dataflow`/`regenerate_asset_from_dataflow`), all Python-exposed. Built the full chain headless: added all 4 cloth nodes (**node type = the full struct name WITH the `F` prefix**, e.g. `FChaosClothAssetStaticMeshImportNode`; un-prefixed returns empty), set `StaticMesh`/`SkeletalMesh`/`PhysicsAsset` (asset refs = object-path strings) = True, connected StaticMeshImport→TransferSkinWeights→SetPhysicsAsset→Terminal (terminal input pin = **`CollectionLod0`**, not "Collection LOD 0") = True, `regenerate_asset_from_dataflow` = True. `evaluate_dataflow` False **on the test only** — fed a Hunyuan blob → Jedi skeleton (`TransferSkinWeightsNode: Transferring skin weights failed` = data mismatch, not API). **Verdict: the entire authoring graph is scriptable headless — no feasibility blocker for the auto-skin/static-garment path.** **SEAM SCAFFOLDED 2026-07-22: `src/lib/visual-gen/chaos-cloth.ts`** (`buildClothGraphPython` pure recipe emitter + `attachClothToCharacter` dispatch via the `ue-experiment` runner; declares `CHAOS_CLOTH_PLUGINS` via a NEW `ExperimentSpec.enablePlugins` runner option so no `.uproject` edit is needed; `bound`=`evaluate_dataflow` is the Tier-1 gate — false ⇒ garment not fitted to skeleton). TDD 13 cloth + 2 runner tests, full ue-experiment+visual-gen suites green (157), tsc/eslint clean. **`apparel` catalog step BUILT** (candidate #5): step 07 `Apparel` in `character-pipeline.ts` (after UE Import; `archetype:'custom'`, manifest View, `runtimeDeferred('VSCharacterApparelTest')` L3; names the proven graph chain + the chaos-cloth.ts seam; walker auto-covers via the seeded Jinx entity; test updated to 11 steps; catalog guards green). **Remaining:** live end-to-end run on a REAL fitted garment (not yet run — the probe pair was mismatched-on-purpose) + weight-map painting (interactive, editor/bridge-gated). Weight-map painting stays brush-interactive (physics region-weighting = the one editor/bridge-gated part). **Launch gotcha:** `-script=<relative>` resolves against the engine `Binaries/Win64` CWD, not the project → pass an ABSOLUTE `-script=` path. Knowledge SHIPPED this run: `chaos-cloth-asset-5-8-workflow` `ue-python` UE_GOTCHAS entry. Spec (full recipe recorded): `docs/research/chaos-cloth-headless-spec.md`. Reusable probes: `<UE project>/Content/Python/cloth_probe.py` + `cloth_probe2d.py`. → reopenable (only the build + real garment input remain). **Already-have catch same run:** the "extract garment parts + review the part list, don't mesh a full costume at once" practice is ALREADY covered by `reference-roles.ts` `GEN_PROMPTING_PRACTICES` #7 (grid-combined multi-view segmentation + review-the-part-list) — the video reinforces it, adds nothing new.
- **VIDEO backlog (2026-07-13, user-requested — 3 linked items; NOT built):** the missing modality. PoF generates 2D images, 3D meshes, and audio, but **never video** — and video is now load-bearing because **MetaHuman Animator's footage input can be GENERATED, not just captured** (it wants `DataInputType.MONO_FOOTAGE` — an ordinary single-camera clip; it does not care whether a camera or a model produced it). That reframes the ingest blocker in `metahuman-animator-video-to-anim-spec.md`.
  - **(V1) Leonardo VIDEO generation** — extend the proven `scripts/visual-gen/pof_leonardo.mjs` seam (v2 `POST /generations` submit → v1 poll → download → **ALWAYS-delete** cleanup, per `[[feedback_leonardo_cleanup]]`) to Leonardo's video/motion models (text→video and image→video). Same auth/poll/cleanup discipline, new endpoint + task type; mirror the `tripo-runner.ts` pure-core + injectable-HTTP-seam shape so it's unit-testable. **Effort M.** Unlocks: motion reference clips, and (with V3) synthetic footage for the MetaHuman solve.
  - **(V2) Qwen frame analysis — "see what's inside a video" — EXPERIMENT RUN + VALIDATED 2026-07-13.** ffmpeg-sample N frames from an mp4 → feed the filmstrip to the ALREADY-COMMITTED `src/lib/anim-critique/qwen.ts` seam (`makeQwenVision`, DashScope `qwen3.7-plus`, OpenAI-compatible, multi-image in ONE call, built-in quota fallback) → structured JSON verdict. **Proven live on the OpenArt dance clip: $0, 66s, ZERO new infrastructure** — returned `body_solve_score 4/10`, `face 2/10`, `suitable_for_body_mocap:false`, naming *merged footwear (frame 2)*, *blob hands (frame 3)*, *inconsistent pants*. **Every cited defect independently verified by eye → the gate is DISCRIMINATING, not hallucinating.** (Note: the *local* `pof_vlm_critique.py` Qwen3-VL-4B path also exists but is mesh-critique-prompted; the **API seam is the better V2 base** — no VRAM, stronger model, already committed.) **Effort S-M** — productize as `videoFrameCritique` (ffmpeg sample → vision seam → parsed verdict) with a **foot/contact-coherence** criterion, not a generic "looks AI" score. Gives PoF its first video-understanding gate. **PRODUCTIZED 2026-07-27 as `src/lib/visual-gen/footage-gate.ts`** (`gateFootage` — ffprobe/ffmpeg pure cores + injectable sample seam → `makeQwenVision` in ONE call → shared SCORE/DEFECTS/VERDICT parse + Scorecard; fused-feet named as THE body-solve disqualifier, blob hands tolerated) + `POST /api/visual-gen/footage-gate` ({videoPath, purpose:'body'|'body+face', frameCount} → card; can't-run = 502 with reason). 12 tests; mirror of input-gate.
  - **(V3) Generated-video → MetaHuman Animator footage** — the payoff chain: a generated clip → `FootageCaptureData` (via `MetaHumanCaptureSource`) → solve → `export_animation_sequence` → a UE AnimSequence. **text → video → animation**, no camera, no mocap gear. Still gated on 2 open questions (is footage ingest scriptable headless? can a body-only solve via `MetaHumanBodyTracker_5.8` skip the face `MetaHumanIdentity`?). **QUALITY CEILING NOW MEASURED (2026-07-13, via V2):** current AI-generated video is **NOT yet a reliable footage source** — the OpenArt dance clip's torso/limb/head skeleton is coherent + trackable (clean silhouette, full body in frame, static camera, dark-on-light backdrop = near-ideal markerless conditions), but the **EXTREMITIES morph**: the two shoes FUSE into one mass, hands become blobs. For a body solve, blob hands are survivable (body trackers don't solve fingers) but **fused feet are disqualifying — foot contact drives root motion.** → **Do NOT build V3 on the assumption generated footage will solve.** The cheap reliable footage source is a **REAL camera clip** (a phone video has zero extremity morphing); generated footage stays the ambitious path, with V2 as the instrument that says when generation is good enough. **Effort L, XL with V1.** **NEW EVIDENCE 2026-07-27 (Stefan 3D AI "2027 workflow" run): the frontier may have crossed the ceiling** — the demo's UE markerless-mocap footage was itself AI-GENERATED (Gemini/Veo-class, logo visible in-frame) and the solve produced usable body+face animation. The 2026-07-13 "fused feet disqualifying" measurement was on an OpenArt clip and is likely stale for current Gemini/Veo output. The re-measure is now one command: run `gateFootage` (above) on one current-gen clip.
  - **Test asset on hand:** `C:/Users/kazda/Downloads/023-poping-content-video_1772720409703_5be35721.mp4` (OpenArt-generated popping/dance, h264 768×1168, 30fps, 144 frames, **4.83s** — under the 30s RAM ceiling, so a first solve needs no `set_processing_range` windowing).
- **SET-DRESSING / prop composition (2026-07-28, Composition Maker run — BUILT):** `generators/` covered terrain / dungeon / vegetation but had **no clutter-composition generator** — vegetation is 2D Poisson scatter with no stacking, support, or affordance notion, and PCG knowledge covers biome-scale placement, not "arrange these props so they rest on each other". Shipped `generators/placement-tags.ts` (the `PlacementAffordance` contract — `place: floor|surface|any`, `stackable`, `copies`, `maxStack`; size-class presets + `affordanceForSize` from mesh bounds; **round-trips to real UE actor tags** `place_floor`/`stack_true`/`copy_3`/`max_stack_10` so in-editor-authored props are readable and generated compositions are spawnable) + `generators/composition.ts` (pure seeded solver → transform manifest: largest-first fill, **support footprint ≥ prop footprint** — the rule that actually prevents "large prop on a thin surface", children laid across a support's top so wide supports carry several props and same-width ones become vertical stack rungs, run height capped by the floor prop's `maxStack`, yaw jitter, and **`unplaced[]` with a reason** rather than silent drops). 21 tests. Consumers to wire (not built): a set-dressing catalog step, and the UE-side spawn script that reads the manifest.
- **⛔ HEADLESS PHYSICS SETTLE — GROUND-TRUTHED IMPOSSIBLE in the commandlet (2026-07-28, 2 live 5.8 probes).** The natural follow-on ("settle the composition with physics, bake the transforms") is **not** available under `-run=pythonscript`, despite full API resolution. Probe 1: everything introspects fine (`PrimitiveComponent.set_simulate_physics`/`is_simulating_physics`/`put_rigid_body_to_sleep`, `LevelEditorSubsystem.editor_play_simulate`/`is_in_play_in_editor`, `EditorActorSubsystem.spawn_actor_from_class`, `SystemLibrary.begin_transaction`, `PhysicsAsset`/`BodyInstance`/`ChaosSolverActor`). Probe 2 (the decisive one): the commandlet world is a transient `/Temp/Untitled_0` with **no physics scene** → `set_simulate_physics(True)` leaves `is_simulating_physics()` **False** and a cube spawned at z=500 never falls; and `editor_play_simulate()` is a **FATAL crash** (callstack through `UnrealEditor-PythonScriptPlugin.dll`, exit 3) — not a catchable exception. No scriptable time-advance exists (`delay_until_next_tick`/`set_timer_for_next_tick` need a tick that never comes). **Split:** the BAKE half (read/write transforms, actor tags, transactions, map save) is fully headless; the SETTLE half needs a ticking world → the **`-game` scenario path** (`ScenarioController`, already proven) or the interactive editor. Same shape as the headless-render finding — the commandlet is an asset-authoring tool, not a simulation host. Gotcha `headless-physics-needs-ticking-world` shipped. **Backlog (M, not built):** a settle verb on the scenario controller — spawn the manifest, enable physics, tick N seconds, emit settled transforms through the observation spine, python stamps them back. Reusable probes: `<UE project>/Content/Python/physics_settle_probe.py` + `physics_settle_probe2.py`.
- **Docs/memory:** `docs/visual-generation-roadmap.md`, `docs/ue5-capability-integration-candidates.md`, `docs/research/metahuman-animator-video-to-anim-spec.md`, `[[project_animation_alternatives]]`, `[[project_anim_critique]]`, `[[reference_leonardo_api]]`, `[[feedback_leonardo_cleanup]]`.

### Stores / API
- **Anchors:** `src/stores/` (Zustand v5 + persist), `src/lib/*-db.ts` (better-sqlite3), `src/types/api.ts` + `src/lib/api-utils.ts` (`apiSuccess`/`apiError`, `apiFetch`).
- **Lands here:** persistence, the `{success,data}` envelope, new API routes.
- **(2026-07-29) `pipeline_artifacts` now has version history.** It is keyed `(catalog_id, entity_id, step)` and upserted, so **every re-produce destroyed the prior output** — galleries survived only because their batches live inside `data.genHistory`; ~200 static steps had no recovery at all. `upsertArtifact` archives into **`pipeline_artifact_revisions`** but ONLY when `contentChanged` (data/ueAssets differ) — a gate drain, `verify-static` and `verify-packaging` all re-upsert identical data with a new verdict and would otherwise bury the real versions. Bounded to `MAX_REVISIONS` (20)/step; each row keeps its own `updated_at` (when written) + `archived_at`. **`GET/POST /api/pipeline-artifacts/revisions`**: the restore RE-GRADES via `gradeArtifact` (an archived verdict can be stale, and trusting a stored `status` would re-open the fabricated-pass hole the produce POST closed) and returns `regraded`/`archivedStatus` so a changed verdict is never silent. A restore is itself a content-changing upsert ⇒ undoable. UI: `StepHistoryPanel`, loaded on demand.
- **Docs/memory:** `docs/architecture/state-and-persistence.md`.

### Marketplace / Asset Scout (gap → asset → integration codegen)
- **Anchors:** `src/types/marketplace.ts` (`MarketplaceSource='fab'|'ue-marketplace'`, `MarketplaceAsset`, gap/recommendation types), `src/lib/marketplace/` (`integration-generator.ts` `generateIntegration` → C++ adapter header/impl + build/plugin deps + TODO-warnings; `asset-catalog.ts` `ASSET_CATALOG`), UI `src/components/modules/evaluator/AssetScoutView.tsx`.
- **Lands here:** matching feature-matrix GAPS to marketplace assets (time-saved scoring) + generating a C++ adapter that bridges a bought asset to the project's GAS/inventory/character systems.
- **Already-has:** curated-catalog recommendation + adapter codegen (with scaffold-TODO warnings so nothing is presented as "done").
- **Backlog delta (from /research 2026-07-05, Aura demo):** the catalog is a **static `ASSET_CATALOG`**, not a LIVE Fab search; and there is no in-editor **auto-import + auto-wire** ("hook it up") step. Live Fab search needs Epic's Fab API (auth); auto-import/wire is editor-side (mcp-unreal / PoF Bridge). → descope-reopenable (editor-dependent + external API).

---

## C — UE bridge / MCP

- **mcp-unreal (`:8090`):** external C++ plugin (`…/Unreal Projects/PoF/Plugins/MCPUnreal/`), driven by a stdio MCP server declared in `.mcp.json` → the `mcp__mcp-unreal__*` tools. ~37 hand-registered routes, no tool-search. **Lands here:** raw UE control tools, `execute_script` ("code mode").
- **PoF Bridge (`:30040`):** `…/Plugins/PillarsOfFortuneBridge/` (auth-token REST); app client `src/lib/pof-bridge/client.ts`, `src/stores/pofBridgeStore.ts`, `LabBridgeStrip`. **Lands here:** status/manifest/test-runner/snapshot, the verification+auth layer.
- **UE 5.8 convergence:** `docs/ue58-mcp-convergence-plan.md` (adopt first-party `AICallable`/Toolset-Registry control surface, keep PoF's verification moat). **Engine targets 5.8 — UE_5.8.0 IS installed** (`C:/Program Files/Epic Games/UE_5.8`; `PoF.uproject` EngineAssociation `5.8`; corrected 2026-07-05, the old "targets 5.7 / not installed" note was stale). MetaHuman + MetaHumanBodyTracker plugins enabled; the `MetaHumanCharacter` conform plugin is present-but-not-enabled.
- **Docs/memory:** `docs/ue5-companion-plugin-design.md`, `docs/ue5-capability-integration-candidates.md`, `[[project_ue58_official_mcp]]`.
- **Backlog deltas (from /research, target the external plugin — not in this repo):** (1) **runtime Python-API introspection tools** (list a class's methods/signatures/docstrings/subsystems) so the agent self-discovers UE APIs instead of guessing — `mcp-unreal` has `lookup_class`/`lookup_docs`/`subsystem_query` but not full reflection; VibeUE ships this. (2) **blueprint graph auto-layout/tidy pass** (generated BPs are "spaghetti"; no auto-layout tool exists). (3) **StateTree editing tools** (VibeUE ships ~94 methods; PoF targets StateTree AI — verify the plugin's coverage on reconnect). Source: `Research/2026-06-18-claude-took-over-ue5.md`.

---

## D — Prompt / knowledge / skill

- **Anchors:** `src/lib/module-registry.ts` (checklist prompts), `src/lib/prompts/` + `prompt-context.ts` (`buildProjectContextHeader`, 6-section builder), `src/lib/evaluator/module-eval-prompts.ts` (4-pass eval), `src/lib/knowledge/ue-gotchas.ts` (`UE_GOTCHAS` + `formatGotchas`, filtered by `PromptKind`), `src/components/cli/skills.ts` (12 domain skill packs), `src/lib/cli-task.ts` (`TaskFactory`, `@@CALLBACK`).
- **Lands here:** prompt-quality ideas, new skill packs, UE_GOTCHAS/tripwires (UE_GOTCHAS + tripwire + known-assets + module `knowledgeTips` via `formatKnowledgeTips` all reach dispatch prompts). To add a UE pitfall: append to `UE_GOTCHAS` with `appliesTo` (`ue-python`/`ue-cpp`/`packaging`) + a test in `__tests__/knowledge/ue-gotchas.test.ts`.
- **Already-has:** prompt-injected UE pitfalls incl. (2026-06-18) a `ue-python` "introspect-before-you-guess" gotcha + 3 `lumen-*` gotchas (SWRT thin-geo Distance-Field-Resolution / detail-vs-global by world scale / HWRT surface-cache reflections → Hit Lighting for Reflections) + a modular-character accessory/rigging gotcha (rigid-accessory single-bone weight / occluded-mesh hiding / exclusive swap-slots) + 2 Niagara optimization gotchas (Effect Types significance/max-instance/visibility-cull — hidden systems still tick; Insights needs `stat named events`) + 2 GAS `ue-cpp` gotchas (meta-attribute damage via GE + clamp in PostGameplayEffectExecute, never SetX; replicated-attr GAMEPLAYATTRIBUTE_REPNOTIFY + cues are cosmetic-only) + a Motion Matching gotcha (anims need root motion even w/o capsule root motion; **Phase channel crashes the editor**; cost-bias tuning; foot-sliding fixes via play-rate window / Dead Blending) + an AI-mesh segment-into-named-parts-before-rigging gotcha (`ue-python`: text/image-to-3D yields one fused mesh that is hard to rig/animate/modularize → segment into named parts via geometry-accurate cutters Tripo V2/Rodin over latent-resample CubePart, then apply the modular-character rules) + (2026-07-05) 2 MetaHuman-conform `ue-python` gotchas (`metahuman-conform-input-prep` — for a clean UE 5.8 Mesh-to-MetaHuman auto-solve the AI mesh needs A-pose / separated fingers / armpit+leg clearance / hair+lashes removed / separate high-poly head / scale-to-conform-body-ref; `metahuman-conform-texture-export` — save the DNA pose BEFORE it changes so baking works / shift UDIMs −1 to one UE UV tile / bake-free AI-texture path keeps 'use original UV' / disable Add-Leaf-Bones on FBX export / flip normal-map green channel) + (2026-07-05) a `ue-cpp`+`ue-python` GAS work-decomposition gotcha (`gas-author-abilities-incrementally` — author an ability one coupled piece at a time: tag → input → effect → ability → grant/bind → cue; one-shotting a whole ability/system leaves it partially-wired and silently inert; verify each layer before the next); evaluator `arpg-world` performance checks incl. (2026-06-18) static-mesh polygon-budget / Nanite-LOD criteria + `arpg-animation` quality checks incl. generated/AI-mocap source guidance + `arpg-world` structure checks incl. PCG procedural-placement (data-flow graphs, graph instances, runtime hierarchical gen).
- **Docs/memory:** `[[reference_prompt_knowledge_injection]]`, `docs/architecture/prompts-and-cli.md`.

---

## E — Framework / cross-cutting (automation-framework bets)

- **Verification moat (Tiers of Truth T0-T4):** no "done" without a ground-truth observation; T4 = an agent reads a rendered frame. Anchors: `src/lib/test-gate-runner/`, observation spine. `[[project_llm_ue_interface]]`. **L3 live-UE drain RE-VERIFIED on UE 5.8 (2026-06-21):** spawn path (`UnrealEditor-Cmd … -ExecCmds="Automation RunTests <name>;Quit" -nullrhi -abslog`) launches + runs + emits `Result={Success/Fail}`; full drain flow proven end-to-end (`src/__tests__/lib/test-gate-runner/live-drain.integration.test.ts`, env-gated `POF_UE_EDITOR_CMD`/`POF_UE_UPROJECT`/`POF_RUN_UE_GATES`, skipped by default): deferred gate → `drainAll([spawn])` → UE runs GenFireball → flips **deferred→pass** in SQLite. ⚠️ `Automation RunTests` matches the REGISTERED name (dotted path/substring) not the C++ class name. **Name alignment DONE (2026-06-21), verified vs UE's enumerated `Automation List` (26 registered tests):** of 30 gates, **10 resolve** / **22 planned**. Simple-automation gates register `Project.Functional Tests.PoF.<Topic>.<Sub>` (align to the dotted path: characters/currency/spellbook/status-effect/**bestiary**); functional map-tests register `…Maps.<Map>.<ClassName>` so a class-name deferral already resolves (items/loot as-is; **hud-elements**→VSHUDFunctionalTest, **zone-map**→AshenForestSetupTest, **combat-map**→VSArenaSetupTest). 22 have no registered test → correctly skip. Note: materials' VSMasterMaterialInstanceTest.cpp exists but doesn't enumerate (UE-side follow-up). `deleteArtifact` added to `pipeline-artifacts-db`.
- **Pipeline data contract:** UE↔SQLite (SQLite = authoring SOR, UE = realized truth), 4-tier acceptance ladder (L0-L4 + `deferred`). `docs/catalog/WIRING-AND-ACCEPTANCE.md`, `[[project_pipeline_data_contract]]`. **Battle-test 2026-06-20/21:** core produce→accept wiring SOUND — 322/322 steps config-complete across 30 pipelines (new fast guard `src/__tests__/catalog/pipeline-produce-accept.test.ts`, browser-free; the e2e walker is perf-fragile under `npm run dev` + silently latched onto a non-PoF dev server on a shared port). **L2 gap CLOSED:** the `staticChecks` tier (`cppSymbolExists`/`seedRowPresent` vs real UE `Source/*.h` + `Content/Python/`) was declared on 24 pipelines (54 checks) but had ZERO production consumers — now wired via `src/lib/catalog/acceptance/staticVerify.ts` (`aggregateStatic` + `verifyStaticAll`, deps-seam, 7 tests) + `POST/GET /api/pipeline-artifacts/verify-static` (drain-style: filesystem, no bridge) + `listAllArtifacts` (db). Against the real UE root: 17 ARPG symbols present (pass), 17 not-yet-built (deferred). **Walker HARDENED (2026-06-21):** `e2e/global-setup.ts` runs once — IDENTITY GUARD (waits for the `harness-lab-ready` marker → one actionable error via `e2e/helpers/pof-identity.ts` `pofNotDetectedMessage` if a non-PoF dev server squats on the port, instead of N silent timeouts) + WARM-UP (compiles the heavy `/layout` lab so each spec's `gotoLab` stays inside 30s); `reuseExistingServer: !CI` (CI starts a fresh PoF server). **`player-movement` orphan RESOLVED (2026-06-21):** given a `NEW_CATALOGS` section + starter (Manny Locomotion), and its bridge checkers (`pythonStepSuccess`/`pythonStepOk`/`humanConfirmed`, used only by it) reclassified not-yet-run `pending`→**L3-deferred-to-the-bridge** (config-complete, the correct model — a dispatched-but-not-run python step IS runtime-deferred), so the walker + headless guard now cover it; `WALKER_SKIP` cleared. REMAINING: optional prod-build e2e; L3/L4 need the live bridge. **L2 static-checker tests FIXED (2026-06-21):** 13 tests (`ueStaticCheckers.test.ts` + ~9 `staticChecks` pipeline tests) were failing under vitest's global jsdom env — `serverModules()`'s `typeof window` guard tripped (jsdom defines `window`) + its indirected `require` doesn't exist in ESM. Fix: `serverModules()` now resolves fs/path via **`process.getBuiltinModule`** (sync builtin accessor, Node 20.16+, works in CJS *and* ESM incl. vitest jsdom-on-node) — it guards on "is there a Node runtime", not "is `window` defined", and still uses no static import (client-bundle-safe; browser → no getBuiltinModule → throws server-only). **230 catalog tests green (was 13 red).** Bonus: also unblocks `resolveUeRoot`/`runStaticChecks` under vitest, so #1's L2 verify is now testable end-to-end.
- **Event bus / lifecycle / suspend:** `src/lib/event-bus.ts`, `src/lib/lifecycle.ts`, `src/hooks/useSuspend.ts`. `docs/architecture/runtime-patterns.md`.
- **Server cron:** `src/instrumentation.ts` (the only server wall-clock interval; nightly builds). `[[project_server_cron_scheduler]]`.
- **Lands here:** orchestration patterns, contracts, closed-loop idea→UE→tested, anything that makes the whole pipeline more autonomous/trustworthy.
- **Research validation (2026-06-18):** the 2025-26 agentic-3D-asset-generation frontier (arxiv 2505.20129 / SAGE / 3DCodeBench / Articraft) **converges on PoF's architecture** (render→VLM-critique→refine + structured-KB working memory = observation spine + pipeline-artifacts/impact-map); PoF adds bounded iteration + persistent authoring truth the papers lack. Synthesis: `docs/research/agentic-3d-asset-generation.md`. **Gap:** asset-GENERATION has no self-correction loop (`visual-gen/poll.ts` is fire-and-poll) → spec `docs/research/self-correcting-asset-gen-spec.md` (reuse the L4 visual critic as the asset critic).

---

## Maintenance log
- **2026-07-29** — run `tesana-ai-indie-games` (Stefan 3D AI, "This Is the Best AI Tool for Making Indie Games — Tesana AI"; **9th run from this channel**). Source class: a hosted competitor PRODUCT (Godot-based AI game engine, chat-left/preview-right studio, model picker, exportable source) — the platform itself is off-domain by the standing GUI/closed-tool user-pref, and the user's framing was the right one: mine its **studio UX mechanisms** for `/layout`. Expected product-demo yield held (6 already-have catches at the gate + more at verification), but **two picks changed shape under verification and one collapsed into another**, which is where the run's value was. The "clickable recommendations" idea resolved to an already-have (`NextStepCoach` + `GlobalCoach` + `coachLadder`) and was **re-scoped to the honest gap the coach still had** — its terminal "All done." asserted at entity level exactly the overclaim `ProvenanceStrip` removes per step. The "failure inbox" and "prompt history" picks were each half-present (`ProduceErrorBanner`, `data.produceDirection`) and both were invisible outside the one open step, so they merged into ONE per-entity produce log. **The biggest find was not in the video at all:** chasing "show the cost" led to `taskClassForDispatchType('one-shot-step') → null` — the lab's live CLI produce was the one dispatch in the app the Quality Program's model policy never governed (see the A-bucket warning above). Two schema-level gaps confirmed and closed: `pipeline_artifacts` had no history (every static re-produce was a one-way door) and no step could reach `asset_library`. **6 commits, all TDD** (`001704a8` coach honesty · `d7f30ba5` produce log + `errorAt` · `468ecb26` model pin + dispatch plan · `a6e58988` step evidence · `+` revisions + restore · `32242258` library picker). Recurring design rule across all six, worth reusing: **when a surface can't prove something, say the count and name the weakest case — never render a neutral default** (an unqualified "All done.", a `$0.00` cost, a blank license, a cited swatch, a silent re-grade all fail this). **Shared-tree note:** a parallel session's `git cherry-pick 85417c65` conflicted mid-run in `cli-spend/preflight.ts`; resolved keeping BOTH label sets and staged, without running `--continue` (their operation to finish). Pre-existing failures observed and deliberately NOT touched: 4 `task-prompt-golden` drifts from the 2026-07-28 composition-maker gotchas (goldens never re-recorded), 1 `statusModel` dual-execution `realization` assertion (Group B in flight), 1 load-sensitive `viewTransition` 5s timeout that passes in isolation.
- **2026-07-28 (2)** — run `ai-character-with-skins` (Stefan 3D AI, "From AI to 3D Game Character with Skins"; **8th run from this channel** — the standing expectation that a creator demo yields knowledge + one pipeline gap, not tools, held again; the tool shown, Customuse, is a web node canvas and was pre-filtered under the GUI user-pref along with annotate-region editing and the multiplayer canvas). User picked 1/2/3/4/6. **The run's load-bearing result is a CORRECTION, and it arrived before any code was written:** finding #1 was going to build the impact-map's own tracked "highest-value follow-on" — Tripo `texture_model` to texture Hunyuan3D's untextured shapes — and the SDK reference says `texture_model` accepts `original_model_task_id` and nothing else. **No mesh upload path exists**, so it can never texture a Hunyuan shape, a TripoSR mesh, or our brand-new `mesh-finish.ts` low-poly, and the video's own "download → Blender UV → re-upload → texture" ordering is not reproducible through it. The backlog delta was wrong as written for as long as it has been tracked; it is now re-scoped in place, and texturing externally-authored meshes is still an OPEN gap needing a different engine. **What the same endpoint does do natively turned out to be finding #2** — re-texturing one geometry N times IS the skin modality — so #1 and #2 collapsed into one honest build (`tripo-skins.ts`, 22 tests, + `character-pipeline` step 10 "Skins"; see the SKINS entry above). **Also shipped:** `mesh-finish` `cullInterior` (#6, interior-face delete before decimation) and 2 `GEN_PROMPTING_PRACTICES` (#3 noise mask driving roughness AND metallic — the named cause of flat generated materials, and the cheapest per-asset-free upgrade; #4 emission as its own mask, never baked into albedo — which is also what lets skins differ in glow). **Catches:** per-part polycount budgets (`polycount-presets.ts` already has the `modular-part` 8k class covering the video's 3.5k–8k range) · mirror-the-symmetric-half and clean-UV-before-texture (both shipped yesterday) · part extraction (practice #7) · accessory physics rigging (Chaos Cloth) · AccuRig (standing 2026-06-18 decline). **Process note:** the spec linter caught the first Skins step declaring a `skins` view field that acceptance never graded — the guard did its job, and the fix (grade the field you display) made the step better.
- **2026-07-28** — run `composition-maker-ue5` (Andrew Averkin, "Composition Maker for Unreal Engine 5" — an artist's editor-Python tool that auto-arranges prop clutter from tag-declared placement rules). Source class: tooling demo whose *tool* is a GUI editor menu (off-domain per user-pref) but whose **mechanism** is plain UE Python. User picked 1-5. **Built (M):** the set-dressing generator pair `generators/{placement-tags,composition}.ts` — PoF's `generators/` had terrain/dungeon/vegetation but nothing that arranges props ON each other; candidate #4 (affordance metadata) folded into `placement-tags.ts` as the typed contract + UE-actor-tag round-trip rather than a separate prompting practice, since it is data a generator consumes, not prompt text. 21 tests. **Knowledge shipped (TDD, 120 green):** `prop-placement-affordances-not-bounds` (`ue-python`, world-scoped — the tag taxonomy + largest-first + footprint-must-fit + jitter, and "prefer a physics settle over more solver rules for piles") and `headless-physics-needs-ticking-world` (below); `arpg-world` qualityChecks gained 2 set-dressing believability criteria (rest-on-a-big-enough-support, nothing on a non-flat top, bounded runs, rotation variation, flush contact, settle-and-bake for piles). **The run's hardest-won fact came from candidate #3's probe pair:** a headless physics settle is IMPOSSIBLE in `-run=pythonscript` — full API introspection is a false positive; the commandlet has no physics scene (`is_simulating_physics()` stays False) and `editor_play_simulate()` fatally crashes it. Recorded as a gotcha + a `-game`-path backlog item. **Catch:** #7 copy/variation batches = `CandidateGallery` (already-have). **Verified-distinct:** the existing `arpg-world` PCG criterion is biome-scale scatter — it does not cover rule-based composition, so #1/#5 were real gaps, not restatements.
- **2026-07-28** — run `marvel-rivals-free-ai-character` (Stefan 3D AI, "I Recreated a Marvel Rivals 3D Character Using Only FREE AI"; 7th run from this channel — the expected knowledge-not-tools yield held, and the shipped build came from a *step* in the workflow, not a tool in it). User picked 1/3/4/5/6. **The load-bearing find: the video's middle third (retopo → UV → high→low bake) was a whole pipeline STAGE PoF did not have** — the impact-map's own visual-gen entry said texturing was "deferred" and every generated mesh went to UE dense and UV-less. **BUILT + LIVE-PROVEN (see the MESH-FINISH entry above):** `src/lib/visual-gen/mesh-finish.ts` + `scripts/visual-gen/pof_mesh_finish.py` — 1.49M → 40k faces with real baked normal/AO on `jinx_hd.glb`, $0, no new dependency (Blender 4.2 was already installed and already used headlessly by the ARDY converters, just never from a TS seam). **Two picked practices landed as ENFORCED CODE rather than knowledge text** (#3 never-unwrap-the-high-poly → `unwrapPlan` + `UNWRAP_FACE_CEILING` with a surfaced `unwrapSkippedReason`; #5 mirror-the-symmetric-half → the `mirror` option) — a better home than a prompt line, since a prompt can be ignored and a guard cannot. **Knowledge shipped (TDD):** 3 `GEN_PROMPTING_PRACTICES` — rebuild eyes as UV'd sphere primitives (#4: image-to-3D bakes eyes into the face surface; the single most read-on-sight character defect, and the prerequisite for an eye/blink rig), finish-one-half-and-mirror (#5, documents the code option + the asymmetric-design exception), and texture-by-projection-then-inpaint (#6's transferable half). **Catches/declines:** #6's tool (Modif) is a web GUI → declined per the promoted user-pref rule, and PoF's real texturing path is the already-tracked Tripo `texture_model` backlog delta — the video's contribution there was the *repair discipline*, not the tool; #7 Hunyuan3D Studio retopo/UV = same GUI rule (and now redundant — the Blender path does it headless); #8 AccuRig = the standing 2026-06-18 decline. **Structural facts recorded:** `blender-mcp/scripts/` is a LIVE-Blender-over-TCP surface (needs the addon running) and is level-geometry oriented — it is not the headless asset path, and the two are now distinct entries in this map. Verification note: the run followed `[[feedback_verify_runtime_outputs]]` — the bake was not called done until the PNGs were opened and checked for real value range (a blank bake would have passed every unit test).
- **2026-07-27 (2)** — run `ue58-rigging-animation-state` (Epic, "State of Rigging and Animation Tools in UE 5.8", Unreal Fest Chicago 2026 — authoritative engineering talk on PoF's weakest domain). User picked all 5. **Flagship VERIFIED LIVE: Dataflow rig-transfer authoring is headless-scriptable** (see the DATAFLOW RIG-TRANSFER backlog entry above; spec `dataflow-rig-transfer-spec.md`; probe `rig_transfer_probe.py` — 4th probe in the MetaHuman-conform/MHA/Chaos-Cloth "new UE capability → ground-truth it headless" series, and the engine-SOURCE grep for node struct names before probing made the probe converge in one iteration). **3 gotchas shipped (TDD 72/72):** `dataflow-rig-transfer-5-8` (full node chain + 4-arg add_dataflow_node), `control-rig-dynamics-secondary-motion` (5.8 particle secondary motion, RigUnit_SpawnDynamicsChains Python-exposed — evidence against the braid-physics descope's solver gap; chains still need bones), `layered-physics-anim-transition-smoothing` (5.8 layered-rig physics + FBIK smooths clip-boundary hard pops — the in-UE alternative to npz-level re-anchor; bake w/ warm-up frames). **Notes:** Zebra "Striker Stripe" + Monster + "Fortnite rigged modules" plugin released free on Fab (production-grade modular body+face reference rigs; face deformers run at RUNTIME via post-process AnimBP) — reference target for rig study, Asset-Scout curated-catalog candidate. Tab-utility-actions (contextual editor-viewport Python) = editor-side, mcp-unreal backlog crumb. Pre-filtered as GUI-interactive per user-pref: gizmos, curve-editor/sequencer UX, DMC painting, face sculpting, shot correctives, auto-bake toggles. Catches: direct-reroute ≠ the descoped BP auto-layout (manual clicking).
- **2026-07-27** — run `pro-ai-character-workflow-2027` (Stefan 3D AI, "This Is How 3D Artists Will Work in 2027 — Pro AI Character Workflow"; knowledge source, 6th run from this channel — expected knowledge-not-tools yield held). User picked all 4. **The load-bearing find: the demo's UE markerless-mocap footage was itself AI-GENERATED (Gemini) and solved** → contradicts the 2026-07-13 OpenArt fused-feet ceiling → **BUILT the V2 instrument**: `src/lib/visual-gen/footage-gate.ts` + `POST /api/visual-gen/footage-gate` (Tier-0 mocap-suitability gate, ffmpeg sample seam → Qwen seam → shared gate protocol; fused feet = named disqualifier, blob hands tolerated; 12 tests) — the MHA-V3 re-measure is now one command against any clip. **Knowledge shipped (TDD):** `metahuman-body-weight-transfer-garments` gotcha (rig garments via Blender weight transfer from the conformed MetaHuman body — skip AccuRig/Mixamo; pairs with the accessory-FBX export rules + Chaos-cloth UE-side node); `ai-lowpoly-generation-not-final` gotcha (AI low-poly/UV is 80-90%/dice-roll — high-poly → retopo → deterministic UV → bake is the quality path); `metahuman-conform-input-prep` gained point (6) neutral facial expression (neutralize via image edit BEFORE conform; re-add scars/closed eyes after via head sculpt or zeroed weights); `GEN_PROMPTING_PRACTICES` gained split-color-vs-PBR (AI albedo production-usable; property maps layered procedurally; PBR-on-top tools need the 3D model as input). **Catches:** canvas tool ("Leechy Studio", GUI/Figma-like → off-domain per user-pref; parts extraction = practice #7; edit history = CandidateGallery) · conform+UDIM (proven 2026-07-05) · markerless mocap (MHA spec) · hair-from-Fab (Asset Scout descope) · cloth physics (Chaos Cloth probes). Declines: none.
- **2026-07-22** — run `easy-clothing-ai` (Stefan 3D AI, "Easy Clothing with AI for Any Character — Best Workflow"; asset-gen/creator demo — reference→extract-parts→Tripo P1→Blender→AccuRig→UE 5.8 **Chaos Cloth Asset**). User picked all 5. **The one automation-shaped thread:** PoF has NO clothing/apparel modality, and UE 5.8's node-based Chaos Cloth Asset (default/production) + 5.8 Dataflow Python scripting + `unreal.ChaosClothComponent` = the same headless-authoring signal that unblocked MetaHuman-conform/ARDY. **Shipped (in-repo, S):** `chaos-cloth-asset-5-8-workflow` `ue-python` UE_GOTCHAS entry (Dataflow node recipe: preset→static-mesh→transfer-skin-weights[2 methods]→weight-map→set-physics-asset collider→solver→Transform-Position penetration fix; reusable saved asset; static-garment path needs no rig; quality checks folded in from candidate #4) — TDD 34/34, tsc/eslint clean. **Spec (XL, not built):** `docs/research/chaos-cloth-headless-spec.md` — Transfer-Skin-Weights is the automatable core, weight-paint stays interactive; next step = one live `-EnablePlugins=ChaosClothAsset*` probe → `chaos-cloth.ts` seam + apparel step. → descope-reopenable. **Catches:** #3 parts-extraction = already-have (`reference-roles.ts` GEN_PROMPTING_PRACTICES #7 grid-multi-view segmentation); #4 folded into the gotcha (a cloth-physics modality PoF can't yet produce doesn't warrant a standalone eval pass); #5 apparel step folds into the #2 spec. **Declines:** none. Expected-low tooling yield from a Stefan 3D AI creator demo held (per user-pref) — the transferable value was knowledge + one feasibility spec.
- **2026-07-22** — run `kimi-k3-one-shot-games` (tef, "KIMI K3 is Ridiculous." — entertainment demo: Kimi K3 in Kimi Code one-shots Minecraft/Fortnite/GTA clones as BROWSER games, ~1-2h autonomous runs each). User's framing: "text→videogame looks possible there, yet our animation bottleneck makes it feel impossible." **Core analysis (the run's main deliverable):** the demos work because they target the browser stack — all-code artifacts, no binary asset pipeline, and all "animation" is PROCEDURAL code transforms (grass sway, break/kill anims), i.e. they SIDESTEP the skeletal-animation problem, they don't solve it; and PoF's own text→motion chain (ARDY, proven 2026-07-15/16 in-game) already covers what the demos can't do. The "practically impossible" premise is outdated vs the project's own July progress. **Shipped (docs-only):** (1) spec `docs/research/browser-feel-prototype-spec.md` — the one transferable mechanism: one-shot a self-contained three.js/canvas mechanic sandbox as a T0 FEEL gate (seconds/iteration vs the full UE round-trip), tuned constants exported as the single-source input to the UE implementation prompt; L, not built, reconsider-trigger = next feel-tuning campaign. (2) **Kimi K3 backlog delta (A-bucket):** Moonshot exposes an Anthropic-compatible `/anthropic` endpoint expressly so Claude Code works → K3 (2.8T MoE, 1M ctx, tops several agentic benchmarks, far cheaper than frontier Claude) is a REAL candidate as a cheap fleet/batch spawn model via `ANTHROPIC_BASE_URL` on the existing `claude-session.ts`/`cli-service.ts` seams — but `model-policy.ts` is Claude-only by design and the experiment is key-gated (needs a Moonshot API key + a compat smoke of stream-json/@@CALLBACK) → descoped-reopenable. **Catches (already-have):** self-playtest-before-handover ("Kimi playtested ~20×") = harness self-heal + behavioral verdicts + the Qwen loop; "autonomous senior game studio" mega-prompt = 6-section prompt-builder + TaskFactory; procedural code-authored animation = `[[project_code_authored_animation]]` + ARDY. Declined: none (nothing forced).
- **2026-07-16 (session 6)** — **THREE-LAYER BLINDNESS peeled with the Qwen loop** (roll v2 + slash v2 cycle): (L1) headless captures froze the player's pose — a skeletal mesh only ticks its pose when rendered; ScenarioController now forces `AlwaysTickPoseAndRefreshBones` + URO-off on the player mesh every scenario (C++ fix, was play_anim-only); (L2) **`rename_asset` UPDATES referencers** — every asset-swap-at-path was a no-op for gameplay (BPs followed the original to its backup: melee BP → `AM_SwordSlash_PreArdy`, dodge slots → `AM_Roll`!); fixed via BP CDO `set_editor_property` (all 5 dodge slots + AttackMontage); (L3) **GA_MeleeAttack had an unconditional self-heal override** hard-locking `AM_SwordSlashC` regardless of config (C++ fix: heal only when null/empty). **Runtime-verified via observations `montage_name`**: roll plays `AM_Dodge_Forward` (ARDY v2), melee plays `AM_SwordSlash` (ARDY slash2). New gotcha `asset-swap-at-path-does-not-repoint-referencers` (TDD 64/64). Honest Qwen scores of the REAL content: melee 25, roll 30 — quality gap is now measurable + attributable (20 fps snappiness, stiff tuck; plus the loop needs denser sampling for fast actions — 0.33 s stills read as teleports). Roll v2 (2.5 s regen w/ Qwen feedback) reached warn 48 pre-refs-fix captures. Verdict rule going forward: **no montage claim without observing `montage_name` in the run's samples**.
- **2026-07-16 (session 5)** — **Qwen-VLM validation loop closed over the ARDY pipeline** (user: "still half blind — use image LLM"; pof-exp `f430c2a`): the EXISTING `/api/verify/animation` `{provider:'qwen', frameDir:<pof_exp_scn_* temp dir>, cam:'side', name, intent}` judges live scenario frames — zero new infra, QWEN_API_KEY already set. **Discriminating on first contact:** combo concat clip fail 15/100 (named the static freezes + collapsed rigging the numeric gates passed); roll fail 35/100 with GENERATION-level feedback (hand-plant vault entry, stiff tuck, snap recovery → next-prompt inputs). Root-cause via AnimPose probes: **`duplicate_and_retarget` explodes on the CONCATENATED clip only** (bones at 78 m; source anim + individually-retargeted combo1/2/3 all clean at ~6 cm feet) — open question, not root-travel (run 8.3 m fine); until diagnosed, retarget clips individually. Fix shipped: `AM_MeleeCombo` = clean single combo1 slash → scenario PASS + Qwen 15→45 (silhouette 30→70). Loop refinement: match capture window to montage span or Qwen dings idle frames. Verdict lesson: my earlier montagePlaying=1/20 sample was the early-death signal I glossed over — the VLM caught it.
- **2026-07-15 (session 4)** — **ARDY melee 3-hit chain IN-GAME** (pof-exp `2658fee`): 3 slash variants generated (2.5 s each, seed 3) → `scripts/visual-gen/ardy/pof_npz_concat.py` (npz-level concat, per-clip root re-anchor, section starts printed) → one clip through the proven chain → `AM_MeleeCombo` swap (another EMPTY placeholder) → live scenario PASS (`Ability.Melee.LightAttack`: montage✓, 26° swing, standing). **New gotcha `fbx-animsequence-import-fresh-folder`** (TDD 63/63): the automated FBX REIMPORT path silently skips AnimSequence creation → fresh folder via filesystem rm (stale .uasset survives `delete_directory`), `replace_existing=False`, `save=False` + explicit saves (task.save = mesh only; unsaved skeleton/anim vanish with the session). 5.8 Python walls: `CompositeSection.start_time`/`AnimMontage.notifies` unscriptable → upstream concat is the pattern; input-gated combo branching = editor pass or PoFEditor C++ helper (backlog). Also: `AnimSegment.start_pos` read-only; in-session assets need `ARFilter(include_only_on_disk_assets=False)`.
- **2026-07-15 (session 3)** — **ARDY roll IN-GAME, full loop closed** (pof-exp `70be0e8`): `AM_Dodge_Forward` was an EMPTY placeholder (0.00 s — the SPACE-roll never had a real animation) → `ardy_montage.py` swapped in a montage from `roll_Anim_Manny` (root motion on; backup `_PreArdy`; BP_VSPlayer's SK_Mannequin = the retarget target). Live scenario via the experiment API: **behavioralVerdict PASS** (montage✓, 766 u, root-motion speed profile), peak frame = player mid-tumble in the arena. So: text prompt → generated motion → retarget → GAS ability → observed gameplay, all headless/$0. **Scenario gotchas:** action inputs need explicit `value` (`[1,0]` for buttons; value-less = (0,0) = never triggers) — the `activate_ability` event verb (gameplay tag, e.g. `Ability.Dodge`) is the reliable ability path. Next: sectioned combo montage for slash (GA_MeleeAttack expects sections), `ardy-runner.ts` app seam.
- **2026-07-15 (session 2)** — **ARDY→Manny RETARGET PROVEN, including the dodge roll** (the historic failure the Mixamo pipeline never overcame): `ardy_retarget.py` (pof-exp `efd64c9`) re-imports the 4 clips onto ONE shared ARDY skeleton (anim-only onto slash's skeleton — each import otherwise creates its own skeleton), builds `IK_ArdyCore` (**5.8 auto-template MATCHED the Mixamo-convention Core-27** — no manual chains needed), `RTG_ArdyToManny` vs the existing `IK_Manny`, then `IKRetargetBatchOperation.duplicate_and_retarget` → `/Game/Generated/Ardy/Manny/*_Anim_Manny`. **Verified numerically** via `ardy_anim_probe.py` (`AnimPoseExtensions.get_anim_pose_at_time(seq, t, AnimPoseEvaluationOptions())` — NOT `AnimPose.get_anim_pose_at_time`, and `evaluation_options` is required): roll = full inversion (head 6 cm off ground, feet 60–82 cm airborne at t≈1.1 s) → clean recovery, 2.3 m forward; run advances 7.8 m upright; slash in place. New headless facts: (a) anim FBX export with `export_preview_mesh=True` asserts (`SkinnedMeshComponent MeshObject`) under BOTH `-nullrhi` AND `-RenderOffScreen` — anim-only export works; (b) Blender box-proxy renders of UE-exported skeletons are unreliable (helper bones + bone-orientation conventions) — judge retargets by the AnimPose numeric probe + Persona, not by the proxy render. Remaining: play the Manny clips on the VerticalSlice player (montage wiring), `ardy-runner.ts` app seam.
- **2026-07-15** — ARDY descope-trigger FIRED (user: "download the weights and experiment"): installed + **proved the whole text→motion→UE chain live** same session (see the TEXT→MOTION entry above). Hard-won facts: (1) pip cache lock aborts `pip install -e .` → `--no-cache-dir` (TripoSR lesson repeats); (2) the C++ motion-correction ext builds on Windows with pip CMake + VS2022, no MinGW; (3) **UE 5.8 Interchange silently eats commandlet FBX imports** ("nothing to import" with legacy FbxImportUI options) → `Interchange.FeatureFlags.Import.FBX 0` console cmd fixes it (gotcha `interchange-fbx-commandlet-crash` updated from the 5.7 use-full-editor advice); (4) Blender FBX `use_selection` must include the skinned proxy mesh or UE fails with bare "Import failed"; (5) BVH export is provably scramble-free because ARDY's FK == BVH semantics — FK round-trip 0.000 mm; (6) quality at seed 0 with zero cherry-picking: all 4 game verbs (slash/run/roll/idle) coherent — a categorically better source than Tripo preset retargets. Motion converters promoted to the repo: `scripts/visual-gen/ardy/`.
- **2026-07-14** — run `ardy-text-to-motion` (Stefan 3D AI, "NVIDIA ARDY: The Real-Time Leap in AI Animation"). Verified the garbled "ARDY/RD" name against the real repo: **nv-tlabs/ardy** (SIGGRAPH 2026, Apache-2.0 code, NVIDIA Open Model weights, headless CLI + Python API, text+constraint→motion at ~33 ms/step). User picked 1+2+3 of 6. Shipped: (#3) `ai-motion-generator-ue-ingestion` `ue-python` gotcha (npz-on-foreign-skeleton → Blender-bake FBX → IK Retargeter; Add-Leaf-Bones/axis = the Tripo scramble family; HF-gated Llama + 24 GB notes) — TDD, knowledge suite 62/62, golden mixamo-import snapshot updated, tsc/eslint clean; (#1) **spec `ardy-text-to-motion-spec.md`** (runner mirrors triposr/hunyuan seam; npz→FBX Blender hop; Tier-1 foot-contact/root-continuity math gate) — build blocked on the user-gated install → descoped-reopenable; (#2) animation-strategy reframe recorded above (ARDY deletes the footage problem for BODY motion; MHA = face path). Catches/declines: RunPod Claude-over-SSH provisioning = already-have catch (CubePart run, and the 4090 meets ARDY's real-time tier locally); playblast→video-AI shots = off-domain (user-pref rule); keyboard/scenario constraint authoring folded into the spec (#5→#1).
- **2026-07-13 (follow-up)** — `3d-ai-studio-flow` deferred #1 REDESIGNED + BUILT as the **Tier-0 input gate**: not the video's pose-EDIT node but PoF's gate+regenerate pattern — `src/lib/visual-gen/input-gate.ts` (pure prompt/parse/score cores; SCORE/DEFECTS/VERDICT one-line protocol shared with `pof_vlm_critique.py`; pass ≥7 = the proven gap-loop line; 0-100 Scorecard shape shared with mesh-critique) over the **anim-critique `makeQwenVision` seam** (DashScope Qwen-VL, quota-fallback — per-call fast; the local `pof_vlm_critique.py` path reloads the model ~1min/call, 25× slower per the gap-loop lesson) + `POST /api/visual-gen/input-gate` ({imageDataUrl, subject} → card; gate-can't-run = 502 with reason, never a silent pass). Mechanically enforces the two shipped GEN_PROMPTING_PRACTICES upstream of every image→3D dispatch. TDD 11/11, visual-gen suite 106/106, tsc/eslint clean. Remaining wiring (deliberate cuts): forge UI pre-check call + an optional local-script vision adapter behind the same seam. Redesign assessment also concluded: #8 mood-board → style-DNA distiller (next candidate, see below); #7 partial-run and #5 agent-authored-workflow stay catches (harness/backlog-owned).
- **2026-07-13 (follow-up 2)** — `3d-ai-studio-flow` catch #8 REDESIGNED + BUILT as **Style DNA**: not the video's per-generation mood-board node but distill-once/inject-everywhere — `src/lib/visual-gen/style-dna.ts` (pure prompt/parse/fragment cores over the same `makeQwenVision` seam; PALETTE/MATERIALS/MOOD/RENDER/MOTIFS one-line protocol; fragment caps 4 items/dimension for prompt budget) + `style-dna-db.ts` (asset-library-db injectable-db pattern; single active profile) + `POST/GET/PATCH /api/visual-gen/style-dna` + **opt-in injection in `/api/leonardo`** (`applyStyleDna:true` → append the active fragment capped at MAX_PROMPT_LENGTH, `styleDnaApplied` name in the response — never silent mutation). $0, no image-reference API dependency, provider-agnostic (works for Leonardo, SDXL, Tripo text prompts). Realizes the tracked backlog idea `project-style-dna-art-direction`. TDD 15/15 new (121/121 visual-gen), tsc/eslint clean. NOTE (corrected from the assessment): the injection home is the generation PROMPT path, not `style-keywords.ts` — that file is material-analyzer keyword DATA, wrong layer. Remaining wiring (deliberate cuts): mood-board upload UI (forge) + gap-loop batch scripts adopting `applyStyleDna`.
- **2026-07-13 (follow-up 3)** — Style DNA **forge UI shipped**: `asset-forge/StyleDnaPanel.tsx` ("Project style" collapsible card above GenerationPanel — mood-board multi-upload w/ thumbnails → Distill → the **DNA strip** (5 labeled chip clusters, the profile's style genome) + saved-profile switcher (PATCH) + "Apply to prompts" toggle; errors via shared `InlineErrorRetry`, auto-expands on empty state). `useForgeStore` gained `activeStyleDna`/`applyStyleDna` (+ setters); `GenerationPanel` appends `styleDnaToPromptFragment` to the SUBMITTED prompt (MCP + placeholder paths; prompt history keeps the user's own words) with a visible `style-dna-indicator` line — style never rides invisibly. Extended `leonardo-route.test.ts` w/ 3 `applyStyleDna` cases (mocked `@/lib/db` + `style-dna-db`). Tests 33/33 affected, FULL suite 3830/3830, tsc clean; 10 repo lint errors are pre-existing (cli-spend/procgen require-imports, DevInspector/PBREditor set-state-in-effect, ue-launch prefer-const — not this change). Known gap noted in passing: forge text-to-3d never reaches `submitLocalJob` (image-only signature), so Tripo text-to-3d runs only as a placeholder job — separate wiring item.
- **2026-07-13 (follow-up 5)** — **Packaging truth engine Tier 1 BUILT** (the run's XL spec, executed): pure collector (`packaging/collect.ts` — sibling data walk: file refs + `/api/visual-gen/asset/…`→`generated/triposr/` serve-route mapping + embedded data-URL swatches + `ueAssets` declarations) → `buildPackage` (hash-in-place + materialize data-URLs → `generated/packages/<cat>/<ent>/manifest.json` w/ honest `missing[]`; fs/crypto via `getBuiltinModule`, deps-injected) → `packagingVerify` drain (aggregate: staged→L2 pass / missing→deferred+reasons / empty→"declarations only" deferred; NEVER fail from a clean produce — walker-safe) → `GET/POST /api/pipeline-artifacts/verify-packaging` (mirror of verify-static). **Key design win vs the spec: `isPackagingStep` label-fallback ("UE Packaging") covers all 30 pipelines with ZERO contended-file edits** (items carries the explicit `packaging: true` flag as reference). **Live dry-run vs real DB: 32 graded → 30 pass on REAL staged+hashed files** (gap-loop art materialized; char-captain-vael = 9 files incl. 3.3 MB rigged glb) **/ 2 honest empty-row deferrals**; first pass caught 3 dead `/api/visual-gen/asset` refs → resolved by teaching the collector the route's own disk mapping. 13 unit tests + env-gated `POF_PACKAGING_DRYRUN=1` integration dry-run (live-drain pattern); guards + 233 lib/catalog tests green; tsc/eslint clean. Docs: WIRING-AND-ACCEPTANCE §3 two-drain section + spec flipped to Built. **APPLIED same day:** verdicts written to the real DB (30 pass / 2 deferred, re-drain idempotent; `POF_PACKAGING_APPLY=1` mode on the integration runner) + all 30 step-facts packaging entries flipped (trueEngine 'Packaging engine' / generatorWired / checkerMeaningful true) — the UNPOWERED packaging block is retired from /status. Status suites green after the flip (96 status+catalog tests). Remaining: Tier 2 = L3 gates verify the manifests' `ueDeclarations`.
- **2026-07-13 (follow-up 6)** — **Packaging truth engine Tier 2 BUILT + APPLIED**: (Rung A) `buildPackage` disk-checks every `/Game/...` declaration against `<ueRoot>/Content/<path>.uasset|.umap` (shared `resolveUeRoot`; `ManifestDeclaration {path, realized, diskPath}`); `siblingsForPackaging` includes the packaging artifact's OWN `ueAssets` declarations-only (its declared list IS its claim — first dry-run under-collected by excluding it) while its hand-typed data stays out of the file collector; `aggregatePackaging` v2: files+declarations all realized → pass, anything missing → deferred w/ reasons, unchecked (no UE root) → Tier-1 verdict saying so; no-files-but-all-declarations-realized → pass (UE-side package exists). (Rung B) **`scripts/ue/verify_package_declarations.py` proven live on UE 5.8** — headless `-run=pythonscript`, `does_asset_exist` per manifest declaration (`POF_PKG_MANIFEST` env; `POF_PKG_DECL/SUMMARY/DONE` markers, judge by abslog): spellbook/off-arc-fp → **1/9 realized (`DT_GeneratedAbilities`) — IDENTICAL to rung A's disk check**, validating the cheap check as a registry proxy. **Applied: UE Packaging now 5 pass / 27 deferred** — the honest map; each deferral's reason lists the unrealized declarations (hand-typed audio/VFX/anim aspirations; ground-truthed: `Content/Items/` doesn't even exist) = a per-step UE-side work list. Step-facts notes updated (Tier 2 wording). 102 catalog+status tests, tsc/eslint clean. Loading/behavior stays with the L3 gate suite (unchanged scope).
- **2026-07-13 (follow-up 4)** — Style DNA **gap-loop wiring shipped** (closes the last Style DNA cut): the batch-3 scratchpad fan-out chain PROMOTED to the repo as **`scripts/gap-loop/{power-icon,batch-generate,batch-inject,flip-stepfact}.mjs`** (scratchpad copies were one temp-clean from extinction; paths now argv/env-driven — `POF_ORIGIN` default :3001, manifest paths as args, repo-relative outputs). `power-icon` + `batch-generate` pass **`applyStyleDna: true` to `/api/leonardo` by default** (`POF_STYLE_DNA=off` opts out), log the echoed `styleDnaApplied`, and stamp it through the chain: genHistory `direction`/candidate `payload.styleDna` + VLM-verdict findings name the profile — campaign art carries its style provenance. `.claude/skills/gap-loop.md` engine palette now instructs: check `GET /api/visual-gen/style-dna` before a 2D batch, distill a profile if none active (or journal a deliberate `POF_STYLE_DNA=off`); `.claude/gap-loop/state.md` pointers updated (chain now repo-durable). Verified: `node --check` ×4 + the route's 3 `applyStyleDna` unit cases; no live smoke (dev server down, and a real gen spends Leonardo credits).
- **2026-07-13** — run `metahuman-animator-video-to-anim` (Curtis Holt, *"I'm Building a Human Animation Pipeline!"* — solo artist, plain video → MetaHuman markerless mocap → Blender in ~4 days). **Knowledge/workflow source**; user goal = master "video → animation WITHOUT mocap gear" (markerless mocap IS the no-mocap path — only hardware is a camera). Picked 1/2/3/4/5/8. **LIVE-PROBED the decisive question:** MetaHuman Animator's solve API is Python-exposed + loads HEADLESS on the installed 5.8 (`MetaHumanPerformance`/`…ExportUtils`/`MetaHumanIdentity`/`FootageCaptureData` all resolved with `-EnablePlugins=MetaHuman -EnablePlugins=MetaHumanAnimationTools -nullrhi`) → clears the GUI-only rule; a real automatable engine for PoF's weakest domain. **Shipped:** 2 `ue-python` UE_GOTCHAS (memory-window via `set_processing_range`; multi-window root-drift stitch), TDD 23/23, tsc/eslint clean on changed files; + spec `metahuman-animator-video-to-anim-spec.md` (XL, not built — gated on EULA clearance + a live end-to-end solve probe under `-RenderOffScreen`). **Catches:** #5 reference-not-final = already-have (`module-eval-prompts.ts:110`); #4 Euler→quaternion = Blender-import-specific, no PromptKind home → folded into spec + the stitch gotcha; #8 chunk+stitch = folded into spec (no standalone consumer). **Structural fact:** `set_processing_range` is the API-level answer to the RAM-ramp that forces GUI users to pre-cut to 30s.
- **2026-07-13** — run `3d-ai-studio-flow` (Stefan 3D AI, "Flow" agentic node canvas — product demo). **First run under the new status/pipeline impact lens** (SKILL.md now requires a `status_impact` per candidate; lens = autonomous-pick tie-breaker). Shipped: (1) `src/lib/visual-gen/polycount-presets.ts` — per-asset-class face budgets (character locked to the pipeline's 40k) feeding Tripo `face_limit` via the generate route's new `assetClass` + a `thresholds` deps seam on `critiqueMesh` → the Tier-1 geometry gate is now class-aware (a 150k-face prop warns; used to pass the class-blind 200k default); (2) `GEN_PROMPTING_PRACTICES` gained the grid-combined-multi-view-as-segmentation-input + review-the-part-list practice. (3) **XL spec `docs/research/packaging-truth-engine-spec.md`** — the video's export-node (zip + manifest) pattern applied to the ~30 UNPOWERED "UE Packaging" steps (manifest+hash filesystem truth → packagingVerify; rollout via /gap-loop). Catches: agent-authors-workflow (CLI sessions do), partial run (step replay), mood-board style ref (reference-roles), batch-kept variations (CandidateGallery). Declined: Flow as provider (web GUI, no API — user-pref rule). Deferred: executable 2D pose-normalize step (knowledge already shipped; face problem solved upstream).
- **2026-07-06 (session 2)** — `jinx-standalone-project` candidates C1-C3: **the "Tripo anim scramble" was NOT the assets.** Isolation matrix (statues ref/anim + unpossessed Character instance placed beside the pawn) proved Tripo anims (API glb via Interchange AND Blender constraint-bake FBX) play PERFECTLY in UE — the HQ Jinx runs beautifully. The REAL residual: only the runtime-spawned POSSESSED pawn renders crumpled under `-game -RenderOffScreen -DumpMovie`; survived pristine-BP/CDO-only/anim-class-None/fresh-Character-subclass/`ALWAYS_TICK_POSE_AND_REFRESH_BONES` — suspected headless-render-only artifact (PIE check pending). Gotchas: `Skeleton.bone_tree` retargeting modes read-only from python; `add_new_subobject` works headless (added a CameraComponent). C3 IK-Retargeter deferred (unneeded as fix; remains the full-locomotion upgrade).
- **2026-07-06** — run `jinx-standalone-project` (best-effort loop: clean UE 5.8 BP project `…/Unreal Projects/jinx` — Tripo Jinx as WASD player replacing Manny). **BIG INFRA WINS:** (1) **ROOT-CAUSED the "project-less commandlet" bug: PS 5.1 `Start-Process -ArgumentList` does NOT auto-quote args with spaces → UE silently splits `…/Unreal Projects/…` and boots the FALLBACK project (`AppData/Local/UnrealEngine/5.8/`)** — this retroactively explains most of the earlier PoF headless "blockers" (module not loading / assets not found / ExecCmds "hang" — they ran in the wrong project!). Fix = pass ONE pre-quoted arg string (`jinx_run.ps1` launcher in scratchpad; port to `ue-launch` later). Assets accidentally written to the fallback project live in `AppData/Local/UnrealEngine/5.8/Content/Generated/`. (2) **Blank→playable project seeding recipe:** copy `Templates/TP_ThirdPersonBP/Content/*` + `TemplateResources/High/{Characters,Input,LevelPrototyping}/Content` + merged Config → full WASD/camera/jump project, headless-validated; **delete `Intermediate/CachedAssetRegistry*` after copying content into an already-booted project** (stale empty-registry cache otherwise hides all assets). (3) **Headless visual-validation loop for a BP project:** `-game -RenderOffScreen -benchmark -fps=30 -DumpMovie` → per-frame PNGs in `Saved/Screenshots/WindowsEditor/` — the missing headless in-engine render path (no ScenarioController needed). (4) Tripo HQ asset set: `texture_quality:detailed` image→model + rig + run/idle/walk retargets (~95cr); `--texture-quality` added to `pof_tripo.mjs`. **OPEN BLOCKER — Tripo rigged anim → UE = bind-pose SCRAMBLE:** every import variant tried renders the skeleton crumpled/inverted in UE while **Blender plays all takes perfectly**: direct FBX (both duplicate takes), anim-only-onto-skeleton, Blender FBX re-export (FBX_SCALE_ALL → lying flat = axis flip), Blender→GLB via Interchange, `use_t0_as_ref_pose`. Structure: 1 armature (54 bones) + 2 duplicate FBX takes per file. **Next candidates:** Tripo retarget with `out_format:"glb"` directly (skip FBX+Blender); Blender constraint-bake onto a FRESH clean armature then export; UE-side IK-Retargeter from Manny anims (PoF `mixamo_*` tooling is the precedent); interactive Persona bind-pose diff. Jinx project state: Jinx IS the possessed WASD player pawn in `Lvl_ThirdPerson` (mesh+textures render, camera live) — only the anim pose is scrambled.
- **2026-07-06** — run `tripo-animation-ue-wiring` **(Tripo rig/animate services + UE 5.8 e2e wiring)**. Explored Tripo's image-to-3d + rig + run-animation on a Jinx concept and wired the result into UE 5.8 (`scripts/visual-gen/pof_tripo_animate.mjs` + UE `Content/Python/jinx_build_map.py`). **Proven:** image-to-3d (recognizable textured Jinx) → `animate_rig` biped → `animate_retarget preset:run` → `jinx_run.fbx` → UE **FBX skeletal import** (SkeletalMesh + Skeleton + run AnimSequence + PBR material) → **placed in a clean map** `/Game/Generated/JinxRunMap` (3 forward-spaced actors). Visual via **Blender 4.2 headless** filmstrip (run gait cycles; model good; aggressive forward-lean sprint + rigid braids). **UE 5.8 HEADLESS RECIPE (hard-won, reusable):** (1) FBX skeletal import + any asset op → use the **`-run=pythonscript -script=<file>` commandlet** (`FbxImportUI` + `mesh_type_to_import=FBXIT_SKELETAL_MESH` + `import_animations=True`); it runs post-init + self-exits clean. (2) **Startup `-ExecCmds="py exec(open(abspath).read())"` HANGS in the PoF project** — the editor loads the `OpenWorld` default map + never dispatches the `py` (both `-nullrhi` and `-RenderOffScreen`); `py import <module_in_Content/Python>` also didn't dispatch. So editor-time python is unreliable headless here; the commandlet is the workhorse. (3) In the commandlet, **`spawn_actor_from_class` works but `spawn_actor_from_object` CRASHES** (EXCEPTION_ACCESS_VIOLATION — no editor viewport); `load_level` works; save-as via `EditorLoadingAndSavingUtils.save_map(world, DST)`. (4) **Headless in-engine RENDER is BLOCKED:** editor-viewport `take_high_res_screenshot` needs editor-python (won't dispatch); commandlet `-RenderOffScreen` + `SceneCapture2D.capture_scene()` + `export_render_target` → *"render target has been released"* (the commandlet render thread doesn't complete the capture). → for an in-engine frame use the **`-game` scenario path** (`captureScenarioFrame`, proven) or the interactive editor; for a quick asset/anim visual use **Blender headless**. Recorded as the durable UE-wiring gap below. **(2026-07-06, task-b follow-up) The `-game`-player-as-Jinx wiring is BLOCKED headless — two hard constraints:** (a) **`-run=pythonscript` does NOT load the PoF game module** — `UnrealEditor-PoF.dll` exists (built) but the commandlet loads only engine/editor modules, so `load_asset('/Game/VerticalSlice/BP_VSPlayer')`→None and `ARPGCharacterBase`/`ScenarioController` are unresolvable (every commandlet success used ENGINE classes only). Authoring a game-module-dependent pawn (`BP_JinxPlayer` subclass of `BP_VSPlayer`, `BP_JinxGameMode`) therefore needs the **interactive editor or the live PoF Bridge** (where the module loads — that's where the Jedi `author_jedi.py`/`set_default_jedi_pawn.py` scripts ran), NOT a headless commandlet. (b) A **manual `-game /Game/Maps/VerticalSlice -PoFScenario=<inbox> -RenderOffScreen -benchmark -fps=60` replication CRASHED on early init** (near-empty abslog + CrashGUID) — the proven scenario capture must go through the app's `captureScenarioFrame` harness (exact args + a real gate scenario), not a hand-rolled arg list. **Ready-to-run authoring script written:** UE `Content/Python/jinx_setup_player.py` (BP_JinxPlayer = BP_VSPlayer subclass, CharacterMesh0→Jinx + single-node run anim + scale 1.8; BP_JinxGameMode default_pawn=BP_JinxPlayer) — run it from the interactive editor's Python console (or the bridge), then launch `<map>?game=/Game/Generated/Jinx/BP_JinxGameMode.BP_JinxGameMode_C` through the scenario harness. **Net: Jinx→UE asset+map integration is proven; the driven-player in-engine capture is the one step that requires the live editor/bridge (mcp-unreal `:8090` / PoF Bridge `:30040`), currently disconnected.**
- **2026-07-06** — run `tripo-cloud-3d` **(character gen LIVE-PROVEN — warrior.glb, 20 cr; dual-wallet credit model confirmed)**. Update to the same-day entry below: after the user funded the SEPARATE developer-API wallet (platform.tripo3d.ai) to 600 credits, the full route generated a textured character end-to-end (9.67 MB glb) — Tier-1 gate flagged it fail/fragmented (214 components), the expected single-shot-character pattern. The blocker was NOT a bug: Tripo's API credits are a distinct wallet from the free Studio 200/mo.
- **2026-07-06** — run `tripo-cloud-3d` (video "Tripo Just Killed Meshy", Stefan 3D AI — Tripo-vs-Meshy product demo; user goal: is Tripo's free plan API-integrable + wire a cloud route next to the open-source route + test a character). **Ground-truthed the API live** (base `api.tripo3d.ai/v2/openapi`, `Bearer` auth, `POST /task` + `GET /task/{id}` + `POST /upload`). **Q1 = YES** the free plan includes developer-API access (key authenticates), but the free key has **balance:0** → `2010 not enough credit` on both text/image tasks (free tier 200 cr/mo ≈ 8 models, 1 concurrent, NON-COMMERCIAL CC BY 4.0). **BUILT the primary finding** (the user's pre-authorized ask): a full CLOUD generation route next to the local route — `tripo-runner.ts` + `tripo-job-store.ts` + `generate`/`status` wiring + `providers.ts` flip + `pof_tripo.mjs` smoke; **24 tests, tsc/eslint clean, 88/88 visual-gen suite green**. **Live-proven up to the credit wall** (auth + submit correct; blocked only on 0 credits → character gen is one command away once the user tops up). **Multi-attachment analysis** (user hint "more than one place"): recorded texturing/segmentation/rigging/format-conversion as Tripo-cloud backlog deltas above (segmentation now promoted over the CubePart descope). `.env` got `TRIPO_API_KEY` (gitignored, not committed).
- **2026-07-05** — run `aura-unreal-ai-agent` (Aura / tryoura.dev, competitor product demo — Unreal AI agent doing 2D→3D→rig→retarget→GAS→C++→headless, driven partly *by* Claude Code via its MCP). Direct architectural analog to PoF's mcp-unreal + visual-gen + harness → expected shape held (few knowledge findings + many already-have catches). User picked **all**. Shipped **2 findings**: (1) image→3D INPUT-PREP practices in `reference-roles.ts` `GEN_PROMPTING_PRACTICES` (plain-bg + simple canonical pose — Aura's 3D-quality trick, absent from PoF's gen best-practices), TDD 7/7, commit `a341690`; (2) `gas-author-abilities-incrementally` `ue-cpp`+`ue-python` gotcha (decompose GAS, don't one-shot), TDD 21/21 + snapshot refresh, commit `f68f943`. **Catches:** headless mode / plan-mode+clarifying-Qs / MCP-tools-for-Claude-Code / auto-rig-from-mesh (MetaHuman-conform descope) / template-anim retarget (the whole animation pipeline) = all already-have or already-tracked. **Structural fact discovered:** PoF has a **Marketplace/Asset-Scout** subsystem (added a B entry) — curated-catalog recommendation + C++ adapter codegen; the LIVE-Fab-search + in-editor auto-import/wire delta → descope-reopenable. **Shared-tree hazard, live:** a concurrent session committed metahuman-conform gotchas to the SAME `ue-gotchas.ts` mid-run — re-synced to their HEAD, staged only my hunk, verified 0 foreign content before commit.
- **2026-07-05** — run `ai-to-metahuman-conform` (Stefan 3D AI, UE 5.8 AI→MetaHuman workflow): user picked #1/#2/#3/#5. Shipped **2 `ue-python` UE_GOTCHAS** (`metahuman-conform-input-prep`, `metahuman-conform-texture-export` — #5 AI-texture-via-UV folded into #2), TDD 20/20, tsc/eslint 0. **#3 (conform auto-rig pipeline) = strategic already-have at the PLAN level** (Candidates B + H already in `ue5-capability-integration-candidates.md`), so the run's #3 value was **calibration + recipe, not a build**: web-verified the 5.8 conform IS Python-scriptable + wrote the concrete recipe `docs/research/ai-to-metahuman-conform-recipe.md`. **CORRECTION mid-run (user: "we DO have UE 5.8 installed"):** the old "5.8 not installed / targets 5.7" fact was stale — UE_5.8.0 IS installed. **GROUND-TRUTHED it** with 2 headless introspection probes (`-run=pythonscript -nullrhi` on the real project): the conform API loads **headless** — `MetaHumanCharacterEditorSubsystem.conform_to_target_meshes` / `set_body_mesh` / `get/set_face_model_coefficients` (+ `MetaHumanIdentityFace.conform`) — once the `MetaHumanCharacter` Experimental plugin is enabled (`-EnablePlugins`, MH-class count 108→243). So the "editor-dependent, not headless" tempering was itself too pessimistic → re-corrected line 10 + Candidate B + this map to **verified-headless-feasible**; #3 moved from "blocked spec" to "prototype-and-wire" (next: enable the plugin in `PoF.uproject` + a `conform_to_target_meshes` script). Lesson: research runs audit forward-looking docs — and a live probe beats both the release-notes claim AND my inference. Still confirms the user-preference read: the durable knowledge was the 2 gotchas + a now-*ground-truthed* capability calibration.
- **2026-06-20** — run `cubepart-3d-segmentation` (Stefan 3D AI): picked #3 → `ue-python` gotcha "AI-generated 3D is one fused mesh — segment into NAMED parts before rigging" (geometry-accurate Tripo V2/Rodin over latent-resample CubePart; pairs with the modular-character rules). Catches/descopes: CubePart itself = research-only license + heavy + lossy → **descope-reopenable** (reconsider on MIT relicense); part-segmentation as a `visual-gen` provider/step = **reinforced backlog** (Tripo/Rodin API, M-L, the 3D-gen→rig bridge); cloud-GPU Claude-bypass auto-install = already-have catch. Creator asset-gen source = expected low tooling yield (per user-preference); the transferable value was the knowledge gotcha.
- **2026-06-19** — run `motion-matching-problems` (Unreal DevOP): Motion Matching gotcha (root motion needed even w/o capsule RM; Phase channel crashes editor; cost-bias tuning; foot-sliding via play-rate window/Dead Blending; mirroring caution; Exclude-From-Database for T-pose frames). Directly serves the animation-quality focus; complements arpg-animation MM eval criteria.
- **2026-06-19** — run `pcg-best-practices` (Unreal Fest 2025, M. Oztalay): `arpg-world` PCG structure criterion (data-flow graphs, expose-params + graph-instances, runtime hierarchical gen, layer-weight biome filtering). Complements Candidate D (PCG emission). Eval-surface finding (variety vs the gotchas).
- **2026-06-19** — run `gas-intro` (Danny Goodayle): 2 GAS `ue-cpp` gotchas (meta-attribute damage via GE + PostGameplayEffectExecute clamp; replicated-attr REPNOTIFY + cosmetic-only cues). PoF is GAS-mature (arpg-gas module, gas_ops) so the rest = catches; the gotchas were absent from the knowledge base.
- **2026-06-19** — run `niagara-optimization` (Unreal Fest 2025, A. Kurali): 2 Niagara perf gotchas (Effect Types significance/culling — hidden systems still tick + GPU dispatch cost; Insights needs `stat named events`). Authoritative Epic source. Niagara perf maps to `niagara_ops` / VFX modules; no dedicated VFX evaluator module (candidate future home: `arpg-polish`).
- **2026-06-19** — run `modular-character` (Stefan 3D AI): modular-character rigging/optimization gotcha (rigid-accessory single-bone weight, occluded-mesh hiding, exclusive swap-slots). AccuRig/Tripo = catches (off-domain per user-pref). Cleaner hardened (strip VTT cue-numbers).
- **2026-06-18** — seeded.
- **2026-06-18** — run `claude-took-over-ue5` (Stefan 3D AI): added D-bucket gotcha; logged 3 C-bucket backlog deltas (introspection/BP-layout/StateTree). Mostly already-have catches (visual self-review, build automation, git checkpoints, mcp tool surface) — confirms PoF is ahead of the manual workflow.
- **2026-06-18** — run `weekend-game-pipeline` (Stefan 3D AI): first two-gate run (candidate list → user picked C2/C4). Added a **visual-gen** subsystem entry (was thin). C4 evaluator poly-budget check implemented (S). C2 AccuRig deep-verify → **GUI-only/manual, declined** (off automation-goal; MetaHuman-conform is the better path). Asset-pipeline-centric source = off the harness/MCP north star.
- **2026-06-18** — run `lumen-best-practices` (Karim Yasser): **knowledge source** mined into prompt-consumable form (user's lens: best practices → pipelines/presets/knowledge-base). K1 = 3 Lumen gotchas; K3 = `lighting-presets.ts` (4 tier presets); K4 = `environment-lighting` pipeline spec (not built). Shows the knowledge-injection path (D/B) raising automation output quality — distinct from tooling sources.
- **2026-06-18** — run `ai-render-engine` (Stefan 3D AI): promo-video workflow, **mostly off-domain**; mined the one transferable best practice → `reference-roles.ts` (block-first + role-tagged refs + multi-view master for on-model generation consistency). Declined off-domain video relight/VFX/bg tooling. Focus per user: high-quality **asset/animation generation** is the real indie gap (engineering/harness talks don't exist yet — UE 5.8 MCP too fresh).
- **2026-06-18** — run `agentic-3d` (**web-discovery**, no URL): searched/fetched 2025-26 arxiv research on agentic 3D-asset generation. W1 synthesis (`agentic-3d-asset-generation.md`) — frontier converges on PoF's architecture (validation). W2 spec (`self-correcting-asset-gen-spec.md`) — close the asset-gen self-correction gap. W3 — `arpg-animation` mocap-source criterion. Thread A (Mixamo alt tooling) = GUI/credit-based, off-domain (→ promoted user-preference). Exercised bucket E. Skill gained a web-discovery source mode.
- **2026-08-12** — run `cain-design-batch-5` (Timothy Cain ×5, FIRST BATCH run: Eight Level Design Guidelines / Dialog Quest Solutions / Code Standards / Melee Attacker Positioning / GURPS Fallout Characteristics — the designer-talk class hardening run). User picked the recommended 7-finding slate + 1 spec + catches. **Shipped (TDD, full `npm run validate` green):** (1) `arpg-world` structureChecks — anchored vs non-anchored encounter taxonomy (wander cap 1-2: pathing/conga/wipes) + POI-visible-from-anywhere orientation rule; (2) **NEW `dialogue-quests` MODULE_CONTEXTS entry** — the module existed in the registry but had NO eval context (generic fallback = effectively unjudged): data-driven dialog trees, declarative condition/effect entries, no one-shot dialog solutions (multi-stage via other skills — the Fallout Master pattern), playthrough-build solution-coverage balance; (3) `ai-behavior` qualityChecks + **generation-side `ai-5` prompt enrichment** (first dual-home finding, per the Lessons watch item): the WildStar melee RESERVATION system — sized positional slots, request→offer→confirm-or-timeout, adjoining slots for large attackers w/ reseat, mass-cancel on death/jump/teleport/flight; (4) `arpg-progression` trait economy (advantages cost / disadvantages grant points, quirks = 1-pt flavor hooked in 1-2 checks, paired ± reputation traits per faction); (5) UE_GOTCHAS `warning-vs-error-policy` (ue-cpp universal): cosmetic failure warns-and-continues, gameplay-invariant violation hard-fails, never fabricate missing state. **Spec note:** `docs/research/systemic-dialog-solutions-note.md` — Cain's open "systemic dialog solutions" challenge; PoF's LLM stack is the systemizer (data model: NPC knows/wants/fears + fact-gated dialog branches + countable per-build coverage); XL backlog, pairs with radiant-quest idea. **Catches (#9):** `#pragma once` in generated-header templates (`ability-forge.ts:48`), WASD/IMC input standards in character prompts, Esc/CommonUI nav in ui prompts; `RequestAttackToken` pre-existed in ai-5 (Cain's system is the superset — enriched, not duplicated). **Gotcha found:** module-registry checklist ids are only unique PER SUB-MODULE (`ai-5` = arpg-inventory AND ai-behavior) — scope lookups by module id.
- **2026-08-12** — run `realtime-vs-turnbased-combat` (Timothy Cain, 13:44 — pure game-DESIGN talk from the Fallout creator; first "high-level design concepts" source class). Knowledge source, low expected yield → 6 candidates, user picked 1-4. **Shipped (S, TDD 6/6):** `module-eval-prompts.ts` `arpg-combat.qualityChecks` gained a "real-time design semantics" block — 3 criteria flagging turn-based rules ported unchanged into PoF's real-time ARPG: (1) AoE/targeted abilities must define moving-target behavior (ground telegraph that can whiff vs homing that tracks — the BG1-fireball complaint), (2) timed effects count in real-time seconds with visible telegraphs + escapable radii (overlapping timers force spatial decisions), (3) active defenses split the skill axes — player-timed trigger (IF), character-stat magnitude (HOW WELL). **#4 (GDD camera↔combat-mode coherence) dropped honestly: no attachment point** — `gdd-compliance.ts` is a mechanical checklist-vs-scan engine and `gdd-synthesizer.ts` is DB aggregation; neither judges design coherence, and PoF's identity (solo isometric real-time ARPG) is already baked into module prompts and coherent by Cain's own heuristic. #5 (companion AI policies) / #6 (sim concurrency) not picked. **Structural fact:** design-principle knowledge has exactly ONE prompt-reaching home today — eval criteria; UE_GOTCHAS is API-pitfall-shaped and knowledgeTips are UI-only. A future "design-pillar" judge surface would be the home for GDD-level coherence rules.
- **2026-07-24** — run `qwen-image-3.0` (Qwen blog, article/vendor source; user premise: "promote 3.0 to primary for image recognition"). **Premise did not survive verification** — `qwen-image-*` is generation-only (no VQA), and the requested separate-quota fallback hierarchy already existed in `anim-critique/qwen.ts`. Honest answer beat the requested change. Shipped two real adjacent deltas instead: (S) `QWEN_CRITIQUE_FALLBACKS` env so the VL chain is re-tierable without a code edit, + extracted the shared `isQuotaError()` classifier; (M) `visual-gen/qwen-image-runner.ts` — Qwen-Image as a second 2D-generation provider aimed at the text-in-image/UI-layout niche Leonardo is weakest at. Lesson: a "promote model X" ask needs the model's **task family** verified first — same vendor + same API key ≠ same capability.
- **2026-08-14** — run `ai-vs-human-retopology` (Top 3D AI / Stefan, 19:46 — a controlled bake-off: one AI high-poly sculpt retopologised by a $30 junior on QuadRemesher in 2h, a $150 middle artist on RetopoFlow in 11h, and AI (Tripo/Hunyuan), capped at 15K. **13th run from this channel; the predicted high catch rate held.**). User picked the recommended slate (1, 2, 3 + wiring). **#1 dissolved instructively** — the video's "bring any 3D model here and retopologise it" is Tripo **Studio (GUI)**, not the REST API; every mesh-consuming task type keys off a prior Tripo task id and `upload_file` is images-only. Same constraint as the 2026-07-28 `texture_model` re-scope, reached from a different direction → recorded as a consolidated catch so the web budget is never spent on it a third time. **Shipped (3 commits, TDD, 306 visual-gen tests green):** `face-budget.ts` (the authored unit is TRIANGLES; explicit conversion at the provider edge; `gradeFaceBudget` → honored/over/**unmeasured**) — mined from the run's most expensive real-world mistake, a "15K" budget delivered as 15K *quads*; `scoreMesh` now grades the delivered mesh against the budget REQUESTED, not just the class ceiling; **`critiqueThresholdsFor` was found to be dead code** (zero production call sites — every mesh graded class-blind since 2026-08-09) and is now wired via `critiqueDepsForSpec`; `TripoResult.formatMismatch` measures the delivered container instead of trusting the `quad` flag; and **the 3-run-old `runMeshFinish` zero-call-sites blocker is CLOSED** (job store + `POST/GET /api/visual-gen/mesh-finish`). **Catches:** per-part generation, retopo→UV→bake ordering and the low-poly-must-cover-high-poly bake rule are all already in the `ai-lowpoly-generation-not-final` gotcha; "remove occluded parts before retopo" reinforces the known `cullInterior`-between-shells backlog without adding a method. **Process note:** a PARALLEL research session was live in the same files throughout and swept this run's first 4 files into its own commit (`5dd91758`) via `git add -A`; remaining commits used pathspec `git commit -- <paths>` to avoid the reverse. See [[feedback_shared_tree_add_p]].
