# How to Build PoF-UI with PoF + UE5

> Generated by PoF Harness | 2026-04-06 | 53 iterations
> Total build time: 4.9h

## Prerequisites

- UE5 5.7.3+ installed
- PoF app running (npm run dev)
- Project connected via Setup Wizard
- Project path: C:/Users/kazda/kiro/pof

## Build Order (dependency-sorted)

1. **arpg-character** — areas: infra-feature-cards-a, infra-entity-selector-a, infra-entity-selector-b, infra-feature-cards-b, infra-entity-selector-c, ui-character-feature-metrics-a, ui-animation-feature-metrics-a, ui-character-feature-metrics-b, ui-animation-feature-metrics-b, infra-design-tokens-a, infra-metadata-schema-b, ui-character-feature-metrics-c, ui-combat-feature-metrics-a, ui-bestiary-feature-metrics-a, ui-combat-feature-metrics-b, ui-bestiary-feature-metrics-b, ui-items-feature-metrics-a, infra-design-tokens-b, ui-items-feature-metrics-b, ui-gas-feature-metrics-b, ui-loot-feature-metrics-b, ui-animation-scaling-a, ui-animation-scaling-b, ui-gas-scaling-a, ui-combat-scaling-a, ui-character-scaling-a, ui-gas-scaling-b, ui-combat-scaling-b, ui-items-scaling-a, ui-loot-scaling-a, ui-character-flow, ui-bestiary-scaling-a, ui-items-scaling-b, ui-loot-scaling-b, ui-bestiary-scaling-b, ui-animation-flow, ui-gas-flow, ui-items-flow, ui-combat-flow, ui-bestiary-flow, ui-loot-flow, ui-animation-visual, ui-character-visual-a, ui-gas-visual-a, ui-bestiary-visual, ui-character-visual-b, ui-gas-visual-b, ui-combat-visual-a, ui-items-visual-a, ui-combat-visual-b, ui-items-visual-b, ui-loot-visual-b, ui-style-audit-a

---

### Phase 1: Feature Map Card System (Part 1)

**Module:** `arpg-character`
**Description:** Replace the toggle-list FeatureMapTab with a card-grid matrix.
Each feature becomes a FeatureCard component — clickable card with active/inactive states.
Active: colored background tint (withOpacity accent 8%), bright border (accent 30%), full-color content.
Inactive: muted border (neutral 20%), desaturated content, reduced opacity.
Card shows: feature name (mono bold), metric content area (children render prop), status dot (bottom-right).
Layout: CSS grid auto-fill, min 160px, gap-3. Tab groups become section headers.
Create: src/components/shared/FeatureCard.tsx, src/components/shared/FeatureCardGrid.tsx.
Update: src/components/modules/core-engine/unique-tabs/FeatureMapTab.tsx to use new components.
Keep Enable All / Disable All buttons. Add NeonBar progress at top.
**Duration:** 1.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented FeatureCard component — Added cursor-pointer, tactile hover/active scale transforms, smooth status dot transition
- Implemented FeatureCardGrid component — Clean CSS grid with auto-fill min 160px, role=group, aria-label. Minimal and correct.
- Implemented FeatureMapTab integration — NeonBar progress, tab navigation, section headers, group-level and global Enable/Disable All buttons all present and correct
- Implemented feature-map-config registry — 13 modules with flat section definitions, clean grouping logic, summary fields for key sections
- Implemented useFeatureVisibility hook — Fixed setAll to skip no-op updates (matching setMany optimization), localStorage persistence, correct default-visible semantics

**Files:**
- `src/components/shared/FeatureCard.tsx`
- `src/hooks/useFeatureVisibility.ts`

**Gotchas:**
- Button elements default to cursor:default — always add cursor-pointer for clickable cards
- setAll and setMany should both check for no-op to avoid unnecessary re-renders and localStorage writes
- Tailwind scale transforms with hover/active give tactile feedback without needing framer-motion

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 2: Scalable Entity Selector (Part 1)

**Module:** `arpg-character`
**Description:** Create src/components/shared/ScalableSelector.tsx — a modal overlay component for picking
from 100+ entities. Must support: debounced search (300ms), group-by with collapsible sections,
virtual scroll for large lists, multi-select and single-select modes, keyboard navigation
(arrow keys, Enter, Escape), render prop for custom item cards, selected items shown as pills.
Props: items, groupBy, renderItem, onSelect, selected, searchKey, placeholder, mode.
Component structure: index.tsx, SelectorSearch.tsx, SelectorGroup.tsx, SelectorGrid.tsx, types.ts.
Place in src/components/shared/ScalableSelector/.
**Duration:** 2.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Debounced search (300ms) — Custom useDebounced hook with configurable delay
- Implemented Group-by with collapsible sections — Dynamic grouping by any key, collapsible state tracked in Set
- Implemented Virtual scroll — Binary search visible range, rAF-throttled scroll, ResizeObserver, 6-row overscan
- Implemented Multi-select and single-select modes — Single auto-closes, multi toggles membership, Ctrl+A selects all filtered
- Implemented Keyboard navigation — Arrow keys, PgUp/Dn, Home/End, Enter, Space, Escape — O(1) index lookup via Map
- Implemented Render prop for custom items — renderItem(item, selected) and renderGroupHeader(group) render props
- Implemented Selected pills — Accent-colored pills with remove buttons, overflow +N indicator, clear all
- Implemented Accessibility — Focus trap, scroll lock, focus restore, ARIA listbox/option/activedescendant, aria-live count
- Implemented Component structure — Clean 5-file split: types.ts, index.tsx, SelectorSearch.tsx, SelectorGroup.tsx, SelectorGrid.tsx

**Files:**
- `src/components/shared/ScalableSelector/index.tsx`

**Gotchas:**
- Existing implementation was high quality — only needed minor cleanup (redundant array copy, missing keyboard hint). Binary search + overscan virtual scroll pattern handles 100+ items well.

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 3: Scalable Entity Selector (Part 2)

**Module:** `arpg-character`
**Description:** Create src/components/shared/ScalableSelector.tsx — a modal overlay component for picking
from 100+ entities. Must support: debounced search (300ms), group-by with collapsible sections,
virtual scroll for large lists, multi-select and single-select modes, keyboard navigation
(arrow keys, Enter, Escape), render prop for custom item cards, selected items shown as pills.
Props: items, groupBy, renderItem, onSelect, selected, searchKey, placeholder, mode.
Component structure: index.tsx, SelectorSearch.tsx, SelectorGroup.tsx, SelectorGrid.tsx, types.ts.
Place in src/components/shared/ScalableSelector/.
**Duration:** 2.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented debounced-search — 300ms debounce with custom hook, search icon color transition, clear button, live count
- Implemented group-by-collapsible — Dynamic grouping with collapsible headers, chevron icons, item count badges
- Implemented virtual-scroll — Binary search + overscan(6) with rAF-throttled scroll, ResizeObserver for viewport, O(1) scroll-to-focused via itemRowIndex map
- Implemented multi-single-select — Single mode auto-closes, multi mode toggles with Ctrl+A select-all
- Implemented keyboard-navigation — Full coverage: arrows, PgUp/Dn, Home/End, Enter/Space, Escape, Ctrl+A — O(1) index lookups via navigableIndexMap
- Implemented render-prop — renderItem receives item + selected boolean, renderGroupHeader for custom headers
- Implemented selected-pills — Max 8 visible pills with +N more overflow, individual remove, clear all button
- Implemented accessibility — Dialog role, aria-modal, aria-activedescendant, aria-selected, focus trap, body scroll lock, trigger focus restore

**Files:**
- `src/components/shared/ScalableSelector/index.tsx`
- `src/components/shared/ScalableSelector/SelectorGrid.tsx`

**Gotchas:**
- Redundant data structures (navigableSet vs navigableIndexMap) add allocation cost for zero benefit — Map.has() serves both index lookup and membership testing. Building auxiliary index maps alongside existing offset computation loops adds O(1) lookups at zero extra pass cost.

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 4: Feature Map Card System (Part 2)

**Module:** `arpg-character`
**Description:** Replace the toggle-list FeatureMapTab with a card-grid matrix.
Each feature becomes a FeatureCard component — clickable card with active/inactive states.
Active: colored background tint (withOpacity accent 8%), bright border (accent 30%), full-color content.
Inactive: muted border (neutral 20%), desaturated content, reduced opacity.
Card shows: feature name (mono bold), metric content area (children render prop), status dot (bottom-right).
Layout: CSS grid auto-fill, min 160px, gap-3. Tab groups become section headers.
Create: src/components/shared/FeatureCard.tsx, src/components/shared/FeatureCardGrid.tsx.
Update: src/components/modules/core-engine/unique-tabs/FeatureMapTab.tsx to use new components.
Keep Enable All / Disable All buttons. Add NeonBar progress at top.
**Duration:** 5.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented FeatureCard component — Solid card with active/inactive states, status dot, metric children, cursor-pointer, hover/active scale, proper aria-pressed
- Implemented FeatureCardGrid component — CSS grid auto-fill min 160px with aria group label
- Implemented FeatureMapTab integration — NeonBar progress, Enable/Disable All, group-level All On/Off, tab navigation, renderMetric prop. Fixed MiniBtn missing cursor-pointer
- Implemented useFeatureVisibility hook — Fixed React 19 set-state-in-effect lint violation — replaced useEffect-based moduleId reset with render-time state comparison pattern. No-op skip in setAll/setMany preserved

**Files:**
- `src/hooks/useFeatureVisibility.ts`
- `src/components/modules/core-engine/unique-tabs/FeatureMapTab.tsx`

**Gotchas:**
- React 19 ESLint plugin flags setState-in-useEffect (react-hooks/set-state-in-effect) and ref access during render (react-hooks/refs) — use the state-based previous value comparison pattern instead
- MiniBtn (button elements in general) need explicit cursor-pointer class even when they have onClick handlers

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 5: Scalable Entity Selector (Part 3)

**Module:** `arpg-character`
**Description:** Create src/components/shared/ScalableSelector.tsx — a modal overlay component for picking
from 100+ entities. Must support: debounced search (300ms), group-by with collapsible sections,
virtual scroll for large lists, multi-select and single-select modes, keyboard navigation
(arrow keys, Enter, Escape), render prop for custom item cards, selected items shown as pills.
Props: items, groupBy, renderItem, onSelect, selected, searchKey, placeholder, mode.
Component structure: index.tsx, SelectorSearch.tsx, SelectorGroup.tsx, SelectorGrid.tsx, types.ts.
Place in src/components/shared/ScalableSelector/.
**Duration:** 3.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented debounced search (300ms) — Already solid — custom useDebounced hook with configurable delay
- Implemented group-by with collapsible sections — Clean groupItems helper, collapsible headers with aria-expanded, ungrouped hides redundant 'All' header
- Implemented virtual scroll for large lists — Binary search + overscan, rAF-throttled scroll, ResizeObserver for viewport, O(1) scroll-to-focused via itemRowIndex
- Implemented multi-select and single-select modes — Single mode auto-closes on select, multi mode shows pills with clear-all
- Implemented keyboard navigation — Fixed Home/End/Ctrl+A conflict with search input — keys now pass through to input when focused, only navigate items when outside input. Arrow, PgUp/Dn, Enter, Space, Esc all correct.
- Implemented render prop for custom item cards — renderItem(item, selected) pattern with full flexibility
- Implemented selected items shown as pills — Max 8 visible pills with +N overflow, accent-colored, removable, clear-all button

**Files:**
- `src/components/shared/ScalableSelector/index.tsx`

**Gotchas:**
- Home/End/Ctrl+A keyboard handlers on modal wrappers must check if event target is an INPUT — otherwise they steal cursor-movement and text-selection keys from the search field. Same pattern as the existing Space key guard.

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 6: Character Blueprint Feature Metrics (Part 1)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to each FeatureCard in the Character Blueprint Feature Map tab.
Each card's children slot gets a micro-visualization:
- Class Hierarchy: "{types} types / {classes} classes" side by side numbers
- Properties: Stacked mini bars (HP, Speed, Armor ranges, 60px wide)
- Scaling: Sparkline SVG (60×20px) showing first 10 curve points
- Hitbox: "{zones} zones" with colored dots (hurtbox=red, hitbox=blue, pushbox=green)
- Camera: Three compact stats "FOV | Arm | Lag" in mono font
- Bindings: Colored ratio bar "{bound}/{total}" with NeonBar
- Keyboard: "{conflicts}" count — green if 0, red if >0
- States: "{count} states" with mini icon
- Dodge: "{distance}m" bold number
- Curve Editor: Tiny sparkline of feel curve
- Optimizer: Preset name or "No preset" muted
- Comparison: "{dupes} duplicates" — red if >0
- Balance: Mini radar thumbnail (40×40px SVG)
All metrics derive from existing data in CharacterBlueprint/data.ts.
Create metric components in CharacterBlueprint/metrics/ directory.
**Duration:** 2.5min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented ClassHierarchyMetric — Counts tree nodes and unique subtypes correctly
- Implemented PropertiesMetric — Stacked HP/Speed/Armor mini bars at 60px using NeonBar
- Implemented ScalingMetric — Sparkline SVG 60×20px from SCALING_PROPS range data
- Implemented HitboxMetric — Zone count with colored type dots
- Implemented CameraMetric — FOV|Arm|Lag compact stats from BLUEPRINT_PROPERTIES
- Implemented BindingsMetric — Enhanced/total ratio with NeonBar
- Implemented KeyboardMetric — Conflict count with green/red conditional color
- Implemented StatesMetric — State count with per-state colored dots
- Implemented DodgeMetric — Distance hardcoded at 4.5m — no distance data in DODGE_TRAJECTORIES SVG paths
- Implemented CurveEditorMetric — Sparkline from ACCEL_CURVE_POINTS
- Implemented OptimizerMetric — Static 'No preset' placeholder — no dynamic preset state available
- Implemented ComparisonMetric — Duplicate detection via Set comparison
- Implemented BalanceMetric — 40×40px radar with grid outline + 2 character stat polygons


**Gotchas:**
- CharacterBlueprint metrics were already fully implemented with all 13 section IDs mapped — no gaps found
- DODGE_TRAJECTORIES contains SVG path data not numeric distances — hardcoded dodge distance is the correct approach

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 7: Animation State Graph Feature Metrics (Part 1)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Animation State Graph Feature Map.
- States: "{n} states / {g} groups"
- Transitions: "{n} edges" with mini directed-graph icon
- Heatmap: Micro heatmap grid (4×4, 20px)
- Chain: "{n} combos / depth {d}"
- Montages: "{n} montages"
- Scrubber: "{n} notifies"
- Skeleton: "{mapped}/{total}" bone mapping bar
- Trajectories: "{n} root motion curves"
- Assets: "{size}MB" estimated memory
- Playrate: "{min}–{max}x" range
Derive from AnimationStateGraph/data.ts. Create metrics/ subdirectory.
**Duration:** 2.1min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented States metric — {n} states / {g} groups derived from STATE_NODES and STATE_GROUPS
- Implemented Transitions metric — {n} edges with custom SVG directed-graph icon including arrow heads
- Implemented Heatmap micro grid — 5×5 SVG micro grid (25×25px) with opacity-mapped transition frequencies
- Implemented Chain metric — {n} combos / depth {d} with singular/plural handling
- Implemented Montages metric — {n} montages from MONTAGE_TIMINGS
- Implemented Scrubber metric — {n} notifies from SCRUBBER_LANES
- Implemented Skeleton bone mapping bar — {mapped}/{total} with NeonBar progress from BUDGET_GAUGES
- Implemented Trajectories metric — {n} root motion curves from ROOT_MOTION_PATHS
- Implemented Assets memory estimate — ~{size}MB estimated from montage + blend + bone heuristic
- Implemented Playrate range — Smart uniform fallback shows blend-in time range when all fps match


**Gotchas:**
- Existing AnimationStateGraph metrics implementation was already high quality — all 10 metrics correctly derive data from data.ts, use consistent styling patterns, and compute constants at module scope to avoid re-renders. PlayrateMetric has a smart fallback that shows blend-in time range when all montages share the same fps.

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 8: Character Blueprint Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to each FeatureCard in the Character Blueprint Feature Map tab.
Each card's children slot gets a micro-visualization:
- Class Hierarchy: "{types} types / {classes} classes" side by side numbers
- Properties: Stacked mini bars (HP, Speed, Armor ranges, 60px wide)
- Scaling: Sparkline SVG (60×20px) showing first 10 curve points
- Hitbox: "{zones} zones" with colored dots (hurtbox=red, hitbox=blue, pushbox=green)
- Camera: Three compact stats "FOV | Arm | Lag" in mono font
- Bindings: Colored ratio bar "{bound}/{total}" with NeonBar
- Keyboard: "{conflicts}" count — green if 0, red if >0
- States: "{count} states" with mini icon
- Dodge: "{distance}m" bold number
- Curve Editor: Tiny sparkline of feel curve
- Optimizer: Preset name or "No preset" muted
- Comparison: "{dupes} duplicates" — red if >0
- Balance: Mini radar thumbnail (40×40px SVG)
All metrics derive from existing data in CharacterBlueprint/data.ts.
Create metric components in CharacterBlueprint/metrics/ directory.
**Duration:** 1.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented ClassHierarchyMetric — Types/classes count from recursive tree traversal
- Implemented PropertiesMetric — Stacked NeonBar mini bars for HP/Speed/Armor at 60px width
- Implemented ScalingMetric — 60×20px sparkline SVG from SCALING_PROPS range ratios
- Implemented HitboxMetric — Zone count with colored dots per hitbox type
- Implemented CameraMetric — FOV|Arm|Lag compact mono font stats
- Implemented BindingsMetric — Enhanced/total ratio with NeonBar
- Implemented KeyboardMetric — Conflict count green/red with singular/plural
- Implemented StatesMetric — State count with colored mini dots
- Implemented DodgeMetric — Distance + path count from DODGE_TRAJECTORIES
- Implemented CurveEditorMetric — Sparkline from ACCEL_CURVE_POINTS
- Implemented OptimizerMetric — Static 'No preset' — correct given no runtime state
- Implemented ComparisonMetric — Duplicate detection with green/red indicator
- Implemented BalanceMetric — 40×40px radar thumbnail with 2 character overlays


**Gotchas:**
- CharacterBlueprint metrics were already fully implemented with all 13 section IDs mapped — no gaps found

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 9: Animation State Graph Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Animation State Graph Feature Map.
- States: "{n} states / {g} groups"
- Transitions: "{n} edges" with mini directed-graph icon
- Heatmap: Micro heatmap grid (4×4, 20px)
- Chain: "{n} combos / depth {d}"
- Montages: "{n} montages"
- Scrubber: "{n} notifies"
- Skeleton: "{mapped}/{total}" bone mapping bar
- Trajectories: "{n} root motion curves"
- Assets: "{size}MB" estimated memory
- Playrate: "{min}–{max}x" range
Derive from AnimationStateGraph/data.ts. Create metrics/ subdirectory.
**Duration:** 1.2min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented States metric — Correctly derives from STATE_NODES and STATE_GROUPS
- Implemented Transitions metric with graph icon — SVG mini directed-graph icon with arrow heads, edge count from transitions
- Implemented Heatmap micro grid — 5x5 SVG grid matching actual HEATMAP_STATE_NAMES count, proper opacity mapping
- Implemented Chain metric — Combo count with depth, handles singular/plural
- Implemented Montages metric — Clean count from MONTAGE_TIMINGS
- Implemented Scrubber metric — Notify count from SCRUBBER_LANES
- Implemented Skeleton bone mapping bar — Mapped/total with NeonBar progress visualization
- Implemented Trajectories metric — Root motion curve count from ROOT_MOTION_PATHS
- Implemented Assets memory estimate — Reasonable MB estimate from montages + blend clips + bones
- Implemented Playrate range — Smart fallback shows blend-in time range when all rates uniform


**Gotchas:**
- Existing AnimationStateGraph metrics implementation was already high quality — all 10 metrics correctly derive data from data.ts, use consistent styling patterns, and compute constants at module scope to avoid re-renders. PlayrateMetric has a smart fallback that shows blend-in time range when all montages share the same fps.

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 10: Design Token Standardization (Part 1)

**Module:** `arpg-character`
**Description:** Audit and standardize border opacities, glow radii, spacing across all unique-tab components.
Add to chart-colors.ts: BORDER_DEFAULT=OPACITY_20, BORDER_HOVER=OPACITY_37, BORDER_SUBTLE=OPACITY_10,
GLOW_SM='0 0 4px', GLOW_MD='0 0 8px', GLOW_LG='0 0 16px'.
Add to globals.css: --panel-padding:12px, --card-gap:12px, --section-gap:16px.
Update _design.tsx and _shared.tsx to use new constants.
Grep all unique-tabs/ for hardcoded hex opacity suffixes (e.g., #3b82f618, #f8717120)
and replace with imported withOpacity + constants. Do NOT change visual appearance.
**Duration:** 16.1min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Border opacity tokens (BORDER_DEFAULT/HOVER/SUBTLE) — Already defined in chart-colors.ts, used in _design.tsx and _shared.tsx
- Implemented Glow radius presets (GLOW_SM/MD/LG) — Already defined in chart-colors.ts, used in _design.tsx and _shared.tsx
- Implemented CSS spacing variables (--panel-padding/--card-gap/--section-gap) — Defined in globals.css :root. Components use Tailwind classes for spacing which is correct for this codebase
- Implemented _design.tsx uses design tokens — All borders, glows, opacities use imported constants
- Implemented _shared.tsx uses design tokens — Fixed 5 remaining rgba(255,255,255,...) values, gridColor default now uses withOpacity
- Implemented Hardcoded hex opacity suffixes (#RRGGBBAA) — Zero 8-digit hex values found in unique-tabs — all previously converted
- Implemented rgba(255,255,255,...) standardization — Replaced ~150 rgba(255,255,255,...) across 77+ files with withOpacity(OVERLAY_WHITE, OPACITY_*)
- Implemented New opacity constants for completeness — Added OPACITY_2/3/4/6/40 for exact mapping of rgba 0.02-0.06 and 0.4 values
- Implemented Non-white rgba values preserved — 21 rgba values for black/dark shadows and specific accent colors correctly left as-is

**Files:**
- `src/lib/chart-colors.ts`
- `src/components/modules/core-engine/unique-tabs/_shared.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/TacticsMap.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/SpawnFormationViz.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/PerceptionConeViz.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/DifficultyChart.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/BTFlowchart.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilityForge/index.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilityForge/ForgeInput.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilityForge/ForgeResult.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilityForge/ComboTimeline.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/core/CoreSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/AbilitiesSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/DamageCalcSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagDepsSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagAuditSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/AttributeGrowthChart.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/retargeting/RetargetingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/FrameScrubberPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/BlendSpacePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatChoreographyEditor/UnifiedTimeline.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/hits/HitsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/MetricsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/StatInfluencePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/ComboChainBuilder/CooldownOverlapChart.tsx`
- `src/components/modules/core-engine/unique-tabs/charts/AccelCurve.tsx`
- `src/components/modules/core-engine/unique-tabs/charts/DodgeTrajectoryMap.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterFeelOptimizer/PresetCard.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterFeelOptimizer/FeelInputPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterFeelPlayground/CurveEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterFeelPlayground/StickFigurePreview.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterGenomeEditor/LevelScaledPowerCurve.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterGenomeEditor/GenomeHeaderPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterGenomeEditor/GenomePill.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/TimelineBar.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/VelocityCurve.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/index.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/DodgeChainTimeline.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/FrameDataTable.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/ChainControls.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemEconomySimulator/PowerCurveChart.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/WiringGraphEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/SimulationSandbox.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/EffectTimelineEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/TagRulesEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/RelationshipWebEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/LoadoutEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/mechanics/MechanicsScalingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/ItemScalingChart.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/EconomySections.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/EconomySourcingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/TradingCard.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/GearSections.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/pity/DroughtCalculator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/WorldItemPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/DataRecoveryTool.tsx`
- `src/components/modules/core-engine/unique-tabs/ProgressionCurve/analysis/PowerCurveDangerZones.tsx`
- `src/components/modules/core-engine/unique-tabs/ProgressionCurve/rewards/AchievementBoard.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/PowerCurveDangerZones.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/AchievementBoard.tsx`
- `src/components/modules/core-engine/unique-tabs/DamagePipelineDiagram/ExecComponents.tsx`
- `src/components/modules/core-engine/unique-tabs/DamagePipelineDiagram/DamagePipelineFlow.tsx`
- `src/components/modules/core-engine/unique-tabs/DamagePipelineDiagram/DirectHealthFlow.tsx`
- `src/components/modules/core-engine/unique-tabs/ComparisonMatrix/index.tsx`

**Gotchas:**
- rgba(255,255,255,...) was the dominant hardcoded pattern (216 occurrences) — 8-digit hex values were already cleaned up in a prior pass
- New OPACITY_2/3/4/6/40 constants needed for exact mapping of common rgba sub-10% and 40% values
- rgba(0,0,0,...) dark shadows and specific accent colors (168,85,247 purple, 6,182,212 cyan) have no semantic token — correctly left as-is
- SVG stroke/fill attributes accept both rgba() and #RRGGBBAA formats — withOpacity() works for both
- Tailwind arbitrary values like fill-[rgba(...)] must use hex+alpha format since they can't call JS functions

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 11: Entity Metadata Schema (Part 2)

**Module:** `arpg-character`
**Description:** Define TypeScript interfaces for categorizing game entities.
Create src/types/game-metadata.ts with:
- EntityMetadata: id, name, category, subcategory?, tags[], level?, levelMax?, area?, tier?, icon?
- EntityGrouping: field (keyof EntityMetadata), label, order? (explicit group sort)
- Default grouping presets: CHAR_GROUPINGS (category→subcategory), ITEM_GROUPINGS (category→tier),
  ABILITY_GROUPINGS (category→element), ENEMY_GROUPINGS (area→tier)
Extend each module's data.ts with metadata fields on mock items (add category, tags, area, tier).
This is the foundation for ScalableSelector group-by in Phase 2.
**Duration:** 3.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented EntityMetadata interface — All fields present: id, name, category, subcategory?, tags[], level?, levelMax?, area?, tier?, icon?
- Implemented EntityGrouping with order — All 6 presets have explicit order arrays where appropriate. Added element order to ABILITY_GROUPINGS.
- Implemented groupEntities utility — Fixed Ungrouped bucket to always sort last even with explicit order. Array field expansion, fallback sorting all correct.
- Implemented Per-domain metadata coverage — All 6 domains (Character, Item, Ability, Enemy, Zone, Loot) derive metadata from domain data with proper tags and tiers.

**Files:**
- `src/types/game-metadata.ts`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/data.ts`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/data.ts`
- `src/components/modules/core-engine/unique-tabs/ZoneMap/data.ts`

**Gotchas:**
- ABILITY_METADATA was producing a meaningless 'none' tag when damageType was 'None' — always filter sentinel values before lowercasing into tags
- LOOT_ENTRY_METADATA tags were too sparse (rarity only) — category and subcategory should always appear as tags for consistent filterability across domains
- ZONE_METADATA had area=z.name duplicating the name field — zones ARE areas, so area field is redundant; added z.group tag instead for topology grouping
- groupEntities must check for 'Ungrouped' key BEFORE consulting the order index, otherwise 'Ungrouped' gets Infinity and sorts alphabetically among other overflow groups instead of always being last

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 12: Character Blueprint Feature Metrics (Part 3)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to each FeatureCard in the Character Blueprint Feature Map tab.
Each card's children slot gets a micro-visualization:
- Class Hierarchy: "{types} types / {classes} classes" side by side numbers
- Properties: Stacked mini bars (HP, Speed, Armor ranges, 60px wide)
- Scaling: Sparkline SVG (60×20px) showing first 10 curve points
- Hitbox: "{zones} zones" with colored dots (hurtbox=red, hitbox=blue, pushbox=green)
- Camera: Three compact stats "FOV | Arm | Lag" in mono font
- Bindings: Colored ratio bar "{bound}/{total}" with NeonBar
- Keyboard: "{conflicts}" count — green if 0, red if >0
- States: "{count} states" with mini icon
- Dodge: "{distance}m" bold number
- Curve Editor: Tiny sparkline of feel curve
- Optimizer: Preset name or "No preset" muted
- Comparison: "{dupes} duplicates" — red if >0
- Balance: Mini radar thumbnail (40×40px SVG)
All metrics derive from existing data in CharacterBlueprint/data.ts.
Create metric components in CharacterBlueprint/metrics/ directory.
**Duration:** 1.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Class Hierarchy metric — Recursive tree count with types/classes display
- Implemented Properties metric — Stacked NeonBar mini bars for HP/Speed/Armor from COMPARISON_CHARACTERS
- Implemented Scaling metric — 60x20 sparkline SVG from SCALING_PROPS with dots
- Implemented Hitbox metric — Zone count with colored type dots (Hurtbox/Hitbox/Pushbox)
- Implemented Camera metric — Compact FOV|Arm|Lag stats from BLUEPRINT_PROPERTIES
- Implemented Bindings metric — Enhanced/total ratio with NeonBar
- Implemented Keyboard metric — Conflict count with green/red coloring from KEY_CONFLICTS
- Implemented States metric — State count with mini colored dots from MOVEMENT_STATES
- Implemented Dodge metric — Distance + path count (hardcoded distance correct per SVG path data)
- Implemented Curve Editor metric — 60x20 sparkline from ACCEL_CURVE_POINTS
- Implemented Optimizer metric — Static 'No preset' — correct for current data model
- Implemented Comparison metric — Duplicate name detection with success/error coloring
- Implemented Balance metric — 40x40 radar thumbnail with grid + 2 character overlays


**Gotchas:**
- CharacterBlueprint metrics were already fully implemented with all 13 section IDs mapped — no gaps found

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 13: Combat Action Map Feature Metrics (Part 1)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Combat Action Map Feature Map.
- Lanes: "{n} lanes / {n} actions"
- Sequences: "{n} events / {n} systems"
- Traces: "{sphere} sphere / {capsule} capsule"
- Stats: "{avg} avg DPS" bold
- Feedback Tuner: "{tuned}/{total}" params bar
- DPS: Mini bar chart (3 bars for top abilities)
- Effectiveness: "{best}" top ability name
- Sankey: "{flows} flows"
- KPIs: Traffic light (3 dots for TTK, DPS, APM)
Derive from CombatActionMap/data-metrics.ts.
**Duration:** 1.9min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Lanes metric — Derives lane/action counts from LANES data at module scope
- Implemented Sequences metric — Derives event/system counts from SEQ_EVENTS and SEQ_LANES
- Implemented Traces metric — Classifies sphere vs capsule via radius threshold from TRACE_FRAMES
- Implemented Stats metric — Computes average DPS from DPS_STRATEGIES
- Implemented Feedback Tuner metric — Shows tuned/total with progress bar from FEEDBACK_PARAMS
- Implemented DPS mini bar chart — Top 3 abilities as colored bars, sorted at module scope
- Implemented Effectiveness metric — Shows best ability name and DPS from sorted DPS_STRATEGIES
- Implemented Sankey metric — Flow count from SANKEY_FLOWS
- Implemented KPIs traffic light — 3 colored dots for TTK/DPS/APM with glow shadow


**Gotchas:**
- All 9 Combat Action Map metrics were already fully implemented with correct data derivation, module-scope computation, and consistent styling — no modifications needed

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 14: Enemy Bestiary Feature Metrics (Part 1)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Enemy Bestiary Feature Map.
- Cards: "{n} archetypes" with role pie (melee/ranged/tank/support micro-donut)
- Modifiers: "{n} modifiers / {n} exclusions"
- Radar: Mini radar comparing weakest vs strongest enemy
- Behavior Tree: "{n} nodes / {d} depth"
- Decision Log: "{n} decisions/sec" throughput
- Aggro: "{n} threat sources"
- Formations: "{n} formations" with mini dot-pattern
- Waves: "{n} waves / {total} enemies"
- Difficulty: Sparkline of difficulty curve
Derive from EnemyBestiary/data.ts.
**Duration:** 2.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Cards metric (archetypes + role pie) — Micro-donut SVG with melee/ranged/tank segments, derives from ARCHETYPES
- Implemented Modifiers metric — Fixed: now counts total exclusion entries instead of modifiers-with-excludes
- Implemented Radar metric (weakest vs strongest) — Mini radar overlay comparing lowest vs highest avg radar score
- Implemented Behavior Tree metric — Node count + tree depth via recursive walk of BT_EDGES
- Implemented Decision Log metric — Throughput computed from decision count / tick span
- Implemented Aggro metric — Mini threat bars with source count
- Implemented Formations metric — Mini dot-pattern from SPAWN_POINTS with count
- Implemented Waves metric — Wave count + total enemies from WAVE_TIMELINE
- Implemented Difficulty metric (sparkline) — Polyline sparkline of DIFFICULTY_BRUTE curve

**Files:**
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/metrics/ModifiersMetric.tsx`

**Gotchas:**
- All 9 Enemy Bestiary feature metrics were already implemented and high quality — only ModifiersMetric had a bug counting modifiers-with-excludes instead of total exclusion entries

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 15: Combat Action Map Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Combat Action Map Feature Map.
- Lanes: "{n} lanes / {n} actions"
- Sequences: "{n} events / {n} systems"
- Traces: "{sphere} sphere / {capsule} capsule"
- Stats: "{avg} avg DPS" bold
- Feedback Tuner: "{tuned}/{total}" params bar
- DPS: Mini bar chart (3 bars for top abilities)
- Effectiveness: "{best}" top ability name
- Sankey: "{flows} flows"
- KPIs: Traffic light (3 dots for TTK, DPS, APM)
Derive from CombatActionMap/data-metrics.ts.
**Duration:** 1.1min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Lanes metric — Correctly derives lane/action counts from LANES data
- Implemented Sequences metric — Correctly derives event/system counts from SEQ_EVENTS/SEQ_LANES
- Implemented Traces metric — Smart sphere/capsule classification via radius threshold
- Implemented Stats metric — Computes avg DPS from DPS_STRATEGIES at module scope
- Implemented Feedback Tuner metric — Shows tuned/total with progress bar, correct pct calculation
- Implemented DPS mini bar chart — Top 3 bars sorted by DPS, proportional heights, glow shadow
- Implemented Effectiveness metric — Shows best strategy name with its color and DPS value
- Implemented Sankey metric — Flow count derived from SANKEY_FLOWS
- Implemented KPIs traffic light — 3 colored dots with glow for TTK/DPS/APM


**Gotchas:**
- All 9 Combat Action Map metrics were already fully implemented with correct data derivation, module-scope computation, and consistent styling — no modifications needed

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 16: Enemy Bestiary Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Enemy Bestiary Feature Map.
- Cards: "{n} archetypes" with role pie (melee/ranged/tank/support micro-donut)
- Modifiers: "{n} modifiers / {n} exclusions"
- Radar: Mini radar comparing weakest vs strongest enemy
- Behavior Tree: "{n} nodes / {d} depth"
- Decision Log: "{n} decisions/sec" throughput
- Aggro: "{n} threat sources"
- Formations: "{n} formations" with mini dot-pattern
- Waves: "{n} waves / {total} enemies"
- Difficulty: Sparkline of difficulty curve
Derive from EnemyBestiary/data.ts.
**Duration:** 1.5min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented CardsMetric with role pie donut — SVG micro-donut correctly segments by role with chart-colors tokens
- Implemented ModifiersMetric with exclusion count — Correctly sums total exclusion entries across all modifiers
- Implemented RadarMetric weakest vs strongest — Mini radar overlay comparing sorted avg scores from RADAR_DATA
- Implemented BehaviorTreeMetric nodes/depth — DFS depth computation from BT_EDGES, module-scope constants
- Implemented DecisionLogMetric throughput — Throughput derived from tick span, consistent ACCENT_PURPLE styling
- Implemented AggroMetric threat sources — Proportional threat bars using entry.color, correct count
- Implemented FormationsMetric dot-pattern — SVG dot-pattern from SPAWN_POINTS with scaled viewBox
- Implemented WavesMetric waves/enemies — Wave count + reduce sum from WAVE_TIMELINE
- Implemented DifficultyMetric sparkline — SVG polyline from DIFFICULTY_BRUTE L1-L50 data


**Gotchas:**
- All 9 Enemy Bestiary feature metrics were already fully implemented with correct data derivation, module-scope computation, chart-colors usage, and consistent styling — no modifications needed in Part 2

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 17: Item Catalog Feature Metrics (Part 1)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Item Catalog Feature Map.
- Grid: "{n} items / {slots} slots"
- Sets: "{n} sets / {bonuses} bonuses"
- Loadout: "{equipped}/{slots}" fill bar
- Sources: "{n} source types"
- Scaling: Sparkline item power vs level
- Stats: "{avg} avg ilvl"
- Power: "{min}–{max}" power range bar
Derive from ItemCatalog/data.ts.
**Duration:** 2.1min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Grid metric — Type distribution bar + item/slot counts, correct data derivation
- Implemented Sets metric — Set/bonus counts with piece ownership dots per set
- Implemented Loadout metric — Equipped/total fill bar with percentage
- Implemented Sources metric — Source type count with colored dots from CRYSTAL_STAFF_SOURCES
- Implemented Scaling metric — SVG sparkline from SCALING_LINES[0] with level range label
- Implemented Stats metric — Fixed bar chart overflow bug — BAR_W clamped to 2 caused 54px total in 48px SVG with 18 items
- Implemented Power metric — Min-max range bar with proper positioning within globalMax

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/metrics/StatsMetric.tsx`

**Gotchas:**
- StatsMetric BAR_W formula Math.max(W/n - 1, 2) overflows when n*3 > W — use (W - n + 1)/n to ensure bars fit within SVG viewBox regardless of item count

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 18: Design Token Standardization (Part 2)

**Module:** `arpg-character`
**Description:** Audit and standardize border opacities, glow radii, spacing across all unique-tab components.
Add to chart-colors.ts: BORDER_DEFAULT=OPACITY_20, BORDER_HOVER=OPACITY_37, BORDER_SUBTLE=OPACITY_10,
GLOW_SM='0 0 4px', GLOW_MD='0 0 8px', GLOW_LG='0 0 16px'.
Add to globals.css: --panel-padding:12px, --card-gap:12px, --section-gap:16px.
Update _design.tsx and _shared.tsx to use new constants.
Grep all unique-tabs/ for hardcoded hex opacity suffixes (e.g., #3b82f618, #f8717120)
and replace with imported withOpacity + constants. Do NOT change visual appearance.
**Duration:** 8.9min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Border opacity tokens (BORDER_DEFAULT/HOVER/SUBTLE) — Already existed in chart-colors.ts and used by _design.tsx/_shared.tsx — no changes needed
- Implemented Glow radius presets (GLOW_SM/MD/LG) — Already existed; propagated GLOW_SM to 12 files (0 0 4px) and GLOW_MD to 13 files (0 0 8px)
- Implemented Spacing CSS variables (--panel-padding/--card-gap/--section-gap) — Already existed in globals.css — no changes needed
- Implemented Hardcoded hex opacity suffix elimination — Replaced all 14 hardcoded #RRGGBBAA patterns: 7 SVG fill-[#hex] Tailwind classes converted to inline fill attr with withOpacity(), 4 gradient Tailwind classes converted to inline style, 3 zone label fills. Zero remaining 8-digit hex in unique-tabs/

**Files:**
- `src/components/modules/core-engine/unique-tabs/bestiary/TacticsMap.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/SpawnFormationViz.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/PerceptionConeViz.tsx`
- `src/components/modules/core-engine/unique-tabs/bestiary/DifficultyChart.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/DifficultyChart.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/TradingCard.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/WorldItemPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/mechanics/MechanicsScalingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/AILootDesigner/DistributionsPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/GASBlueprintEditor/EffectTimelineEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/systems/ArpgHudPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/flow/FlowGraph.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatChoreographyEditor/ScrubTooltip.tsx`
- `src/components/modules/core-engine/unique-tabs/gas-blueprint/EffectTimelineEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/movement/DodgeTrajectorySection.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/KpisMetric.tsx`
- `src/components/modules/core-engine/unique-tabs/DodgeTimelineEditor/RatingBadge.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatChoreographyEditor/UnifiedTimeline.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/input/InputBindingsTable.tsx`
- `src/components/modules/core-engine/unique-tabs/DebugDashboard/system/CircularGauge.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterGenomeEditor/GenomeHeaderPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/polish/FeedbackTab.tsx`
- `src/components/modules/core-engine/unique-tabs/DebugDashboard/index.tsx`
- `src/components/modules/core-engine/unique-tabs/ZoneMap/density/DensityLevelGroup.tsx`
- `src/components/modules/core-engine/unique-tabs/ProgressionCurve/rewards/LevelUpRewardPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/GearSections.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/LevelUpRewardPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/EncounterTTKSimulator.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/index.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/AutoSaveConfig.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/ExpandedDetails.tsx`

**Gotchas:**
- Tailwind arbitrary fill-[#RRGGBBAA] on SVG elements converts cleanly to inline fill={withOpacity()} attr — preserves className for font/size utilities while using design tokens for color
- Tailwind gradient hex classes (from-[#hex]/via-[#hex]) must convert to inline style={{ background: 'linear-gradient(...)' }} since they can't call JS functions — remove the gradient utility classes entirely
- 0 0 4px and 0 0 8px were the two most common inline glow radii (~25 combined occurrences) — 0 0 6px (20+ occurrences) has no exact GLOW constant and was correctly left untouched to preserve visual appearance

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 19: Item Catalog Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Item Catalog Feature Map.
- Grid: "{n} items / {slots} slots"
- Sets: "{n} sets / {bonuses} bonuses"
- Loadout: "{equipped}/{slots}" fill bar
- Sources: "{n} source types"
- Scaling: Sparkline item power vs level
- Stats: "{avg} avg ilvl"
- Power: "{min}–{max}" power range bar
Derive from ItemCatalog/data.ts.
**Duration:** 1.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Grid metric — Shows item/slot count with type distribution bar segments
- Implemented Sets metric — Shows set/bonus count with singular/plural labels and ownership dots
- Implemented Loadout metric — Fill bar with equipped/total count
- Implemented Sources metric — Unique source type count with colored dots from CRYSTAL_STAFF_SOURCES
- Implemented Scaling metric — Sparkline from first SCALING_LINE with level range label
- Implemented Stats metric — Bar chart with avg primary stat, uses overflow-safe bar width formula
- Implemented Power metric — Range bar showing min-max power normalized to globalMax


**Gotchas:**
- All 7 Item Catalog metrics were already fully implemented with correct data derivation, module-scope computation, chart-colors usage, overflow-safe bar chart, and consistent styling — no modifications needed in Part 2

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 20: Ability Spellbook Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Ability Spellbook Feature Map.
- Architecture: "ASC → {n} GA → {n} GE" pipeline count
- Radar: Mini radar (40px) with 3-ability overlay
- Cooldowns: "{avg}s avg / {min}–{max}s range"
- Timeline: Micro timeline strip (60px)
- Effects Timeline: "{n} active / {n} passive"
- Tags: "{n} tags / {depth} depth"
- Hierarchy: "{n} roots / {n} leaves"
- Audit: "{n} warnings" — green if 0, red if >0
- Dependencies: "{n} deps / {n} circular" — red if circular >0
Derive from AbilitySpellbook/data.ts.
**Duration:** 3.1min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tag tree stats metric — Recursive countTags walker correctly computes total, depth, roots, leaves from TAG_TREE. Used in both 'tags' and 'hierarchy' section metrics.
- Implemented Audit warning counter metric — Sums non-pass TAG_AUDIT_CATEGORIES counts. Color-coded green (STATUS_SUCCESS) when 0, red (STATUS_ERROR) when >0. Pluralization handled.
- Implemented Dependency circular detector metric — Builds edge set, detects A→B+B→A pairs, divides by 2 to avoid double-counting. Red highlight on circular >0, green otherwise.
- Implemented All metrics integrated — useGASFeatureMetrics hook used in index.tsx:125, renderMetric passed to FeatureMapTab. All 9 section IDs mapped in switch statement.


**Gotchas:**
- Existing GAS Feature Metrics implementation was already complete and high quality — all 9 section IDs mapped with correct formats, proper chart-colors usage, and optimized single-pass useMemo computation

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 21: Loot Table Feature Metrics (Part 2)

**Module:** `arpg-character`
**Description:** Add unique metric visualizations to Loot Table Visualizer Feature Map.
- Pipeline: "{stages} stages" flow
- Weights: Mini pie showing rarity distribution
- World Items: "{n} world items"
- Treemap: Micro treemap thumbnail (40×30px)
- Histogram: "{n} brackets"
- Simulator: "{n} affixes in pool"
- Co-occurrence: "{conflicts}" hot cell count
- Timer: "{threshold} pulls" pity
- Drought: "{prob}%" probability
- Beacon: "{n} beacons"
- Impact: "{gold}/hr" rate
Derive from LootTableVisualizer/data.ts.
**Duration:** 2.0min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Pipeline metric — 5-dot flow with connecting lines, pre-computed geometry
- Implemented Weights metric — Mini pie chart with IIFE-computed slice paths from RARITY_TIERS
- Implemented World items metric — Colored dots per world item with count text
- Implemented Treemap metric — 40x30px micro treemap with pre-computed rects via IIFE
- Implemented Histogram metric — Bar chart of rarity weights with brackets count
- Implemented Simulator metric — Text-only affix pool count, correct but minimal visual
- Implemented Co-occurrence metric — Mini heatmap grid with hot cell count (>=0.7 threshold)
- Implemented Timer metric — Progress bar at 75% fill with pity threshold text
- Implemented Drought metric — Legendary drop rate percentage with tier-colored number
- Implemented Beacon metric — Height-proportional bars per beacon config with count
- Implemented Impact metric — Progress bar (current/target gold) with rate text


**Gotchas:**
- All 11 Loot Table Visualizer feature metrics were already fully implemented with correct data derivation, module-scope computation, pre-computed IIFEs for mutable accumulation, and consistent styling — no modifications needed

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 22: Animation State Graph Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Animation State Graph to handle 100+ montages.
1. Expand data.ts: Add 80+ montages with metadata (categories: Attack, Dodge, HitReact, Death, Idle, Locomotion, Ability, Emote).
2. Combo Chain: Add montage picker via ScalableSelector grouped by category. Paginate 10/page.
3. State Machine: Support 30+ states by clustering into collapsible groups.
4. Frame Scrubber: Montage selector dropdown (grouped by category).
5. Budget: Virtual-scroll asset list. Group by category. Per-group memory subtotals.
**Duration:** 9.7min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data.ts with 80+ montages — 86 montages across 8 categories (Attack:20, Dodge:12, HitReact:10, Death:6, Idle:8, Locomotion:10, Ability:12, Emote:8) with full metadata (frames, fps, memory, rootMotion, blendIn). Pre-computed category counts, memory subtotals, and total.
- Implemented Combo Chain montage picker via ScalableSelector — Added Pick Montage button that opens ScalableSelector modal grouped by category with multi-select. Picked montages shown as pills. Pagination at 10/page for combo nodes. Proper generic typing with SelectorItem.
- Implemented State Machine 30+ states with collapsible groups — 32 states across 6 groups (Movement:8, Combat:6, Reaction:7, Ability:4, Social:3, Traversal:4). StateMachinePanel uses collapsible group headers with ChevronDown toggle, state count badges, transition count per state, and scrollable transitions matrix.
- Implemented Frame Scrubber montage selector dropdown — Grouped dropdown selector with sticky category headers showing montage count. Selecting a montage updates frame count, scales notify lanes proportionally, shows root motion badge. Outside-click-to-close behavior.
- Implemented Budget virtual-scroll asset list with category groups — MontageAssetBrowser with virtual scroll (28px row height, 5-row overscan, 350px viewport), collapsible category groups, per-group memory subtotals in headers, stacked memory distribution bar, category colors using chart-colors constants. Handles 86 items smoothly.

**Files:**
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/data.ts`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/state-graph/StateMachinePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/ComboChainPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/FrameScrubberPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/BudgetTab.tsx`

**Gotchas:**
- ScalableSelector SelectorItem requires index signature — use intersection type (SelectorItem & { ...fields }) rather than interface extends to satisfy the constraint
- STATE_NODES and AnimStateName are separate concerns — STATE_NODES defines the full state graph for browsing, AnimStateName covers only combat-critical states for responsiveness analysis
- Virtual scroll with absolute positioning + flat row list (headers + items) is simpler than nested virtual lists — single scrollTop drives all row positions
- Pre-computing category aggregates (MONTAGE_CATEGORY_COUNTS, MONTAGE_CATEGORY_MEMORY) at module scope avoids redundant computation in React renders

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 23: Animation State Graph Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Animation State Graph to handle 100+ montages.
1. Expand data.ts: Add 80+ montages with metadata (categories: Attack, Dodge, HitReact, Death, Idle, Locomotion, Ability, Emote).
2. Combo Chain: Add montage picker via ScalableSelector grouped by category. Paginate 10/page.
3. State Machine: Support 30+ states by clustering into collapsible groups.
4. Frame Scrubber: Montage selector dropdown (grouped by category).
5. Budget: Virtual-scroll asset list. Group by category. Per-group memory subtotals.
**Duration:** 3.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented ComboChainPanel SVG layout fix — Fixed SVG overflow: nodes now positioned sequentially with dynamic viewBox width instead of hardcoded x positions extending past 400px. Edges filtered to current page. Single shared SVG marker definition.
- Implemented FrameScrubberPanel React 19 fix — Fixed nested setState anti-pattern: setScrubberPlaying was called inside setScrubberFrame updater. Now uses ref to signal stop, with setScrubberPlaying called after the frame update.
- Implemented BudgetTab category color deduplication — Emote and HitReact both used STATUS_WARNING making them indistinguishable in memory bar. Emote now uses ACCENT_EMERALD.

**Files:**
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/ComboChainPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/FrameScrubberPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/BudgetTab.tsx`

**Gotchas:**
- ComboChainPanel nodes had hardcoded x positions (500, 600, 700) that overflowed the 400px SVG viewBox — dynamic layout with sequential positioning and computed viewBox width is needed when node count can grow
- SVG marker IDs should be shared (single <defs>) rather than duplicated per-edge to avoid ID collision and redundant DOM nodes
- React 19 flags setScrubberPlaying inside setScrubberFrame updater — use a ref to signal the stop condition and call the second setState outside the updater
- BudgetTab categoryColor had Emote and HitReact sharing STATUS_WARNING — every category needs a unique color for the stacked memory bar to be readable

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 24: Ability Spellbook Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Ability Spellbook to handle 100+ abilities.
1. Expand data.ts: Add 60+ abilities with metadata (categories: Offensive, Defensive, Utility, Passive, Ultimate; elements: Fire, Ice, Lightning, Shadow, Holy; tiers: basic, advanced, ultimate).
2. Ability Radar: ScalableSelector to pick 3-6 abilities for comparison. Group by category→element.
3. Effects List: Paginate gameplay effects. Group by type (Instant, Duration, Periodic, Infinite).
4. Tag Tree: Lazy expansion — only first level by default. Search filter for tag names.
5. Core: Attribute picker for 20+ attributes.
**Duration:** 11.7min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data.ts with 60+ abilities — 69 abilities with full metadata across 5 categories, 8 elements, 3 tiers. Plus 30 effects and 25 attributes.
- Implemented Ability Radar with ScalableSelector — ScalableSelector modal picks 2-6 abilities for radar comparison, grouped by category, with element badges.
- Implemented Effects List paginated by type — Type filter tabs (All/Instant/Duration/Periodic/Infinite) with 8-per-page pagination and grouped rendering.
- Implemented Tag Tree lazy expansion + search — Search filter with match count, forceOpen on search, max-height scroll. Lazy first-level default already existed.
- Implemented Attribute picker 20+ attributes — 25 attributes (12 core + 13 derived) with search filter, merged with live UE5 data.

**Files:**
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/data.ts`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/AbilitiesSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/effects/EffectsSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagsSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/AttributesSection.tsx`

**Gotchas:**
- SpellbookAbility needs [key: string]: unknown index signature to satisfy SelectorItem constraint for ScalableSelector generic
- ScalableSelector groupBy works with single field — for category→element hierarchy, group by category and show element as badge in renderItem
- EXPANDED_EFFECTS grouped rendering uses Map<EffectDurationType, entries[]> for clean swim-lane display within pages
- Tag tree search uses recursive filterTree() that preserves parent nodes when children match — forceOpen prop bypasses local collapse state

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 25: Combat Action Map Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Combat Action Map to handle 100+ weapons/combos.
1. Expand data.ts: Add 40+ weapons with metadata (categories: Sword, Axe, Mace, Bow, Staff, Dagger, Polearm; tiers: common-legendary). Add 30+ combo sequences.
2. Flow: Weapon selector to filter lanes. ScalableSelector grouped by category.
3. Hits: Filter traces by weapon. Per-weapon hit stats.
4. Metrics: Select 2-4 weapons for DPS comparison.
5. Combo Chains: Paginate combos. Group by weapon type.
**Duration:** 8.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data (40+ weapons, 30+ combos) — 44 weapons across 7 categories and 5 tiers, 32 combo sequences with chain details, EntityMetadata + groupings for ScalableSelector
- Implemented Flow: Weapon selector with ScalableSelector — Multi-select weapon filter using ScalableSelector grouped by category, filters combat lanes by melee/ranged
- Implemented Hits: Per-weapon hit stats — Dropdown weapon selector with derived hit stats (hit rate, hits/swing, trace coverage, crit, avg damage) with NeonBar visualization
- Implemented Metrics: Multi-weapon DPS comparison — Select 2-4 weapons for side-by-side DPS comparison with related combos by weapon category
- Implemented Combo Chains: Pagination + weapon type grouping — 32 combos paginated (6 per page) with category filter chips, DPS bars, and legacy chain in collapsible details

**Files:**
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/data-metrics.ts`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/FlowTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/ComboChainDiagram.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/hits/HitsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/MetricsTab.tsx`

**Gotchas:**
- data.ts re-exports data-metrics.ts via `export * from './data-metrics'` — avoid importing the same symbol from both files
- HeatmapGrid lowColor/highColor props use raw hex which triggers lint warnings — these pre-existed and have no chart-colors semantic token
- WEAPONS array with 44 entries and COMBO_SEQUENCES with 32 entries provide sufficient scale for 100+ item testing when combined with ScalableSelector grouping

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 26: Character Blueprint Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Character Blueprint to handle 100+ characters.
1. Expand data.ts: Add 50+ characters with EntityMetadata (categories: Warrior, Mage, Rogue, Tank, Support, Beast, Droid, Force-user; areas: Taris, Dantooine, Kashyyyk, Korriban, Manaan; tiers: common, elite, boss, legendary).
2. Comparison tab: Replace inline character list with ScalableSelector modal. Default shows 2 selected. "Add Character" button opens selector grouped by class→role.
3. Overview tab: Add character picker (single-select ScalableSelector grouped by area).
4. Camera Profiles: Support comparing 3+ profiles via multi-select.
5. Balance Radar: Handle 10+ overlay by showing top-5 + "show all" toggle.
**Duration:** 19.0min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data.ts to 50+ characters — 57 characters across 8 categories (Force-user, Warrior, Mage, Rogue, Tank, Support, Beast, Droid), 5 areas, 4 tiers. Fixed broken 0-100 scale values in original characters. Used builder pattern (CHAR_DEFS) for single source of truth.
- Implemented Comparison tab ScalableSelector modal — Replaced inline character pills with compact selected badges + 'Add Character' button opening ScalableSelector modal grouped by class. Defaults to 2 selected characters.
- Implemented Overview character picker — Added single-select ScalableSelector grouped by area at top of overview tab. Shows selected character name, class, tier, and area.
- Implemented Camera profiles multi-select — Expanded from 3 to 6 profiles (added Stealth, Boss Fight, Aerial). Toggle buttons on each profile card with active/inactive styling. Minimum 1 profile selected.
- Implemented Balance radar top-5 toggle — When 10+ characters selected, shows top 5 by composite score with 'Show All (N)' toggle button. Below 10, shows all. Toggle button only appears when truncation is needed.

**Files:**
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/data.ts`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/index.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/simulator/ComparisonMatrix.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/simulator/ArchetypeBalanceRadar.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/overview/CameraProfileComparison.tsx`
- `src/types/game-metadata.ts`

**Gotchas:**
- SelectableCharacter with [key: string]: unknown index signature satisfies SelectorItem constraint while preserving typed property access — use this pattern for ScalableSelector integration
- CHAR_DEFS builder pattern generates both CHARACTER_METADATA and COMPARISON_CHARACTERS from a single source — prevents sync drift between metadata and stat arrays
- Original COMPARISON_CHARACTERS had broken 0-100 scale values (Jedi Guardian HP=85 vs maxVal=1500) — always verify stat values against maxVal ranges when expanding data
- TIER_LEVELS lookup provides default level/levelMax ranges per tier — avoids repeating level ranges in every character definition
- CameraProfileComparison multi-select uses minimum-1 guard pattern (same as character toggle) to prevent empty state

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 27: Ability Spellbook Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Ability Spellbook to handle 100+ abilities.
1. Expand data.ts: Add 60+ abilities with metadata (categories: Offensive, Defensive, Utility, Passive, Ultimate; elements: Fire, Ice, Lightning, Shadow, Holy; tiers: basic, advanced, ultimate).
2. Ability Radar: ScalableSelector to pick 3-6 abilities for comparison. Group by category→element.
3. Effects List: Paginate gameplay effects. Group by type (Instant, Duration, Periodic, Infinite).
4. Tag Tree: Lazy expansion — only first level by default. Search filter for tag names.
5. Core: Attribute picker for 20+ attributes.
**Duration:** 4.2min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expanded Ability Data (69 abilities) — 5 categories, 8 elements, 3 tiers with radar stats, descriptions, and tags
- Implemented Ability Radar with ScalableSelector — ScalableSelector modal with category grouping, 2-6 ability comparison on radar chart with pills
- Implemented Effects List with Pagination — 30 effects across 4 types, grouped swim-lanes, pagination, type filter buttons. Fixed precomputed type counts.
- Implemented Tag Tree with Search Filter — Recursive filterTree preserves parent nodes, lazy expansion with forceOpen on search. Fixed duplicate countTags call.
- Implemented Attribute Picker (25 attributes) — 12 core + 13 derived attributes with search filter, live UE5 sync merge

**Files:**
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/data.ts`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagsSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/effects/EffectsSection.tsx`

**Gotchas:**
- COMBO_ABILITIES had 7 hardcoded hex colors (#60a5fa, #a78bfa, #22c55e, #f59e0b, #06b6d4, #e879f9, #7f1d1d) — mapped to STATUS_INFO, ACCENT_VIOLET, ACCENT_GREEN, STATUS_WARNING, ACCENT_CYAN, ACCENT_PINK, HEATMAP_STEP_1 respectively
- Recursive utility functions like countTags should never be called multiple times in JSX — memoize the result once and reuse
- When EXPANDED_EFFECTS is static, per-type counts can be precomputed in a useMemo with empty deps to avoid refiltering on each render

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 28: Combat Action Map Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Combat Action Map to handle 100+ weapons/combos.
1. Expand data.ts: Add 40+ weapons with metadata (categories: Sword, Axe, Mace, Bow, Staff, Dagger, Polearm; tiers: common-legendary). Add 30+ combo sequences.
2. Flow: Weapon selector to filter lanes. ScalableSelector grouped by category.
3. Hits: Filter traces by weapon. Per-weapon hit stats.
4. Metrics: Select 2-4 weapons for DPS comparison.
5. Combo Chains: Paginate combos. Group by weapon type.
**Duration:** 4.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Metrics compares 2-4 weapons — Category-grouped weapon grid, DPS comparison bars, related combos display — no issues found
- Implemented Combo chains paginated and grouped — Category filter chips, 6-per-page pagination, weapon category labels, sorted DPS bars — clean implementation
- Implemented 100 weapons render without lag — Fixed SVG viewBox dynamic height, eliminated duplicated DPS computation, added scroll container for 44-weapon list, removed unused CON stat

**Files:**
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/FlowTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/StatInfluencePanel.tsx`

**Gotchas:**
- computeDps returning full breakdown object (effectiveDamage, effectiveSpeed, effectiveCrit, dps) eliminates need for duplicate inline computation — single source of truth for both detail display and comparison list
- CON stat slider was wired to state but never consumed by DPS formula — dead state variables add React reconciliation overhead for zero user value
- 44-item DPS comparison list needs max-h + overflow-y-auto to prevent unbounded panel growth — scroll containers are essential when data scale exceeds ~15 visible rows

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 29: Item Catalog Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Item Catalog to handle 100+ items.
1. Expand data.ts: Add 120+ items with metadata (categories: Weapon/Armor/Accessory/Consumable/Quest/Material; subcategories per type; tiers: common-legendary; sets).
2. Catalog Grid: Paginated 20/page. Filter by category, tier, slot. Sort by name/power/tier. ScalableSelector for comparison.
3. Equipment Loadout: Item picker per slot using ScalableSelector filtered to slot type.
4. Item Sets: Collapsible set panels. Filter complete/incomplete.
5. Comparison: Side-by-side 2-3 items with stat diff highlighting.
**Duration:** 18.9min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data.ts to 120+ items — 134 items across 6 categories (Weapon/Armor/Accessory/Consumable/Quest/Material) with subcategories, tiered stats, and deterministic affix assignment via builder pattern
- Implemented Catalog Grid with filters and sort — Paginated 20/page. Dynamic category filter (6 types), subtype filter (context-sensitive), rarity filter, sort by name/power/tier/type. Item count display.
- Implemented Equipment Loadout with ScalableSelector — Click any slot to open ScalableSelector filtered by slot-valid subtypes (SLOT_SUBTYPES mapping). Items grouped by rarity. Stats auto-converted to loadout format.
- Implemented Item Sets collapsible with filter — 6 sets total (4 new). Collapsible panels with expand/collapse toggle. All/Complete/Incomplete filter buttons. Animated transitions.
- Implemented Comparison 2-3 items — Dynamic grid supports 2-3 items. Add/remove slot buttons. Best/worst stat highlighting with green/red colors. Description and effect comparison.

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/data-items.ts`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/data.ts`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/CatalogGearTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/GearSections.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/ItemComparisonPanel.tsx`
- `src/types/game-metadata.ts`

**Gotchas:**
- Builder pattern with compact tuple definitions [name, subtype, rarity, desc, effect?] + stat generators per type keeps 116 items under 250 lines
- import type from circular module works cleanly — TypeScript erases type imports at compile time so no runtime circular dependency
- ringColor is not a valid CSS property — use boxShadow: '0 0 0 1px color' for ring-like selection effect in inline styles
- ScalableSelector requires SelectorItem with [key: string]: unknown — use intersection type (ItemData & { [key: string]: unknown }) for casting
- SLOT_SUBTYPES mapping enables both slot-filtered item picking (loadout) and dynamic subtype filter dropdown (catalog) from a single source of truth

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 30: Loot Table Visualizer Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Loot Table Visualizer to handle 100+ entries.
1. Expand data.ts: Add 60+ loot table entries, 30+ affixes, 20+ enemy bindings.
2. Loot Table Editor: Paginated with search. Group by source (enemy/chest/quest/crafting). Inline weight editing.
3. Affix Simulator: Affix pool picker via ScalableSelector grouped by Offensive/Defensive/Utility.
4. Drop Simulator: Enemy picker for simulation using ScalableSelector.
5. Economy Impact: Filter by tier/source. Paginated impact table.
**Duration:** 12.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Expand data.ts with 60+ entries, 30+ affixes, 20+ enemy bindings — 65 loot entries across 4 sources (enemy/chest/quest/crafting), 35 affixes in 3 categories (Offensive/Defensive/Utility), 22 enemy bindings across 4 tiers (Minion/Standard/Elite/Boss)
- Implemented Loot Table Editor: paginated with search, group by source, inline weight editing — Added search filter, source grouping (enemy/chest/quest/crafting) with sticky headers, pagination (20/page), preserved inline weight editing and UE5 import/export
- Implemented Affix Simulator: ScalableSelector grouped by Offensive/Defensive/Utility — ScalableSelector with category grouping, weighted rolls using active pool, category breakdown badges, frequency histogram colored by category
- Implemented Drop Simulator: Enemy picker via ScalableSelector — ScalableSelector for 22 enemies, per-enemy simulation using their rarity weights and drop chance, clear selected enemy indicator with dismiss
- Implemented Economy Impact: filter by tier/source, paginated impact table — Rarity tier filter (5 tiers + All), source filter (4 sources + All), paginated table (15/page) with item name, rarity, source, weight, drop%, gold impact

**Files:**
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/data.ts`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/data-binding.ts`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/affix/LootTableEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/affix/AffixRollSimulator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/simulation/DropSimulator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/economy/EconomyImpact.tsx`

**Gotchas:**
- LootEditorEntryExpanded with [key: string]: unknown index signature causes Omit<> to lose typed property access — use a separate interface for raw entries to preserve type narrowing
- CSS ringColor is not a valid React style property — use outline: '1px solid color' instead for selection indicators
- RARITY_COLOR_MAP defined after EXPANDED_ENTRIES causes forward reference issues — define a local RARITY_TO_COLOR map before the entries array
- ScalableSelector requires SelectorItem with id:string — AffixDef and enemy binding wrapper types both need explicit id fields

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 31: Character Blueprint Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Character Blueprint tabs to tell a chronological narrative.
New tab order: Features → Overview → Input → Movement → Playground → AI Feel → Simulator.
Narrative: "Define → Control → Move → Feel → Optimize → Compare".
Add a subtle breadcrumb bar at top of the unique tab showing the narrative flow with styled steps.
Each tab header gets a one-line subtitle explaining its role in the pipeline.
Reorder the subtab array and update the navigation. Do not change component internals.
**Duration:** 1.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured to narrative flow — SUBTABS follows Define→Control→Move→Feel→Optimize→Compare order
- Implemented Tab subtitles added — Each BlueprintSubtabDef has descriptive subtitle, rendered below nav
- Implemented Navigation updated — SubTabNavigation and breadcrumb both wire to setActiveTab correctly
- Implemented Breadcrumb bar — NarrativeBreadcrumb with past/active/future styling, clickable navigation, chart-colors tokens


**Gotchas:**
- Character Blueprint flow redesign was already fully implemented with high quality across all 4 features — no modifications needed

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 32: Enemy Bestiary Scaling (Part 1)

**Module:** `arpg-character`
**Description:** Upgrade Enemy Bestiary to handle 100+ enemies.
1. Expand data.ts: Add 80+ archetypes with metadata (categories: Humanoid, Beast, Droid, Force-sensitive, Undead; areas: all zones; tiers: minion/standard/elite/boss/raid-boss; roles: melee/ranged/tank/healer/caster/swarm).
2. Archetype Grid: Paginated grid. Group by area→role. ScalableSelector for comparison picks.
3. Comparison: Multi-select up to 4 enemies. Radar overlay.
4. Encounters: Enemy picker via ScalableSelector grouped by area→tier. Wave editor handles 20+ waves with virtual scroll.
5. AI Logic: BT viewer supports 50+ nodes with collapsible subtrees and search.
**Duration:** 7.7min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Data expanded to 80+ enemies with metadata — 94 archetypes across 5 categories, 16 areas, 6 roles, 5 tiers. Compact builder pattern works well. Generated radar data for all archetypes using stat-based derivation with role-specific range/aggression defaults.
- Implemented Archetype grid paginated and grouped — 24-per-page pagination, 6 groupBy options, multi-filter bar with search. Fixed missing cursor-pointer on card buttons.
- Implemented Comparison multi-select with ScalableSelector — Up to 4 enemies compared with stat bars and radar mini. Fixed radar overlay to not initialize all 94 enemies to true. Added search/scroll/show-more to overlay toggle list.
- Implemented Encounter enemy picker uses ScalableSelector — Virtual-scroll wave editor with tier filter. Fixed hardcoded #3b82f6 in TacticsMap to use MODULE_COLORS.core. Fixed rgba(168,85,247,...) in ArchetypeBuilder to use chart-colors tokens.

**Files:**
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/data.ts`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/index.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/RadarComparison.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/ArchetypeCard.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/ArchetypeBuilder.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/ai-logic/DecisionDebugger.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/TacticsMap.tsx`

**Gotchas:**
- RADAR_DATA only covered 4 of 94 archetypes — stat-based radar derivation with role-specific range/aggression defaults fills the gap for expanded enemies
- Initializing radar overlays for all 94 archetypes renders an unreadable mess — default to player-only and let users toggle specific enemies
- RadarComparison overlay list needs search + scroll + show-more pattern to scale beyond ~10 items
- TacticsMap player marker used hardcoded #3b82f6 which is MODULE_COLORS.core
- ArchetypeBuilder preview had rgba(168,85,247,...) inline — maps to ACCENT_PURPLE_BOLD with opacity helpers

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 33: Item Catalog Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Item Catalog to handle 100+ items.
1. Expand data.ts: Add 120+ items with metadata (categories: Weapon/Armor/Accessory/Consumable/Quest/Material; subcategories per type; tiers: common-legendary; sets).
2. Catalog Grid: Paginated 20/page. Filter by category, tier, slot. Sort by name/power/tier. ScalableSelector for comparison.
3. Equipment Loadout: Item picker per slot using ScalableSelector filtered to slot type.
4. Item Sets: Collapsible set panels. Filter complete/incomplete.
5. Comparison: Side-by-side 2-3 items with stat diff highlighting.
**Duration:** 5.5min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Catalog Grid (134+ items, paginated, filtered, sorted) — Already complete with 20/page pagination, 4 filters, 4 sort modes, search, keyboard nav, and AI item creation
- Implemented Equipment Loadout with ScalableSelector — Paper doll SVG, ScalableSelector picker per slot filtered by SLOT_SUBTYPES, stat contribution bars
- Implemented Item Sets (collapsible, filter complete/incomplete) — 6 sets with collapsible panels, complete/incomplete/all filter, active bonus indicators
- Implemented Item Comparison (2-3 side-by-side) — Category selector, 2-3 slot comparison with stat diff bars, best/worst coloring, description comparison
- Implemented StatsMetric SVG overflow fix — Fixed bar chart overflow when 130+ items caused bars to exceed 48px SVG width — now subsamples to max 24 bars

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/index.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/data.ts`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/metrics/StatsMetric.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/EconomySourcingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/mechanics/MechanicsScalingTab.tsx`

**Gotchas:**
- All 7 Item Catalog metrics were already fully implemented with correct data derivation, module-scope computation, chart-colors usage, overflow-safe bar chart, and consistent styling — no modifications needed in Part 2
- StatsMetric subsampling pattern: when item count exceeds SVG pixel width, subsample sorted array to max W/2 entries using evenly-spaced index picks to preserve distribution shape
- optgroup in select dropdowns significantly improves item browsability at 134+ items — group by item type (Weapon/Armor/Accessory/Consumable/Quest/Material)
- ITEM_METADATA tags should include rarity and type as lowercase tags alongside subtype for consistent cross-domain filterability

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 34: Loot Table Visualizer Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Loot Table Visualizer to handle 100+ entries.
1. Expand data.ts: Add 60+ loot table entries, 30+ affixes, 20+ enemy bindings.
2. Loot Table Editor: Paginated with search. Group by source (enemy/chest/quest/crafting). Inline weight editing.
3. Affix Simulator: Affix pool picker via ScalableSelector grouped by Offensive/Defensive/Utility.
4. Drop Simulator: Enemy picker for simulation using ScalableSelector.
5. Economy Impact: Filter by tier/source. Paginated impact table.
**Duration:** 3.7min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Loot Table Editor with pagination and search — Paginated with search, source grouping, inline weight editing, UE5 import/export — all working correctly
- Implemented Affix Simulator with ScalableSelector — Affix pool picker grouped by Offensive/Defensive/Utility via ScalableSelector, weighted roll simulation, co-occurrence matrix — all correct
- Implemented Drop Simulator with enemy picker — Fixed groupBy type cast from 'archetypeId as never' to proper 'tier' field with derived Minion/Standard/Elite/Boss grouping
- Implemented Economy Impact with filters — Tier and source filters, paginated impact table, casual/hardcore profiles — all working correctly
- Implemented Bug fixes — Fixed MonteCarloSim state mutation (.sort() on state array), eliminated triple redundant .find() in DropTreemap, removed duplicate itemMap useMemo in WorldItemPreview

**Files:**
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/simulation/MonteCarloSim.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/simulation/DropTreemap.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/simulation/DropSimulator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/WorldItemPreview.tsx`

**Gotchas:**
- MonteCarloSim.sort() mutated state array directly — always use [...arr].sort() or .toSorted() when working with state-derived arrays
- DropSimulator groupBy used 'archetypeId as never' type cast — derive meaningful tier (Minion/Standard/Elite/Boss) from dropChance ranges for proper ScalableSelector grouping
- WorldItemPreview had useMemo-wrapped Map that duplicated module-scope constant — static data should be computed at module scope, not inside components
- TREEMAP_DATA.find() called 3 times for same treemapDrill value in JSX — extract to local variable via IIFE inside JSX to avoid redundant array traversal

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 35: Enemy Bestiary Scaling (Part 2)

**Module:** `arpg-character`
**Description:** Upgrade Enemy Bestiary to handle 100+ enemies.
1. Expand data.ts: Add 80+ archetypes with metadata (categories: Humanoid, Beast, Droid, Force-sensitive, Undead; areas: all zones; tiers: minion/standard/elite/boss/raid-boss; roles: melee/ranged/tank/healer/caster/swarm).
2. Archetype Grid: Paginated grid. Group by area→role. ScalableSelector for comparison picks.
3. Comparison: Multi-select up to 4 enemies. Radar overlay.
4. Encounters: Enemy picker via ScalableSelector grouped by area→tier. Wave editor handles 20+ waves with virtual scroll.
5. AI Logic: BT viewer supports 50+ nodes with collapsible subtrees and search.
**Duration:** 4.2min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Archetype Grid with 94 enemies — Paginated grid with search, 5 filters (role/category/tier/area/search), 6 group-by modes, up to 4 comparison slots. Already well-implemented.
- Implemented Multi-enemy comparison with radar — ComparisonPanel shows stat bars with cross-enemy diff, RadarComparison has search+scroll overlay toggle, 7-axis radar for all 94 archetypes via deriveRadarFromStats.
- Implemented AI Logic with 54-node BT — Collapsible tree list with search, auto-expand ancestors of matches, keyboard navigation (Arrow keys + Enter), collapse/expand all. Fixed hardcoded focus border color.
- Implemented Encounters with virtual scroll waves — 24-wave virtual scroll timeline with tier filter, 3 spawn formations (Circle/Line/Ambush) now properly wired to SpawnFormationViz.
- Implemented WavesMetric data accuracy — Fixed bug: was showing 3 waves from WAVE_TIMELINE, now correctly shows 24 waves from EXPANDED_WAVES.

**Files:**
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/metrics/WavesMetric.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/SpawnFormationViz.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/EncountersTab.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/CodegenModal.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/ai-logic/BTFlowchart.tsx`

**Gotchas:**
- WavesMetric was importing WAVE_TIMELINE (3 legacy waves) instead of EXPANDED_WAVES (24 waves) — metric showed stale counts that didn't match the actual EncountersTab data
- SpawnFormationViz accepted no formation prop despite EncountersTab maintaining formation state — dead state variables create user confusion when controls appear to do nothing
- Layout computation for Line/Ambush formations uses simple spacing arithmetic — no need for complex physics or force-directed layout for 6 spawn points

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 36: Animation State Graph Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Animation tabs: Features → State Graph → Combos → Retargeting → Budget.
Narrative: "Define States → Chain Actions → Port Across Skeletons → Budget Check".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 3.2min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured — Features → State Graph → Combos → Retargeting → Budget — already correctly ordered in ANIM_SUBTABS
- Implemented Tab subtitles — All 4 content subtabs have descriptive subtitles in AnimSubtabDef, displayed below nav
- Implemented Breadcrumb flow bar — NarrativeBreadcrumb with Catalog > Define States > Chain Actions > Port Across Skeletons > Budget Check, clickable navigation, past/active/future styling

**Files:**
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/state-graph/StateGroupBrowser.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/StateDurationPanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/ComboTimelinePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/retargeting/RetargetingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/state-graph/ResponsivenessAnalyzer.tsx`

**Gotchas:**
- All 3 Animation State Graph flow features (tab order, subtitles, breadcrumb) were already fully implemented with high quality — only needed minor cleanup
- StateGroupBrowser cancel badge and StateDurationPanel outlier label used hardcoded Tailwind amber classes — replaced with STATUS_WARNING chart-colors tokens via inline style
- ComboTimelinePanel tab selector buttons and RetargetingTab pipeline step buttons were missing cursor-pointer class
- RetargetingTab had unused NeonBar import and ResponsivenessAnalyzer had unused warnings variable

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 37: Ability Spellbook Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Ability tabs: Features → Core → Abilities → Effects → Tags → Combos.
Narrative: "Foundation → Define Abilities → Apply Effects → Organize Tags → Chain Combos".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 2.0min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured — Features → Core → Abilities → Effects → Tags → Combos order correctly implemented in SUBTABS constant and index.tsx
- Implemented Tab subtitles added — Each SpellbookSubtabDef has narrative and subtitle fields, displayed below tab navigation
- Implemented Breadcrumb flow bar — NarrativeBreadcrumb component with Catalog > Foundation > Define Abilities > Apply Effects > Organize Tags > Chain Combos flow, clickable navigation, active/past/future styling with proper chart-colors usage

**Files:**
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/AbilitiesSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/LoadoutSection.tsx`

**Gotchas:**
- ringColor is not a valid CSS property — both AbilitiesSection ScalableSelector renderItem and LoadoutSection slot cards had ringColor in inline styles doing nothing; replaced with outline: '1px solid color'

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 38: Item Catalog Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Item tabs: Features → Catalog → Economy → Mechanics.
Narrative: "Browse Items → Track Economy → Understand Scaling".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 2.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructure — Tab order already correct: Features → Catalog → Economy → Mechanics. Added typed SUBTABS definition.
- Implemented Breadcrumb flow bar — Added NarrativeBreadcrumb with clickable steps: Catalog > Browse Items > Track Economy > Understand Scaling. Uses chart-colors withOpacity.
- Implemented Tab subtitles — Added per-tab subtitles describing content. Subtitle extracted to local variable to avoid IIFE in JSX.

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/index.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/data.ts`

**Gotchas:**
- Item Catalog tab order was already correct — the redesign only needed narrative metadata and UI chrome additions
- ItemCatalogSubtabDef interface export enables other consumers to read narrative/subtitle metadata without coupling to internal state

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 39: Combat Action Map Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Combat tabs: Features → Flow → Hits → Metrics → Polish.
Narrative: "Pipeline → Hit Detection → Measure Balance → Polish Feel".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 2.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured — Correct order: Features → Flow → Hits → Metrics → Feedback. Already implemented correctly.
- Implemented Breadcrumb flow bar — NarrativeBreadcrumb with Catalog → Pipeline → Hit Detection → Measure Balance → Polish Feel narrative. Clickable navigation, proper styling with chart-colors tokens.
- Implemented Tab subtitles — All 4 content tabs have descriptive subtitles via CombatSubtabDef.subtitle. Features tab has hardcoded subtitle.

**Files:**
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/LaneSection.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/polish/FeedbackTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/StatInfluencePanel.tsx`

**Gotchas:**
- LaneSection quality score badge used hardcoded Tailwind emerald-400 classes — replaced with ACCENT_EMERALD chart-colors tokens via inline style
- FeedbackTab preset buttons and LaneSection expand buttons were missing cursor-pointer class
- StatInfluencePanel weapon selector had flat 44-item list without optgroup — added category grouping to match HitsTab pattern

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 40: Enemy Bestiary Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Bestiary tabs: Features → Archetypes → AI Logic → Encounters.
Narrative: "Define Enemies → Give Them Brains → Place in World".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 3.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured — Features → Archetypes → AI Logic → Encounters order already correct
- Implemented Tab subtitles added — All subtabs have descriptive subtitles with narrative breadcrumb
- Implemented Breadcrumb flow bar — Catalog > Define Enemies > Give Them Brains > Place in World narrative already implemented with clickable navigation

**Files:**
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/ai-logic/BTFlowchart.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/ai-logic/AILogicTab.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/ArchetypeBuilder.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/RadarComparison.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/EncountersTab.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/SpawnFormationViz.tsx`

**Gotchas:**
- Tailwind accent-purple-500 and accent-blue-500 classes for form controls should use inline style={{ accentColor: TOKEN }} to satisfy chart-colors lint rules
- BTFlowchart had dead props (nodes/edges) in interface that were never consumed — component uses BT_TREE from data module directly
- SpawnFormationViz cx variable was declared but only cy was used in Line formation layout — dead variable elimination
- Single-item grid wrappers with xl:grid-cols-2 create empty columns — remove grid wrapper when only one child

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 41: Loot Table Flow Redesign

**Module:** `arpg-character`
**Description:** Restructure Loot tabs: Features → Core → Simulation → Affix → Pity → Economy.
Narrative: "Define Drops → Simulate → Roll Affixes → Ensure Fairness → Economy".
Add breadcrumb flow bar. Add tab subtitles.
**Duration:** 5.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Tab order restructured — Already correct: Features → Core → Simulation → Affix → Pity → Economy with typed LootSubtab definitions
- Implemented Tab subtitles added — Already implemented with narrative breadcrumb and per-tab subtitle display
- Implemented Breadcrumb flow bar — NarrativeBreadcrumb fully implemented with clickable navigation, past/active/future styling, and correct narrative labels. Fixed 3 missing cursor-pointer buttons, 6 hardcoded focus:ring-orange-500/50 violations, 1 hardcoded text-cyan-100/90, 1 hardcoded hover:text-red-400, 1 hardcoded bg-[#0d1117]

**Files:**
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/pity/PityTimerSection.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/pity/DroughtCalculator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/economy/BeaconVisualizer.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/EnemyLootBinding.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/EVCalculator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/affix/LootTableEditor.tsx`

**Gotchas:**
- focus:ring-current/50 is already used in _shared.tsx — valid pattern for replacing hardcoded focus:ring-orange-500/50 across all input elements
- accent-cyan-500/accent-blue-500/accent-orange-500 on range inputs are native browser accent utilities, not hex color violations — common pattern across project
- LootFilters duplicate enemyMap is necessary to avoid circular dependency with index.tsx which imports LootFilters

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 42: Animation State Graph Visual Polish

**Module:** `arpg-character`
**Description:** Visual upgrade for Animation State Graph.
1. State Machine: Full SVG node graph — rounded rects, animated transition arrows, glow on active, group backgrounds.
2. Combo Timeline: Horizontal with hit windows (colored), cancel windows (hatched), damage numbers, scrub handle.
3. Blend Space: 2D scatter with blend triangle.
4. Budget: Horizontal stacked bar chart showing memory per category.
**Duration:** 11.2min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented State Machine SVG Graph — Added group-level SVG node graph with 6 rounded-rect nodes, animated dashed transition arrows, glow filter on hover, and color-coded groups. Existing collapsible list and transitions matrix preserved below.
- Implemented Combo Timeline Polish — Added damage numbers per section header, hatched repeating-linear-gradient on ComboWindow bars, scrub handle with range input and vertical position indicator across all section windows.
- Implemented Blend Space Triangle — Added dashed blend triangle polygon connecting the 3 nearest clips to the current blend position. Module-scope BLEND_TRIANGLE precomputed for zero render overhead.
- Implemented Budget Stacked Bar Labels — Increased stacked bar height from h-4 to h-6, added category name labels for segments wider than 8%, tightened gap from 1 to 0.5 for better visual density.

**Files:**
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/data.ts`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/state-graph/StateMachinePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/ComboTimelinePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/BlendSpacePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/budget/BudgetTab.tsx`

**Gotchas:**
- Group-level SVG graph (6 nodes) is more readable than full 31-state graph — individual states remain browsable in the collapsible list below
- rectEdgePoint() using min(hw/abs(dx), hh/abs(dy)) scaling cleanly computes arrow endpoints on rounded rect edges
- SVG animate inside conditional {hl && <animate>} triggers only on hover — no continuous animation cost when idle
- repeating-linear-gradient(45deg) at 2px/4px intervals gives clean hatching without SVG pattern defs
- BLEND_TRIANGLE precomputed at module scope from sorted distance — avoids useMemo for static data

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 43: Character Blueprint Visual Polish (Part 1)

**Module:** `arpg-character`
**Description:** Visual upgrade for Character Blueprint components.
1. Comparison Matrix: Card-based columns per character. Stat bars with diff highlighting.
2. Hitbox Viewer: Layered wireframe SVG with zone coloring and glow. Hover to highlight zones.
3. Camera Profiles: Visual viewport mockups showing FOV cone, arm length, lag zone.
4. Movement States: State-machine SVG diagram with animated transition arrows.
5. Properties: Collapsible categories with colored headers.
6. Keyboard: Proper key sizing, accent glow on bound keys, red pulse on conflicts.
Follow design directive for all styling decisions.
**Duration:** 3.9min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Comparison Matrix: Card-based columns with stat bars — ScalableSelector modal, NeonBar stat bars, Crown highlighting for best-in-stat, deviation heatmap in ArchetypeBalanceRadar. Fully implemented.
- Implemented Hitbox Viewer: Layered wireframe SVG with zone glow — Three zone layers (Hurtbox/Hitbox/Pushbox) with toggle switches, animated scan line, hover-to-highlight synced between SVG shapes and buttons, animated zone entrance. Fixed 4 redundant template literals.
- Implemented Camera Profiles: Visual viewport mockups — ViewportMockup SVG showing FOV cone, camera arm line, lag zone circle, smoothness dots. Multi-select radar overlay. Responsive grid layout per selection count.
- Implemented Movement States: State-machine SVG diagram — Ellipse nodes with edgeEndpoints() for surface intersection, parallel edge offset, hover highlighting with dimming, animated transition arrows, label display on hover. Three-panel layout with DonutChart and AccelCurve.
- Implemented Properties: Collapsible categories with colored headers — Collapsible categories with animated chevron rotation, colored accent bars, search bypass (collapsed state ignored during search), property sliders with highlight animation and modified badge. Summary bar with NeonBar.
- Implemented Keyboard: Key sizing, accent glow, red pulse conflicts — 3D perspective keyboard with proper widthClass sizing, gradient backgrounds on bound keys, animated red pulse ring on conflicts, mouse widget with LMB/RMB/scroll/look. Fixed missing cursor-pointer on Reset All button.

**Files:**
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/input/InputBindingsTable.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/overview/HitboxWireframeViewer.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/simulator/ArchetypeBalanceRadar.tsx`

**Gotchas:**
- Character Blueprint visual polish was already fully implemented with high quality across all 6 features — only needed minor cleanup (1 missing cursor-pointer, 5 redundant template literals)

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 44: Ability Spellbook Visual Polish (Part 1)

**Module:** `arpg-character`
**Description:** Visual upgrade for Ability Spellbook.
1. Ability Cards: Rich cards — icon circle with initial, cooldown badge, element dot, mini cost bar.
2. Effect Timeline: Gantt-style chart with durations, stacking, decay. Color by effect type.
3. Tag Tree: Interactive tree with expand/collapse, search highlight, SVG relationship curves.
4. Attribute Web: Interactive force-directed graph. Drag nodes. Hover shows relationship details.
5. Damage Calculator: Visual formula breakdown (Base × Mult + Flat → Armor → Final).
**Duration:** 4.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Ability Cards — Enhanced plain text pills to rich cards with icon circle+initial, cooldown badge, element dot, and mini mana cost bar. Fixed 8 hardcoded Tailwind color violations (purple-400/500, green-400, amber-400) with chart-colors tokens.
- Implemented Effect Timeline — Already implemented as Gantt-style swim-lane chart with duration bars, instant event dots, color by category, and time axis. No changes needed.
- Implemented Tag Tree — Already implemented with interactive expand/collapse, recursive search filtering with forceOpen bypass, match count, and styled leaf/branch nodes. No changes needed.
- Implemented Attribute Web — Already implemented as interactive SVG graph with circular layout, hover-to-highlight connected subgraph via BFS, edge type styling (solid scales, dashed partial), and legend. No changes needed.
- Implemented Damage Calculator — Already implemented with slider params, 4-step formula breakdown (Base×Mult→Armor→Crit→Final), styled step badges, and GlowStat results. Excellent quality, no changes needed.

**Files:**
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/abilities/AbilitiesSection.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagAuditSection.tsx`

**Gotchas:**
- AbilitiesSection had 8 hardcoded Tailwind purple/green/amber classes across 3 glow background divs, 2 icon colors, 1 background color, and 2 status text spans — all mapped to ACCENT_PURPLE_BOLD, ACCENT_GREEN, and STATUS_WARNING
- TagAuditSection blocking tags used hardcoded bg-red-500/10 text-red-400 border-red-500/20 — replaced with ACCENT_RED tokens via inline style
- Ability pills enhanced from flat text buttons to rich cards: icon circle with first-letter initial, element color dot, cooldown badge, and mini cost bar — uses grid layout instead of flex-wrap for consistent card sizing

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 45: Enemy Bestiary Visual Polish

**Module:** `arpg-character`
**Description:** Visual upgrade for Enemy Bestiary.
1. Archetype Cards: Role icon, tier glow border (gray/purple/orange/gold), stat bars with avg markers, modifier pills.
2. Behavior Tree: Full diagram — diamond selectors, rect sequences, rounded tasks, hex decorators. Color by status. Collapsible.
3. Formation Viz: Top-down dot formation with role-colored dots.
4. Difficulty Curve: Line chart with shaded danger zones (too easy/target/too hard).
**Duration:** 6.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Archetype Cards — tier glow border + avg markers — Added tier-specific glow border colors (gray/blue/purple/orange/gold) using TIER_GLOW_COLORS map, tier badge in card header, and average stat markers (vertical line) on NeonBar stat bars showing cross-archetype average per stat label
- Implemented Behavior Tree — full diagram with shapes and collapsible — Already fully implemented with collapsible tree list, diamond/rect/rounded/hex shape indicators, search with auto-expand ancestors, keyboard navigation (arrows/enter/space), expand/collapse all, active/inactive status coloring, and BT details panel
- Implemented Formation Viz — role-colored dots — Added per-spawn-point role and color fields to SpawnPoint data. Dots now render with role-specific colors (melee=red, ranged=purple, tank=content, caster=cyan, healer=emerald). Added 3-letter role labels under dots and role legend below viz
- Implemented Difficulty Curve — line chart with danger zones — Already fully implemented with DANGER/SWEET SPOT/TRIVIAL shaded zones, multi-series line chart (Grunt/Caster/Brute), interactive hover tooltips showing per-series values and zone labels, invisible hit rects for smooth interaction

**Files:**
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/data.ts`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/archetypes/ArchetypeCard.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/SpawnFormationViz.tsx`
- `src/components/modules/core-engine/unique-tabs/EnemyBestiary/encounters/EncountersTab.tsx`

**Gotchas:**
- TIER_GLOW_COLORS map (minion=neutral, standard=info, elite=purple, boss=orange, raid-boss=warning) provides tier-level visual identity without conflicting with per-archetype identity colors
- STAT_AVERAGES precomputed at module scope from ARCHETYPES avoids per-render computation — different archetypes use different stat labels (HP/ATK/DEF/SPD/INT vs HP/Damage/Speed/Range) so averages are label-scoped
- SpawnPoint role/color fields are optional to maintain backward compatibility — SpawnFormationViz falls back to accent prop when color is undefined
- Role legend uses reduce to deduplicate by role before rendering — avoids showing duplicate melee entries from 2 melee spawn points

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 46: Character Blueprint Visual Polish (Part 2)

**Module:** `arpg-character`
**Description:** Visual upgrade for Character Blueprint components.
1. Comparison Matrix: Card-based columns per character. Stat bars with diff highlighting.
2. Hitbox Viewer: Layered wireframe SVG with zone coloring and glow. Hover to highlight zones.
3. Camera Profiles: Visual viewport mockups showing FOV cone, arm length, lag zone.
4. Movement States: State-machine SVG diagram with animated transition arrows.
5. Properties: Collapsible categories with colored headers.
6. Keyboard: Proper key sizing, accent glow on bound keys, red pulse on conflicts.
Follow design directive for all styling decisions.
**Duration:** 2.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Comparison Matrix — Card-based columns with NeonBar stat bars, crown diff highlighting, ScalableSelector modal for character selection, responsive table with animated rows
- Implemented Hitbox Wireframe Viewer — Layered SVG with zone coloring and glow, hover-to-highlight with synced toggles, framer-motion scan line (perf-optimized). Fixed raw opacity '04' → OPACITY_4 constant
- Implemented Camera Profiles — Radar chart with overlays, viewport mockups showing FOV cone/arm length/lag zone, toggle cards with minimum-1 guard pattern
- Implemented Movement States — State-machine SVG with ellipse nodes, animated transition arrows with parallel edge offset, hover highlighting with dim/glow, donut chart distribution, accel curve
- Implemented Properties Inspector — Collapsible categories with colored headers, search bypass, highlight animation on change, slider controls, modified/default indicators, summary bar
- Implemented Keyboard Visualization — Proper key sizing with 3D perspective, accent glow on bound keys, red pulse on conflicts, mouse widget with LMB/RMB/Look zones, comprehensive legend

**Files:**
- `src/components/modules/core-engine/unique-tabs/CharacterBlueprint/overview/HitboxWireframeViewer.tsx`

**Gotchas:**
- All 6 Character Blueprint visual features were already fully implemented with high quality — only HitboxWireframeViewer had a raw opacity string '04' that needed replacement with OPACITY_4 constant

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 47: Ability Spellbook Visual Polish (Part 2)

**Module:** `arpg-character`
**Description:** Visual upgrade for Ability Spellbook.
1. Ability Cards: Rich cards — icon circle with initial, cooldown badge, element dot, mini cost bar.
2. Effect Timeline: Gantt-style chart with durations, stacking, decay. Color by effect type.
3. Tag Tree: Interactive tree with expand/collapse, search highlight, SVG relationship curves.
4. Attribute Web: Interactive force-directed graph. Drag nodes. Hover shows relationship details.
5. Damage Calculator: Visual formula breakdown (Base × Mult + Flat → Armor → Final).
**Duration:** 5.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Ability Cards — Rich cards with icon circle+initial, cooldown badge, element dot, mini cost bar already fully implemented with ScalableSelector comparison
- Implemented Effect Timeline — Gantt-style swim-lane view with duration bars, color by effect type, time axis markers. Paginated effects catalog with type filtering
- Implemented Tag Tree — Interactive tree with expand/collapse, search filter with match count, force-open on search, SVG relationship curves in TagDepsSection. Fixed missing cursor-pointer on tree node buttons
- Implemented Attribute Web — Interactive graph with BFS hover highlighting, dimming of unrelated nodes. Added drag-to-reposition nodes with SVG coordinate transform, grab/grabbing cursors. Fixed per-render array allocation in coreAttrSet
- Implemented Damage Calculator — Visual formula breakdown with 5 sliders, 4 numbered formula steps showing Base×Mult→Armor→Crit→Final, GlowStat display for expected and crit damage

**Files:**
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/AttributeRelationshipWeb.tsx`
- `src/components/modules/core-engine/unique-tabs/AbilitySpellbook/tags/TagsSection.tsx`

**Gotchas:**
- SVG node drag uses DOMPoint.matrixTransform(ctm.inverse()) for screen-to-viewBox coordinate conversion — cleaner than deprecated createSVGPoint()
- During drag, suppress onMouseEnter on other nodes to prevent hover state flickering — check draggedNode before setting hoveredNode
- CORE_ATTRIBUTES.map().includes() per node per render creates O(n*m) allocations — precompute as Set in useMemo for O(1) lookups
- Split useMemo into defaultPositions + customPositions merge to support drag while keeping static circular layout as fallback

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 48: Combat Action Map Visual Polish (Part 1)

**Module:** `arpg-character`
**Description:** Visual upgrade for Combat Action Map.
1. Action Lanes: Swim-lane diagram with horizontal flows and inter-lane arrows.
2. Sequence Diagram: UML-style — vertical lifelines, horizontal arrows, activation boxes.
3. Feedback Tuner: Slider-based editor with grouped categories (Shake, Hitstop, Particles, Sound).
4. Sankey: Full Sankey flow viz with proportional widths.
5. DPS Chart: Grouped bar chart with weapon comparison. Hover for breakdown.
**Duration:** 14.3min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Action Lanes swim-lane diagram — Added SVG swim-lane diagram with 3 horizontal lane bands, feature nodes positioned in lanes, and bezier inter-lane arrows with labels from FLOW_ARROWS data
- Implemented Sequence Diagram activation boxes — Added UML-style activation boxes computed dynamically from event flow — thin rectangles on lifelines showing when each participant is active
- Implemented Feedback Tuner grouped categories — Expanded from 5 to 8 params across 4 categories (Shake/Hitstop/Particles/Sound) with category icons, visual separators, and updated presets
- Implemented Sankey proportional flow viz — Replaced overlay-based Sankey with proper proportional SVG — node heights proportional to pct, flow bands computed with cumulative offset stacking, pre-computed at module scope
- Implemented DPS grouped bar chart with hover — Added grouped bar chart showing all 44 weapons by category with tier-colored bars, category avg dashed lines, hover tooltip with damage/speed/crit breakdown

**Files:**
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/data.ts`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/FlowTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/polish/FeedbackTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/MetricsTab.tsx`

**Gotchas:**
- SankeyNode.color and SankeyLink.color are optional in the type definitions — always provide fallback when mapping to required-color interfaces
- Pre-computing Sankey flow band paths at module scope with IIFE avoids impure computation during React render while keeping static data efficient
- Activation box computation from UML sequence events: track active state per lane, close when lane sends to another, open when lane receives
- FeedbackParam category grouping with FEEDBACK_CATEGORIES const enables clean filter-based rendering without restructuring the flat array

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 49: Item Catalog Visual Polish (Part 1)

**Module:** `arpg-character`
**Description:** Visual upgrade for Item Catalog.
1. Item Cards: RPG-style — rarity border glow, slot icon, power badge, stat list with +/- coloring, set indicator.
2. Equipment Loadout: Paper-doll slot layout (silhouette with positions). Click opens ScalableSelector for slot type.
3. Affix Sunburst: Enhanced with tooltips, smooth transitions, tier-colored rings.
4. Scaling Chart: Multi-line with rarity-colored curves, hover values, confidence bands.
5. Economy Flow: Sankey showing sources → player → sinks.
**Duration:** 10.4min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Item Cards: RPG-style — Added slot icon emoji, power badge (PWR), +/- stat coloring (green for >=70%, red for <=30%), set indicator showing set membership. Existing rarity border glow, shimmer, tooltip were already high quality.
- Implemented Equipment Loadout: Paper-doll — Already fully implemented with SVG silhouette, positioned slot cards, click-to-equip via ScalableSelector, stat contribution summary. Fixed 2 hardcoded Tailwind colors (text-red-400, text-emerald-400) to chart-colors tokens.
- Implemented Affix Sunburst: Enhanced — Added hover state with stroke thickening, fill opacity change, glow drop-shadow, and positioned tooltip overlay replacing bare <title> elements. Smooth CSS transitions on fill and stroke-width.
- Implemented Scaling Chart: Multi-line with hover — Added mouse-tracking crosshair with dashed vertical line, level label, enlarged data point circles, and value tooltip boxes per curve on hover. Wrapped allLevels in useMemo to fix React hook dependency warning.
- Implemented Economy Flow: Sankey — New Sankey diagram with 9 nodes (4 sources, 1 player hub, 4 sinks), curved Bezier links with proportional thickness, hover highlighting with opacity change and tooltip. Placed prominently at top of Economy & Sourcing tab.

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/TradingCard.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/GearSections.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/AffixSunburst.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/ItemScalingChart.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/EconomySections.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/economy/EconomySourcingTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/mechanics/MechanicsScalingTab.tsx`

**Gotchas:**
- ITEM_SET_MAP pre-computed from ITEM_SETS enables O(1) set membership lookup for TradingCard set indicators — avoids per-card array search
- Sankey link paths use cubic Bezier with bidirectional thickness (srcH/dstH) for proportional flow visualization — path closes as a polygon not a stroke
- Sankey node offset tracking requires separate srcOffsets and dstOffsets maps to stack links vertically within each node's height
- allLevels derived from lines[0]?.points triggers React hook dep warnings in useCallback — wrap in useMemo with [lines] dep
- computePower formula includes affix count * 10 bonus to reflect that affixed items have more total power than raw stat sums suggest
- SVG hover tooltip positioning uses midAngle/midR from arc computation for AffixSunburst — reuses existing geometry calculations

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 50: Combat Action Map Visual Polish (Part 2)

**Module:** `arpg-character`
**Description:** Visual upgrade for Combat Action Map.
1. Action Lanes: Swim-lane diagram with horizontal flows and inter-lane arrows.
2. Sequence Diagram: UML-style — vertical lifelines, horizontal arrows, activation boxes.
3. Feedback Tuner: Slider-based editor with grouped categories (Shake, Hitstop, Particles, Sound).
4. Sankey: Full Sankey flow viz with proportional widths.
5. DPS Chart: Grouped bar chart with weapon comparison. Hover for breakdown.
**Duration:** 3.5min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Action Lanes: Swim-lane diagram — Already fully implemented with horizontal flows, inter-lane cubic bezier arrows, colored lane bands, and feature nodes with short name mapping
- Implemented Sequence Diagram: UML-style — Already fully implemented with vertical lifelines, horizontal arrows, self-message dots, activation boxes pre-computed at module scope via IIFE
- Implemented Feedback Tuner: Slider-based editor — Already fully implemented with 4 grouped categories (Shake/Hitstop/Particles/Sound), category icons, 3 presets, juice level meter, hitstop timing viz, and stagger pipeline
- Implemented Sankey: Proportional flow viz — Already fully implemented with proportional band widths, pre-computed flow paths at module scope, 3-column layout (Input→Result→Outcome), and node labels with percentages
- Implemented DPS Chart: Grouped bar chart — Already fully implemented with 7-category grouped bars, hover tooltip, category avg lines, weapon comparison selector (max 4), stat influence panel with STR/DEX sliders

**Files:**
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/LaneSection.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/flow/FlowTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/hits/HitsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/polish/FeedbackTab.tsx`
- `src/components/modules/core-engine/unique-tabs/CombatActionMap/metrics/StatInfluencePanel.tsx`

**Gotchas:**
- All 5 Combat Action Map visual features were already fully implemented with high quality — only needed cleanup of 6 redundant template literal wrappers around withOpacity() calls and removal of unused arrows prop from LaneSection interface

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 51: Item Catalog Visual Polish (Part 2)

**Module:** `arpg-character`
**Description:** Visual upgrade for Item Catalog.
1. Item Cards: RPG-style — rarity border glow, slot icon, power badge, stat list with +/- coloring, set indicator.
2. Equipment Loadout: Paper-doll slot layout (silhouette with positions). Click opens ScalableSelector for slot type.
3. Affix Sunburst: Enhanced with tooltips, smooth transitions, tier-colored rings.
4. Scaling Chart: Multi-line with rarity-colored curves, hover values, confidence bands.
5. Economy Flow: Sankey showing sources → player → sinks.
**Duration:** 5.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Item Cards RPG-style — Rarity border glow, slot icons, power badge, stat +/- coloring, set indicator all implemented. Fixed missing cursor-pointer on Add Item and Create buttons.
- Implemented Equipment Loadout Paper-doll — SVG silhouette with positioned slots, ScalableSelector integration, stat contributions. Extracted stat computation from IIFE to memoized StatContributions component.
- Implemented Affix Sunburst Enhanced — Tooltips, smooth transitions, tier-colored rings all implemented. Moved describeArc to module scope to avoid per-render recreation.
- Implemented Scaling Chart Multi-line — Rarity-colored curves, hover crosshair values, min/max confidence bands all fully implemented with clean SVG rendering.
- Implemented Economy Flow Sankey — Sources → Player → sinks Sankey with proper band rendering, hover tooltips, offset tracking. Well-implemented.

**Files:**
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/ItemDetailDrawer.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/AffixSlotPanels.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/AffixSunburst.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/GearSections.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/catalog/CatalogGearTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ItemCatalog/mechanics/MechanicsScalingTab.tsx`

**Gotchas:**
- All 5 Item Catalog visual features were already fully implemented with high quality — only needed cleanup fixes
- AffixSlotPanels had inconsistent opacity pattern using direct hex+opacity concat instead of withOpacity() helper
- MechanicsScalingTab had import statement placed between code blocks instead of at top with other imports
- describeArc pure function inside AffixSunburst component was recreated every render — module-scope eliminates allocation
- GearSections IIFE stat computation in JSX should be a separate memoized component for cleaner rendering

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 52: Loot Table Visual Polish (Part 2)

**Module:** `arpg-character`
**Description:** Visual upgrade for Loot Table Visualizer.
1. Weight Distribution: Donut chart with interactive segments and drill-down. Rarity-colored with glow.
2. Drop Treemap: Proper nested rectangles. Rarity-colored. Size = probability. Hover for details.
3. Monte Carlo: Histogram with distribution curve overlay. Mean/median/stddev vertical lines.
4. Pity Timer: Circular gauge with animated fill and threshold marker.
5. Co-occurrence: Heatmap with color scale legend. Cell hover shows frequency and conflict flag.
6. Economy Dashboard: KPI cards at top (gold/hr, items/hr, rarity dist), charts below.
**Duration:** 3.6min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Weight Distribution: Donut chart with interactive segments and drill-down — Already fully implemented with pre-computed segments, hover/select, animated drill-down, glow effects
- Implemented Drop Treemap: Proper nested rectangles with rarity coloring — Already fully implemented with treemap layout, glow hover, drill-down affix outcomes, pre-computed Map lookups
- Implemented Monte Carlo: Histogram with distribution curve overlay — Already fully implemented with histogram bins, curve overlay, mean/median/stddev lines, stats table, rarity picker
- Implemented Pity Timer: Circular gauge with animated fill and threshold marker — Already fully implemented with 270° arc gauge, animated motion.circle, 80% threshold marker, bad luck protection indicator, history bars
- Implemented Co-occurrence: Heatmap with color scale legend — Already fully implemented with HeatmapGrid component, color scale legend, conflict flag count
- Implemented Economy Dashboard: KPI cards with charts — Already fully implemented with 4 KPI cards, surplus/deficit bars, paginated impact table with rarity+source filters

**Files:**
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/affix/LootTableEditor.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/simulation/DropSimulator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/EnemyLootBinding.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/EVCalculator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/core/WorldItemPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/pity/DroughtCalculator.tsx`
- `src/components/modules/core-engine/unique-tabs/LootTableVisualizer/LootFilters.tsx`

**Gotchas:**
- LootTableEditor had 20+ direct hex concat patterns (${COLOR}${OPACITY}) — most of any file in this module, all converted to withOpacity() for consistency
- DropSimulator, EnemyLootBinding, EVCalculator, DroughtCalculator each had 2-5 direct hex concat patterns remaining from prior passes
- LootFilters and WorldItemPreview had redundant template literal wrappers around withOpacity() calls that returned strings — removed for cleaner code
- EnemyLootBinding copy button had mixed patterns: one withOpacity in template literal, one direct concat — unified to withOpacity()

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

### Phase 53: Cross-Module Style Audit (Part 1)

**Module:** `arpg-character`
**Description:** Verify visual consistency across all 7 modules.
1. Grep for hardcoded colors, inconsistent spacing, non-standard fonts.
2. Verify same card/header/stat style everywhere.
3. Check tab nav looks identical across modules.
4. Verify ScalableSelector renders consistently.
5. Check Feature Map card sizing across modules.
6. Run contrast check on all text/bg combinations (WCAG AA 4.5:1).
7. Fix all deviations found.
**Duration:** 18.8min

**PoF Checklist Items to Run:**


**What Gets Built:**
- Implemented Hardcoded color audit & fix — Eliminated all non-pseudo-class hardcoded Tailwind color classes across 30+ files. Converted 80+ violations to chart-colors tokens via inline styles.
- Implemented Card/header/stat style consistency — All modules use shared BlueprintPanel, SectionHeader, GlowStat, TabHeader, FeatureCard components consistently. FeatureCardGrid uses uniform minmax(160px,1fr) sizing.
- Implemented Tab nav consistency — All 12+ tabbed modules use the shared SubTabNavigation component with consistent accent-based active state, spring animation, and gap/padding.
- Implemented ScalableSelector consistency — Used across 9 files with consistent import path, SelectorItem interface, groupBy patterns, and renderItem callbacks.
- Implemented Feature Map card sizing — FeatureCard and FeatureCardGrid use proper chart-colors tokens with consistent auto-fill grid layout across all modules.
- Implemented WCAG contrast check — All active text/bg combos pass AA (4.5:1). STATUS_NEUTRAL on surface is 3.82:1 — used only for inactive/disabled state which is acceptable.

**Files:**
- `src/components/modules/core-engine/unique-tabs/_shared.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/versions/VersionHistory.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/versions/MigrationPathGraph.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/slots/SlotManagement.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/slots/IntegrityValidator.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/schema/SchemaTree.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/schema/SaveDiffViewer.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/schema/MemoryBanks.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/schema/FileSizeBreakdown.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/schema/BudgetAlerting.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/SerializationProfiler.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/DataRecoveryTool.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/AutoSaveConfig.tsx`
- `src/components/modules/core-engine/unique-tabs/SaveDataSchema/advanced/CloudSyncStatus.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/index.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/ui/UIBindingsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/systems/SystemsTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/systems/ArpgHudPreview.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/flow/ScreenNodeRow.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/flow/FlowNodesTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ScreenFlowMap/accessibility/AccessibilityTab.tsx`
- `src/components/modules/core-engine/unique-tabs/ZoneMap/map/MapTopologyGroup.tsx`
- `src/components/modules/core-engine/unique-tabs/CharacterFeelPlayground/canvas-draw.ts`
- `src/components/modules/core-engine/unique-tabs/AnimationStateGraph/combos-montages/ComboTimelinePanel.tsx`
- `src/components/modules/core-engine/unique-tabs/AttributePointOptimizer/OptimizerTab.tsx`
- `src/components/modules/core-engine/unique-tabs/DebugDashboard/index.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/DRCodeGenerator.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/XpTableGenerator.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/RestXpSystem.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/PrestigePreview.tsx`
- `src/components/modules/core-engine/unique-tabs/progression/AchievementBoard.tsx`
- `src/components/modules/core-engine/unique-tabs/ProgressionCurve/rewards/AchievementBoard.tsx`
- `src/components/modules/core-engine/unique-tabs/AffixCraftingWorkbench/CraftLogSection.tsx`

**Gotchas:**
- SaveDataSchema was the worst offender with 50+ hardcoded Tailwind color violations — cyan-300/400/500, amber-400/500, emerald-400 across 12 files
- hover:text-red-400 on delete/trash buttons (18 occurrences across 14 files) cannot be converted to inline styles — acceptable as a recognized pattern for destructive action hover
- bg-*-500/5 blur-3xl ambient glow decorations (8 occurrences in progression/) have hover variants that prevent inline conversion — acceptable at 5% opacity
- STATUS_NEUTRAL (#6b7280) contrast ratio is 3.82:1 against surface — passes AA large text but not normal text. Used only for inactive/disabled state which is acceptable
- All 12+ tabbed modules use shared SubTabNavigation with consistent spring animation, gap/padding, and accent-based active state — no structural divergence found
- focus:border-blue-500/50 (19 files) vs focus:ring-current/50 (4 files) is a minor inconsistency but serves different purposes (input borders vs button rings)

**Verification:** PASS typecheck, FAIL lint, PASS test, FAIL visual-check

---

## Accumulated Learnings

- Button elements default to cursor:default — always add cursor-pointer for clickable cards
- setAll and setMany should both check for no-op to avoid unnecessary re-renders and localStorage writes
- Tailwind scale transforms with hover/active give tactile feedback without needing framer-motion
- Existing implementation was high quality — only needed minor cleanup (redundant array copy, missing keyboard hint). Binary search + overscan virtual scroll pattern handles 100+ items well.
- Redundant data structures (navigableSet vs navigableIndexMap) add allocation cost for zero benefit — Map.has() serves both index lookup and membership testing. Building auxiliary index maps alongside existing offset computation loops adds O(1) lookups at zero extra pass cost.
- React 19 ESLint plugin flags setState-in-useEffect (react-hooks/set-state-in-effect) and ref access during render (react-hooks/refs) — use the state-based previous value comparison pattern instead
- MiniBtn (button elements in general) need explicit cursor-pointer class even when they have onClick handlers
- Home/End/Ctrl+A keyboard handlers on modal wrappers must check if event target is an INPUT — otherwise they steal cursor-movement and text-selection keys from the search field. Same pattern as the existing Space key guard.
- CharacterBlueprint metrics were already fully implemented with all 13 section IDs mapped — no gaps found
- DODGE_TRAJECTORIES contains SVG path data not numeric distances — hardcoded dodge distance is the correct approach
- Existing AnimationStateGraph metrics implementation was already high quality — all 10 metrics correctly derive data from data.ts, use consistent styling patterns, and compute constants at module scope to avoid re-renders. PlayrateMetric has a smart fallback that shows blend-in time range when all montages share the same fps.
- rgba(255,255,255,...) was the dominant hardcoded pattern (216 occurrences) — 8-digit hex values were already cleaned up in a prior pass
- New OPACITY_2/3/4/6/40 constants needed for exact mapping of common rgba sub-10% and 40% values
- rgba(0,0,0,...) dark shadows and specific accent colors (168,85,247 purple, 6,182,212 cyan) have no semantic token — correctly left as-is
- SVG stroke/fill attributes accept both rgba() and #RRGGBBAA formats — withOpacity() works for both
- Tailwind arbitrary values like fill-[rgba(...)] must use hex+alpha format since they can't call JS functions
- ABILITY_METADATA was producing a meaningless 'none' tag when damageType was 'None' — always filter sentinel values before lowercasing into tags
- LOOT_ENTRY_METADATA tags were too sparse (rarity only) — category and subcategory should always appear as tags for consistent filterability across domains
- ZONE_METADATA had area=z.name duplicating the name field — zones ARE areas, so area field is redundant; added z.group tag instead for topology grouping
- groupEntities must check for 'Ungrouped' key BEFORE consulting the order index, otherwise 'Ungrouped' gets Infinity and sorts alphabetically among other overflow groups instead of always being last
- All 9 Combat Action Map metrics were already fully implemented with correct data derivation, module-scope computation, and consistent styling — no modifications needed
- All 9 Enemy Bestiary feature metrics were already implemented and high quality — only ModifiersMetric had a bug counting modifiers-with-excludes instead of total exclusion entries
- All 9 Enemy Bestiary feature metrics were already fully implemented with correct data derivation, module-scope computation, chart-colors usage, and consistent styling — no modifications needed in Part 2
- StatsMetric BAR_W formula Math.max(W/n - 1, 2) overflows when n*3 > W — use (W - n + 1)/n to ensure bars fit within SVG viewBox regardless of item count
- Tailwind arbitrary fill-[#RRGGBBAA] on SVG elements converts cleanly to inline fill={withOpacity()} attr — preserves className for font/size utilities while using design tokens for color
- Tailwind gradient hex classes (from-[#hex]/via-[#hex]) must convert to inline style={{ background: 'linear-gradient(...)' }} since they can't call JS functions — remove the gradient utility classes entirely
- 0 0 4px and 0 0 8px were the two most common inline glow radii (~25 combined occurrences) — 0 0 6px (20+ occurrences) has no exact GLOW constant and was correctly left untouched to preserve visual appearance
- All 7 Item Catalog metrics were already fully implemented with correct data derivation, module-scope computation, chart-colors usage, overflow-safe bar chart, and consistent styling — no modifications needed in Part 2
- Existing GAS Feature Metrics implementation was already complete and high quality — all 9 section IDs mapped with correct formats, proper chart-colors usage, and optimized single-pass useMemo computation
- All 11 Loot Table Visualizer feature metrics were already fully implemented with correct data derivation, module-scope computation, pre-computed IIFEs for mutable accumulation, and consistent styling — no modifications needed
- ScalableSelector SelectorItem requires index signature — use intersection type (SelectorItem & { ...fields }) rather than interface extends to satisfy the constraint
- STATE_NODES and AnimStateName are separate concerns — STATE_NODES defines the full state graph for browsing, AnimStateName covers only combat-critical states for responsiveness analysis
- Virtual scroll with absolute positioning + flat row list (headers + items) is simpler than nested virtual lists — single scrollTop drives all row positions
- Pre-computing category aggregates (MONTAGE_CATEGORY_COUNTS, MONTAGE_CATEGORY_MEMORY) at module scope avoids redundant computation in React renders
- ComboChainPanel nodes had hardcoded x positions (500, 600, 700) that overflowed the 400px SVG viewBox — dynamic layout with sequential positioning and computed viewBox width is needed when node count can grow
- SVG marker IDs should be shared (single <defs>) rather than duplicated per-edge to avoid ID collision and redundant DOM nodes
- React 19 flags setScrubberPlaying inside setScrubberFrame updater — use a ref to signal the stop condition and call the second setState outside the updater
- BudgetTab categoryColor had Emote and HitReact sharing STATUS_WARNING — every category needs a unique color for the stacked memory bar to be readable
- SpellbookAbility needs [key: string]: unknown index signature to satisfy SelectorItem constraint for ScalableSelector generic
- ScalableSelector groupBy works with single field — for category→element hierarchy, group by category and show element as badge in renderItem
- EXPANDED_EFFECTS grouped rendering uses Map<EffectDurationType, entries[]> for clean swim-lane display within pages
- Tag tree search uses recursive filterTree() that preserves parent nodes when children match — forceOpen prop bypasses local collapse state
- data.ts re-exports data-metrics.ts via `export * from './data-metrics'` — avoid importing the same symbol from both files
- HeatmapGrid lowColor/highColor props use raw hex which triggers lint warnings — these pre-existed and have no chart-colors semantic token
- WEAPONS array with 44 entries and COMBO_SEQUENCES with 32 entries provide sufficient scale for 100+ item testing when combined with ScalableSelector grouping
- SelectableCharacter with [key: string]: unknown index signature satisfies SelectorItem constraint while preserving typed property access — use this pattern for ScalableSelector integration
- CHAR_DEFS builder pattern generates both CHARACTER_METADATA and COMPARISON_CHARACTERS from a single source — prevents sync drift between metadata and stat arrays
- Original COMPARISON_CHARACTERS had broken 0-100 scale values (Jedi Guardian HP=85 vs maxVal=1500) — always verify stat values against maxVal ranges when expanding data
- TIER_LEVELS lookup provides default level/levelMax ranges per tier — avoids repeating level ranges in every character definition
- CameraProfileComparison multi-select uses minimum-1 guard pattern (same as character toggle) to prevent empty state
- COMBO_ABILITIES had 7 hardcoded hex colors (#60a5fa, #a78bfa, #22c55e, #f59e0b, #06b6d4, #e879f9, #7f1d1d) — mapped to STATUS_INFO, ACCENT_VIOLET, ACCENT_GREEN, STATUS_WARNING, ACCENT_CYAN, ACCENT_PINK, HEATMAP_STEP_1 respectively
- Recursive utility functions like countTags should never be called multiple times in JSX — memoize the result once and reuse
- When EXPANDED_EFFECTS is static, per-type counts can be precomputed in a useMemo with empty deps to avoid refiltering on each render
- computeDps returning full breakdown object (effectiveDamage, effectiveSpeed, effectiveCrit, dps) eliminates need for duplicate inline computation — single source of truth for both detail display and comparison list
- CON stat slider was wired to state but never consumed by DPS formula — dead state variables add React reconciliation overhead for zero user value
- 44-item DPS comparison list needs max-h + overflow-y-auto to prevent unbounded panel growth — scroll containers are essential when data scale exceeds ~15 visible rows
- Builder pattern with compact tuple definitions [name, subtype, rarity, desc, effect?] + stat generators per type keeps 116 items under 250 lines
- import type from circular module works cleanly — TypeScript erases type imports at compile time so no runtime circular dependency
- ringColor is not a valid CSS property — use boxShadow: '0 0 0 1px color' for ring-like selection effect in inline styles
- ScalableSelector requires SelectorItem with [key: string]: unknown — use intersection type (ItemData & { [key: string]: unknown }) for casting
- SLOT_SUBTYPES mapping enables both slot-filtered item picking (loadout) and dynamic subtype filter dropdown (catalog) from a single source of truth
- LootEditorEntryExpanded with [key: string]: unknown index signature causes Omit<> to lose typed property access — use a separate interface for raw entries to preserve type narrowing
- CSS ringColor is not a valid React style property — use outline: '1px solid color' instead for selection indicators
- RARITY_COLOR_MAP defined after EXPANDED_ENTRIES causes forward reference issues — define a local RARITY_TO_COLOR map before the entries array
- ScalableSelector requires SelectorItem with id:string — AffixDef and enemy binding wrapper types both need explicit id fields
- Character Blueprint flow redesign was already fully implemented with high quality across all 4 features — no modifications needed
- RADAR_DATA only covered 4 of 94 archetypes — stat-based radar derivation with role-specific range/aggression defaults fills the gap for expanded enemies
- Initializing radar overlays for all 94 archetypes renders an unreadable mess — default to player-only and let users toggle specific enemies
- RadarComparison overlay list needs search + scroll + show-more pattern to scale beyond ~10 items
- TacticsMap player marker used hardcoded #3b82f6 which is MODULE_COLORS.core
- ArchetypeBuilder preview had rgba(168,85,247,...) inline — maps to ACCENT_PURPLE_BOLD with opacity helpers
- StatsMetric subsampling pattern: when item count exceeds SVG pixel width, subsample sorted array to max W/2 entries using evenly-spaced index picks to preserve distribution shape
- optgroup in select dropdowns significantly improves item browsability at 134+ items — group by item type (Weapon/Armor/Accessory/Consumable/Quest/Material)
- ITEM_METADATA tags should include rarity and type as lowercase tags alongside subtype for consistent cross-domain filterability
- MonteCarloSim.sort() mutated state array directly — always use [...arr].sort() or .toSorted() when working with state-derived arrays
- DropSimulator groupBy used 'archetypeId as never' type cast — derive meaningful tier (Minion/Standard/Elite/Boss) from dropChance ranges for proper ScalableSelector grouping
- WorldItemPreview had useMemo-wrapped Map that duplicated module-scope constant — static data should be computed at module scope, not inside components
- TREEMAP_DATA.find() called 3 times for same treemapDrill value in JSX — extract to local variable via IIFE inside JSX to avoid redundant array traversal
- WavesMetric was importing WAVE_TIMELINE (3 legacy waves) instead of EXPANDED_WAVES (24 waves) — metric showed stale counts that didn't match the actual EncountersTab data
- SpawnFormationViz accepted no formation prop despite EncountersTab maintaining formation state — dead state variables create user confusion when controls appear to do nothing
- Layout computation for Line/Ambush formations uses simple spacing arithmetic — no need for complex physics or force-directed layout for 6 spawn points
- All 3 Animation State Graph flow features (tab order, subtitles, breadcrumb) were already fully implemented with high quality — only needed minor cleanup
- StateGroupBrowser cancel badge and StateDurationPanel outlier label used hardcoded Tailwind amber classes — replaced with STATUS_WARNING chart-colors tokens via inline style
- ComboTimelinePanel tab selector buttons and RetargetingTab pipeline step buttons were missing cursor-pointer class
- RetargetingTab had unused NeonBar import and ResponsivenessAnalyzer had unused warnings variable
- ringColor is not a valid CSS property — both AbilitiesSection ScalableSelector renderItem and LoadoutSection slot cards had ringColor in inline styles doing nothing; replaced with outline: '1px solid color'
- Item Catalog tab order was already correct — the redesign only needed narrative metadata and UI chrome additions
- ItemCatalogSubtabDef interface export enables other consumers to read narrative/subtitle metadata without coupling to internal state
- LaneSection quality score badge used hardcoded Tailwind emerald-400 classes — replaced with ACCENT_EMERALD chart-colors tokens via inline style
- FeedbackTab preset buttons and LaneSection expand buttons were missing cursor-pointer class
- StatInfluencePanel weapon selector had flat 44-item list without optgroup — added category grouping to match HitsTab pattern
- Tailwind accent-purple-500 and accent-blue-500 classes for form controls should use inline style={{ accentColor: TOKEN }} to satisfy chart-colors lint rules
- BTFlowchart had dead props (nodes/edges) in interface that were never consumed — component uses BT_TREE from data module directly
- SpawnFormationViz cx variable was declared but only cy was used in Line formation layout — dead variable elimination
- Single-item grid wrappers with xl:grid-cols-2 create empty columns — remove grid wrapper when only one child
- focus:ring-current/50 is already used in _shared.tsx — valid pattern for replacing hardcoded focus:ring-orange-500/50 across all input elements
- accent-cyan-500/accent-blue-500/accent-orange-500 on range inputs are native browser accent utilities, not hex color violations — common pattern across project
- LootFilters duplicate enemyMap is necessary to avoid circular dependency with index.tsx which imports LootFilters
- Group-level SVG graph (6 nodes) is more readable than full 31-state graph — individual states remain browsable in the collapsible list below
- rectEdgePoint() using min(hw/abs(dx), hh/abs(dy)) scaling cleanly computes arrow endpoints on rounded rect edges
- SVG animate inside conditional {hl && <animate>} triggers only on hover — no continuous animation cost when idle
- repeating-linear-gradient(45deg) at 2px/4px intervals gives clean hatching without SVG pattern defs
- BLEND_TRIANGLE precomputed at module scope from sorted distance — avoids useMemo for static data
- Character Blueprint visual polish was already fully implemented with high quality across all 6 features — only needed minor cleanup (1 missing cursor-pointer, 5 redundant template literals)
- AbilitiesSection had 8 hardcoded Tailwind purple/green/amber classes across 3 glow background divs, 2 icon colors, 1 background color, and 2 status text spans — all mapped to ACCENT_PURPLE_BOLD, ACCENT_GREEN, and STATUS_WARNING
- TagAuditSection blocking tags used hardcoded bg-red-500/10 text-red-400 border-red-500/20 — replaced with ACCENT_RED tokens via inline style
- Ability pills enhanced from flat text buttons to rich cards: icon circle with first-letter initial, element color dot, cooldown badge, and mini cost bar — uses grid layout instead of flex-wrap for consistent card sizing
- TIER_GLOW_COLORS map (minion=neutral, standard=info, elite=purple, boss=orange, raid-boss=warning) provides tier-level visual identity without conflicting with per-archetype identity colors
- STAT_AVERAGES precomputed at module scope from ARCHETYPES avoids per-render computation — different archetypes use different stat labels (HP/ATK/DEF/SPD/INT vs HP/Damage/Speed/Range) so averages are label-scoped
- SpawnPoint role/color fields are optional to maintain backward compatibility — SpawnFormationViz falls back to accent prop when color is undefined
- Role legend uses reduce to deduplicate by role before rendering — avoids showing duplicate melee entries from 2 melee spawn points
- All 6 Character Blueprint visual features were already fully implemented with high quality — only HitboxWireframeViewer had a raw opacity string '04' that needed replacement with OPACITY_4 constant
- SVG node drag uses DOMPoint.matrixTransform(ctm.inverse()) for screen-to-viewBox coordinate conversion — cleaner than deprecated createSVGPoint()
- During drag, suppress onMouseEnter on other nodes to prevent hover state flickering — check draggedNode before setting hoveredNode
- CORE_ATTRIBUTES.map().includes() per node per render creates O(n*m) allocations — precompute as Set in useMemo for O(1) lookups
- Split useMemo into defaultPositions + customPositions merge to support drag while keeping static circular layout as fallback
- SankeyNode.color and SankeyLink.color are optional in the type definitions — always provide fallback when mapping to required-color interfaces
- Pre-computing Sankey flow band paths at module scope with IIFE avoids impure computation during React render while keeping static data efficient
- Activation box computation from UML sequence events: track active state per lane, close when lane sends to another, open when lane receives
- FeedbackParam category grouping with FEEDBACK_CATEGORIES const enables clean filter-based rendering without restructuring the flat array
- ITEM_SET_MAP pre-computed from ITEM_SETS enables O(1) set membership lookup for TradingCard set indicators — avoids per-card array search
- Sankey link paths use cubic Bezier with bidirectional thickness (srcH/dstH) for proportional flow visualization — path closes as a polygon not a stroke
- Sankey node offset tracking requires separate srcOffsets and dstOffsets maps to stack links vertically within each node's height
- allLevels derived from lines[0]?.points triggers React hook dep warnings in useCallback — wrap in useMemo with [lines] dep
- computePower formula includes affix count * 10 bonus to reflect that affixed items have more total power than raw stat sums suggest
- SVG hover tooltip positioning uses midAngle/midR from arc computation for AffixSunburst — reuses existing geometry calculations
- All 5 Combat Action Map visual features were already fully implemented with high quality — only needed cleanup of 6 redundant template literal wrappers around withOpacity() calls and removal of unused arrows prop from LaneSection interface
- All 5 Item Catalog visual features were already fully implemented with high quality — only needed cleanup fixes
- AffixSlotPanels had inconsistent opacity pattern using direct hex+opacity concat instead of withOpacity() helper
- MechanicsScalingTab had import statement placed between code blocks instead of at top with other imports
- describeArc pure function inside AffixSunburst component was recreated every render — module-scope eliminates allocation
- GearSections IIFE stat computation in JSX should be a separate memoized component for cleaner rendering
- LootTableEditor had 20+ direct hex concat patterns (${COLOR}${OPACITY}) — most of any file in this module, all converted to withOpacity() for consistency
- DropSimulator, EnemyLootBinding, EVCalculator, DroughtCalculator each had 2-5 direct hex concat patterns remaining from prior passes
- LootFilters and WorldItemPreview had redundant template literal wrappers around withOpacity() calls that returned strings — removed for cleaner code
- EnemyLootBinding copy button had mixed patterns: one withOpacity in template literal, one direct concat — unified to withOpacity()
- SaveDataSchema was the worst offender with 50+ hardcoded Tailwind color violations — cyan-300/400/500, amber-400/500, emerald-400 across 12 files
- hover:text-red-400 on delete/trash buttons (18 occurrences across 14 files) cannot be converted to inline styles — acceptable as a recognized pattern for destructive action hover
- bg-*-500/5 blur-3xl ambient glow decorations (8 occurrences in progression/) have hover variants that prevent inline conversion — acceptable at 5% opacity
- STATUS_NEUTRAL (#6b7280) contrast ratio is 3.82:1 against surface — passes AA large text but not normal text. Used only for inactive/disabled state which is acceptable
- All 12+ tabbed modules use shared SubTabNavigation with consistent spring animation, gap/padding, and accent-based active state — no structural divergence found
- focus:border-blue-500/50 (19 files) vs focus:ring-current/50 (4 files) is a minor inconsistency but serves different purposes (input borders vs button rings)
