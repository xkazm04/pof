# Fleet Shared Memory

One line per entry, append-only, newest last. This file is the cross-session
memory for parallel Claude sessions working in this repo: **key decisions,
deliveries, and reusable conventions only** — never logs, never file dumps.
Cap: keep the file under ~200 lines; when it grows past that, the oldest
DELIVERED lines are the ones to prune (decisions and conventions outlive them).

Format (one line, no wrapping):

```
- [YYYY-MM-DD] [area] KIND: one sentence
```

KIND is one of:
- `CONVENTION` — a reusable pattern or primitive future sessions should follow
- `DECISION` — a choice with a reason that others must not silently contradict
- `DELIVERED` — a completed piece of work worth knowing about

## Entries

- [2026-07-24] [shared] CONVENTION: use the shared Tooltip primitive (src/components/ui / shared) instead of title= attributes for hover help.
- [2026-07-24] [a11y] CONVENTION: loading/error/empty states use live regions (aria-live=polite) and honest copy — no silent spinners, no fake-empty on fetch failure.
- [2026-07-24] [a11y] CONVENTION: interactive lists/filmstrips get listbox/option roles with keyboard navigation; visible focus rings are required.
- [2026-07-24] [level-design] CONVENTION: SVG canvases can't use the box-shadow focus-ring utilities — draw an explicit dashed rect/circle gated on `:focus-visible` and give nodes roving tabindex + listbox/option roles.
- [2026-07-24] [ui-hud] DECISION: UIHudView now mounts the five previously-unreachable panels as two composite tabs (HUD Theme, Damage Numbers) — do not re-orphan them.
- [2026-07-24] [visual-gen] DECISION: qwen-image-* is generation-only (no VQA) — image recognition stays on the Qwen-VL seam anim-critique/qwen.ts; never route recognition through qwen-image.
- [2026-07-24] [shared] CONVENTION: SQLite `datetime('now')` columns (updated_at etc.) reach the client as naive-UTC "YYYY-MM-DD HH:MM:SS" — normalize to UTC before `new Date()` when comparing them with JS ISO stamps, or the diff skews by the local offset.
- [2026-07-24] [charts] CONVENTION: zoom an SVG by narrowing its viewBox, never `transform: scale()` — a scaled element keeps its layout box, so it crops inside an overflow-hidden container instead of magnifying.
- [2026-07-24] [a11y] CONVENTION: never wrap a row containing DecoratedCrashText/CrashTerm in a <button> (it renders tooltip buttons) — put the disclosure semantics on a dedicated chevron button instead.
- [2026-07-27] [catalog-pipeline] CONVENTION: gallery view.field IS the selection field (= accept field = genCandidates payload key); lab grades via labCheckerContext ONE context (banner=persisted); wiringContract is graded (wiringContractSound) — spec linter enforces all of it.
- [2026-07-27] [ue-ops] DECISION: never taskkill /IM — capture.ts now PID-scoped; 3 more machine-wide kill sites remain (ue-experiment/runner.ts:326, packaging/smoke-test.ts:86, e2e/helpers/ue-verification.ts:94) — fix when touching those contexts.
- [2026-07-27] [animation] DECISION: the generated-video→MetaHuman-Animator path has new evidence (Gemini-generated clip solved in the wild) — re-measure via gateFootage on one current-gen clip before assuming the 2026-07-13 fused-feet ceiling still holds.
- [2026-07-28] [ue-python] CONVENTION: reflection proves a UE class EXISTS, not that it is CURRENT — grep the engine header for meta=(Deprecated=...) before building on a name a probe resolved.
- [2026-07-28] [ue-headless] DECISION: a physics settle CANNOT run in -run=pythonscript (no physics scene; editor_play_simulate fatally crashes it) — settle belongs on the -game scenario path; bake half stays headless.
- [2026-07-28] [visual-gen] DECISION: Tripo texture_model cannot texture external meshes (task-id input only) — texturing Hunyuan/mesh-finish output needs a different engine; backlog delta re-scoped.
- [2026-07-29] [catalog-e2e] DECISION: the Playwright walker is 27/32 RED and predates round 9 — `pipeline-e2e-coverage.test.ts` only checks registration+seeding, never walk success, so `validate` stayed green while it rotted; artifacts grade pass locally then persist as null (suspect POST /api/pipeline-artifacts re-grade). Do not treat a red walker as your regression.
- [2026-07-29] [catalog-e2e] DECISION (CORRECTS my earlier line same day): the walker's 27/32 red is NOT null-persistence. All 27 preserved failures are `"fail" is not config-complete` / persisted `pass` — the judge bridge downgrades pass->fail ON SCREEN only (`useStepAcceptance`), while `labGrade` + `POST /api/pipeline-artifacts` deliberately persist the PURE checker verdict (`route.ts:30-34`); the walker asserts on-screen == persisted, which is structurally impossible whenever a rubric-3 judge fail exists. Compounded by e2e having NO DB isolation (uses the dev's real ~/.pof/pof.db, which holds 305 rubric-3 fails).
- [2026-07-29] [catalog-e2e] CONVENTION: `e2e/walk-status.json` now records which pipelines walked green and `pipeline-e2e-coverage.test.ts` FAILS `npm run validate` if a registered non-skipped pipeline has no green walk on record — so adding a pipeline now requires running the walker once (`PLAYWRIGHT_PORT=<free> npx playwright test e2e/catalog-pipeline-walker.spec.ts`, ~5 min, hermetic per-run DB, server reuse OFF by default). Trust `test-results/.last-run.json`, not the reporter tail.
- [2026-07-29] [ui-honesty] CONVENTION: when a surface cannot prove something, state the COUNT and name the weakest case — never a neutral default (an unqualified 'All done.', a $0.00 cost, a blank license, a cited swatch, a silent re-grade all read as claims nobody made).
- [2026-07-29] [build-env] CONVENTION: `next.config.ts` now pins `turbopack.root`/`outputFileTracingRoot` to the project dir — the shared `kiro/package-lock.json` could make Next pick the PARENT as workspace root, silently voiding `serverExternalPackages` and bundling better-sqlite3 for the browser (symptom: Playwright webServer times out with a bare "module-not-found" and no named module). Also: Next refuses a 2nd dev server in the same dir, so the hermetic walker CANNOT run while any PoF dev server is up.
- [2026-07-30] [status] DECISION: /status paints ONE production-readiness ladder R0-R5 (src/lib/status/readiness.ts) — acceptance tier L0-L4 is now evidence-class metadata only, not a second rating; readinessOf is a pure projection over deriveCell so grading is unmoved.
- [2026-07-30] [judge] DECISION: rubric v4 = corrected harness INPUT (strip produceDirection; project nested siblings). On IDENTICAL content v4 ≈ v3 (n=31, mean −1.5) — it does NOT systematically move scores. The A/B that predicted +16.9 was CONFOUNDED: it omitted canonContext, which judge-run always passes. Bigger trap: 59/90 swept cells had their artifact rewritten AFTER its v3 verdict, so any "v3→v4 delta" over them compares different content — ALWAYS compare pipeline_artifacts.updated_at vs judge_verdict_history.judged_at before believing a delta. Re-judging contaminated cells REQUIRES --rejudge. The CALIBRATION set is unwired and media-only — never cite it as rubric-safety evidence.
- [2026-07-30] [craft-loop] CONVENTION: craft gauges are display-only by test (craftDisplayOnly.test.ts) — never import craft modules from acceptance/statusModel/readiness; lens changes bump lensVersion in BOTH frontmatter and lens-versions.ts.
- [2026-07-31] [items] DECISION: unified the items catalog on ONE price law (vendorBase 8g x ilvl x {Normal 1.0/Magic 1.35/Rare 2.2/Unique named-pool anchor}, sell=30%, goldPerPower derived from the rung) — the rival baseValue x60 ladder and the 50% buyback are retired.
- [2026-07-31] [pipelines] DELIVERED: produce() identity parameterized across all 30 specs (a019d8d6) + 342 test-headless-mcp clone rows re-produced — asset-ownership collisions dead; re-produce now writes per-entity identity.
- [2026-07-31] [green-loop] DELIVERED: 31-catalog Stage-A campaign against craft-audit worklists — 744/815 rows re-authored (636 pass/164 def/1 honest fail); 739 craft gauges now stale pending re-gauge; drained-L3 rows are READ-ONLY to content POSTs (any POST resets the verdict — re-drain queue: items x4, ambient x1).
- [2026-08-03] [green-loop] DELIVERED: honest v4 re-baseline of every judgeable text cell (288 cells, avg 75.2, 3 at R4, 56 at 85-89) — the v3 "avg 81 / 44 green" map is superseded.
- [2026-08-03] [catalog-canon] DECISION NEEDED: 26 seeded entities carry Star Wars IP (sith-lord, darth-malgrave, item-saber-*, recipe-custom-saber) — canon-aware judge scores them as low as 28; sanction the duel content in canon or re-fiction it.
- [2026-08-04] [green-loop] DELIVERED: achievements::achievement-first-blood 85.8→87.6 avg, Anti-Cheat/Validation to R4 (90) — and single-draw baselines proven unreliable for target-picking (one cell: 89 single vs 82 median-3), so build harvest lists from medians.
- [2026-08-04] [green-loop] DELIVERED: progression-curves::curve-hero-level 84.8→87.1, Caps & Catch-up to R4 (90); found+fixed the endgame gap-factor hole that made L90-100 price 945 min/level against the published 94.5 (three cells had each caught one edge of it).
- [2026-08-04] [catalog] DELIVERED: /api/pipeline-artifacts/bind-icons — idempotent disk-truth pass binding the generated/icons library into gallery artifacts (58 verdicts deferred→pass, 16 cells off R4 WAITING).
- [2026-08-04] [status] CONVENTION: the R-map REGRESSES whenever a campaign re-produces artifacts — re-measure first, then re-run the idempotent passes (drain · bind-icons · verify-static · verify-packaging) before authoring anything new.
- [2026-08-04] [catalog] DELIVERED: /api/pipeline-artifacts/drain-python — runs a step's UE python module through the editor bridge and re-grades with its own checker (built + 16 tests; needs the editor RUNNING, the inverse of /drain).
- [2026-08-04] [judge] DECISION: rubric routes by catalogId::step, not catalog alone — ui-sheet/ui-diagram added so a glyph SET and an annotated wireframe stop being judged as 'exactly one glyph' (PROMPT_VERSION q1→q2).
- [2026-08-04] [craft-loop] DELIVERED: items catalog re-gauge (78 verdicts: A3x59/A1x14/A2x4/A4x1) — only 13/143 entities hold any artifact (rest are 0-step/R0, all item-item-1xx..6xx + item-kotor-* unproduced).
- [2026-08-04] [craft-loop] DECISION: item-lightsaber Economy capped A1 — un-retired uniqueAnchor block still reads 900g while baseValue reads 1200g in the same step, a live self-contradiction the 07-31 price-law campaign missed.
- [2026-08-04] [craft-loop] DELIVERED: loot-tables catalog re-gauge (23 verdicts: A4x7/A3x2/A2x10/A1x4) — only 4/22 entities carry artifacts; lt-MeleeGrunt is the strongest single-entity showing yet (8xA4, closed faucet+sink ledger, real Test Gate evidence).
- [2026-08-04] [craft-loop] DECISION: loot-tables Icon 2D Art A1 on both populated entities — the served image (a bright coin/gem cluster) is shared per-(catalog,step) not per-entity, tonally wrong for dark-fantasy canon, and has no styleDNA profile recorded.
- [2026-08-04] [craft-loop] DELIVERED: bestiary catalog re-gauge (19 verdicts: A3x11/A2x5/A1x2, process A2) — only 4/94 entities carry any artifact (melee-grunt full 12/12, 3 duel archetypes 2/12 each); rest are unproduced.
- [2026-08-04] [craft-loop] DECISION: bestiary Concept 2D Art A1 — the sole generated image (catalog+step-keyed filename, not entity) shows a fleshy bearded barbarian contradicting the entity's own stone-construct lore, plus a visible watermark; 3D & Rig A1 — zero real meshes across 36 candidates, swatch-only.
- [2026-08-04] [craft-loop/achievements] DELIVERED: re-gauged achievements catalog (17 verdicts) — 4/5 named contradictions fixed (ms/s, audit-events, icon-size), `_provenance.engine:"unknown"` still stands; new findings: Test Gate self-contradicts its own evidence field, arena-victor/trophy-taker each only 2/12 steps (first-blood's exemplar bar never propagated).
- [2026-08-04] [craft-loop/achievements] DECISION: Icon 2D Art's swatch-only gap is now partly closed (real image bound+shipped) but stays A1 — the delivered art (dagger) contradicts its own recorded style-DNA brief (droplet), a content defect independent of the gallery being populated.
- [2026-08-07] [green-loop/hud-elements] DELIVERED: hud-health-bar Concept Brief lift (was 85) — owner-refs replace restated literals, real hysteresis rule, ghost/chip recent-damage layer authored (State Logic · recentDamage) + sweep of Data Binding/Animation/Accessibility; all 5 POSTs re-graded pass, Test Gate L3 untouched.
- [2026-08-07] [green-loop] CONVENTION: when a new HUD visual needs a colour, reuse an existing measured Accessibility token at a stated alpha and record the composited contrast math in Accessibility — never mint an unmeasured token.
- [2026-08-07] [green-loop/factions] DELIVERED: faction-ashen-order Concept Brief Stage-A fixes posted (severance/redemption disambiguated to the unsevered ten-kill floor, 400x25=10000/20-cycle math closes, HUD-binding deferral to Standing UI stated, L2 checks named, meta-voice stripped); probe 88 vs 85 baseline, Test Gate drain untouched.
- [2026-08-07] [green-loop/bestiary] DELIVERED: Stage-A fix on bestiary-melee-grunt Concept & Role — wiringContract added, Ground Slam only-clause reconciled with Abilities (220-320cm OR 6s timer), no-chase claim anchored to ARPGCharacterBase WalkSpeed 600; probe shows craft gaps closed, official median pending.
- [2026-08-07] [green-loop/input-schemes] DELIVERED: input-gamepad Concept Brief defects fixed at source (held-row hold-to-toggle dodge resolved with a real Settings latch; action/binding ledger made explicit) with coherent sweeps into Accessibility, Action Mapping, Rebinding UI, Localization; all POSTs re-graded pass, drained Test Gate untouched.
- [2026-08-07] [green-loop] CONVENTION: when a judge flags a hold/held accessibility dodge, answer with a mechanism (settings latch mirroring the sprint pattern) threaded through ALL owning siblings (settings surface + save scalars + loc key + counts), never with "may be left unbound".
- [2026-08-07] [green-loop/cutscenes] DELIVERED: cutscene-prologue Concept Brief re-anchored (Thornwall = Ashen Order forward garrison on faction-ashen-order's Ember Frontier; 'three days after the Sundering' removed — it contradicted the faction's seasons-deep post-Sundering timeline; tc=58 marker vs 62.0 Facial lock stated as edge-fire convention; meta-commentary stripped; skip-exit contract authored in Skip/Replay Rules).
- [2026-08-07] [green-loop/cutscenes] DECISION: VaelEyeContact\@58.0 naming wart is gate-locked — drained L3 Test Gate asserts the literal name+tc, so the rename (VaelEyeContact->VaelApproach across Beat Sheet/Skip/Concept/Test Gate + UE VSCutsceneTimingTest) is queued for re-drain, not patched now.
- [2026-08-07] [combat-map] DELIVERED: green-loop Stage-A arena-ravaged-courtyard — resolved mod-proximity-shield to the bestiary range-gate rule, re-derived threat 99.4→95.1 (band 88.1–103.8, Duststone documented outlier) across 9 steps; Test Gate L3 untouched, queued for re-drain.
- [2026-08-07] [green-loop/progression-curves] DELIVERED: curve-hero-level Concept Brief re-authored for the v4 84-fail — rarity-weighted 135-equivalents budget, composition-derived 3.0/min + 60.4-84.5h sensitivity band, levelGapFactor/single-player closure, two-stream ramp ladder; zero shared numbers moved, Caps&Catch-up(90) and drained Test Gate untouched.
- [2026-08-07] [green-loop/spellbook] DELIVERED: Stage-A coherence pass on off-arc-fp+off-fire-01 Concept Briefs (priced control ledger w/ bestiary-grunt 22.76 DPS breakeven, per-rank roll band, de-tautologized fire balance claim, meta strips) across 10 artifacts; drained Test Gates untouched.
- [2026-08-07] [green-loop/spellbook] DECISION: Concept Brief owns throughput for off-arc-fp — Test Gate's stale controlEffectiveDPS 17.7 is superseded by the controlLedger; its mirror needs refresh at next L3 re-drain (queued, do NOT content-POST the gate row).
- [2026-08-07] [quests] DELIVERED: green-loop Stage-A quest-ember-pact — FORFEIT failure terminal (death attribution replaces the vael damage-forcefield), abandon now first-class via State.Dialog.EmberPactWalked memory tag + vael_offer_return re-offer, xp re-anchored to XP Sources tier-2 row (0.30 x xpToNext(turn-in), 44 XP at gate) across 9 sibling rows; Test Gate L3 left drained (stale checks queued for re-drain).
- [2026-08-07] [quests] DECISION: quest close tags stay a pure State.Quest.EmberPact.* family (exactly one of four on close); abandon memory lives outside it as State.Dialog.EmberPactWalked, mirroring dialog-gatekeeper's VaelUnanswered scheme.
- [2026-08-07] [green-loop/progression-curves] DECISION: curve-hero-level ramp 10x stretch is structurally pinned (drained Test Gate pins floor=6/crossing/45.0-beat; ratio=1.08^30 is canon's own curve law) — round-2 answered it with anchored mechanism (ARPG-LAWS s9 cite, tops-out-at-94-pct-of-beat, fastest-third clock, free-death band), not restructure.
- [2026-08-07] [green-loop] DELIVERED: Concept-Brief wave 1 — 11 Fable Stage-A agents + median-3 Stage-B over 62 verdicts; progression-curves Concept Brief R2->R5 SHIPPED at [90,90,90]; 48 artifacts re-authored, re-drain queue 5 gates, 2 dialog-trees R4 seams recorded (.claude/green-loop/state.md).
- [2026-08-07] [judge] CONVENTION: never re-edit an 87-89 median cell hoping for variance — each POST re-rolls the whole verdict and can regress (tutorial 83->77, quests Triggers 89->76); the [90,90,90] recipe is zero-number canon-anchored mechanism + owner-cited quotes.
- [2026-08-07] [green-loop/cutscenes] DELIVERED: wave-2 entity-coherent pass on cutscene-prologue (Concept Brief, Beat Sheet, Lighting, Skip/Replay, Subtitles & Loc + verdict-free VFX micro-patch) — dual-clock 30fps/60Hz reconciliation, 82.0 event vs 82.4 teardown split, shot-ID camera-cut index, per-arm loc budgets (keyboard 47), CC policy + 85-percent-panel contrast closure, skip audio exit; all POSTs re-graded pass, drained Test Gate untouched.
- [2026-08-07] [combat-map] DELIVERED: wave-2 coherence pass on arena-ravaged-courtyard (6 cells): stripped meta-audit fields, anti-turtle cover reframe, hazard three-ledger closure, arena-derived clear-time band, buildable mask/BC7 material spec; Waves+TestGate untouched per churn rule.
- [2026-08-07] [green-loop/spellbook] DELIVERED: wave-2 pass on 11 sub-86 cells (arc-fp x6, fire-01 x5) — deleted the controlEffectiveDPS repudiation field everywhere (arguing with the gate cost more than differing), added per-target CC diminishing returns, Kindled steady state 24.43/125.3, GAS execution-routed combo conditions; fire Effect Logic (86) churn-locked untouched; targeting range/requiresLoS/projectileSpeed are SCHEMA-REQUIRED keys (checker pends without them — don't collapse them as duplicates).
- [2026-08-07] [green-loop/input-schemes] DELIVERED: wave-2 entity-coherent pass on input-gamepad (8 cells + 1 forced Rebinding UI edit) — deadzone r^2 math corrected entity-wide (4.0/1.0/6.25%), turbo pinned to the drained gate's 16-fire fixture via TriggerOnStart=false, Special_Left platform facts fixed (View/Minus bindable), Platform Cert grown to a 15-row state matrix, Tutorial HUD surface + stand-down chain closed; all POSTs re-graded pass.
- [2026-08-07] [green-loop/factions] DELIVERED: wave-2 entity-coherent pass on faction-ashen-order's six sub-87 text cells (band-interval derivations, bucket-bound 20-cycle redemption, uniform DT_Factions grant columns homing stipend/overlay/title, two-doors trade rule, 55-key loc ledger w/ cs nominative-slot law, package-contract UE manifest); >=87 cells and drained Test Gate untouched.
- [2026-08-07] [green-loop/props] DELIVERED: prop-reinforced-crate 6 sub-87 text cells re-authored honestly (repair-fee ledger from vendor repairTable, bLootDispensed cross-path latch, half-open damage bands, PoF_Destructible profile, interior end-grain, MID wear mechanism); drained Test Gate untouched.
- [2026-08-07] [green-loop] CONVENTION: vendor Buy/Sell/Repair repairTable is the owning anchor for any kit-repair gold figure (54g/86g are FEES at 0.002xlist/pt, never list prices).
- [2026-08-07] [green-loop/bestiary] DELIVERED: wave-2 whole-band pass on bestiary-melee-grunt (7 steps 66-71): all phantom UE claims purged vs Source ground truth (GE_InitStats/GE_InitResistances/GE_GrantAbility/VSBestiarySpawnTest never existed; real anchors GE_InitAttributes, UGA_EnemyMeleeAttack/GroundSlam, UGE_Cooldown_GroundSlam 6s, Brute archetype, squad tokens), wiringContracts on all 7, threat-104/pack-576 kept via ceiling-stance + forward-derived pack band; Test Gate note carries stale GE_InitResistances ref -> re-drain queue.
- [2026-08-07] [green-loop/vendors] DELIVERED: wave-2 Stage-A authored vendor-wandering-merchant's 5 sub-87 cells (Concept 80/BSR 76/InvPool 72/Pricing 82/ShopUI 84) — standing-conditional cycle figures, 1 g repair floor (sinkProof coherence), buyback-ring session lifetime vs tick-scoped shelf, repChannel collapsed, owner-cited Hated/consignment/multiplier tables; protected >=86 cells untouched.
- [2026-08-07] [green-loop/state-graph] DELIVERED: anim-atk-combo1 wave-2 Stage-A — 5 text cells re-authored code-anchored (real EvaluateNext 4-step model, WalkSpeed 600, per-edge effects, leash 3000 via target-drop, chain delay 0.50); drained Test Gate untouched; probe 81 fed a plausibility patch (root-motion 90 cm, chase-speed thesis).
- [2026-08-07] [green-loop/status-effects] DELIVERED: wave-2 Stage-A authored all 8 non-gate cells (burning CB/EL/Balance, dazed CB/EL, knockback CB/EL/Balance) — fixed daze-chain math (26.1%->32%/44.4% floor), snapshot/mod-bucket law, grunt-420 target anchor, k-tier ladder, cap-exit semantics; drained L3 gates untouched; dazed CB probe 72->84 single-draw.
- [2026-08-08] [green-loop] DELIVERED: Concept-Brief wave 2 — 11 agents / ~60 cells / full median-3 re-baseline; zero new greens but entity bands compressed 66-85 -> 70-88; measured ceiling recorded: ~85-89 text oscillates +-6/draw, so median-3 90 needs near-dock-free content — remaining lift routed to re-drain queue (6 gates), icon media, UE code gaps (.claude/green-loop/state.md).
- [2026-08-07] [redrain] DELIVERED: all 6 stale-mirror L3 Test Gates (spellbook/combat-map/cutscenes/quests/input-schemes + vendors/state-graph/bestiary batch) refreshed to content truth and re-drained pass with fresh report-path evidence; VaelApproach rename + UARPGVendorRules sell/buyback canon reconcile recorded pending (C++ rebuild).
- [2026-08-09] [research/visual-gen] DELIVERED: Tier-1 mesh gate now judges fragmentation by per-component FACE SHARE (histogram + per-class `maxComponents`) instead of a class-blind count of 8 — an assembled character (head/lashes/eyes/mouth-interior/hands/hair/cape) no longer fails as "fragmented"; live-proven harsher not weaker on real Tripo output (375 components -> 61 parts + 314 specks = 36% of face budget).
- [2026-08-09] [research/mesh-finish] CONVENTION: `bpy.ops.mesh.select_interior_faces()` sees only WELDED interior (Blender 4.2 probe: enclosed separate shell = 0/12 faces selected, welded wall = 1) — so `cullInterior` cannot cull between assembled parts; never let a `facesCulled: 0` stand as "nothing hidden", report the unevaluated shell count.
- [2026-08-12] [evaluator] DELIVERED: /research run on Timothy Cain's RT-vs-TB talk added 3 real-time design-semantics criteria to arpg-combat eval prompts (AoE moving-target telegraph/homing, seconds-based escapable hazard timers, player-timed trigger vs stat-driven magnitude for defenses) — commit 7c1cfb8f.
- [2026-08-12] [research/visual-gen] DELIVERED: /api/texture-maps now returns the full derivable tile set (normal+height+roughness, no invented metalness; 3cceac16) — still ZERO client callers, wiring is the next consumer.
- [2026-08-12] [research/rigging] DECISION: SkinTokens (VAST-AI, MIT, UniRig successor) specced as the non-humanoid rig path (docs/research/skintokens-rigging-spec.md) — install user-gated on flash-attn; build only after a live install, never blind.
- [2026-08-12] [evaluator+ai+knowledge] DELIVERED: Cain 5-video research batch (75b9cac1) — new dialogue-quests eval context, encounter/POI world criteria, melee reservation-slot pattern (eval + ai-5 prompt), trait-economy criterion, warning-vs-error ue-cpp gotcha; /research skill v1.1 codifies the designer-talk class.
- [2026-08-14] [research/visual-gen] DELIVERED: mesh-finish now exports a TEXTURED asset (2b19d876) — bakes shared ONE Principled material instead of clearing the low-poly's slots per map (a normal+AO run was exporting no base colour at all), + diffuse/roughness passes and `--uv-mode pack-existing` to keep authored UVs; metallic refused by name (Cycles has no metallic pass). Blender path NOT live-proven yet.
- [2026-08-14] [research] CONVENTION: a GUI-only tool stays declined, but when its demo drives the SAME pipeline stage PoF automates, read the demo steps as a checklist to FAIL against our script — this run's two findings came from that reading, not from the tool.
- [2026-08-14] [research/visual-gen] DELIVERED: AI-low-poly run — auto-smooth in mesh-finish (78d2aa48; every finished mesh was exporting flat-shaded because decimate kills the source normals), provider-agnostic `generateUntilAcceptable` gate-retry in best-of-n, and `planPartBudget` (faef59da) enforcing the assembled budget the modular-part rationale only claimed.
- [2026-08-14] [git] CONVENTION: this repo's index is SHARED across concurrent sessions — a pathspec-less `git commit` commits THEIR staged files too (it happened this run). Always `git commit -m … -- <your paths>`; `git add <file>` alone is not enough.
- [2026-08-14] [research/visual-gen] DELIVERED: face budgets now carry a UNIT and are enforced — new `face-budget.ts` (authored in TRIANGLES, explicit quad conversion at the provider edge, honored/over/**unmeasured** grading), `scoreMesh` grades the delivery against the budget REQUESTED not just the class ceiling, `critiqueThresholdsFor` was found to be DEAD CODE (zero call sites — every mesh graded class-blind since 2026-08-09) and is now wired (1bda8be7); `runMeshFinish`'s 3-run zero-call-sites blocker is CLOSED via a job store + `POST/GET /api/visual-gen/mesh-finish` (e781d60b).
- [2026-08-14] [research/tripo] DECISION: the Tripo REST API has NO mesh-upload path — `smart_lowpoly`/`refine_model`/`convert_model`/`mesh_completion`/`mesh_segmentation` all key off a prior Tripo task id and `upload_file` is images-only; videos showing "upload your mesh and retopologise" are showing Studio (the GUI). Don't re-research it; quad retopo IS reachable for meshes we generated through Tripo (we hold those task ids).
- [2026-08-17] [research/visual-gen] DELIVERED: Tier-1 mesh gate now grades WORLD SCALE — every generator returns a ~1 m box (measured over generated/: hero 1.0 m, hilt 1.0 m); new `world-scale.ts` (`Scorecard.scale`, `targetExtentM` on Tripo/MeshFinish specs, character defaults to the 1.8 m Mannequin, reports the ImportUniformScale that fixes it) + `ai-mesh-unit-normalized` gotcha; import-template consumer still unwired.
- [2026-08-17] [research/cutscenes] DECISION: LevelSequence authoring is PROVEN headless on 5.8 (spawnable CineCamera + keyed transform + bound camera-cut, save/reload verified) — build the cutscene engine per docs/research/headless-cutscene-authoring-spec.md; the prompt-golden rail was red on HEAD since 08-09 (gotchas added without re-record) and is re-recorded in this run's commit.
- [2026-08-17] [catalog] DECISION: status-effect + spellbook Test Gates now ALWAYS write a per-entity `automationName`, so no row can be "proven" by another entity's UE test (Knockback rode the Burning gate; Force Push rode the Daze gate).
- [2026-08-17] [ue-tests] CONVENTION: UE registers MAP-based functional tests under the ACTOR LABEL, not the C++ class — `AVSForcePushKnockbackTest` registers as `...Maps.VSForcePush.ForcePushKnockback`; enumerate with `Automation List` before declaring a gate name.
- [2026-08-17] [skills/perfect] CONVENTION: a user-level `~/.claude/skills/<name>/` copy SHADOWS the project's `.claude/skills/<name>/` — `/perfect` here was silently running the personas method (npm run check, cargo, src-tauri, 13-locale i18n); detect it by comparing a distinctive phrase from the project copy's `description:` against the registered skill listing, and check `git ls-files .claude/skills/` for lowercase `skill.md` (core.ignorecase hides it, breaks discovery on a case-sensitive clone).
- [2026-08-17] [context-map] DELIVERED: incremental Personas rescan 33 -> 38 contexts / 286 -> 441 files (`2b8dd12d`), driven headlessly via the dev-tools bridge on 127.0.0.1:**17400+** (`POST /dev-tools/scan-codebase {project_id, delta_mode:true}`; 17320 is the test-automation port and answers nothing) — the 5 new contexts have `group: null` and the bridge has NO group-assignment route, so CLAUDE.md's "38 contexts" header disagrees with its per-group counts until someone assigns them in the app UI.
- [2026-08-18] [perfect/wave1] DELIVERED: 11 directions across harness/drain/judge/craft/prompts merged ff to master (15 commits, integration gate 5982 pass / 0 fail / 840 files) — self-heal spend now inside the budget ceiling, atomic state writes with a loud corrupt-stop, shared process-tree kill, unique-match bridge attribution (the one false-verdict path), a measured deferred-queue index (116.6ms -> 5.1ms at 20x scale), a real --calibrate guard, drain-not-kill budget stops, craft verdict history + spend metering, and the authored animation prompt actually shipping.
- [2026-08-18] [perfect/method] CONVENTION: a literal NUL byte used as a composite map-key delimiter makes git classify a .ts file BINARY (no diff, no blame, no 3-way merge) while tsc and every test stay green — write `\u0000` as the two-char escape instead; and gate lint by set-intersecting error files against the diff's files, since this repo carries 18 pre-existing lint errors that are nobody's regression.
- [2026-08-18] [perfect/wave2] DELIVERED: 9 directions merged ff (14 commits, gate 6075 pass / 0 fail / 855 files) — step crash containment on the homepage canvas, archetype-to-view lint + filename/catalogId parity + explicit packaging exemptions, /harness operator controls, a reachable drain worker (auto-start opt-in, default OFF), and the lab's blob-free summary route + catalog-wide refresh + changed-since digest.
- [2026-08-18] [perfect/method] CONVENTION: an "this input is unused, so dropping it is free" claim must be proven by FOLLOWING the consumer, not by reading what the consumer is for — the coach was documented as ranking on status only, but re-grades data, compares drift and binds judge verdicts to a content hash; the builder caught it and carried both hashes into the projection.
- [2026-08-18] [perfect/wave3] DELIVERED: 12 directions merged ff (14 commits, gate 6172 pass / 0 fail / 865 files) — the readiness map now honours verdict provenance (17 cells lifted OFF red: it was condemning on provisional v3 verdicts its own drill-down already refused), an unrecognised engine is no longer silently trusted (95 audited steps were mis-classed), a failed catalog read renders UNKNOWN not R0, /status shares one deduped read (96 requests/23.5 MB -> 32/7.8 MB per tab walk), crash module attribution is scored evidence (two samples were misfiled), a null crash diagnosis is stated instead of dressed up, and both UE bridges now name their failure modes.
- [2026-08-18] [status] CONVENTION: `engineClass()` falls back to `unaudited`, NOT `llm` — an engine name absent from ENGINE_CLASS is UNKNOWN, never trusted. Adding a new engine requires adding it to the map; that friction is the point. Audited `trueEngine` strings must match the map EXACTLY (`Leonardo` was mapped while step-facts said `Leonardo (Lucid Origin)`, so 43 gen2d steps read as trusted LLM work).
- [2026-08-18] [visual-gen] DELIVERED: the app's Tripo path can finally express PoF's own audited recipe — `tripo-models.ts` pins `v3.1-20260211` + `texture_quality=detailed` for every class (incl. unknown, so it can't fall back to the account default the character-pipeline arena graded FAIL), the server seam sends `texture_quality` and keeps the provider preview render, `renderUrl`+`formatMismatch` are surfaced by the status route instead of computed and dropped, and quad topology is reachable-but-refused (Tripo delivers it as FBX; the .glb path feeds GlbViewer, the trimesh gate and the UE import).
- [2026-08-18] [visual-gen] CONVENTION: a face budget is a SHAPING parameter, not just a ceiling — a generator spends what it is handed, so an over-generous budget degrades the mesh (hair: 3,000 quads resolved strands, 6,000 grew a spurious head). Request per-asset, well under the class ceiling; `polycount-presets` caps what may SHIP, not what to ask for. Also: pick an image-to-3D model by TIER not by date (the P-series is a low-poly topology tier), and never author text as geometry.
- [2026-08-18] [perfect/wave4] DELIVERED: 6 directions on master (gate 6245 pass / 0 fail) — StepSpec.copy retired with a linter rule that bites, Project Setup mounts a refcounted single-EventSource UE5 connection, the status model takes the row content hash (/status reads 40.0x fewer bytes with grades pinned identical), crashes match by SIGNATURE not id-equality, and observed crashes persist in a new crash_history keyed on that fingerprint.
- [2026-08-18] [perfect/method] CONVENTION: a HELD direction must be RE-SCOUTED before dispatch, never trusted. Three simulation-tools directions sat "accepted, awaiting build" for five weeks while all three were implemented the very day they were accepted and left half-wired — canon-kernel.ts, catalog-enemy-adapter.ts (zero consumers) and canon-conformance.ts (2 of 5 checkers called). Building them blind would have re-implemented working code three times.
- [2026-08-18] [simulation-tools] DELIVERED: GAS/ability sim routed onto the canon damage kernel (designer numbers moved: armour mitigation 16.67%->9.09%), sim policed by canon laws, enemies hydrated from the bestiary catalog with on-screen provenance.
- [2026-08-18] [catalog] CONVENTION: a step label is a DB key (pipeline_artifacts upserts on catalog/entity/step) — never rename one to align two specs; teach the server the label via the bespoke-checker fallback, and stamp any still-ungradable row UNGRADED: rather than trusting the producer (Rule 4b).
- [2026-08-18] [layout-lab] DELIVERED: write-through guards a thrown grade — syncError names the checker reason, nothing is POSTed, network failure mode kept distinct (8500f83e).
- [2026-08-18] [modules] DELIVERED: forge background poll now has a stated stop (30-min deadline + operator stop, visible in the queue) and all 6 unsuspended setInterval sites under components/modules moved to useSuspendableEffect (14 audited, 1 left running by design).
- [2026-08-18] [visual-gen] DELIVERED: `smart_low_poly` wired into TripoSpec/pof_tripo.mjs (what Tripo markets as "P1/P2" — a flag on the SAME v3.1-20260211 model, not a separate model_version) + an arena harness (`pof_tripo_smartlowpoly_arena.ts`) A/B-ing it against the audited baseline; blocked mid-run on real Tripo account credit exhaustion (error 2010) before a verdict — weapon/modular-part unattempted, prop/smart_low_poly incomplete.
- [2026-08-18] [visual-gen] CONVENTION: `tsx` does not type-check (esbuild transpile-only) — an object literal with extra/misplaced properties compiles and runs silently instead of erroring; always run `npm run typecheck` on a new tsx script before trusting its options handling, especially anything shaping a live paid API call.
- [2026-08-18] [layout-lab+shell] DELIVERED: wave 6 shipped 8 robustness directions (drain cancel/invalidation honesty, write-through fail-closed, LRU eviction observability, forge poll terminal conditions, 14-site timer suspend sweep, canon defence bands) — full gate 886 files / 6349 tests / 0 failures.
- [2026-08-18] [visual-gen] CONVENTION: never trimesh mesh.split() a generated mesh to count components — on fragmented HD-PBR Tripo output it materializes a textured mesh per component (measured 211GB VM, crashed the host); use trimesh.graph.connected_components(mesh.face_adjacency) + isolated-face accounting (see pof_mesh_critique.py).
- [2026-08-18] [layout-lab+shell] DELIVERED: wave 7 shipped 4 directions — unified ActivityChip (unknown outranks idle, old chips deleted), derive-side UNGRADED guard (4 call sites, whole-canvas crash closed), LRU double-touch fix (was an infinite re-render loop, production-reachable), setTimeout chain audit (78 sites, silent batch-stall bug fixed) — gate 890 files / 6393 tests / 0 failures.
- [2026-08-18] [layout-lab] DECISION: a checker that throws during DERIVATION degrades that step to UNGRADED fail via gradeStepGuarded — never null (fabricated pass), never deferred (stale server-pass adoption), never pending (reads transient); labGrade stays throw-transparent.
- [2026-08-18] [visual-gen] DELIVERED: P-series arena completed on weapon/prop/modular-part — `smart_low_poly` REJECTED (overshot its own face budget 1.26x-1.35x in all 3 classes vs baseline honoring it every time; 15x-30x more raw components; never won Qwen aesthetic score); recorded as a completed negative audit in `tripo-models.ts` SMART_LOW_POLY_VERDICT, not left as an open gap.
- [2026-08-18] [shell+modules] DELIVERED: wave 8 — one-visible-pane rule (sub-module wins, resolveVisibleModule single-sources veil+suspend+predicates, 56707768) + rAF hidden-pane sweep (28 sites, 5 unbounded loops suspend-gated lossless, fc201f9d); gate 892 files / 6410 tests / 0 failures.
- [2026-08-18] [gdd-compliance] DECISION: scout found the compliance loop is CIRCULAR (no GDD artifact exists — checklist vs feature_matrix, and the "GDD" synthesizes from the same sources; zero-evidence modules score 70/100) — wave-9 direction splits coverage from conformance; real GDD artifact parked L for human gate.
- [2026-08-18] [visual-gen] DELIVERED: v2->v3 Tripo migration scoped down after checking the vendor's own SDK source (not just docs) — image/text-to-model still route through v2's universal POST /task in Tripo's real client (last commit 7wk old); only mesh segmentation uses v3. Added v3 transport + the one verified request shape (TRIPO_V3_BASE, buildMeshSegmentBody, parseV3TaskStatus, createMeshSegmentTask) as unused-by-production groundwork; TripoSpec/runTripo/TRIPO_BASE untouched, generation stays v2.
- [2026-08-18] [visual-gen] CONVENTION: before trusting a vendor's "migration guide" or docs-site endpoint list, check the vendor's own official SDK source (GitHub) for what it ACTUALLY calls — a docs page can describe a target/future shape ahead of what's live; the SDK reflects real production behavior. Caught here: the guide listed image-to-model on a dedicated v3 path, but the SDK still uses v2 for it.
- [2026-08-18] [audio+gdd+level-design] DELIVERED: wave 9 — audio painter + level-design editor commit on boundaries not keystrokes (10→1 and 37 PUT+38 GET→1 PUT, measured against reconstructed HEAD), audio import reports recorded reality w/ pre-dispatch preflight, audio delete removes bytes, compliance score split coverage×conformance (27/34 modules now honestly UNMEASURED), GDD view 6→1 syntheses; gate 914 files / 6549 tests / 0 failures.
- [2026-08-18] [ui] CONVENTION: an editing surface holding a local buffer commits through a THROWING path (e.g. useAudioScene.commitDoc / Result<T,string>), never useCRUD.mutate — a swallowed null is indistinguishable from success and silently discards the edit; debounce via UI_TIMEOUTS.textEditDebounce.
- [2026-08-18] [hooks+audio+gdd+level-design+shell] DELIVERED: wave 10 — useEntityCommitBuffer extracted (3 hand-rolled buffers migrated, contract suites unmodified), forge tts/music claims removed + quota stated informational, shared ScoreRing gains UNMEASURED + canonical ladder (7 consumers enumerated), explicit checklist→feature map (88→177/216, dark gap categories woke: code-ahead 15→36), level-design sync loop closed via real callback route, procgen run ledger + honest params, terminal attribution single-sourced (game-director was attributed to null), Blender export full-size + floor-validated spawns (58% were in walls); gate 932 files / 6735 tests / 0 failures.
- [2026-08-18] [gdd-compliance] CONVENTION: checklist item ids are NOT globally unique across modules (ai-1..7 exists in both arpg-inventory and ai-behavior) — any map keyed on item id must be keyed module→item first, or it silently cross-wires modules.
- [2026-08-18] [audio+gdd] DELIVERED: wave 11 — audio property panel commits on boundaries via useRecordCommit (the RECORD face of useEntityCommitBuffer; 10/4/19 writes→1), emitters bind to library asset sets and codegen emits the REAL imported cue path or an honest placeholder comment, Asset Studio feature definitions close the checklist map to 216/216 (import-route validation hole closed); gate 938 files / 6781 tests / 0 failures.
- [2026-08-18] [game-director] DECISION: scout found the Game Director is a canned simulator (15 hardcoded findings, testCoverage = Math.random()) presented as an autonomous agent with zero disclosure — wave-12 direction adds a session source column + ScoreRing measured=false provenance; real Gauntlet ingestion parked L (the route already exposes a complete external-writer API).
- [2026-08-19] [game-director+visual-gen+feature-matrix] DELIVERED: wave 12 — Game Director discloses simulation everywhere (source column, Math.random gone, "Simulate Playtest"), regression sweep scoped superset-of-categories, forecast wired anchored on last-session time; forge queue shows server verdict + no phantom providers; critique gate 3 paid rolls→1 when critic unavailable + gallery serves every provider dir (?dir= allow-list); feature rows carry source provenance, review history recent-window (sparkline had never rendered), 7 status consumers→1 hook; gate 955 files / 6981 tests / 0 failures.
- [2026-08-19] [visual-gen] CONVENTION: build asset URLs with assetUrl(name, dir) from @/lib/visual-gen/generated-assets — the ?dir= param is validated against ASSET_DIRS (triposr default so legacy URLs resolve byte-identically); any client list of servable dirs must DERIVE from ASSET_DIRS, never restate it.
- [2026-08-19] [visual-gen+feature-matrix+catalog] DELIVERED: wave 13 — forge asset-class picker with explicit ungated/gradedAs projection and a 3-state queue, MCP jobs get the same honest ungraded vocabulary (critique structurally infeasible: the mesh never reaches this server), one shared aggregate fetch path across 3 consumers with real error states, packaging resolves ?dir= via ASSET_DIRS and REPORTS what it cannot; gate 960 files / 7019 tests / 0 failures.
- [2026-08-19] [ue-experiment] DECISION: scout found every Experiment Lab visual verification has ALWAYS failed — the runner posts {screenshotPath,mode,prompt} but /api/verify/visual requires moduleId+itemId and 400s, so it falls back to its own literal {status:fail}; a comparison that never ran reads as a fail. Also: runner issues unscoped `taskkill /IM UnrealEditor.exe /F`, killing the operator editor with unsaved work. Both queued as wave-14 directions.
- [2026-08-19] [ue-experiment+visual-gen+material-lab] DELIVERED: wave 14, 13 directions — editor-kill hazard removed (scoped PID kill reusing ue-launch/capture + live-editor refusal + real drain lease, no force-kill override), Experiment Lab visual verify actually runs with deferred-on-outage (proven by a JOINT test, 10/10 red pre-fix), captures durable with UNAUDITABLE labelling, power-icon fabricated pass closed, style-transfer phantom analysis removed (route AND prompt), input gate wired before paid generation, material presets persist + edits reach Blender with AO real + UE5 instance emitter; gate 977 files / 7194 tests / 0 failures.
- [2026-08-19] [checklist] CONVENTION: POST /api/checklist/complete now validates itemId via resolveProgressKey (checklist|aux|migrate|ungoverned|unknown) — a STRICT registry-only rule would break legitimate open-ended surfaces (AnimationStateMachine emits runtime scanned-<StateName> ids); declare an auxiliary surface WITH its owner rather than refusing it, and migrate orphan keys without downgrading a completion.
- [2026-08-19] [feature-matrix+visual-gen+ue-experiment] DELIVERED: wave 15 (first operator-gated L wave) — feature_matrix/review_snapshots project scoping phase 1 (165/165 rows measured unattributed, legacy rows visible+counted never adopted, takeover reported with a 409 naming the owner), the 2D provider front (first prompt→image path in the app, NO KEY vs NOT WIRED distinct, magic-number sniff so Content-Type cannot lie), generated/ measured at 286 files / 1022.1 MB with a DELETE-FREE prune plan that downgrades to UNKNOWN whenever a referrer channel failed, image routes store-and-revalidate (45→1 syscalls, 0 body bytes on repeat), audio diagram on real aud-* ids with a no-downgrade migration; gate 989 files / 7381 tests / 0 failures.
- [2026-08-19] [tooling] CONVENTION: an unescaped backtick inside a db.exec(`...`) template literal in src/lib/db.ts is a SYNTAX error, so tsc suppresses semantic checking program-wide and unrelated vitest suites fail to transform — it hit three separate lots today. Recognise the signature (18+ suites failing to parse at once) and re-run rather than chasing a phantom regression; never fix a foreign session file.
- [2026-08-19] [feature-matrix] DELIVERED: wave 16 executed the operator gate — feature_matrix UNIQUE key widened to (project_id, module_id, feature_name) and ALL unattributed rows backfilled to PoF (165/165 + 10/10 preserved, 0 left, user_version 2->3, Director-verified read-only on the real DB); auto-verify now stamps the project on BOTH the pre-diff read and the write; a four-state scope banner distinguishes own/mixed/legacy/foreign so "another project owns these rows" can no longer read as "never reviewed".
- [2026-08-19] [db] CONVENTION: a SQLite table rebuild must INSERT ... SELECT by EXPLICIT column list (never SELECT *, which copies positionally and is how a rebuild silently shifts values), and must verify the copied row count and throw INSIDE the transaction BEFORE dropping the old table. Migration 42a71127 is the reference.
- [2026-08-19] [nba+state+ui-primitives] DELIVERED: waves 17-18, 10 directions — NBA now scores the CHECKLIST_FEATURE_MAP binding instead of a first-word guess (measured: 67 of 216 items were mis-scored, and 0 items are unmapped so the heuristic was OVERRIDING an exact map, never falling back) and its pitfalls warning is fed at last; module progress carries a project identity so a switch/reset/failed-load cannot union one project's "done" marks into another; deep-eval baselines are project-scoped and interrupted workflows say so; five hand-rolled dialogs converged onto ui/Modal; semantic Badge variants and budget meters stopped encoding status by hue alone; gate 1007 files / 7590 tests / 0 failures.
- [2026-08-19] [tooling] CONVENTION: in this SHARED checkout, write a commit message to a file and use `git commit -F <file>` — NEVER `git commit --amend` (a lot amended on 2026-08-19, raced two siblings that had committed in the intervening seconds, and rewrote ANOTHER lot's message; repair needed reflog + `commit-tree` rebuilding four commits, verified byte-exact by the reconstruction reproducing the original SHA). And never use PowerShell here-string syntax (@'...'@) in the Bash tool — it silently yields a "@" subject line, which hit two separate lots the same day.
- [2026-08-19] [db+catalog+shell] DELIVERED: waves 19-20, 11 directions — the vitest suite STOPPED WRITING INTO ~/.pof/pof.db (342 of 817 artifact rows were test residue; redirect now rides vitest.config test.env so no import order can bypass it, keyed by pid); getDb() bootstrap made atomic after a scout RAN the legacy feature_matrix rebuild and it threw, leaving a stray table that breaks every later boot; telemetry+build_history scoped (telemetry chooses which skill packs enter CLI prompts); NBA success odds read real runs (86 sessions, 0 successes — the 0.5 constant is gone); framer finally honours prefers-reduced-motion; LRU stops evicting observed live work; entity lifecycle derived from proven state; gate 1028 files / 7764 tests / 0 failures.
- [2026-08-19] [tooling] CONVENTION: a builder must COMMIT EACH COHERENT PIECE AS SOON AS IT IS GREEN, never save one commit for the end — on 2026-08-19 four builders were killed by a harness watchdog mid-lot; their code survived in the working tree and was recoverable, but their forced-failure proof did NOT, so six directions landed with gate verification and no red-before-green evidence.
- [2026-08-19] [items-pipeline] DELIVERED: the bespoke Items gallery steps grade the generated ASSET not an integer (Icon 2D Art delegates to gradeGallerySelection; withGeneratedAsset composes shape-then-asset for 3D Generation / Material / Texture — labels unchanged per Rule 4b), and the Test Gate reads RESOLVED sibling verdicts via the new optional CheckerContext.siblingVerdict, naming the layer (checker/drain/judge) that condemned each blocker; 7 items rows go green->deferred on screen and 2 server-side, and a gate blocked only by deferred upstreams now reports deferred/L3, never fail.
- [2026-08-19] [catalog] CONVENTION: a DERIVED checker (one that grades from upstream steps) must consume ctx.siblingVerdict — the resolved checker->drain->judge merge — never re-run a sibling's raw checker on ctx.siblings; it is a lazy FUNCTION so only the depended-on steps are graded, and the sibling is graded with siblingVerdict absent so recursion is structurally impossible.
- [2026-08-19] [animation] DECISION: the ARDY text->motion install had VANISHED from this machine (uncapped find returned nothing; only the committed converters survived) and was REBUILT + re-verified the same session at kiro/ardy (torch 2.13+cu126, motion_correction C++ ext, text_encoders reassembled, generation confirmed) — and NVIDIA Kimodo is the EARLIER model, not ARDY's successor (same Bones Rigplay data = no quality lever, SOMA-77 would strand the Core-27 IK_ArdyCore rig), so do not propose switching to it; note `pip install -e .` does NOT build the motion_correction ext and its absence is SILENT (import MotionCorrection resolves the source folder as an empty namespace package).
- [2026-08-19] [animation] CONVENTION: a generated clip that is meant to loop must clear the Tier-1 numeric gate (src/lib/motion-gate/) BEFORE the VLM anim-critique tier — and loop closure is three axes not one, because a clip can match "first frame equals last frame" exactly and still hitch every cycle on seam VELOCITY; one-shot clips return n/a, never pass.
- [2026-08-19] [items-pipeline+settings] DELIVERED: wave 21 — the items lab renders BOTH step specs so 31 of 90 persisted rows stop being invisible (item-3 read 6/13 while holding 11 produced steps), the bespoke preview panels stop painting fabrications as output and reach the real generated icon, the bespoke acceptance banner keeps its explanation and fix button after a server/judge overlay, and four settings blob stores stop letting an unreadable row authorise the write that erases it; e2e walker 64 passed in a throwaway worktree; gate 1035 files / 7859 tests / 0 failures.
- [2026-08-19] [tooling] CONVENTION: in this SHARED checkout never run `git checkout HEAD -- <file>` on a file another lot may hold uncommitted work in (a lot did on 2026-08-19 and discarded a sibling's WIP) — back the file up to the scratchpad and restore from there; and NEVER invoke python in any form, including `python -c` and `python - <<EOF` shell probes, which two separate lots tripped as a reflex after the 211 GB crash of 2026-08-18.
- [2026-08-19] [animation] DELIVERED: wave 22 — the AnimBP "Scan Project" button worked for the FIRST time (the caller stored the {success,data} envelope, so a SUCCESSFUL scan threw on the next render) and the graph names its provenance BRIDGE/PROJECT SCAN/TEMPLATE instead of badging a hardcoded fallback RUNTIME; the real buildLedger reaches the UI behind an explicit click (never a poll); the notify-coverage arrays are DELETED with a guard and the budget gauge type lost its `current` field so a literal cannot render as a reading; the responsiveness analyzer rendered 31 rows and 62 ms values about a project it had never read and now renders none; gate 1039 files / 7897 tests / 0 failures.
- [2026-08-19] [db+scope] CONVENTION: a project-scoping change is only HALF-shipped until every CALLER passes the scope — wave 20 taught insertBuild to write project_id, no UI caller passed one, and projectScopeSql('') resolves to legacy-rows-only, so the Builds tab showed ZERO of a project's cooks while a fully green gate passed. The test DB cannot catch this class: every fixture row predates the column. When you add a scope, grep every caller and add a test that a row written WITH the scope is readable by the surface that displays it.
- [2026-08-19] [blender-mcp] DECISION: the bridge 'status' action is now a REAL probe (svc.probe() round-trips get_scene_info, 8s FAST class, queued on the serialized chain so it can neither pre-empt nor be timed out by a long user script) and the connection bar adopts it on mount instead of trusting a rehydration-reset flag; BlenderConnection.blenderVersion was DELETED (nothing on this bridge can report one — the only values in the tree were test fixtures).
- [2026-08-19] [blender-mcp] CONVENTION: every /api/blender-mcp/execute dispatch goes through executeViaMCP (ScriptRunner.tsx) so the script is recorded AND its Result is surfaced — execute-dispatch-guard.test.ts fails on a new raw fetch and carries 9 reasoned residuals; Blender scripts escape names with py() and RAISE on a missing object (never a bare 'if obj:' with no else), and never interpolate a name into an f-string.
- [2026-08-19] [packaging+blueprint+blender] DELIVERED: wave 23 — the Builds tab is scoped and DISCLOSES what it hid (closing the wave-20 regression) and the interactive smoke verdict now appends its note, scopes its target and CONDEMNS the build it verified instead of leaving a dead exe green; the blueprint transpiler's emitted header finally survives UHT (generated.h last, sanitized API macro, U-prefix for components, no undefined overrides) and its body walker refuses to fabricate (PrintString(Player Spawned!) → UE_LOG + TODO + warnings); the Blender bridge status is a real 8s probe (queue-safe under an 8.6s script) with the phantom blenderVersion deleted; the 3D viewer stops showing one model's stats under another's name and its inspector grades through polycount-presets/face-budget/world-scale instead of a rival constant table; gate 1052 files / 8026 tests / 0 failures.
- [2026-08-19] [testing] CONVENTION: the strongest forced-failure is one that REPRODUCES THE ORIGINAL LIE, not one that merely goes red — wave 23 pointed the new mesh grader back at the rival budget table and watched an 83,728-triangle asset grade `honored` again, which proves the fix addresses the actual defect rather than an adjacent one. Corollary: flag your own pre-change passes that are TRIVIALLY green and do not count them as proof.
- [2026-08-19] [scope+mcp] DECISION: wave 16's feature_matrix backfill (correct, operator-decided, 165/165 preserved) SILENTLY BROKE four headless MCP tools — projectScopeSql('') is legacy-only and after the backfill there are no legacy rows, so pof_feature_matrix / _all / _aggregate return 0 of 165 and pof_gdd_compliance scores every module against zero evidence and writes gap resolutions under ''. Director-verified 2026-08-19. Fix is accepted and UNBUILT; a guard test must enumerate every pof-mcp handler hitting a project-scoped route so an eighth instance cannot appear silently.
- [2026-08-19] [tooling] CONVENTION: `git commit --only <path>` commits the WORKING TREE content of that path, so rebuilding a co-owned file from a `git show HEAD:<path>` snapshot reverts whatever a sibling landed in the window (~90s was enough on 2026-08-19: d3779edd reverted 0d156da4, repaired in 0984081e). Never split ONE file across two parallel lots — that partition error is the Director's to avoid; if a file is co-owned, give it wholly to one lot and have the other report the hunks it needs.
- [2026-08-20] [status+catalog] DELIVERED: wave 25 lot MC — /status now says whether a cell's engine name is AUDITED / AUTHORED / UNAUTHORED (glyph+word, italic-dotted for a guess, loud UNSOURCED for a missing provenance; 11/14 red pre-fix), a shrink-only per-archetype ratchet + dispute list guards StepSpec.engine, and gallery+balance are fully authored from each step's OWN code (12/344 -> 65/344; gallery ceiling 44->6, balance 15->0). Re-measured: the backlog's "96.5% are heuristic guesses" is STALE — 336/344 resolve from the step-facts audit and only 5 reach the heuristic.
- [2026-08-20] [status] CONVENTION: resolveEngine prefers the AUDITED StepFact.trueEngine over StepSpec.engine, so authoring `engine` is display-inert wherever a fact exists — author it anyway as an independent SECOND WITNESS and let the spec linter make spec-vs-audit disagreement loud (it already surfaced character-pipeline "UE Import", where the audit's `Code` sits in TRUSTED_CLASSES while the step's own produceNote names a UE Python commandlet, and 9 balance steps the audit calls `Claude` whose own notes concede the numbers are hardcoded).
- [2026-08-20] [visual-gen] DELIVERED: wave 25 lot MB — the "gate fails raw output ~100% of the time on face count" claim is DISPROVEN in mechanism (measured over all 52 .glb in generated/: 10/52 fail = 19.2% upper bound, ALL TEN on `floaters`, and face count can never fail a mesh — 1,492,072 faces vs the 12,000 modular-part ceiling grades warn/85), so the gate now emits typed `findings` + a per-verdict stage caveat instead of a false blanket one, the paid loop stops at 1 roll (was 2 = 40 credits) on a stage-determined verdict, and critique→mesh-finish is routed end-to-end with basename+ASSET_DIRS guards, cullInterior permanently refused, and a strict before→after re-grade.
- [2026-08-20] [visual-gen] CONVENTION: retopo/decimate is NOT a remedy for floater specks and MULTIPLIES them — measured on the real pair generated/tripo3d/jinx_v32_run.glb (1,482,446 faces / 2 comps / 1 speck → warn) vs its decimated jinx_v32_run_game.glb (46,791 / 17 comps / 16 specks → fail); so a floater-only failure must be REFUSED by any fail→finish route (47 of the corpus's 50 failing mesh×class combinations are refused on exactly this), and `pof_mesh_finish.py loose_shell_count` is still a latent 211 GB-class memory bomb whose only protection is that nothing sets `cullInterior`.
- [2026-08-21] [visual-gen] DELIVERED: TRELLIS.2 wired as a real provider (runner+job store+WSL seam); retired the metadata-only `trellis2` registry ghost.
- [2026-08-21] [visual-gen] DECISION: TRELLIS.2 is NOT commercial-safe despite its MIT badge — every gen config conditions on gated Meta DINOv3; TripoSR stays the only unencumbered local route.
- [2026-08-22] [visual-gen] DECISION: the Qwen-VL recognition chain is re-tiered BY MEASUREMENT to `qwen3.8-27b`→`3.7-flash`→`3.6-flash`→`3.8-max`→`3.6-plus`→`3.7-plus` (26 scored calls/model on the real input-gate prompt over 13 eye-verified concepts) — the old primary `qwen3.7-plus` ranked LAST (6 false-passes, incl. a confident 10/10 on a figure whose arms are buried in a cape), every error on every model was a false PASS never a false fail so false-pass count is the ranking metric, and `qwen3.7-max` is TEXT-ONLY (HTTP 400 on images) and is test-barred from the chain.
- [2026-08-22] [visual-gen] CONVENTION: rank a vendor's models by calling them on YOUR prompt with YOUR ground truth — tier names are pricing, not capability (`qwen3.8-max` lost to the 27B open-weights model; `qwen3.7-max` cannot see images at all); and ground-truth generated fixtures BY EYE, never by the generating prompt, because 2 of 6 "hard" fixtures ignored their instruction and would have inverted their labels.
- [2026-08-22] [visual-gen] DECISION: VL chain re-measured over 40 calls/model across TWO blocks (13 purpose-built occlusion-heavy concepts + 7 REAL generated/icons assets) → `qwen3.7-flash` promoted to PRIMARY on a perfect 1.00/FP0 in both, then 3.8-27b → 3.8-max → 3.6-flash → 3.6-plus → 3.7-plus; the real-asset block barely discriminates (4 of 6 models score 1.00, incl. the demoted 3.7-plus) so it proves the ranking does not invert but the separation comes from the hard block.
- [2026-08-22] [visual-gen] CONVENTION: a transport failure (fetch rejecting) must FALL THROUGH the model chain, never escape it — unwrapped it made `gateInputImage` report "unavailable" and the concept was submitted UNGATED to a paid image→3D job, one flaky model defeating five healthy ones behind it; an all-unreachable chain must still throw so an unavailable gate is never silently a pass.
- [2026-08-23] [visual-gen] DELIVERED: mesh-finish `--retopo quadriflow` (regular retopology; QuadriFlow was already in the installed Blender 4.2) + `hunyuanModelFor` pin + 2 ue-gotchas — commit 218eae9b.
- [2026-08-23] [visual-gen] CONVENTION: judge a Blender/CLI OPERATOR by the artifact it changed, never its return code — `quadriflow_remesh` returns FINISHED while silently no-opping on the non-manifold mesh EVERY .glb import produces (glTF splits seam vertices); weld + re-normal first.
- [2026-08-24] [tooling] DELIVERED: the ~26KB Catalog Pipeline Step Authoring section (Rules 1-5, 4b, 24-row Shared Component Manifest, walker/e2e obligations) moved VERBATIM from always-loaded .claude/CLAUDE.md (37127->10987 bytes) into path-scoped .claude/rules/catalog-pipeline.md; a 15-line stub with the rule headlines + reuse invariant remains.
- [2026-08-24] [tooling] CONVENTION: bulky sometimes-needed guidance goes in .claude/rules/*.md with paths: glob frontmatter (loads only when matching files are touched) — always-loaded CLAUDE.md keeps only what most sessions need plus a pointer.
- [2026-08-31] [visual-gen] DELIVERED: texel-density budget (`texel-density.ts`) now sizes bakes from `targetExtentM` (which mesh-finish already had and ignored) + `planKitAtlas`; procedural `generators/linear-prop.ts` replaces gen calls for rope/cable/chain.
- [2026-08-31] [knowledge] CONVENTION: before adding to a knowledge list, grep for a non-test consumer — `GEN_PROMPTING_PRACTICES` (reference-roles.ts) reaches NOTHING; put prompt-reaching knowledge in `UE_GOTCHAS` and run the golden rail (`POF_UPDATE_GOLDEN=1` only after confirming HEAD was green).
- [2026-08-31] [visual-gen] DELIVERED: kit render gate — `pof_mesh_views.py` + `mesh-views.ts`/`view-critique.ts`/`kit-coherence.ts`; PoF can now render a generated mesh from N yaws and judge it (structural 100/100 meshes render as melted blobs).
- [2026-08-31] [testing] CONVENTION: when adding a VLM/judge gate, run a KNOWN-GOOD control through it before shipping — our first build failed 3/3 views on a clean Blender Suzanne, and a gate that condemns everything is indistinguishable from one that works.
- [2026-08-31] [visual-gen] DELIVERED: the render gate is REACHABLE — `view-gate-job-store.ts` + `POST /api/visual-gen/view-gate` (+ /status) compose render→critique→coherence; proven live on chair.glb (3 Blender yaws, Qwen-judged, 121s).
- [2026-08-31] [testing] CONVENTION: after shipping a module, run the consumer census — `grep -rln '<export>' src/ | grep -v __tests__` — green tests prove a module, never a pipeline; four modules shipped that morning had zero production importers.
- [2026-08-31] [visual-gen] DELIVERED: shape route wired into POST /api/visual-gen/generate — a rope/cable/chain prompt is refused before any provider call and pointed at the new POST /api/visual-gen/linear-prop, which computes it for $0 (artifact: watertight, euler 2, bbox 6.5x1.8x1.4 m).
- [2026-09-01] [readiness] DELIVERED: wave 1 of the R4 campaign — 10 Opus agents, one per catalog, lifted 160 judged cells (avg 73.3->81.7, cells >=90 3->21, R4 7->23, BLOCKED 193->180, $117); `scripts/readiness/inventory.ts` was UNDER-REPORTING R4 (it never mapped `content_hash`/`updated_at`, so verdictProvenance degraded every binding to `unknown` and elevation silently fell back to R3) — fixed, and the pre-fix baseline is NOT comparable.
- [2026-09-01] [readiness] CONVENTION: a strict-judge corpus plateaus at 85-89 on REGISTER, not content — prose addressed to a reviewer ("previously", "deliberately", "note that", conceding a point) is the ceiling defect; sweep it MECHANICALLY over the STORED rows (two careful hand passes still left 23 and 28 leaks) and expect ~76 fact-preserving phrase replacements to cross a cell 89->90. Full craft: ai-registry `game-production/content-pipeline/judgeable-spec-authoring`.
- [2026-09-01] [harness] DELIVERED: scan-sweep round on harness-autonomy (12 atomic commits) — GET /api/harness reads the durable run from disk after a restart (+ pof_harness_status.statePath), the visual gate and the guide stop self-certifying (UNVERIFIABLE never reads as pass/FAIL), run end names every gate that returned no verdict, cap-hit reason states the overshoot, projectPath/statePath mismatch is a 400; 8 backlog items in the outbox.
- [2026-09-01] [harness] CONVENTION: a reader of a harness sidecar (progress.json, game-plan.json, cost.json) goes through state-io's readJsonFileState so corrupt never reads as missing — a raw JSON.parse-with-fallback at an API edge re-collapses the two (was GET ?action=progress).
- [2026-09-01] [gates] DELIVERED: fleet-memory-cap.mjs reads this file's length against the 200-line cap (red names the --prune remedy) and runs at the end of scoped-check; pruned 48 oldest DELIVERED lines to land at cap.
- [2026-09-02] [knowledge] DELIVERED: ai-registry game-production 43->51 subjects / 259->319 techniques, harvested from the GameDev-Resources canon (12 units, 4 waves) + 2 new laws (L12 an-instrument-proves-it-had-input, L13 declaring-an-input-is-not-consuming-it); every unit reconciled against THIS repo read-only and the PoF defects it exposed are listed per-unit in ai-registry docs/plans/gdr-harvest-2026-09-02.md - incl. vertical-slice milestone = project-wide breadth fraction, RoomConnection.condition free text the pacing linter never reads (locked doors walked as open), vendor-sell converter typed as a source with an unread input pool, and no unreproducible state in TriageStatus.
- [2026-09-03] [planning] DELIVERED: three unmeasured-reporting defects fixed — `Milestone.currentProgress` is now `number | null` and the vertical slice reports NOT MEASURED (it was `completionPct/30`, a breadth fraction, so 100% slice was reachable with no playable path; all 4 UI consumers route through `lib/roadmap/milestone-progress.ts`), the `milestone_deadlines` store is finally consumed by the NBA engine as a bounded time-to-deadline urgency term (`lib/nba-deadline.ts`, max 8 pts, 90-day horizon, no deadline = byte-identical ranking), and `ai-behavior::Behavior Tree system` now dependsOn `AI Perception setup` (perception before decision).
- [2026-09-03] [economy] DELIVERED: the economy node vocabulary is now the five kinds (source/drain/converter/trader/pool, `lib/economy/node-audit.ts`; faucet/sink kept as the legacy spellings so persisted flows stay valid) — `loot-vendor-sell` is reclassified a CONVERTER whose declared `input` leg the sim actually consumes (empty item pool = no sale, no gold), the death penalty is a declared `percent-of-balance` mechanism taken from the running balance so the negative-gold clamp can no longer mint, and `runSimulation` returns `audit` + `balance` where an unclassified/unfunded/starved node reports `unclassified`/`unspecified`, never `pass`.
- [2026-09-03] [game-director] DELIVERED: findings can now say what they do not know — TriageStatus gains `unreproducible` carrying its attempt count + build (rejected below 1 attempt at the API and DB seam; NOT in TRIAGE_EXCLUDED, so it never silently closes a finding), and `confidence` is nullable with a `confidenceBasis` ('observer' | 'unattributed') so an unscored finding renders "not scored" instead of the old NOT NULL DEFAULT 80; both needed a guarded, idempotent, row-counted table rebuild (game_director_findings, regression_occurrences) since SQLite cannot widen a CHECK or drop NOT NULL in place — pattern documented in docs/architecture/state-and-persistence.md.
