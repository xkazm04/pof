# consult — project overlay (pof)

## Skill improvement log

- 2026-09-08 — `.ai/manifest.yaml` declares `knowledge.domains: [game-production,
  software-engineering]`, but PoF's model/vendor decisions are owned by **media-generation**
  (`visual-generation/generative-provider-routing` — capability-to-vendor-plan,
  extraction-model-bake-off, cost-per-usable-economics) and touched by **llm-observability**
  (`quality-scoring/cross-provider-benchmark-operations`). Both scored at or above the
  in-domain hits when routing a provider question. Read them anyway on any
  vendor/model/benchmark decision, and consider widening the manifest's domains.
