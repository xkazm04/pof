---
paths:
  - "src/components/layout-lab/**"
  - "src/lib/catalog/**"
  - "src/app/api/pipeline-artifacts/**"
  - "src/app/api/catalog/**"
  - "src/__tests__/catalog/**"
  - "e2e/catalog-*.spec.ts"
  - "e2e/helpers/pipeline-coverage.ts"
  - "docs/catalog/**"
---

## Catalog Pipeline Step Authoring (rules for parallel sessions)

The catalog→UE pipeline is built by many parallel CLI sessions. Every pipeline step follows the **View / Produce / Acceptance** model (see `docs/catalog/PIPELINE_REVIEW.md` for the full standard + archetype library + per-row plan). The `/layout` lab (`src/components/layout-lab/`) holds the **reference implementation** — the full Items pipeline. Before building a step, read the manifest below and reuse; do not duplicate.

**Rule 1 — Produce contract.** Every CLI/Produce component has a **text area for the user's direction** + its **own prompt logic** (a `buildPrompt(direction)` callback). Use the shared `CliProduce` (below); never hand-roll a Produce panel. It exposes the built prompt and reports the result/error.

**Rule 2 — Generated code conventions.** Code CLIs generate must be clean and **≤ 200 LOC per file**. Folder structure **mirrors the UI hierarchy** — `Catalog → <Catalog> → <PipelineStep>` (e.g. `.../items/economy/`). Filenames are **camelCase and encode hierarchy position** so a file is identifiable out of context: `itemEconomyBudget.tsx`, `itemEconomyDistribution.tsx`. The component export stays PascalCase (`ItemEconomyBudget`). Split anything over 200 LOC into sub-component files in the step folder.

**Rule 3 — Reuse, don't duplicate (Shared Component Manifest).** Check here before building UI:

| Component | Path | Use for |
|-----------|------|---------|
| `CliProduce` | `layout-lab/steps/shared/CliProduce.tsx` | the Produce face of any step (Rule 1). In LIVE mode it names **which model and effort** will run the dispatch and **what that task type has historically cost** (`useDispatchPlan` → `GET /api/cli-spend?action=dispatch-plan` → `describeDispatchPlan`), instead of the old bare "spends model budget" — an unpinned dispatch says so rather than implying a governed model, and a task type with no recorded runs says it has no history rather than rendering a `$0.00` that reads as free. Fetch is gated on live mode, so stub steps pay nothing. `attachments` lists what the prompt CITES (step evidence + referenced library assets) above the button, so nothing rides into a prompt invisibly. |
| `StepFrame` | `layout-lab/steps/StepFrame.tsx` | step shell: Acceptance banner (with optional `why` / `suggestion` / `onFix`) + responsive View panel grid. The generic `ArchetypeStep` (~330 non-Items steps) fills these for every non-pass status via `shared/genericFixCopy.ts` — a step's bespoke `StepSpec.copy` if it has one, else a neutral fallback derived only from the checker's status + `reason` (never invents catalog content). Its "⚡ Produce fix" runs the corrective direction through the step's own prompt logic (gallery → corrective batch, static → re-produce); `deferred` gets an explanatory `why` but no fix button (a runtime/visual gate isn't locally fixable). **Withheld where `fixEffectOf` returns `no-op`** (wave 26): an already-produced static step whose produce body is direction-blind (`readsDirection(spec)` — arity probe, `StepSpec.readsDirection` override) would re-write byte-identical data, so the banner states what WOULD change it (live CLI produce for brief/graph/rules, else the body must be authored) instead of offering a button that cannot work; the never-produced case keeps its fix. Spec-linter rule (p) ratchets the direction-blind count (344 on 2026-09-04) shrink-only. **The direction is never empty:** it resolves `copy.fixDirection` → `StepSpec.defaultDirection` → `fixDirectionFor(spec, gate)` — a derived instruction naming the step, the criterion, the checker's own `reason` and the archetype's authored corrective act (9 blocks in `genericFixCopy.ts`, one per archetype; never a fabricated target value). It is **previewed verbatim in the banner's `suggestion` before dispatch**. Produce input only — it cannot move a verdict. **Provenance:** pass `catalogId` + `step` and it resolves the audited `StepFact` (`getStepFact`) and renders `ProvenanceStrip` under the banner — so a shape-only "pass" can't read as verified. A step that names itself ALWAYS gets the strip: with its fact, else a loud `PROVENANCE: UNAUDITED` (an anonymous StepFrame with no catalogId/step still renders none). Display only; never touches grading. |
| `ProvenanceStrip` | `layout-lab/steps/shared/ProvenanceStrip.tsx` | compact colorblind-safe honesty strip beneath any StepFrame acceptance banner (`StatusTag`/`MicroLabel`, glyph+word not hue): engine (`trueEngine`) · judge class (loud `JUDGE: NONE`) · checker-meaningfulness (`CHECKER: SHAPE-ONLY` vs `MEANINGFUL`) · `GENERATOR: NOT WIRED`, all from `step-facts.json`; the per-step honesty `note` is reachable in an expandable `<details>`. Fed automatically by `StepFrame`'s `catalogId`+`step` — don't hand-roll a provenance display. **Selection provenance:** a gallery (L1) step also passes `selection` (`selectionSource(history)` from `genHistory`) → `SELECTION: AUTO` (the machine auto-picked the batch's first candidate) vs `SELECTION: HUMAN` (someone clicked), `unrecorded` for legacy histories; the strip renders for fact-less steps too when `selection` is given. **Verdict provenance + derivation chain:** pass `judge` (the `AcceptanceResult.judge` attribution) → `VERDICT: CURRENT / STALE / UNVERIFIED / SUPERSEDED`, so a step condemned by a verdict that judged content it no longer holds says so instead of reading as a plain failure; pass `explain` (a THUNK from `useStepAcceptance`, invoked only while the "Why this grade?" disclosure is open — never per render) → the ordered `explainAcceptance` chain (checker → server-overlay → judge-bridge, which layer won, and which `allOf` member produced the reported status/tier). Both are display-only and provably cannot move a verdict (`explainAcceptance` re-applies the same functions in the same order as `resolveStepAcceptance` and is pinned byte-identical to it). |
| `ChartPanel` | `layout-lab/steps/shared/ChartPanel.tsx` | budget bars / scatter / histogram / waveform — shared `scaleLinear` + axes + staggered grow-in entrance (reuse instead of hand-rolled SVG). **All four variants** are reachable from the generic renderer via the `chart` **view kind** (`ViewDescriptor` → `bars`/`histogram` `{ field; rows/keys; max?; highlightKey? }`, `scatter` `{ field; referenceKey; pointsKey?; xDomain; yDomain; xLabel?; yLabel? }`, `waveform` `{ field; samplesKey; activeKey? }`; supported list in `SUPPORTED_CHART_VARIANTS`): a `balance` step declares which artifact fields feed the chart and which flavor, and `ArchetypeStep`'s ViewPanel renders it through this shared component. **Every `archetype:'balance'` step now declares a chart** (bars for budgets/faucet-vs-sink, histogram for distributions e.g. `combat-map` threat sources; `materials` LOD/Perf Budget was the first) — enforced by the fleet spec linter (`src/__tests__/catalog/pipeline-spec-linter.test.ts`). A balance metric that is only expressible as a negative number (e.g. `music` −16 LUFS) writes a positive display magnitude + band edges for the bars view; acceptance still derives from the original field. |
| `DataTable` | `layout-lab/steps/shared/DataTable.tsx` | attribute / manifest key·value table: present values in `inkDeep` (+ optional `unit`), `null` flagged in `warn` with `missingText`; optional `header` row + `caption`. Backs the generic `ArchetypeStep` `table` view, the `manifest` view when its field holds a **keyed object** (columns derived from the keys; nested arrays/objects flattened for display — arrays keep the checklist-style list), and the bespoke Items `Attributes` step — build tables here, don't hand-roll. Note: a `checklist`/`manifest` field that is **present but the wrong shape** renders a loud `ShapeMismatch` (names expected vs actual shape), never the "Nothing yet" empty-state lie — only genuinely-absent data shows the empty state. **Row mode:** pass `rows` (a list of `{label?, values}`) for a real multi-column table — the shape most produce bodies actually write (a LIST of row records, or a KEYED GROUP of them). The generic `table` view resolves which mode applies through the shared pure `@/lib/catalog/tableView.ts` (`resolveTableView`), which also backs spec-linter rule **(f2)**: every declared column must resolve against the step's own produce stub, so a table can never render a grid of `— missing`. A row list nested one level down is reached with the descriptor's optional `rowsKey` (`hazards` + `rowsKey:'hazardList'`); `field` stays the top-level key so linter rules (f)/(g) are unaffected. |
| `CandidateGallery` (Gallery2D) | `layout-lab/steps/shared/CandidateGallery.tsx` | generative-step candidate browser: every re-roll **batch is kept** (not discarded), each stamped with its direction + an expandable prompt; click any candidate to re-select. Pairs with the pure `genHistory.ts` model (`readHistory`/`appendBatch`/`selectCandidate`/`historyData`) which persists in the step artifact's `data.genHistory` and projects the selected candidate's payload to top-level so derived Acceptance is unchanged. History is **bounded** — `appendBatch` prunes to the last `MAX_KEPT_BATCHES` (12) re-rolls (the batch owning the selected candidate is exempt); batch ids stay unique across pruning via `nextSeq` (never `batches.length`). Both the bespoke Items steps (`ItemArt.tsx`) and the generic `ArchetypeStep` gallery consume the ONE shared engine `shared/useGenerativeStep.ts` (generate/reselect against `produceFrom`, history memoized on the artifact data ref); their candidate generators (`shared/itemGenCandidates.ts` / `shared/genericGalleryCandidates.ts`) share one hash (`shared/hash.ts`). So every `archetype: 'gallery'` step across all catalogs gets browse→compare→select with acceptance unchanged. A candidate may carry a real `imageUrl` (served thumbnail) — the gallery renders the image over its `swatch` (else the swatch is an honest deterministic seed preview). **Real 2D art is AUTOMATIC per step:** ArchetypeStep pre-fetches `GET /api/visual-gen/icons?catalogId=…&step=…` (`shared/useGeneratedImageAssets.ts`) — the per-step library in `generated/icons/` that `scripts/gap-loop/*` writes, served one file at a time by `/api/visual-gen/icon/<name>` (mirrors the 3D `/api/visual-gen/asset/<name>`: basename allow-list, traversal refusal, realpath re-check) — and feeds it to `shared/imageGalleryCandidates.ts`. Matching **re-encodes** the step's `(catalogId, label)` with the generator's own filename rule (`@/lib/visual-gen/generated-icons.ts` → `iconSlug`/`slugOfIconFile`), never splits the filename, so a step can only ever show art generated FOR it. Only `min(candidates, assets)` slots carry a real image (no repeating one icon to fill the grid); the rest — and a step with no art — stay honest deterministic swatches, and the Selected caption says which it is. **Pluggable generators:** a `StepSpec.genCandidates` (`{ needsAssets?, assetKind?, build(direction, seq, assets) }`) overrides the default builder; `assetKind:'3d'` is the opt-in that swaps the manifest for real `.glb` meshes (`shared/useGeneratedMeshAssets.ts` + `shared/meshGalleryCandidates.ts`, candidate carries `payload.glbUrl` → interactive GlbViewer). Empty manifest → honest swatch fallback (never a fake preview). See `character-pipeline` 3D Generation (3D). |
| `GlbViewer` | `layout-lab/steps/shared/GlbViewer.tsx` | interactive `.glb` preview (orbit/zoom, auto-framed, r3f) for 3D steps. `ArchetypeStep`'s gallery view renders it automatically when the selected candidate's `payload.glbUrl` is set. Load via `next/dynamic({ ssr:false })` (WebGL-only). Serve the `.glb` via `/api/visual-gen/asset/<name>.glb[?dir=<provider>]` — `dir` is validated against the `ASSET_DIRS` allow-list in `@/lib/visual-gen/generated-assets.ts` (`triposr` default so every legacy URL resolves byte-identically · `tripo3d` · `hunyuan3d` · `mesh-finish` · `meshes`), basename allow-list + realpath re-check, and `GET /api/visual-gen/assets` lists every dir newest-first with each entry tagged `provider`/`attempt`. Build URLs with `assetUrl(name, dir)`, never by hand; the forge's `SERVED_ASSET_DIRS` DERIVES from `ASSET_DIRS` so client and server cannot drift. **Reuse `GlbPreviewPanel` (below), not GlbViewer directly** — it is the single-sourced viewer+URL-caption block ArchetypeStep uses in both preview slots. |
| `GlbPreviewPanel` | `layout-lab/steps/shared/GlbPreviewPanel.tsx` | the shared 3D preview block (dynamic `GlbViewer` + served-URL caption) + `GLB_PREVIEW_LABEL`. ArchetypeStep renders it for a gallery candidate's `payload.glbUrl` and for non-gallery `data.glbUrl`. Reach REAL generated meshes in a **generic** gallery via `StepSpec.genCandidates = { needsAssets, assetKind:'3d', build: meshGalleryCandidates(...) }` — ArchetypeStep pre-fetches the `.glb` manifest (`shared/useGeneratedMeshAssets.ts`, reuses `/api/visual-gen/assets`) and each candidate carries `payload.glbUrl`; empty manifest → honest swatch fallback (no fake 3D). See `character-pipeline` 3D Generation. |
| `RawArtifactDisclosure` | `layout-lab/steps/shared/RawArtifactDisclosure.tsx` | collapsed `<details>` "Raw artifact" panel showing EXACTLY what a step stored (`data` + `ueAssets` + the persisted server verdict), verbatim. `ArchetypeStep` appends it to every generic step's panel grid — produce bodies write 10–60× what any View renders (e.g. the `wiringContract`), and this is the honest window onto the rest. Serializes only when expanded (controlled `<details>`), so a collapsed step pays nothing. **Serialization cannot throw** — this panel is the payload window inside `StepCrashBoundary`'s fallback, so an unserializable artifact (circular structure, BigInt — all untrusted input from SQLite/MCP/headless drains) is reported verbatim with the top-level key list instead of escalating out of the crash card. |
| `StepCrashBoundary` / `PanelCrashBoundary` | `layout-lab/StepCrashBoundary.tsx` | crash containment for the lab canvas — the lab IS the homepage, so a throw in any of ~350 step renderers used to take down the whole shell. `StepCrashBoundary` wraps the work canvas per `(entity, step)` (wired in `Baseline/index.tsx`, keyed so switching steps is itself an escape): tree, rail, header and search stay mounted and navigable. The card NAMES the step + catalog/entity, states plainly that a render crash is **not an acceptance verdict** (status unknown — never a silent empty panel, the lie class rounds 5–11 removed), shows the error + top stack frames, exposes the stored payload via `RawArtifactDisclosure`, and offers re-render / adopt-server-truth — the adopt path REPORTS when the server holds no row rather than no-oping silently. `PanelCrashBoundary` is the narrow form `ArchetypeStep` puts around its data-driven `View` and gallery panels; it is **deliberately never wrapped around the acceptance banner**, so a genuine `fail` can never be hidden by containment. `src/app/error.tsx` catches anything thrown above the canvas boundary (incl. `useBaseline` derivations, which run before any component renders). |
| `StepHistoryPanel` | `layout-lab/steps/shared/StepHistoryPanel.tsx` | previous versions of ONE step + restore, under the Raw artifact panel. `pipeline_artifacts` is upserted on `(catalog, entity, step)`, so a re-produce used to DESTROY the prior output (galleries survived via `data.genHistory`; static steps did not) — `upsertArtifact` now archives into `pipeline_artifact_revisions`, but **only when `contentChanged`** (a drain / verify pass re-upserts identical data with a new verdict and must not flood the history). Bounded to `MAX_REVISIONS` (20)/step. **A restore re-grades** through the same `gradeArtifact` the produce POST uses — an archived verdict can be stale, and trusting a stored `status` would re-open the fabricated-pass hole — and reports `regraded`/`archivedStatus` so a changed verdict is never silent. Loaded on demand (never on mount across ~342 steps). Route: `GET/POST /api/pipeline-artifacts/revisions`. |
| `stepEvidence` | `layout-lab/steps/shared/stepEvidence.ts` | `collectStepEvidence(data)` → the REAL served artifacts a step is showing (selected gallery candidate's `imageUrl`, its `payload.glbUrl`, top-level `data.glbUrl`; de-duplicated) + `evidenceBlock` → a prompt section saying the direction is feedback ON them. `ArchetypeStep` joins it into `buildPrompt`, so both Produce and the one-click "Produce fix" cite what is on screen instead of re-describing it. **A swatch is never cited** — candidates always carry a deterministic gradient, and quoting one would present a colour PoF invented as the produced asset; nothing real ⇒ empty list ⇒ no prompt section. |
| `StepLibraryPicker` + `libraryReference` | `layout-lab/steps/shared/StepLibraryPicker.tsx` · `libraryReference.ts` | browse the project's own `asset_library` from inside a step's Produce panel (passed as `CliProduce.fields`) and reference what it already holds; `libraryBlock` renders the picks as a prompt section, `libraryAttachmentLines` feeds the attachment list. **License is load-bearing** — shown on the row before picking, carried into the prompt, and an asset with none is flagged `LICENSE NOT RECORDED` rather than omitted (silence reads as unrestricted). Picks are session state, never artifact data: a reference is a produce INPUT and must not look like output or drift into acceptance. Empty library says so — PoF has what the user downloaded, not a bundled catalogue. |
| `Lbl` / `LabButton` / `LabInput` / `LabTextarea` | `layout-lab/steps/controls.tsx` | themed form controls (≥14px) |
| `useStepAcceptance` | `layout-lab/steps/shared/useStepAcceptance.ts` | the ONE on-screen acceptance derivation for every step UI: unified `buildLabCheckerContext` (real siblings + live `has`) → `serverVerdictOverlay` → `bridgeJudgeVerdict`. `ArchetypeStep`, `StaticStepFrame` and `GenerativeStepFrame` all consume it — never hand-roll a `CheckerContext` in a step component. |
| `StaticStepFrame` / `GenerativeStepFrame` | `layout-lab/steps/StaticStepFrame.tsx` · `GenerativeStepFrame.tsx` | the bespoke Items step shells (static vs gallery). Each derives acceptance via `useStepAcceptance`, appends `RawArtifactDisclosure`, and (generative) passes the `selectionSource` provenance chip — so the reference pipeline carries the same honesty affordances as the generic steps. |
| `getStepComponent` | `layout-lab/steps/index.ts` | per-catalog/per-step registry lookup |
| `Modal` | `src/components/ui/Modal.tsx` | ANY dialog. Owns the backdrop, `role="dialog"` + `aria-modal`, Escape-to-close, a Tab/Shift+Tab focus trap, `initialFocusRef`, and focus restore to the trigger — **on close AND on unmount**, because a dialog conditionally MOUNTED by its parent (`{show && <Editor/>}`) never transitions `open` to false and the `[open]` effect alone never fires. Never hand-roll a `fixed inset-0` backdrop: the guard test `src/__tests__/components/ui/no-hand-rolled-dialog-scrim.test.ts` refuses one, matching the inline `rgba(0,0,0,…)` spelling as well as `bg-black/`. Its single allowlist entry (`SpellbookSearchPalette`) carries its reason AND a second test re-asserting the evidence, so the allowlist cannot rot into decoration. `Modal` owns the header — a port passes `title`/`icon` and drops its local one. |
| `MeterBar` / `StatBar` | `src/components/ui/MeterBar.tsx` · `ui/StatBar.tsx` | horizontal progress / budget meters, both `role="progressbar"` with honest unmeasured states. They share ONE geometry, `resolveMeterScale(value, max, overflow)`. **A meter must never announce an over-limit value without saying "over"** — pass `overflow` for a budget bar and an over-limit value rescales (limit tick + hatched excess from `STATUS_TOKENS.bad.pattern`, the non-hue shape cue) and announces its TRUE percent instead of a clamped `100%`; a caller-supplied `valueText` gets " — over budget" appended rather than being trusted to disclose it. `overflow` is **opt-in**, so score/coverage/rate consumers keep byte-identical clamped behaviour. `marks` draws reference lines against the same rescaled geometry. Beware the real-world trap this fixed: `MaterialBudgetBar` clamped at its CALL SITES, before the bar ever saw the value, which made its own error branch unreachable by construction. |
| `MatrixScopeBanner` | `modules/shared/FeatureMatrix/MatrixScopeBanner.tsx` · `matrixScope.ts` | the ONE project-scope disclosure for ANY surface reading the feature matrix — the module matrix, the evaluator roll-ups (`AggregateQualityDashboard`, `CrossModuleFeatureDashboard`, `UnifiedSummaryView`), `FeatureConstellation`, and the NBA card. Four states from `describeMatrixScope`: `own` (**no banner** — nothing was hidden), `mixed`, `legacy`, and `foreign`, which escalates to `bad` when `visibleRows === 0` so "another project owns these rows" can never read as "never reviewed". **The subject noun derives from the report's own `moduleId`** (`null` ⇒ project-wide ⇒ "this project", else "this module") — a project-wide read must not claim per-module knowledge, and that is why the classifier was EXTENDED rather than forked. Never restate the sentence per surface; pass `testId` to distinguish instances. Display-only and deliberately actionless: a test asserts the banner contains zero `<button>` elements, because backfilling attribution is an operator decision. Row counts come from `countModuleRows`/`countAggregateRows`, never a dashboard's `totals.total` (which back-fills from `MODULE_FEATURE_DEFINITIONS` and stays large when the read returned nothing). |
| `useEntityCommitBuffer` | `src/hooks/useEntityCommitBuffer.ts` | the ONE optimistic edit buffer — reach for it before writing another per-keystroke fix. Renders `apply(base, pending)` locally and writes only on a real boundary (`stage` = zero writes for drag frames, `stageDebounced` = one write per typing pause at `UI_TIMEOUTS.textEditDebounce`, `commit` = discrete acts, `flush` = blur/record-switch/opt-in unmount). Guarantees: the buffer clears ONLY when the write resolves; a **stage counter** stops a slow earlier commit clearing a newer gesture's buffer and a **commit counter** stops it clearing `isSaving` under a newer write; a FAILED commit KEEPS the buffer and reports `saveError` for a visible `retry()` (never a silent `console.error` + revert-on-refetch). `commit` MUST reject on failure — a resolve is taken as "the server has it" (so it composes with a THROWING path like `useAudioScene.commitDoc`, never with `useCRUD.mutate`'s swallowed `null`). Domain-free: the caller supplies `apply`/`fold` (both referentially stable), plus optional `entityId` (a buffer staged against another record is neither rendered nor written), `finalize`/`onCommitted` (a flag that flips once per COMMIT, not per staged event) and `peek()` (the merged doc read from refs, for a handler mid-gesture). Exports the `EditCommitMode` vocabulary (`'commit' \| 'stage' \| 'debounce'`). Consumers: level-design (`useDocCommitBuffer`), the audio scene painter, `AudioView/useDebouncedCommit` (a thin single-field face). |

Reusable patterns still to extract to `shared/` when first needed (add to this table when you do): _(none outstanding)_. (`Gallery2D` is now `CandidateGallery`; `DataTable` is now extracted — both above.)

> **Produce contract note:** `CliProduce.onComplete` is called with an optional `{ direction, prompt }` so generative steps can stamp the batch they produce with the user's art direction. Zero-arg handlers stay valid. Dispatch is **async by default**: the button shows an in-flight "Dispatching…" state, awaits `onComplete` (which may return a promise), and surfaces a throw/rejection as the inline error reason with a **"Retry with same prompt"** affordance (Rule 4 — a failing dispatch always reports why). `onComplete` is still invoked synchronously on the click, so a stub-mode produce that writes its artifact synchronously still flips Acceptance immediately (only the success/error message resolves on the microtask). Pass `sync` to opt out to the legacy synchronous path (tests/legacy only); `minDispatchMs` sets an optional minimum in-flight hold.

**Rule 4 — Every step is tested + truthful.** Each step must: (a) **produce data to the UE5 project** and **update the UI**, (b) **fulfill a derived Acceptance** (read from UE/DB truth, never a manual toggle), and (c) if production fails, the **CLI reports the reason** (surface it, don't fail silently — `CliProduce.validate` returns the error reason). Each step ships a test asserting its View renders, Produce dispatches, and Acceptance derives.

**Rule 4b — Server gradability is a four-state contract, and "unknown" is a finding.** (`src/lib/catalog/acceptance/stepGradability.ts`, 2026-08-18.) Every persisted step label is in exactly one of four states: **`registered`** (a catalog pipeline declares it), **`bespoke`** (a legacy label with a server-importable checker — `itemsBespokeCheckers.ts`), **`unservable`** (explicitly recorded WITH a reason, e.g. the synthetic `loot-filter` catalog), or **`unknown`** — which is a defect to fix, never an accepted state. `serverCheckerFor` resolves registered first, then bespoke, so the fallback can only ADD gradability. A row the server could not grade keeps the producer's status (nothing truer exists) but is stamped **`UNGRADED: …`** in its `reason`, naming why nothing graded it — so a preserved producer status can never be read as verified. **Never rename a step label to align two specs:** `pipeline_artifacts` is upserted on `(catalog_id, entity_id, step)`, so a rename ORPHANS every existing row — teach the server the label instead. Bespoke checkers are tier **L0**: they are shape checks, and claiming higher is exactly the overclaim `/status` exists to expose.

**Rule 5 — Every pipeline is e2e-walked.** Every registered catalog pipeline is exercised end-to-end through the real `/layout` lab by the data-driven walker `e2e/catalog-pipeline-walker.spec.ts` (the Items reference pipeline by `e2e/catalog-items-reference.spec.ts`). The walker enumerates `allCatalogPipelines()`, so a **new pipeline is auto-covered** the moment it self-registers — you do not write a new spec. Your obligations when authoring a pipeline:
- Keep **≥1 seeded entity** for the catalog in `CATALOG_SECTIONS` / `NEW_CATALOGS` (the walker opens `entities[0]`).
- Each step's Produce must drive its Acceptance to a **config-complete terminal status**: `pass` for L0/L1/L2 (data/selection/static), `deferred` for L3/L4 (runtime/visual) — **never `fail`/`pending`** after a clean Produce. `deferred` must carry a reason (Rule 4).
- Keep the guard `src/__tests__/catalog/pipeline-e2e-coverage.test.ts` green (it runs in `npm run validate`). It fails if your pipeline has no seeded entity/section or is skipped without a reason.
- If a step genuinely cannot be walked in stub mode, add the catalog to `WALKER_SKIP` in `e2e/helpers/pipeline-coverage.ts` **with a precise reason** — never to mask a real failure. (Current documented gap: `player-movement` is registered but absent from `CATALOG_SECTIONS`, so the lab surfaces no entity — add a section/starter to close it.)

Run the catalog e2e with `npm run test:e2e` (Playwright, real dev server + SQLite, stub mode — no Claude CLI / UE bridge; `CliProduce` writes artifacts synchronously). It is intentionally **not** part of `npm run validate` (vitest-only); the guard test is the fast `validate`-time enforcement. The lab exposes stable test-ids for the walker: `harness-lab-ready`, `harness-catalog-<id>`, `step-dot-stamp-<i>`, `cli-produce-run`, `cli-produce-result`, and `acceptance-banner` (`data-status`); the opened entity is derived from the seed (`seedAllCatalogs()`), not a DOM hook, so the walker needs no instrumentation in `Baseline.tsx`.
