---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dzin/core/ (entire directory - vendored copy)
  - package.json
  - package-lock.json
autonomous: true
requirements: [FOUND-01, FOUND-02]

must_haves:
  truths:
    - "import { useDensity } from '@/lib/dzin/core' resolves without errors"
    - "npm run typecheck passes with zero Dzin-related errors"
    - "npm run build completes successfully with Dzin modules included"
    - "fast-json-patch is listed in package.json dependencies"
  artifacts:
    - path: "src/lib/dzin/core/index.ts"
      provides: "Barrel export for all Dzin modules"
    - path: "src/lib/dzin/core/density/useDensity.ts"
      provides: "Density hook"
    - path: "src/lib/dzin/core/layout/DzinLayout.tsx"
      provides: "Layout component"
    - path: "src/lib/dzin/core/theme/default.css"
      provides: "Dzin default theme CSS"
  key_links:
    - from: "src/lib/dzin/core/index.ts"
      to: "src/lib/dzin/core/density/"
      via: "barrel re-export"
      pattern: "export.*from.*density"
    - from: "src/lib/dzin/core/state/"
      to: "fast-json-patch"
      via: "npm dependency"
      pattern: "import.*fast-json-patch"
---

<objective>
Vendor the Dzin panel framework source into PoF and install its dependency.

Purpose: Provide the foundation for all subsequent Dzin integration work by copying the complete source tree and ensuring it compiles cleanly within PoF's TypeScript/Next.js build pipeline.
Output: `src/lib/dzin/core/` directory with all Dzin modules, `fast-json-patch` installed.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Dzin source and install fast-json-patch</name>
  <files>src/lib/dzin/core/ (entire directory), package.json, package-lock.json</files>
  <action>
1. Copy the entire Dzin source directory verbatim:
   - Source: `C:\Users\kazda\kiro\studio-story\packages\dzin\core\src\`
   - Destination: `C:\Users\kazda\kiro\pof\src\lib\dzin\core\`
   - Use `cp -r` to copy ALL subdirectories: density/, layout/, panel/, registry/, theme/, types/, state/, chat/, llm/, intent/, demo/, __tests__/, and index.ts
   - Do NOT modify any Dzin source files. Keep exactly as-is per user decision.

2. Install fast-json-patch:
   ```bash
   npm install fast-json-patch@^3.1.1
   ```

3. Run `npm run typecheck` to verify Dzin compiles cleanly.
   - If TypeScript errors occur in Dzin files related to missing `'use client'` directives, add `'use client'` at the top of any Dzin file that uses React hooks (useState, useEffect, useContext, useRef, etc.) but lacks the directive.
   - If import path errors occur, check that relative imports within Dzin resolve correctly. Do NOT change them to @/ imports.
   - Fix ONLY compilation errors. Do not refactor, rename, or restructure anything.

4. Run `npm run build` to verify Next.js production build passes with Dzin included.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>
- `src/lib/dzin/core/` exists with all Dzin modules (density/, layout/, panel/, registry/, theme/, types/, state/, chat/, llm/, intent/, demo/, __tests__/, index.ts)
- `fast-json-patch` appears in package.json dependencies
- `npm run typecheck` passes with zero Dzin-related errors
- `npm run build` completes successfully
- `import { useDensity } from '@/lib/dzin/core'` resolves
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify existing tests still pass</name>
  <files>No files modified - verification only</files>
  <action>
Run the full validation suite to confirm no regressions from adding Dzin source:
```bash
npm run validate
```
This runs typecheck + lint + test. All existing tests must still pass. If lint warnings appear for Dzin source files (e.g., console.log usage, any types), suppress them by adding `src/lib/dzin/` to the ESLint ignore patterns in the ESLint config, since Dzin source is vendored and should not be modified.
  </action>
  <verify>
    <automated>npm run validate</automated>
  </verify>
  <done>
- `npm run validate` passes (typecheck + lint + test)
- No regressions in existing test suite
- Dzin source excluded from lint if needed (vendored code)
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `npm run build` completes
- `npm run validate` passes (no regressions)
- `import { useDensity } from '@/lib/dzin/core'` resolves in TypeScript
</verification>

<success_criteria>
Dzin source is vendored at src/lib/dzin/core/, fast-json-patch is installed, and the entire PoF build pipeline (typecheck, lint, test, build) passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
