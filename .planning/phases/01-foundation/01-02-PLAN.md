---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/dzin/core/theme/pof-bridge.css
  - src/app/globals.css
autonomous: false
requirements: [FOUND-03, FOUND-04]

must_haves:
  truths:
    - "A test element with data-dzin-density='compact' renders using PoF's dark theme colors (surfaces, borders, text) -- no Dzin default slate palette visible"
    - "Tailwind utility classes density-micro:, density-compact:, density-full: apply styles conditionally based on data-dzin-density attribute"
    - "Dzin's light mode is neutralized -- PoF dark theme colors always apply regardless of OS preference"
  artifacts:
    - path: "src/lib/dzin/core/theme/pof-bridge.css"
      provides: "Theme bridge mapping --dzin-* tokens to PoF CSS variables"
      min_lines: 40
    - path: "src/app/globals.css"
      provides: "Updated with Dzin CSS imports and density variant declarations"
      contains: "@custom-variant density-compact"
  key_links:
    - from: "src/app/globals.css"
      to: "src/lib/dzin/core/theme/default.css"
      via: "@import directive"
      pattern: "@import.*dzin.*default\\.css"
    - from: "src/app/globals.css"
      to: "src/lib/dzin/core/theme/pof-bridge.css"
      via: "@import directive (after default.css)"
      pattern: "@import.*pof-bridge\\.css"
    - from: "src/lib/dzin/core/theme/pof-bridge.css"
      to: "src/app/globals.css"
      via: "var() references to PoF CSS variables"
      pattern: "var\\(--background\\)|var\\(--surface\\)|var\\(--border\\)"
---

<objective>
Create the CSS theme bridge and Tailwind density variants so Dzin panels render with PoF's design system.

Purpose: Map all 22 Dzin CSS tokens to PoF's existing CSS variables, neutralize Dzin's light mode, and define Tailwind custom variants for density-conditional styling. This ensures all panels built in later phases automatically inherit PoF's dark theme.
Output: `pof-bridge.css` with complete token mapping, `globals.css` updated with imports and density variants.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From src/app/globals.css - PoF CSS variables that the bridge maps to -->
<!-- Key variables: --background, --surface, --surface-hover, --surface-deep, --border, --border-bright, --text, --text-muted, --core -->
<!-- From src/lib/dzin/core/theme/default.css - Dzin tokens to override -->
<!-- Key tokens: --dzin-surface-1, --dzin-surface-2, --dzin-surface-3, --dzin-border, --dzin-text-primary, --dzin-text-muted, --dzin-accent -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create pof-bridge.css and update globals.css</name>
  <files>src/lib/dzin/core/theme/pof-bridge.css, src/app/globals.css</files>
  <action>
1. Create `src/lib/dzin/core/theme/pof-bridge.css` with the COMPLETE token mapping from RESEARCH.md Pattern 2. This file overrides all 22 Dzin tokens with PoF CSS variable references:

```css
/* Override Dzin's :root color tokens with PoF values.
   Structural selectors from default.css are preserved.
   Light mode blocks are neutralized by re-declaring dark values. */

:root {
  /* Surface colors */
  --dzin-surface-1: var(--background);
  --dzin-surface-2: var(--surface);
  --dzin-surface-3: var(--surface-hover);

  /* Border */
  --dzin-border: var(--border);
  --dzin-border-focus: var(--core);

  /* Text */
  --dzin-text-primary: var(--text);
  --dzin-text-secondary: var(--text);
  --dzin-text-muted: var(--text-muted);

  /* Accent */
  --dzin-accent: var(--core);
  --dzin-accent-muted: var(--border-bright);

  /* Panel-specific */
  --dzin-panel-bg: var(--surface);
  --dzin-panel-border: var(--border);
  --dzin-panel-header-bg: var(--surface-deep);

  /* Spacing: tighten to match PoF's compact aesthetic */
  --dzin-space-xs: 4px;
  --dzin-space-sm: 6px;
  --dzin-space-md: 10px;
  --dzin-space-lg: 14px;
  --dzin-space-xl: 20px;

  /* Typography: map to PoF type scale */
  --dzin-font-sm: var(--text-xs);
  --dzin-font-base: var(--text-sm);
  --dzin-font-lg: var(--text-base);
}

/* Neutralize Dzin's light mode -- PoF is dark-only */
@media (prefers-color-scheme: light) {
  :root {
    --dzin-surface-1: var(--background);
    --dzin-surface-2: var(--surface);
    --dzin-surface-3: var(--surface-hover);
    --dzin-border: var(--border);
    --dzin-border-focus: var(--core);
    --dzin-text-primary: var(--text);
    --dzin-text-secondary: var(--text);
    --dzin-text-muted: var(--text-muted);
    --dzin-accent: var(--core);
    --dzin-accent-muted: var(--border-bright);
    --dzin-panel-header-bg: var(--surface-deep);
  }
}

/* Neutralize explicit class toggles */
.light, [data-theme="light"] {
  --dzin-surface-1: var(--background);
  --dzin-surface-2: var(--surface);
  --dzin-surface-3: var(--surface-hover);
  --dzin-border: var(--border);
  --dzin-border-focus: var(--core);
  --dzin-text-primary: var(--text);
  --dzin-text-secondary: var(--text);
  --dzin-text-muted: var(--text-muted);
  --dzin-accent: var(--core);
  --dzin-accent-muted: var(--border-bright);
  --dzin-panel-header-bg: var(--surface-deep);
}
```

2. Update `src/app/globals.css` — add Dzin CSS imports and density variant declarations. Insert AFTER the `@import "tailwindcss"` line and BEFORE the `:root` block:

```css
/* Dzin theme: structural selectors + default dark tokens */
@import "../lib/dzin/core/theme/default.css";
/* Dzin state: panel enter/exit animations, LLM glow, density transitions */
@import "../lib/dzin/core/theme/state.css";
/* PoF bridge: override Dzin tokens with PoF design system values */
@import "../lib/dzin/core/theme/pof-bridge.css";

/* Density variants for Tailwind utilities inside Dzin panels */
@custom-variant density-micro (&:is([data-dzin-density="micro"], [data-dzin-density="micro"] *));
@custom-variant density-compact (&:is([data-dzin-density="compact"], [data-dzin-density="compact"] *));
@custom-variant density-full (&:is([data-dzin-density="full"], [data-dzin-density="full"] *));
```

IMPORTANT: Do NOT modify any existing CSS in globals.css. Only ADD the imports and variant declarations.

3. Run `npm run typecheck` and `npm run build` to verify nothing breaks.

Note on import order: `@import "tailwindcss"` MUST come first (CSS reset), then default.css (structural + tokens), then state.css (animations), then pof-bridge.css (token overrides). This order is critical for the CSS cascade.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>
- pof-bridge.css exists with all 22 token mappings, light mode neutralization, and class toggle neutralization
- globals.css has Dzin imports in correct order (after tailwindcss, before :root)
- globals.css has three @custom-variant density declarations
- Build passes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CSS theme bridge mapping Dzin tokens to PoF's dark theme, and Tailwind density variants for conditional styling.</what-built>
  <how-to-verify>
1. Run `npm run dev` to start the dev server
2. Open the browser dev tools on any PoF page
3. In the Elements panel, add a temporary test element to the body:
   ```html
   <div data-dzin-density="compact" style="padding: 20px;">
     <div data-dzin-panel style="padding: 16px; background: var(--dzin-panel-bg); border: 1px solid var(--dzin-panel-border); color: var(--dzin-text-primary);">
       <p>Test panel text</p>
     </div>
   </div>
   ```
4. Verify the test panel renders with PoF's dark theme:
   - Background should be PoF's surface color (~#111128), NOT Dzin's slate (~#1e293b)
   - Border should be PoF's border color (~#1e1e3a), NOT Dzin's slate (~#334155)
   - Text should be PoF's text color (~#e0e4f0), NOT Dzin's slate (~#f1f5f9)
5. If your OS is set to light mode, verify the panel still shows dark colors (light mode neutralized)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any color mismatches</resume-signal>
</task>

</tasks>

<verification>
- pof-bridge.css exists at src/lib/dzin/core/theme/pof-bridge.css with 22 token mappings
- globals.css imports: tailwindcss -> default.css -> state.css -> pof-bridge.css (in order)
- globals.css has @custom-variant density-micro, density-compact, density-full
- `npm run build` passes
- Visual check: Dzin elements use PoF colors, not Dzin defaults
</verification>

<success_criteria>
Dzin's visual tokens map to PoF's existing dark theme with no default Dzin palette visible. Tailwind density variants (density-micro:, density-compact:, density-full:) are declared and ready for use in Phase 2 panels.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
