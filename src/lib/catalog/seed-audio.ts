import type { AudioCatalogEntry } from './types';

/**
 * Phase 8b substrate: the audio catalog. Empty seed until Phase 10-A lifts
 * real data from `audio/AudioLibraryPanel`'s existing sound-set sources.
 * Same pattern as `seed-materials.ts` (Phase 8 proof).
 */
export function seedAudioEntries(): AudioCatalogEntry[] {
  return [];
}
