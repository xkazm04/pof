import type { AnimationAssetCatalogEntry } from './types';

/**
 * Phase 8b substrate: the animation-assets catalog (separate from state-graph;
 * this one tracks skeletal-mesh + retargeted montage assets, not the AnimBP
 * graph). Empty seed until Phase 10 lifts real data from the existing
 * animation pipeline.
 */
export function seedAnimationAssetEntries(): AnimationAssetCatalogEntry[] {
  return [];
}
