import { MODULE_COLORS } from '@/lib/chart-colors';

/* ------------------------------------------------------------------ */
/*  Constants                                                          */
/* ------------------------------------------------------------------ */

export const WEEK_PX = 56;          // Pixels per week column
export const ROW_HEIGHT = 52;       // Height per milestone bar row
export const HEADER_HEIGHT = 48;    // Month/week header
export const LEFT_GUTTER = 200;     // Label column width
export const BAR_HEIGHT = 28;       // Gantt bar height
export const BAR_Y_OFFSET = (ROW_HEIGHT - BAR_HEIGHT) / 2;
export const TODAY_COLOR = MODULE_COLORS.setup;
