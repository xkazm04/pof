/**
 * Barrel re-export — canonical source lives in AbilitySpellbook/data.ts.
 * Kept so external consumers (ComboChainBuilder, AbilityForge, etc.) continue to work.
 */
export * from './AbilitySpellbook/data';
