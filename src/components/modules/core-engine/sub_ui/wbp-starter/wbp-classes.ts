/**
 * Known BindWidget-coupled HUD widget classes — suggestions for the WBP-starter
 * panel (folder-04 §6 / Phase 2b). These are only autocomplete hints; the
 * agentic dispatch reads the real C++ header for the authoritative child list,
 * and the operator may type any class name.
 */
export const HUD_WBP_CLASSES = [
  'UARPGHUDWidget',
  'UARPGMainHUDWidget',
  'UAbilityBarWidget',
  'UAbilitySlotWidget',
  'UEnemyHealthBarWidget',
  'UCharacterStatsWidget',
] as const;
