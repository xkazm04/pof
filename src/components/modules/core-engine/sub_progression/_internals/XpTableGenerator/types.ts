/* ── Data Table Generation ─────────────────────────────────────────────────── */

export interface ProgressionRow {
  level: number;
  xpRequired: number;
  xpTotal: number;
  hpBonus: number;
  manaBonus: number;
  attrPoints: number;
  unlockReward: string;
}
