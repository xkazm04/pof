/** Debounce delay for search input. */
export const DEBOUNCE_MS = 300;
export const MAX_VISIBLE_PILLS = 8;
/** Number of items to jump with Page Up/Down — ~10 visible items or list size. */
export const PAGE_JUMP = 10;
