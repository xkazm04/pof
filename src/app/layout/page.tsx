import { LayoutLab } from '@/components/layout-lab/LayoutLab';

/** `/layout` — the UI identity prototype lab (see docs/superpowers/specs/2026-05-25-pof-layout-identity-lab-design.md). */
export default function LayoutLabPage() {
  return <LayoutLab />;
}
