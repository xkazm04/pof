export interface ProcgenRun {
  id: number;
  roomCount: number;
  seed: number;
  createdAt: string;
}

export interface ScatterRun {
  id: number;
  instanceCount: number;
  seed: number;
  createdAt: string;
}
